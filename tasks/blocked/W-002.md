# W-002 · 墙 #46「5ce2dcee ~50% 早停 lottery」：板病 vs 软件 bug

> **我是四号，领 W-002，墙 #46，板 `5ce2dcee00000000000000000923012c`。**

- **全局原子号**：L13.A01 PROCESS-ALIVE（`state/ATOM-MAP.md:72`，related）
- **墙序**：LEDGER #46
- **队列**：frontier
- **铸卡人 / 日期**：四号 / 2026-07-11
- **执行账号**：`coding`（sandbox）。**无 `.git` 写权 → 本 lane 无法自行 commit**，需 dispatcher 代提交。见 §8。

## 0. 派工隔离

```text
branch:   (未建 —— coding 账号无 .git 写权，无法 git worktree add)
worktree: (同上；当前在 canonical checkout 上只写本卡 + evidence/W-002/)
```

⚠️ 违反「写入型 worker 必须独占 worktree」的常规纪律，属**已知偏差**。原因：ACL 只开了
`state/ evidence/ tasks/doing/ oracle/`，`.git` 被刻意排除（这同时保证本 lane 不可能污染
W-001 的 dirty tree）。dispatcher 若要合规，需要给 `.git` 开权或代建 worktree。

## 1. 墙签名 (wall signature)

台账原文：`5ce2dcee ~50% 早停 lottery(mainNew=OK 后窗口构造 wlAlloc PhoneWindow)：板退化 vs 解释器 bug`。
声称症状：probe 跑到 `mainNew=OK` 之后、`wlAllocUtd` / PhoneWindow 构造阶段进程消失，约半数 trial 复现。

**🔴 本卡第一动作即证伪了这个签名的证据基础。见 §RESULT「FALSIFY」。**

## 2. 封闭上下文 (self-contained context)

- 板：`5ce2dcee00000000000000000923012c`（alias `5ce2dcee`，tier `big-any`，BOARDS.toml lock=`wall-46`）
- hdc：`/Users/yao/.local/bin/hdc`（3.2.0b）。server 由 `yao` 以 pid 98442 跑在 `127.0.0.1:8710`；
  从 `coding` 跑 client 会连上该 server，**不会另起 server**（已实测）。
- 成功路径的唯一原始输出：`archive/whiteboard-era/AGENT-COORD-ARCHIVE.md:461-478`
  （Agent-B，2026-07-09 00:45，exit=0，走到 `wlAllocUtd=OK appInstBase=OK appCbsInit=OK mAppSet=OK mapp=set pwcls=ok`）
- #46 走的是 **uptodownProbe 应用构造路径**（probe 对 uptodownProbe 显式 skip inputVerify）。
- probe 源：`test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c`
  —— **该文件当前带 W-001 未提交改动（+44 行，改了 `nativeSetApkAssets(...,ZZ)` 签名与 attr）**，
  且 `oracle/verify/atom-43.sh`、`tasks/doing/W-001.md`、`Dayu600ApkStageProbe.java` 同理。**本卡一律不碰。**
- 关联墙 #33（V=?，未复核）声称「迁板到 5583 后 vtable 非确定性仍在 ⇒ 解释器问题非板子」。
  若此条成立，#46 其实已倾向 code bug。但它自己也是 claimed 未复核。

## 3. Oracle 命令 (唯一成功判据)

```bash
# 编写中（W-002 产出物之一）。硬性要求：
#   - serial 前缀为 5583 → 立即 REFUSED 退出（W-001 独占）
#   - 复用 oracle/board-health.sh
#   - hash 锁定所用 probe .so 与板上 substrate；不匹配即 FAIL
#   - 跑 N 次 trial，记录每次 exit code / 最后 stage 标记 / signo / tombstone
#   - 三态输出：PASS / FAIL / INCONCLUSIVE，并打印早停率 + 95% 置信区间
#   - 只清理 /data/local/tmp/w002-*，绝不碰板上 w001-* 文件
oracle/verify/atom-46.sh 5ce2dcee00000000000000000923012c
```

## 4. 板需求 (board)

board: big-any → **已锁 `5ce2dcee`**

## 5. 禁区 (forbidden)

- 板 `5583f5be00000000000000000323012c`（W-001 / 主号独占）—— 任何 hdc 子命令都不得指向它
- 板 `dd011a414436314130101220085cac00`（未登记时曾无保护；含不可重建 blob `16e08711` 的小板）
- W-001 的四个文件：`oracle/verify/atom-43.sh`、`tasks/doing/W-001.md`、
  `test-fixtures/dayu600-apk-probe/Dayu600ApkStageProbe.java`、
  `test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c`
  （文件系统层面已强制：ACL 不回溯继承，这些既有文件对 `coding` 只读）
- 5ce2dcee 的 BOARDS.toml forbidden：`wipe` / `flash` / `runtime 修改` / `framework jar 替换`
- `state/SYNC.md` —— 新规矩：仅 dispatcher 写
- 默认禁区：`class_linker` / `vtable` / `interpreter.cc` / entrypoint / runtime 替换

## 6. 证据要求 (evidence)

落 `evidence/W-002/`：每 trial 的 exit code + stage 标记 + 日志切片；早停率与置信区间；
所用 probe .so 的 sha256 + 板上 substrate hash；板号。并登记 `evidence/INDEX.md`。

## 7. 参照 oracle 路线

`archive/whiteboard-era/AGENT-COORD-ARCHIVE.md:461-478` 的成功 stdout 是「本该长什么样」的唯一对照。

---
## RESULT

verdict: **BLOCKED — 墙 #46 的证据基础已被证伪，「用同 probe 复现」物理上不可能**

oracle: (未跑；前提不成立)

output:

**FALSIFY-1：`~50%` 这个数字没有任何一手数据支撑。**
- 库中唯一可核实的 probe 原始 stdout 是**一次成功**（非早停）的运行：`AGENT-COORD-ARCHIVE.md:461-478`，`exit=0`。
- `~50% mainNew 早停率` 出自秘书的一句口径（`AGENT-COORD-ARCHIVE.md:374`），**无任何原始计数**。
- 仅有的计数是第三方转述：`docs/agent-memory/agent-h-verification-line.md:28/35`
  （`5ce2dcee run1-3 全 miss + USB 断`；`迁板后 n=1 未早停 → INCONCLUSIVE`）。**n=1 与 n=3，不是 ~50%。**

**FALSIFY-2：`COORD L2768` 是失效定位符（dead pointer）。**
- `LEDGER.md:63` 与 `archive/…/REAL-WALL-LEDGER.md:60` 都引用「COORD L2768：面板判 INCONCLUSIVE:n=1…要 N≥20」。
- 实测：`AGENT-COORD.md` 现 2462 行（< 2768）；唯一超 2768 行的 `AGENT-COORD-ARCHIVE.md`（7102 行）其 L2768 是秘书巡检噪音。
- `git log --all -S 'INCONCLUSIVE' -- '*COORD*'` → **空**。该字符串从未在任何 COORD 版本中存在。
- 真实出处是 Agent-H 的 memory 文件，不是 COORD。**引用是事后编造的。**

**FALSIFY-3：N≥20 的对照实验从未执行。**
- 脚手架 `scratchpad-shared/H-lottery-experiment/{README.md,run-trials.sh}`（Jul 9 20:26）是空跑：
  GOOD/STOP marker 从未填写，输出文件从未产出。

**BLOCK-1：#46 的复现基座整个不存在。（机制已修正 —— 初判把锅扣在 `.so` 上，错了）**
- `.so` 只是薄 JNI loader。实测 **12 个 probe `.so` 里，`mainNew` / `wlAllocUtd` / `appInstBase` / `pwcls` / `preUtdApp` 五个标记全部出现 0 次**。
- 这些标记活在 **Java dex**（`Dayu600ApkStageProbe.java`）里，而那才是决定早停行为的那层。
- 更要命：#46 输出里的 `appInstBase` / `appCbsInit` / `mAppSet` / `preUtdApp` / `appClsFound`，
  在 **HEAD 版本和工作树版本里都是 0**。⇒ 产出 `COORD:461-478` 那份输出的 dex 源码**从未进过 git**。
- ⇒ 不是「难以复现」，是**不可能复现**：`.so` 与 dex 两层都没了。
  （`mainNew`/`wlAllocUtd`/`pwcls` 在当前 .java 里还在，所以**新建基线**是可行的；**复现**不可行。）

**FALSIFY-4：即便跑了，设计出来的判据方向是反的。**（对抗复核 4/4 lens `survives=false`）
- 「崩溃顶帧 ∈ {libart, libwestlake_art}」不是**因果**特征，是「谁被打崩」的特征。内存安全故障的崩溃 PC
  由**当时在跑的解释器**决定，与肇事者是软件未初始化读还是硬件位翻转无关。零区分度。
- 反向误判是**系统性**的，不是对称噪声：
  - 本墙自己命名的 bug 机制（vtable 派发损坏 / uninit 读）典型表现是**分散**（坏指针跳飞 → 顶帧非 libart、
    faddr 随机、signo 从 SIGSEGV 漂到 SIGILL）→ 会被判成 `HARDWARE-LEAN`。
  - 而确定性负载命中确定性硬件弱点（同一 DRAM row / cache line 每跑同点命中）表现是**集中**，
    顶帧稳定落在 libart、无 kernel oops → 会被判成 `SOFTWARE-LEAN`。
- 「无 kernel oops」对最相关的用户态 DRAM/cache 故障**零排除力**（kernel oops 只在内核态访存出现）。
- 统计层：真正驱动判决的是 `share`（集中度）点估计，**没有任何置信区间**；被隆重推导的率的 Wilson CI
  在 verdict 里**一次都没被使用**。`12/15 → share=0.800` 恰好越阈判 PASS，但其 Wilson CI 实为 `[0.548, 0.930]`。
  叠加未校正的 optional stopping、同 boot 连跑破坏独立性、以及「看完首轮数据再重标定阈值」＝分叉路径花园。

**BLOCK-2（安全，操作级否决）：跑这个实验会危及 W-001。**
- 实验的本质，就是把 `BOARDS.toml:54` 白纸黑字记为「**需物理断电重插**」的那个 USB-daemon 崩溃，
  **故意诱发 60 次**。
- 主机侧 hdc server 是**全局单例**（`yao` pid 98442）。5ce2dcee 与 5583 是否挂在**独立 USB 控制器**上，
  从未被验证过。每次 gadget 掉枚举引发的总线复位 / server 重扫，都可能打断 5583 上 W-001 的实时会话。
- 恢复路径要求人手在**同一工作台**物理重插 5ce2dcee —— 就在跑 W-001 的 5583 旁边。
- 且 `oracle/board-recover.sh:30` 的 `remount_substrate` 是 **TODO 空桩**，substrate 实际不会被重挂。
- 设计里没有连续失败熔断：板一旦进入崩溃-掉线循环，剩余 trial 仍会逐个再诱发一次 USB 崩溃。

**BLOCK-3：史料里对成因给了三种互斥解释，第三种从没人查过。**
- `westlake-dayu600-oncreate-walls.md:104/116` → 「板环境退化非我代码」
- 同文件 `:179/229` + 墙 #33 → 「vtable lottery，解释器问题」
- 同文件 `:120` → **「reboot 后 `sleep 75` settle 就稳定到 actOnCreate；之前的非确定性是没 settle 的冷启动」**
- 第三种是**测试夹具伪影**，既不是板病也不是解释器 bug。而 `agent-h-verification-line.md:28` 的
  `run1-3 全 miss` 后面紧跟着 `+ USB 断` —— **「早停」和「掉线」从一开始就混在一起了**。
- H 自己还记录了对板退化的反证：5ce2dcee 与干净板 5583 的 `load≈10`、D 状态内核线程**完全相同**。

**结论**：#46 不是一道「已观测、待定性」的墙，而是一条**未经测量的传闻**被提升成了台账行，
配了一个查无实据的引用，且其成因最可能的解释（冷启动未 settle / USB 掉线被误记为早停）**从未被检验**。
在跑任何 trial 之前，`LEDGER.md:63` 的 `~50%` 与证据指针都应标为 unsourced。

**我没有对任何板执行过命令。** 依据 BLOCK-2，我不建议在 W-001 收工前跑这个实验。

**BLOCK-2 已被硬件坐实（2026-07-11 USB 拓扑取证）。**
`system_profiler SPUSBDataType` 证明：5583（W-001）与 5ce2dcee（#46）挂在**同一个 Bridgesil USB2.1 hub
`0x02140000` 的相邻端口**（port 4 / port 3），四块板全在这一个 hub 下。
⇒ 在 5ce2dcee 诱发 USB 崩溃引发的 hub 级复位，几乎必然连带 5583。史料里反复的
`5583f5be❌ 5ce2dcee❌ 均断连` 就是这个拓扑的证据，不是巧合。
**#46 的诱发式实验对 W-001 是拓扑证明的结构性威胁，不是理论担忧。**
详见 `evidence/W-002/2026-07-11-usb-topology.txt`。

## 9. 四号最终裁决与请求改派

**#46 no-go（三重独立否决，任一即足以否决）：**
1. 判据方向系统性反相关（对抗复核 4/4 lens survives=false）——即便跑了也读出反的结论。
2. 复现基座不存在（.so 与 dex 两层的 #46 标记都不在 git 里）——不可能复现，只能新建基线。
3. 跑它会危及 W-001（USB 同 hub 相邻端口，硬件坐实）——在 5583 空出前，物理上不该做。

**成本最低的正解**（若仍想给 #46 一个了断）：先验证「冷启动未 settle」假说——
reboot 后 `sleep 75` 再跑一次，看非确定性还在不在。比 60-trial 便宜两个数量级。
但即便这一次 reboot 也会重枚举同 hub，仍建议等 5583 空出再做。

**四号当前无可安全推进的板上动作。请主号改派**（候选：host 侧 #52 桩表 / 复核 #4 clinit 矛盾 /
#51 host 侧静态准备）。在改派前，本 lane 只做只读取证，不碰任何板。

board: 5ce2dcee（已锁，未使用）

commit: **无法自提交** —— `coding` 账号无 `.git` 写权（刻意，见 §0）。请 dispatcher 代 commit 本卡。

## 8. 给 dispatcher 的三个待裁决

1. **#46 是否还打？** 若打，必须重定义为「用新编 probe **首次建立** 5ce2dcee 早停率基线（n=0 起）」，
   而非「复现」。我可以出 `atom-46.sh` + 跑 N 次 trial。
2. **`.git` 是否给 `coding` 开权？** 不开 → 每张卡都要你代 commit；开了 → 我可能污染 W-001 的 dirty tree。
   折中：由你代建 `worker/W-002` worktree 并把它 chown 给 coding。
3. **cron 机制当前是死的。** 见下方 BLOCK-2。

**BLOCK-2：`coding` 的 cron 读不到本仓库（已实测）。**
`/Users/yao/Desktop/` 是 macOS TCC 保护目录，`/usr/sbin/cron` 无 Full Disk Access：
```
$ cat ~/.local/state/westlake/tcc-probe.log
Sat Jul 11 00:25:00 JST 2026
TCC-BLOCKED
```
cron 本身在跑（时间戳证明），但 `head -c1 state/BOARDS.toml` 被拒。任何在 `coding` 下扫仓库的 cron 都会静默失效。
修法二选一：(a) 系统设置 → 隐私与安全性 → 完全磁盘访问权限，加入 `/usr/sbin/cron`（GUI，只能你做）；
(b) 把仓库移出 `~/Desktop`（TCC 只保护 Desktop/Documents/Downloads）。
