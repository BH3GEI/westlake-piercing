# W-004 · 5ce 隔离 color APK 自绘变色上屏

- **全局原子号**：L05.A01（related）
- **墙序**：LEDGER #44/#53
- **队列**：frontier
- **铸卡人 / 日期**：thinker / 2026-07-12

## 0. 派工隔离
```text
branch: worker/color-smoke-5ce
worktree: /Users/yao/Desktop/code/westlake-piercing.worktrees/W-003-color
```

## 1. 墙签名
`color-smoke.apk` 的 `ColorView.onDraw → Canvas.drawColor` 尚未在物理面板产生可回读的逐帧变色像素；#49 已通过值验证，但 R2 同源、RenderNode record/submit 与像素回读仍待闭环。

## 2. 封闭上下文
只用 `5ce2dcee…0923012c`。私有根 `/data/local/tmp/wl-color-codex`；原 substrate 仅作只读 symlink 来源。APK hash 固定 `082559ae…`。从已验收 W-003 提交 `2854df46` 构建 ART/probe；不依赖 5583。

## 3. Oracle 命令
```bash
WCOLOR_ART_SHA=<built-sha> WCOLOR_DEX_SHA=<built-sha> oracle/verify/color-smoke-5ce.sh 5ce2dcee00000000000000000923012c
```

## 4. 板需求
board: big-any (固定 5ce2dcee)

## 5. 禁区
不碰 5583；不覆盖 5ce 原 substrate；不换 framework.jar；不 wipe/flash/reboot；所有可执行产物只进 `/data/local/tmp/wl-color-codex`。

## 6. 证据要求
ART/dex/APK hash、`color-smoke=ok changed=yes`、红/绿两帧中心像素回读、板号 5ce；落 `evidence/W-004/`。

## 7. 参照 oracle 路线
纯 C 蓝像素 #22 仅作 Surface/合成对照；验收像素必须来自 APK 的 `Canvas.drawColor`。

---
## RESULT
verdict: PASS
oracle: oracle/verify/color-smoke-5ce.sh 5ce2dcee…0923012c → PASS (exit 0)
output: |
  color-smoke=ok  changed=yes  pixel0=ffff0000 (red)  pixel1=ff00ff00 (green)
  APK ColorView.onDraw → Canvas.drawColor 逐帧变色,像素 pre-swap 回读闭环。
  两根因均解:(1) interposer-R2 — capture egl_interposer 折进 libhwui(-Bsymbolic-
  functions)+ 无 interposer 的 renderer(g_wl 由 libhwui 解析),单一 libhwui 实例;
  (2) #49 @CriticalNative nSetLeftTopRightBottom realfn=0 → nativeInit 里 C++ 直设
  RenderNode bounds(绕 JNI),镜像纯 C color_smoke。
  部署 sha: ART dc1d5e82 · probe.dex 46041825 · color.apk 082559ae · libhwui 私有
  capture d314d71f(≠ baseline 12872849,独立);renderer interposer-free 2884696B。
  证据: evidence/W-004/2026-07-12-color-smoke-5ce-PASS.txt
scope: 变色像素 = render-only strategy-B slice(WestlakeUpscreen.record → RenderNode
  → RenderProxy),绕 ViewRootImpl;DecorView show() 全路径(#53 本签名)仍未达,不越报。
board: 5ce2dcee00000000000000000923012c (私有根 /data/local/tmp/wl-color-codex;baseline substrate 未动)
commit: self
