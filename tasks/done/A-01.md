# A-01 · a2oh 7 分叉 commits 考古(noice 出声 / 输入桥 / 联网授权)

> **我是四号(yao),领 A-01,墙 #—(考古 · 关联 #45/#47 输入 · audio 非编号墙),板 host。**

- **全局原子号**：`-`(跨多原子;见 §2 映射)
- **墙序**：LEDGER 无单一对应;素材关联 #45 onClick / #47 IME / audio 线
- **队列**：factory(考古/复现,device-free 可摊弱模型)
- **铸卡人 / 日期**：四号(yao) / 2026-07-11
- **卡种**：`A-NN` = 考古(archaeology,忠实复原分叉/外部 commit)。本卡为**第一张考古卡**,确立该前缀。

## 0. 派工隔离

```text
branch:   dayu600-hwui-gate1-2 (canonical;本卡只新增自己文件,不碰他人 dirty)
worktree: canonical checkout(纯 host 只读 git + 新增 A-01 文件;无板、无设备)
```

## 1. 墙签名 (wall signature)

a2oh fork(`github.com/A2OH/westlake-piercing`,本地 remote `a2oh`)的 `main` 比 `origin/main`
**多出且仅多出 7 个 commit**(分叉点 `b9ede74a`,tip `36126fa9`)。这 7 个 commit 实现了 noice 在
OHOS adapter 上的 **出声(audio→speaker)/ 输入桥(text-entry)/ 联网授权(internet grant)**,
**只存在于该 fork**,未进 `origin/main`,有随 fork 丢失的风险。

**症状(考古缺口)**:canonical 仓无这 7 个 commit 的成品与复现路径;它们的实现散在 fork 的
bridge-src / framework-smali-patches / framework-patch-tools / native-libs / diagnostics,
无索引、无 hash 锚、无"该长什么样"对照。

## 2. 封闭上下文 (self-contained context)

7 个分叉 commit(新→旧;完整清单+每文件 blob-sha 见 `evidence/A-01/commit-inventory.txt`):

| commit | 主题 | 域 | 关键改动文件 |
|---|---|---|---|
| `4c8ce72f` | mediasession NPE 链修 + audio-path 诊断 | 出声 | `framework-smali-patches/*.smali`(MediaSession/MediaRouter/AudioPolicy/DisplayManagerGlobal…)+ `framework-patch-tools/Patch*.java` |
| `b0c35184` | **可闻 MP3 播放端到端(tap play→speaker)** | 出声 | `bridge-src/{oh_inproc_service,oh_mediacodec_shim,activity_manager_adapter}.cpp` · `native-libs/libmedia_jni_stub.c` · `docs/noice-audio-to-speaker-chain.md` |
| `70f677fc` | audible-audio 里程碑 reproduce+status 文档 | 出声 | `REPRODUCE.md` · `STATUS.md` |
| `b94f4713` | in-app service bind 改通用 async create | 输入/服务 | `bridge-src/{activity_manager_adapter,oh_inproc_service}.cpp` |
| `a966ce8f` | **appspawn-x 在 spawn 边界授网(修 adapter-app EPERM)** | 联网授权 | `bridge-src/child_main.cpp`(+675) |
| `941fb825` | **oh_input_bridge in-process 文字输入通道(往聚焦 EditText 打字)** | 输入桥 | `bridge-src/oh_input_bridge.{cpp,h}` |
| `36126fa9` | PatchReturnVoid — no-op ContentResolver observers | 输入/框架 | `framework-patch-tools/PatchReturnVoid.java` |

- 本地已有 remote-tracking `a2oh/main`(= `36126fa9`),**7 commit 的对象已在库**,host 可离线取。
- 语义关联:出声线呼应 [[noice-touch-input-rootcause]] 的 audio 未验证墙;输入桥呼应 #47 IME / #52 tap;
  授网呼应 noice-network-inet-gid。这三主题正是派工书点名的「出声/输入桥/联网授权」。
- **禁区文件(W-001 未提交,勿碰)**:`oracle/verify/atom-43.sh`、`tasks/doing/W-001.md`、
  `test-fixtures/dayu600-apk-probe/Dayu600ApkStageProbe.java`、`test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c`。

## 3. Oracle 命令 (唯一成功判据)

```bash
oracle/verify/a2oh-archaeology.sh   # 末行 PASS/FAIL
```

判据(host,device-free):① `a2oh/main` == `36126fa9`(fork tip 完好)② `origin/main..a2oh/main`
恰好 7 commit 且分叉点 == `b9ede74a` ③ `evidence/A-01/commit-inventory.txt` 里每个 blob-sha 都能
在对象库按 sha 取到(清单没编造、指向真实 fork 对象)。三者全真 → **PASS = 考古清单忠实**。

> **备注 · 两级 oracle**:本卡的 oracle 是**忠实性**(有没有把"该复现什么"记准),已可跑。
> **设备端复现 oracle**(noice 点播放→扬声器出声 / 聚焦 EditText 可打字 / adapter-app 能联网)是
> 各主题真正的成功判据,**需板 + noice + 逐 commit 移植**,属后续 factory 子卡,不在本卡("oracle 写清楚再停")。

## 4. 板需求 (board)

board: none(纯 host 考古;设备端复现留给后续子卡,届时再议板)

## 5. 禁区 (forbidden)

- 任何 board(5583f5be / 5ce2dcee / 两块小板);本卡不跑 hdc、不上板。
- W-001 四文件(见 §2)、三号 #52、coding-四号 W-002/#46 的工作区 —— 一律只读不碰。
- `state/*`(尤其 `SYNC.md` 只 dispatcher 写)、`LEDGER`(无硬证不动)。
- 默认禁区:`class_linker` / `vtable` / `interpreter.cc` / entrypoint / runtime 替换 / wipe / flash。

## 6. 证据要求 (evidence)

落 `evidence/A-01/`:`commit-inventory.txt`(7 commit × 文件 blob-sha,已产)。后续子卡移植时补
每主题的移植 diff + 设备端复现日志/hash。登记 `evidence/INDEX.md`。

## 7. 参照 oracle 路线

**a2oh fork 自身(`a2oh/main` = `36126fa9`)就是"本该长什么样"的对照**。逐 commit `git show <sha>`
即得原始实现;`docs/noice-audio-to-speaker-chain.md`(fork 内)是出声线的成品路书。

---
## RESULT

verdict: **DRAFTED — 考古卡铸成,host oracle 写清楚并实跑 PASS;设备端复现留后续子卡(按派工"再停")**

oracle: `oracle/verify/a2oh-archaeology.sh` → 见 evidence

output:
- 只读 git 采集:确认 `origin/main..a2oh/main` = 恰好 7 commit(分叉点 `b9ede74a`,tip `36126fa9`),
  三主题(出声/输入桥/联网授权)与派工书对齐;每 commit 每文件 blob-sha 已入 `evidence/A-01/commit-inventory.txt`。
- host oracle `a2oh-archaeology.sh` 实跑 = **PASS**(见 `evidence/A-01/oracle-run.txt`)。
- **未触**:任何板、W-001 四文件、三号 #52、coding-四号 W-002;`state/SYNC.md` 未写(留 dispatcher)。

board: host(无板)

commit: self(仅 `tasks/doing/A-01.md` + `evidence/A-01/` + `oracle/verify/a2oh-archaeology.sh`;不含他人 dirty)

## 8. 给 dispatcher 的待裁决

1. **身份撞车**:本会话是「四号(yao,全权限)」,另有「四号(coding,sandbox)」持 W-002/#46。
   请 dispatcher 在花名册消歧(或把本 yao 会话改派编号)。
2. **A-01 下一步**:是否派后续 factory 子卡逐主题移植 + 设备端复现(需板 + noice)?本卡按派工"oracle 写清楚再停"。
