# W-001 · 墙 #43 AppCompatTheme 真 windowActionBar oracle

- **全局原子号**：L10.A06（related；规格无 AppCompat 原子，见 ATOM-MAP #43）
- **墙序**：LEDGER #43
- **队列**：frontier
- **铸卡人 / 日期**：thinker / 2026-07-09
- **owner**：thinker 主攻（卡住问 gpt-5.6-sol / fable / opus，只读且给足上下文）

## 1. 墙签名 (wall signature)
uptodown `AppCompatDelegateImpl.createSubDecor()` 对 `R.styleable.AppCompatTheme_windowActionBar` 调 `hasValue()`。目标 APK 的真实 attr 是 `com.uptodown.R.attr.windowActionBar=0x7f040691`；`AppThemeBar(0x7f15000e)` 直接写了 `<item name="windowActionBar">false</item>`。

旧 probe/白板用了 `0x010100b0`，Android SDK 证明它是 `android:autoLink`；framework 的 `android:windowActionBar` 是 `0x010102cd`，也不是 AppCompat styleable 中的 app attr。故旧 `uamHasWab=false` **不是**双包 parent-chain 断链证据。本卡先建立真 oracle，再 A/B 判断 app-only 与 app+framework，不能预设双包是根因。

## 2. 封闭上下文 (self-contained context)

### 当前认识（2026-07-10 oracle audit）
- **已核验**：APK `public.xml` 为 `windowActionBar=0x7f040691`、`colorPrimary=0x7f040141`；Android SDK 为 `windowContentOverlay=0x01010059`。probe 两处 attr 数组已按此修正。
- **已核验**：AppCompat 的 styleable index 0x75 指向 `com.uptodown.R.attr.windowActionBar`；`createSubDecor()` 正是检查它。
- **2026-07-10 实测**：Java early oracle 的 `nativeW001Append(am, pathBytes) -> int` 形成 static shorty `ILL`；当前 interpreter 在 `interpreter.cc:1189` 明确报 unhandled，连续两次都没有进入 native，随后 `EARLY_THEME_FAIL:step=append:ckBad`。
- **源码审计**：native append 曾查不存在的三参 `(J,[ApkAssets])V`，而本仓 runtime 注册四参 `(J,[ApkAssets],ZZ)V`；旧代码随后仍写 `ck`，因此旧 `ck=2` 只等于 Java array length。现工作树已改成四参调用成功才写 `nativeSet=ok ck=N`。
- **下一修法**：`interpreter.cc` 已有 static `VLL` 分支；把入口改为 void + 两引用参数，状态只从 native heartbeat/最终主题判据读取，不改 interpreter。
- **A/B**：真进入 native 后先测 app-only，再在 applyStyle 前加入 framework 重测。只有数据能决定“双包 parent-chain”是否仍是根因。

### 代码落点
- 主 probe：`/Users/yao/Desktop/code/westlake-piercing/test-fixtures/dayu600-apk-probe/Dayu600ApkStageProbe.java`
  - early oracle / path bytes：~L746–900
- native sidecar：`test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c`
  - `westlake_native_append_apk_assets`：~L1140–1290
  - 当前先改点：JNI 注册/Java 声明从 `ILL` 改为 `VLL` trampoline；attr 与 nativeSet(ZZ) 修正已在本 checkpoint
- 史料：`archive/whiteboard-era/AGENT-COORD-ARCHIVE.md`（AppCompat / BuildDynamicRefTable 分析，约 L292–320、L1585+）
- 板上 substrate 惯例：`/data/local/tmp/westlake-dayu600-substrate/`（见 probe 内路径）

### 板与工具（开工时现场）
- 目标板：`5583f5be`（big-clean），serial 全称见 `state/BOARDS.toml`
- **2026-07-09**：hdc 已恢复（`~/.local/bin/hdc` → openharmony-tools，Ver 3.2.0b）；四板在线；`board-health 5583f5be` OK。见 `evidence/F-002/`。
- 小板：辅助调试用，**不**做本卡验收。

### 本卡要做的（thinker）
1. 领卡并只锁 5583；先读 oracle audit 与 hash-locked ILL baseline。
2. 做最小 `ILL → VLL` trampoline，不恢复已排除的 Method 枚举路线，也不改 interpreter。
3. 加 app-only / app+framework A/B 标记；Mac 编 Java probe，native sidecar 在 compiler 重编。
4. 显式部署 repo-owned launcher + 本地 DEX/SO；hash 全匹配后跑 oracle。PASS 才改 LEDGER。

## 3. Oracle 命令 (唯一成功判据)
```bash
oracle/verify/atom-43.sh 5583f5be00000000000000000323012c
```
脚本把板上 `/data/local/tmp/run-utd-w001.sh` 与 `oracle/device/run-utd-w001.sh` 比 hash，也要求板上 DEX/两份 SO 与本地 build output 一致；随后检查清理、run rc、native/early/probe 三份输出。末行必须是 PASS 或 FAIL。

## 4. 板需求 (board)
board: big-clean

## 5. 禁区 (forbidden)
- 不 wipe/flash；不碰 `class_linker` / `vtable` / `interpreter.cc` / entrypoint（除非顾问+用户明确授权）
- 不用 small 板做本卡验收
- 不把手改 `docs/dashboard/progress.html` 当进度

## 6. 证据要求 (evidence)
- PASS/FAIL：完整日志大于仓库预算时保存 sha256、字节数和关键切片。PASS 至少需 artifact 全匹配、`nativeSet=ok`、`wabAttr=0x7f040691`、`uamHasWab=true`；A/B 结果必须说明是否真的需要 framework 第二包。
- 板号 `5583f5be`；framework-res 实际 md5
- 不要存 hdc/网络失败噪音

## 7. 参照 oracle 路线 (可选)
- 规格：`L10.A06 SYSTEM-ASSETMANAGER-INIT` / `L10.A11 ANDROIDFW-MODERN-RESOURCE-API`（acceptance 口径）
- 档案分析：COORD-ARCHIVE AppCompat / BuildDynamicRefTable 节（只作历史假说，旧 attr ID 已证伪）
- catalog/noice 主题路径可作对照，但本卡以 uptodown probe 为准
- 正确运行入口与编译边界：`docs/reference/host-build.md`

---
## RESULT
verdict:
oracle:
output:
board:
commit:
