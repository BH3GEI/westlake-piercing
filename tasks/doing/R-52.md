<!-- 卡id 约定:R-NN=复核 · F-NN=工厂 · W-NN=穿刺 -->

# R-52 · MotionEvent 52 native 桩表 host 复核 vs A16 smali

> **我是三号,领 R-52,墙#52,板 host**

- **全局原子号**：L06.A01 INPUT-CLICK-DISPATCH
- **墙序**：LEDGER #52（tap → WLTEST CLICK）
- **队列**：factory
- **铸卡人 / 日期**：三号 / 2026-07-11

## 0. 派工隔离
```text
branch: (host 只读复核,无 writer worktree)
worktree: canonical checkout,经 scoped ACL 只写 tasks/doing/R-52.md + evidence/R-52/;无 .git 权,commit 由 yao 会话代做
```

## 1. 墙签名
墙 #52 声称 `westlake_tap → dispatchTouchEvent → onClick`(面板 blue→orange)。承重件是 `libwestlake_input.so` 的一张 native 桩表。问题:这张桩表对 A16 真实框架的 native 声明是否忠实(数量/命名/签名/调用约定),有无缺桩或语义假桩导致"CLICK"是假象。

## 2. 封闭上下文
- 桩表:`scratchpad-shared/wl-input-d/libwestlake_input.so` md5 `4fdbd3e48aadeb01ba9ae52848f6b67f`(ELF aarch64,未 strip)。
- A16 参照:`scratchpad-shared/agent-h/H-fw-decoded/smali_classes4/android/view/{MotionEvent,VelocityTracker,InputEventReceiver,KeyEvent}.smali`(Android16/SDK36,5583 板拉)。
- 枚举:`scratchpad-shared/wl-input-d/INPUT-CHAIN-ANALYSIS.md:64-67`(62 = MotionEvent 52 + VT 7 + KeyEvent 3)。
- LEDGER #52 记的 `bd6d2a77` 是计划 v2(盘上不存在);在盘的是 `4fdbd3e4`。

## 3. Oracle 命令
```bash
oracle/verify/atom-52.sh   # host,无板;末行 PASS/FAIL。判据:.so 覆盖 A16 MotionEvent 全部 52 native(名字级)
```

## 4. 板需求
board: none

## 5. 禁区
不碰 W-001 文件(Dayu600ApkStageProbe.java / westlake_embedded_art_dlopen_probe.c / atom-43.sh);不碰任何板(5583/5ce2dcee/小板/未登记 085cac00),不 wipe/flash;**不改 LEDGER 状态,除非硬证伪**;不碰 SYNC.md(dispatcher 专属)。

## 6. 证据要求
`evidence/R-52/2026-07-11-native-stub-vs-a16-diff.md`(已产)。

## 7. 参照 oracle 路线
A16 framework.jar smali(H-fw-decoded)= "本该长什么样"的对照源。

---
## RESULT
verdict: PASS（host 忠实度:MotionEvent 桩表 52/52 覆盖 A16）
oracle: oracle/verify/atom-52.sh
output: MotionEvent 52/52 名字级覆盖;VT 7/7、KeyEvent 3/3;normal-sig 故意偏离 @CriticalNative(imageless ART);InputEventReceiver 7 未桩=旁路 InputChannel 的必然;桩语义忠实仅由 on-device D3 佐证,host 不复现。
board: host
commit: self（注:coding 无 .git 权,实际 commit 由 yao/主号会话代做）

## 给 dispatcher 的信号（请聚合进 SYNC.md）
- **CLAIM/ORACLE** — R-52 host oracle PASS:`libwestlake_input.so`(4fdbd3e4)MotionEvent native 桩表对 A16 忠实(52/52)。未证伪,#52 维持 ⏳。
- **FACT** — LEDGER #52 的 so 指针 `bd6d2a77` 盘上不存在(=计划 v2),实为 `4fdbd3e4`;建议改 LEDGER 元数据或标注 v2 未产。
- **FACT(基建,非本卡)** — cron `board-watch.sh` 未活:macOS TCC 拦截(repo 在 ~/Desktop 保护目录,cron 无 FDA);`tcc-probe.log` 恒 `TCC-BLOCKED`。脚本本身合规。绕法待 dispatcher 定(授 cron FDA / 迁 repo 出 Desktop / launchd-as-yao)。
