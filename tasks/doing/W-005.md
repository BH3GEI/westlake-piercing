# W-005 · installed HAP 启动 nonce + XComponent 窗口归属 → 面板新帧

- **全局原子号**：L05.A01（related）
- **墙序**：LEDGER #53（前沿切片；不覆盖 #51 全路径）
- **队列**：frontier
- **铸卡人 / 日期**：thinker / 2026-07-17

## 0. 派工隔离
```text
branch: worker/W-005
worktree: /Users/yao/Desktop/code/westlake-piercing.worktrees/W-005
（当前由 kimi thinker 会话直接推进；胶代码在伞仓 harmony/artboot/，本仓只持 oracle + dex 源）
```

## 1. 墙签名
installed HAP（com.westlake.artboot）已在 5ce 装起（2026-07-13 checkpoint），但**没有任何证据证明某次点击启动产生了属于该 HAP 窗口的新鲜 Android 帧**：旧胶走 renderer 自建 RSSurfaceNode display-0 覆盖层（非 HAP 窗口），且结果可被旧文件/旧帧冒充。

## 2. 封闭上下文
- 板刷新现实：5583f5be 在线（lane 已清）、5ce 离线。靶板 = **5583f5be**（big-clean）。
- 焊接：HAP 侧 libwlweld.so 导出 `westlake_ohos_make_display_window`（RTLD_GLOBAL 先于 renderer 加载，PLT 拦截，零改 W-004 二进制），返回 ArkUI OnSurfaceCreated 交付的 XComponent OHNativeWindow；`westlake_ohos_teardown` 覆盖为空操作。源码 `harmony/artboot/wl-artboot-probe-hvigor/entry/src/main/cpp/`（wlweld.c / xcomp_glue.cpp / artboot.c v4 / Index.ets / CMakeLists.txt），NDK 编译已过。
- nonce：胶每次启动铸 `WL<epoch_ms>-<pid>` + 派生色（紫红系，G=0x00，不可能撞 PALETTE/底色），env `WESTLAKE_LAUNCH_NONCE(_COLOR)` 在 VM create 前导出；探针 dex `runTriangleApk` 画一帧该色并回读 swap 中心像素（dex `dbd4e082…`，源 `test-fixtures/dayu600-apk-probe/Dayu600ApkStageProbe.java` B0 段）。
- staging：`harmony/artboot/stage-runtime.sh <lane-root>`（钉 hash 校验：art dc1d5e82 / renderer 577d5099 / libhwui c9ed61d0）。
- 签名：`harmony/wl-tools/resign.sh`（默认 5583 UDID 3F740326…60E9）。
- 运行时件来源：编译机 `~/westlake-local-build/art-latest` + `art-build-recipes`（重建 art）；`gfx-smoke/build-{upscreen-renderer,adapter-libhwui}.sh` + blob `a2oh-source-audit`（renderer/libhwui）；`from-downloads-20260713/adapter-20260706-tree/out/aosp_fwk`（origin 中性 jars）；`b-route-stage/adapter-build/out`（adapter jars dex 重打包）。

## 3. Oracle 命令
```bash
WAH_HAP_SHA=<signed-hap-sha256> oracle/verify/apkhost-fresh-frame.sh \
  5583f5be00000000000000000323012c <path-to-signed.hap>
```
判据：install ok → 同 nonce 贯穿 WLAUNCH(launch) → WELD(XComponent 窗口归属) → WLTRI(nonceMatch=yes) → verdict=PASS；另拉 snapshot 存档。

## 4. 板需求
board: big-clean

## 5. 禁区
不改 W-004 已验二进制（art/renderer/libhwui 原样进包，hash 钉死）；不 wipe/flash/reboot；不改 oracle 判据文本；除靶板外不碰其他板。

## 6. 证据要求
hilog 四行链（nonce 一致）+ snapshot jpeg + signed HAP sha256 + staging-manifest.txt；落 `evidence/W-005/` 并登记 INDEX。

## 7. 参照 oracle 路线
`oracle/verify/triangle-smoke-5ce.sh`（同管线 shell-lane 版，像素 provenance 判据同源）；glxc-hvigor XComponent→panel  rail（窗口归属形态参照）。

---
## RESULT
verdict: IN_PROGRESS（管线全通，剩「libhwui↔framework 同源」墙；换班续）
oracle: oracle/verify/apkhost-fresh-frame.sh（已就位，待墙破后可直接跑）
output: |
  5583 实测管线全通：headless 打包→签名→装机→启动→解包→nonce→libwlweld 焊接
  （XComponent 窗口已交付）→embedded ART VM 起（rc=0）→noice subset 注册
  failures=0→runTriangleApk 进入并加载 TriangleView。
  当前两候选配对各差一墙：
  - framework 3e106350(A14)：subset 内 register_android_graphics_Canvas assert（Wall-1 复现）
  - framework c3a06db5(A15)：record() new RenderNode → DexFile::FindClassDef SIGSEGV
  根因方向：libhwui 须与所选 framework.jar 同源且过 OHOS 适配（编译机重建适配链）。
  排障锤死 9 条（libc++_shared 随包/installer strip 扁平 .so 破坏探针→sidecars 加载/
  子目录保留/板子回锁/沙盒 root 真身路径/签名器自对齐勿预签/大 HAP 签名重试/
  #49 setPosition guard/SKIP_START_REG 下须 dex 调 nativeRegisterHwuiRender）。
  详见 evidence/W-005/2026-07-17-5583-hap-pipeline-booted-two-walls.md
board: 5583f5be00000000000000000323012c（锁仍持）
commit: self
