<!-- 决策日志 · append-only · 一行一条 · 新的在上 -->
<!-- 记"为什么这么定"和战略裁决(值不值得打),不记流水进度(那是 LEDGER/FRONTIER)。 -->

# 决策日志 (DECISIONS)

- **2026-07-12** **M7b 侦察定论 + build 起飞:M7 残余墙 = 系统 AssetManager 空(未装 framework-res),修法 = 复用 W-001 #43 seed 机制并升级为"真装 framework-res",纯 DEX 改非破坏**(M7b scout only-read + M7b build workflow waw8v9tfv)。**确切两段抛点(scout observed,纠正/细化主控独立核验)**:M7 的 `createSystemAssetsInZygoteLocked:284 NPE:charset` 是 **clinit 被 ART tolerated**(→ `getSystem()` 返回**空** AssetManager),**真正终止**在下一行 `isHighEndGfx(ActivityManager.java:1768)→Resources.getBoolean(config_highEndGfx):1199→ResourcesImpl.getValue:269` NPE(= 空 AM 里查不到资源)。→ **W-001 的空哨兵 seed 不够,seed 的 AM 必须真装 framework-res**。**修法(最省力,DEX-only)**:framework-res.apk(35,727,112B)+libandroidfw.so 已在板 substrate 路径;W-001 wall#43 解过一模一样 NPE,机制在 probe DEX(`Dayu600ApkStageProbe.java`:seedSystemAssetManager L2556-2595 / ensureArscNatives L943-989 / nativeAppendApkAssets 自动对含"framework-res"路径用 PROPERTY_SYSTEM=1);broute 路径没跑它(seed 只在 W-001 uptodownProbe 档触发)。5 步 seed(ensureArsc/Trace natives→new AssetManager(true)→nativeAppendApkAssets(framework-res)→反射 set sSystem+sSystemApkAssetsSet)插在 systemMain 前,mac javac+d8 重打 dex 重跑同一 overlay bring-up,**不重编 native/不 dex2oat/不刷机**。CD-mmap rescue interposer + res-JNI CU 全已部署且在 broute VM 解析(M7 log 证 nativeSetApkAssets mod=libandroidfw.so)。**app-child(block2 observed)**:probe 已有 AMS-less 冷启脚手架(activityThreadStepProbe L2017-2113:initializeMainlineModules→prepareMainLooper→new ActivityThread()→attach(false,0) 到 "before Looper.loop");**缺的一环 = AMS-less 下没人发 BIND_APPLICATION → 须反射合成 AppBindData 手调 handleBindApplication**(§C/§G 才登场),origin AppSpawnXInit/liboh_adapter_bridge/10 AMS-adapter natives 不在我方路径(M0 绕 origin prefork)。**§C/§G 已确认烧进运行镜像**(scout observed:`5ce:broute/jars10p/framework.jar` 字节 == `broute/framework.patched.jar` = 44,351,227B → M7 载的 out10p 就是 §C/§G-patched framework AOT)。**架构定论**:systemMain(system_server)**不必跑完**,真 Material Catalog 走 app-child `ActivityThread.main()`;但 `Resources.getSystem/isHighEndGfx` 两路共用 → framework-res seed 是共享前置,先做(顺带把 systemMain 顶过 isHighEndGfx 作即时证明),再转 app-child。**lane 边界**:只改副本,不碰仓库 W-001 probe 原件,broute 专用部署路径。见 [[B-ROUTE-PLAN]] M7b/风险#10。
- **2026-07-12** **M7 首验通过:真 patched-framework arm64 boot image 被部署 libart 真加载并深入 systemMain(决定性 observed,非破坏)**(M7 first-load agent,仅 5ce,零 reboot/kill/改 canonical)。**M0 配对彻底坐实**:部署 libart(`substrate/art/libwestlake_art.so`)加载 M3 真镜像 out10p 成功——`[IMG] Loaded boot-framework.art size=0x217efd0`(~35MB 真 framework)、`MaybeRelocateSpaces diff=0`、`Fixed 363554 method entry points across 10 image spaces`、`JNI_CreateJavaVM rc=0`,**无 version mismatch / 无 imageless fallback**(out10p 与 substrate 现存镜像逐字节同为 `art\n 118`)。**到达度**:image load → framework native register(`startReg rc=0`)→ `ActivityThread.systemMain()` **被调用并深入执行进真 ThreadedRenderer/Resources/AssetManager**(真 AOSP 行号:systemMain:8543→ThreadedRenderer.initForSystemProcess:218→isHighEndGfx:1767→Resources.getSystem→createSystemAssetsInZygoteLocked:284)——对比旧占位 framework 只"到过 systemMain",本次是真 M3 framework 的 AOT 代码真在跑。**下一堵墙(observed,新)= systemMain 里系统 AssetManager 未接 framework-res.apk**(imageless-zygote 无 system assets → `isHighEndGfx→Resources.getBoolean→ResourcesImpl.getValue` NPE 止步);**非** framework 加载失败、**非** §C/§G(那两个在 app-child handleBindApplication,本次 systemMain 路径未触及)。**部署事实**:ART 镜像组件校验阶段就要打开烧死的 dex-location `/system/android/framework/*.jar`(仅靠 vdex 自带 dex 不够,`image_space.cc:2110 Unable to open core-oj.jar`→弃镜像);`/system` 只读 → **私有 mount ns + overlayfs 运行期即可让真镜像跑起来,不必刷机**(退出即消失,已验全局无残留)。复现命令 + 证据 `5ce:/data/local/tmp/m7-fcp2-stdout.txt`(agent 报告在案)。**下一前沿(并行)**:①把 framework-res.apk + AssetManager wiring(W-001 已有机制)接进 broute 路径让 systemMain 跑完 → 再走 app-child main()+attach+handleBindApplication(§C/§G 才登场);②M5 arm64 WMS client(同进程 create+commit)。见 [[B-ROUTE-PLAN]] M7/关键路径 + 风险#11。
- **2026-07-12** **M3✅ + M0 配对✅:B 路 host-build 侧(M0–M3)全绿,第一颗真 patched-framework arm64 boot image 落板**(主控亲手上板)。**M3**:换 framework.patched.jar(M2,a369ebae)在 5ce 跑 dex2oat_final -Xmx1g,产 30 段全非空(art118/oat247/kArm64,framework 完整 AOT),35.9s exit 干净;`5ce:/data/local/tmp/broute/out10p/`(jars10p+run10p.sh 可复跑,stock proof out10/ 保留)。vdex 44.8MB=M2 repack STORED dex 所致非错。**M0 配对(决定性,observed)**:板上现存那颗被部署 libart(7-07 建,28MB)加载到过 systemMain 的镜像 `westlake-dayu600-substrate/android/framework/arm64/boot.art` **就是 art\n 118**→**部署 libart 明确接受 art118**,与我方 v118 dex2oat/M3 镜像同版配对,**加载版本墙(旧忧 v114 mismatch)判绿**。启动器坐实=**appspawn-x**(`android/bin/{appspawn-x.dayu600,start-appspawnx-dayu600.sh}`,WESTLAKE_ROOT 可重定向,hybridspawn daemon;socket /dev/unix/socket/AppSpawnX)。**下一前沿**:M7 首验=可逆换 out10p 真镜像→跑 appspawn-x→看真 framework(§C/§G 守卫)是否到 app-child systemMain(vs 旧占位 framework);并行 M5 WMS client(arm64,同进程 create+commit)。真门(真 UI)仍在 M7→M9。见 [[B-ROUTE-PLAN]] M3/关键路径。
- **2026-07-12** **M3t 完成 + M2 完成:B 路 host-build 侧料齐工具齐**(两 agent 并行落地)。**M3t(自建同源 arm64 dex2oat)= exit 0,产 30 段全非空 boot image**:①"null GC collector" blocker 真因是**我方 -Xmx 过大**(5ce 仅 ~1GB 真空闲,ART large-object-space 按 Xmx 整块 anon mmap→OOM;修法 **-Xmx1g**)②另 3 源码补丁(skip InitializeClasses / 去 image_writer prune CollectGarbage / GetMemoryUsageString 去 std::ostream teardown SIGSEGV),**全在隔离副本,W-003 活 worktree 未碰**③**版本纠正:image v118 / OAT v247**(非旧记 v114,image.cc:38 observed)——M0 配对硬约束改为"部署 libart 须收 v118",dex2oat 复用 libart .o 应同源,M7 加载终判。FINAL dex2oat sha256 8646f712,证据 30 段镜像 138MB(用 stock placeholder framework,5 clinit 容忍)。**M2(patched framework.jar)= host 验通过**:dexlib2 by-signature 移植 origin Material 首帧补丁到 stock A15(§C ConnectivityManager if-eqz 空守卫 + §G ContentResolver + SysServiceRegistry **$88→$90** A15 重认 + PendingIntent×4 + ShortcutManager/DisplayManager/MediaRouter),sha256 a369ebae 确定性可复跑;**关键发现 ConnectivityManager 不在 stock A15 framework.jar**(在 Connectivity mainline 模块)→ origin `new ConnectivityManager()` graft 非可行且不必要,if-eqz 已足;audio/AlarmManager 按 M0 缓打。**净结论**:M2/M3t 两块最硬骨头下,关键路径推进到 **M3 真 boot image(换 patched framework 在 5ce 重跑 dex2oat -Xmx1g)→ M5 WMS client**;真门(dex2oat verify / 加载 / 真 UI)仍未过,逐步上板。见 [[B-ROUTE-PLAN]] M2/M3t/M3。
- **2026-07-12** **像素三墙源级判绿,对这三墙零 /system**(pixel-path derisk workflow,4-agent Opus,纯只读源级+device-lib readelf/objdump,不上板)。**风险#6 RS pid 门降险(从"须写 /system RS/须授权"→ needs_system_write=NO)**:门确活,但**5ce 服务端反汇编(observed)同步 stub 路(0x409178 GetCallingPid→SetCallingPid→0x413700)与异步 RecvParcel 路(0x39025c)都喂真实 binder 鉴权 pid(非0)**——不同于 origin 目标 OH 版本 TF_ASYNC 路 callingPid 掉0(origin 才需 G2.14aq /system pid_ 补丁)。→ **同一冷启 app-child 既 RSSurfaceNode::Create 又 CommitTransaction 时 callingPid==commandPid,门自然放行,零 /system**;**硬约束**=node 创建与提交锁同进程(跨 pid 拆分是唯一会重触发 /system 补丁的情形,已写进 M5)。**风险#7 abilityToken 判死(needs_system_write=NO)**:token 非建窗/合成硬门(CreateWindow 收 null、AddWindow 无 token 形参、WindowNode.abilityToken_ 默认 null);真门=CheckSystemWindowPermission(accessTokenId,非 abilityToken)对 APP_MAIN_WINDOW 放行;SetTokenState 是客户端 bool 非校验;origin SetTokenState(true) 只修"白块泄漏"(AMS→WMS transition 产 starting 窗才有,我方 AMS-less 冷启不发 NotifyWindowTransition→无泄漏)。→ **方案A token=null 零 /system 零 AMS/BMS**。**风险#2 WMS 可达判死**:5ce libwms(BuildID b0f27c3a)readelf/objdump——CreateWindow@0xdcf10/AddWindow@0xdd478 DEFINED,OnRemoteRequest@0x119c70 真解 TRANS_ID_CREATE_WINDOW,SceneSession/SessionManagerService 符号全空=纯 legacy;CheckSystemWindowPermission@0xe7668 对 APP_MAIN_WINDOW 短路放行;纯客户端 libwm+libwmutil 即可调。**净效果:M8 的 RS pid /system 补丁预算削掉,/system 写只剩 arm64 运行时 bring-up 本身,刷机授权范围收窄**。**新增风险#11(才是像素真残余)**:WMS focus/mission-stack 前台仲裁——三墙全过只保证"像素能创建并进主合成",不保证"稳定停前台可见栈"(origin noice 记 RequestFocus rc=1/displayId:-1/焦点被 launcher 抢),独立于 token/pid/权限,须 M7 上板 hidumper Focus/Zorder/displayId 终判,无证据须 /system。不因三墙 go 而对整链上屏乐观。见 [[B-ROUTE-PLAN]] 风险#2/#6/#7/#11 + M5/M8。
- **2026-07-12** **App 落地拍板 = 绕过 BMS,launcher 冷启上屏**(derisk workflow M4/M5/M6 4-agent + 主控板级只读验证)。**M6 observed**:①`.app→.apk` 单字节补丁前提**作废**——arm64 stock libappexecfwk_common(自 5ce /system/lib64/platformsdk,28024B)里 `.apk` 已是原生白名单扩展(file off 0x1d37,CheckFilePath@0x5384 adrp+add 0xd37→bl CheckFileType 原生比对);②stock OH6.1 BMS **无 APP_ANDROID/apk/dex/manifest 安装管线**(common/base/core 三 lib strings 对 APP_ANDROID/android/classes.dex/dalvik 零命中;live `bm dump -n com.ohos.launcher` 只见 bundleType:0,枚举无 10)。→ 传 .apk 只过扩展门下游无 Android-install。**拍板绕过 BMS**(与 M0 appspawn-x 绕过/aa start 冷启同构;GOAL 验收=snapshot 见真 UI,不要求 bm install);M8 重定为"落 /system 运行时+冷启",**不注册 catalog、不给 BMS 移植 install 逻辑**(那条标红,远超一字节)。**M4 observed**:libhwui 折入已 build 通过零 undefined(libhwui-adapter.so sha256 938673bf…,41304992B,旧产物备份 .bak-pre-abipatch),§2 log/abort→HiLogPrint 活性劫持生效→真 app 不静默 abort;§1/§3 对 M133 hwui DEFINED-but-INERT(mangling 不匹配),撞墙再重编。**M5 observed**:WMS 难度 XL 坐实,逼出 **sceneboard 生死问**(uis7885 出厂几乎必 sceneboard=ON,SA 4606 可能只服务 scene 路不解 legacy CreateWindow,仅板上读 hilog 可判——M5→M7 架构分叉生死点)。**主控板级只读验证(5ce,observed)**:libwms.z.so(1.28MB,120 WindowManagerProxy/WindowProperty 符号)、librender_service.z.so、libnative_display_manager.so(导出 OH_NativeDisplayManager_{GetDefaultDisplayId,CreatePrimaryDisplay,DestroyDisplay})**板上全部存在**;**新真墙#6**:librender_service.z.so 含 `RSTransactionData::IsCallingPidValid` + "COMMIT_TRANSACTION/RecvParcel IsCallingPidValid check failed" 字符串→uis7885 RS pid 门是活的,非法 pid 的 APP_WINDOW_NODE 事务被丢(buffer 到队列永不合成),origin 靠 G2.14aq pid_ fallback 绕它、补在 /system RS。**sceneboard 生死问板级降险(observed,推翻 synthesis"几乎必 ON")**:5ce 已装 bundle 仅 launcher/systemui/intelligentscene(无 sceneboard),桌面=经典 com.ohos.launcher.MainAbility,`hidumper -ls` 注册经典 WindowManagerService(非 SceneSessionManager)且真响应→legacy WMS 活着,origin client 大概率可沿用(终判仍须真 CreateWindow 读 hilog)。见 [[B-ROUTE-PLAN]] 风险#2/#4/#5/#6/#7。
- **2026-07-12** **M0 框架版本拍板 =(a)站 A15**(framework-version agent 34 tool 调查 + source 核验)。**认知更正(observed)**:我方"已到 systemMain"用的 framework 是 headless 占位 shim(prepare-substrate.ps1 自述 role="temporary headless framework shim … does not provide real Android UI"),**真 framework 的 arm64 boot image 至今不存在**(prepare-boot-workdir 列 framework.jar/adapter jars/boot.art 为 `<not recovered yet>`)。→ 问题实为"第一颗真 framework 从哪版起造",非"保 A15 vs 换 A14"。**定 A15**:保我方 A15 libart+libcore+boot,新起 stock A15(API35)framework.jar + BCP core,origin A14 补丁当 dexlib2 结构化配方按签名重打。排除(c)A14-fw 压 A15-libcore(最坏 skew,fix-art-libcore-mismatch.sh 佐证 libart↔core-oj/libart 是最硬耦合);备选(b)回 origin 全栈 A14 仅 A15 外源失败时回退。**理由**:保 M0 硬约束 + 避最坏 skew + 首屏承重补丁(§B PackageInfoBuilder 空 Bundle/§C Connectivity/§G ContentResolver/§D W9 vtable/§E W22 proxy-skip)全高可移植。**头号卡口=M2a**:编译机无任何真 BCP jar(任何版本都没有),须外源 stock A15 framework.jar + BCP core(优先 android-15_r10 同源保 libcore↔libart 配对);已派活取料。**移植注意**:§C ActivityThread hunk 不在 origin committed 集须自补;SystemServiceRegistry$88/$7 匿名类编号 A15 必不同按内容重认;W9/W22 撞墙再打勿预打。见 [[B-ROUTE-PLAN]] M2a。
- **2026-07-12** B 路作战计划成形(workflow 6-agent 调查 + thinker source 核验),脊柱落 `state/B-ROUTE-PLAN.md`(M0-M9,关键路径 M0→M1→M2→M3→M5→M7→M8→M9)。**M0 base 拍板**:**不 rebase 到 origin 的 art@814cc93/OAT230,复用我方已在 DAYU600 起 VM 的 v114 arm64 ART**(dc1d5e82,到 systemMain),boot image 我方 dex2oat 自建同源;origin 的 W 系/framework/smali 补丁当"该打哪些洞"的知识而非按行号硬套的 diff。**依据(source-verified)**:origin 那颗不可重建 blob(16e08711)的墙本质=render-path 墙(G3.0–G3.8 EGL/render saga,origin 持源都重建不出、会 regress 在 Thread::Init),而 render-path 正是我方 arm64 已独立打通并验证的最强资产(egl_interposer new-surface EGL fix = origin 丢失的那个修复;rs_abi_shims 解掉 origin 栽的 OH ABI 一致性)。**appspawn-x 源缺口判为可绕**:我方 launcher 已实现"加载 arm64 boot image→ActivityThread.systemMain",桌面 ondemand fork 非上屏阻塞(aa start 绕过)。**待解**:origin framework=AOSP-14 vs 我方 ART=AOSP-15 版本 skew,须实测定 framework/boot 组合(M0 收尾)。见 [[pivot-to-appspawn-x-arm64]] memory 命门更正。
- **2026-07-12** 用户拍板转 **路线 B**:松绑「不刷机/不碰 /system」约束,把出发点 A2OH 的 **appspawn-x AOSP-app adapter 移植/重编到 DAYU600 arm64(uis7885)**,目标=快出真 stock app(Material Catalog)真 UI,不再以 imageless embedded static ART 的窄硬-provenance 切片为主线。**触发**:用户发现出发点已在 32 位小板用 appspawn-x 显示完整 Material(经调查那是 origin 产物、非我方 imageless 输出;小板 085cac00 携 origin blob 16e08711),质疑 imageless 广度太窄(148 commit 仅 1075B 自造 color APK 红↔绿、render-only 绕 ViewRootImpl;#51/#53 未达;53 墙 38 未复核)。**复用判断(observed)**:B 路可直接接我方最硬资产=**arm64 图形上屏管道**(自建 arm64 libhwui + ohos_display_surface/oh_anativewindow_shim/egl_interposer/skia_interposer/rs_abi_shims,已 snapshot_display 证 DAYU600 面板红/绿满屏)——origin 自身图形桥(libhwui 8b8f84ec + bridge 2967c30c)是 32 位 RK3568 版搬不到 arm64,arm64 版正是我方已造并验证的那块;另复用 arm64 交叉编译链/A2OH 源码树/compiler 机 + DAYU600 实测拓扑知识。**作废**:imageless 特有绕行(#49 C++ 直设 bounds、forced-interpreter 配置、无-boot-image 补丁)随约束松绑淘汰。**B 路主干新工作(我方无现成)**=origin 的定制 libart+W 补丁、boot image(dex2oat64 AOT)、framework/adapter jars、smali 补丁、input/IME 桥 → 移植/重编到 arm64。**板约束变更(须用户逐板授权)**:B 路要刷 base image+写 /system/android,与 BOARDS.toml 现有 forbidden(wipe/flash/覆盖 substrate)直接冲突;执行破坏性刷机前用户须指定可动的 arm64 大板(5583/5ce/或另备板),小板 085cac00 的 blob 16e08711 仍绝不可擦。
- **2026-07-12** 用户明确指定 color APK 线完全使用 5ce2dcee，不再依赖/触碰 5583。授权边界收窄为 `/data/local/tmp/wl-color-codex` 私有 runtime/probe；原 substrate、framework.jar、reboot/wipe/flash 仍禁。W-004 完成只认 APK 自绘红→绿两帧 + `g214bb_raw_read_pixel` 回读 oracle PASS。
- **2026-07-11** 跨 lane 通信定为**星型 + 有界信号台账**,不重建白板、不设 cron。理由:lane 各在自己的 worktree/branch 上,**看不见彼此的未提交状态**,白板在物理上就传不了信;cron 轮询只会生产 AGENTS.md 合同 4 明令禁止的空转 commit。唯一活线 = dispatcher;共享只读事实 = `BOARDS.toml`(板锁) + `FRONTIER.md`(前沿) + 新增 `state/SYNC.md`(≤60 行,append-only,只记 CLAIM/RELEASE/ORACLE/BLOCKED/HAZARD/FACT 六类会影响别的 lane 的信号,不讨论)。lane 只写自己的 `tasks/doing/<卡>.md` + `evidence/<卡>/`,只读别人**已提交**的卡。
- **2026-07-11** `coding` sandbox 账号获仓库 ACL 写权(用户裁决,`sudo chmod +a`),范围仅 `state/` `evidence/` `tasks/doing/` `oracle/verify/`。**`.git` 不授权** → coding 不能 commit,W-001 的 dirty tree 因此受保护;commit 一律由 yao 会话执行。macOS ACL 不回溯继承,故既有文件(`tasks/doing/W-001.md`、`oracle/verify/atom-43.sh`)对 coding 仍只读 —— "禁改他人卡"由文件系统而非纪律强制。取代旧的"每次 `sudo -u yao` 写入"方案(会留下属主混杂/root 属主文件)。
- **2026-07-10** CLI 舰队复核取代下方 2026-07-09 的「CLI 舰队」「claude --bg」「派力工」三条旧结论：Kimi 走 managed `kimi-code/kimi-for-coding`，但无 sandbox/tool allowlist，只领无秘密/远程/设备权限的机械卡；主 `claude` 是 first-party Max，`--bg` 不自动建 worktree且因 transient daemon 仅审计使用；Codex 固定 Desktop binary + 显式 Sol xhigh/max。真实 binary/model/退化状态只认 `docs/reference/cli-fleet.md` 与 `oracle/verify/cli-fleet.py`，旧 403、Kimi/MiniMax route、裸 gpt-5.5 主力说法均已 superseded。
- **2026-07-10** 写入型 worker 并发必须一人一卡、一 branch、一 sibling worktree；canonical checkout 只给 thinker/dispatcher 管理 claim、板锁与验收。领卡状态先 commit，再从该 commit 建 `worker/<卡id>` worktree；worker 禁改 state，thinker 复跑 oracle 后合并并清锁。只读顾问可并行；共享 checkout 禁止并发 writer。
- **2026-07-10** W-001 oracle 纠错：旧 probe/白板把 `0x010100b0`(android:autoLink)当 windowActionBar；目标 APK 的 AppCompat attr 实为 `0x7f040691`，AppThemeBar 直接定义 false。旧 `uamHasWab=false` 不再作为双包断链证据。native `ck` 也必须在四参 `nativeSetApkAssets(...,ZZ)` 成功后才有效；oracle 锁 launcher/dex/so hash、清理结果和 run rc。
- **2026-07-10** 入口与事实域校准：独立接班会话默认 thinker，先跑只读 `dot_runner.py handoff`；worker 仍只读卡。live 进度/板锁认 state，版本/hash/setup/deploy 认 LOCK/PIPELINE，README/STATUS 明确为 DAYU200 legacy。结构 PASS 不等于 oracle PASS。
- **2026-07-10** `gpt-5.6-sol` 已用 Codex Desktop 内置 CLI 0.144.0-alpha.4 实跑只读 hello（model=sol, effort=max, exit 0），可作单问题顾问；默认 `max`，`ultra` 会自动派生任务，只在明确需要多 agent 时用。gpt-5.5 保留为旧 CLI fallback。
- **2026-07-10** Git 发布状态不冒充合并：当前开发分支未进 main/未完整推到同名远端；orphan 的 PiercingInfra delivery 独立管理。任何 agent 不自动 merge/push。
- **2026-07-09** 落盘重构：白板+长驻 agent 形态停用。改为「无状态 thinker(班次制) + 弱模型一次性 worker + 四板 lane」。状态全落 state/,session 用完即扔。理由:D→D2→D3 接班丢状态、九 agent 伪并行陪跑一道墙、弱模型在长上下文里放大短板。
- **2026-07-09** 仓库 /Users/yao/westlake-piercing → /Users/yao/Desktop/code/westlake-piercing,旧路径留软链。4 条 agent cron 注释停用(备份 ~/crontab-backup-2026-07-09.txt)。
- **2026-07-09** 撤掉 opus-dispatcher 层:派活是搬文件级动作,由 thinker 顺手做,不单设贵模型。dispatcher = 脚本。
- **2026-07-09** 协作拓扑定为星型:worker 之间零通信,一切过 thinker。mesh(白板互聊/接班)是上一版停滞主因。
- **2026-07-09** 事实债优先:42 个 claimed PASS 大多 V=?,#4 与 boot-image clinit 矛盾。复核先于扩张;claimed 未复核不得当事实。
- **2026-07-09** 采纳 A2OH-Factory 方法论:TECHNICAL_ATOM 标准当卡片 schema;Playbook 重试阶梯(≤5 次升级人工)当 worker harness;门先收敛到 2 道(oracle 命令 + 证据 hash),不上 G0-G7 八门。
- **2026-07-09** 采纳"oracle 路线"方法(Unity 显示综述):坏路线的每道墙,拿已跑通的路线当"本该长什么样"对照修(团结↔Unity;catalog/noice↔uptodown)。
- **2026-07-09** 暂不建总线/数据库。任务卡=一目录一文件 Markdown,dispatcher=shell loop。等 todo/ 堆到管不过来再谈结构化存储。
- **2026-07-09** 7 份架构/规格/方法论文档(桥接总量、167 原子看板+清单、Unity 分解/楼层/四路线、A1)落 `docs/reference/`,配 INDEX。定位=长期知识,按需定点取,勿整包读进上下文。Unity 线(D600-5bb5,HanBing)不进本仓库 state,仅存文档作 oracle/方法论参照。
- **2026-07-09** 电源不做遥控自动化(用户裁决)。板掉线 → board-recover.sh **bark** 用户手动断电重插+守着等回来重挂。bark 只推:里程碑/只有用户能解的硬墙/被明确要求;普通汇报不推(Stop hook 已自动推)。
- **2026-07-09** CLI 舰队逐条实测定格 → `docs/reference/cli-fleet.md`。要点:agent(-p --trust --mode ask --model fable/opus)=thinker 求助通道不当 worker;codex=稳定执行器(必须 </dev/null,守 stdin 会挂);kimi 本周期 403;mmx=内容 API 无文件工具不能领卡;claude=便宜力工(自家路由接 kimi/minimax 后端,settings.json 的 model 被路由重置,自报不可信)。thinker 型号不写死(用户在 Cursor UI 选)。
- **2026-07-09** 顾问咨询纪律:thinker 主攻;卡住才问 fable/opus。必须 `--workspace` 挂本仓 + prompt 给具体上下文(先读哪些文件/问题/已尝试/约束)。空问一句禁止。见 `protocol/THINKER.md` 与 `docs/reference/cli-fleet.md`。
- **2026-07-09** 证据纪律:只留对穿刺/复核有帮助的事实。推 GitHub/VPN/代理失败等基建噪音不进 `evidence/`。F-001 的 push 失败 log 已删;PASS 只记远程 SHA。见 `evidence/INDEX.md`。
- **2026-07-09** 墙↔规格对齐:`state/ATOM-MAP.md` 映射 #1–53 到 L{NN}.A{NN}(多为 related;AppCompat 等为 none 规格缺口)。进度事实源仍是 LEDGER;看板 `docs/dashboard/progress.html` 由 `oracle/refresh-dashboard.sh` 生成,力工刷板,禁止手改 HTML。big-clean=打墙;small=辅助调试。定稿 protocol/AGENTS chmod 只读。
- **2026-07-09** claude `--bg` 会在 `.claude/worktrees/` 建 git worktree(已 gitignore)。F-001 残留已 unlock+remove+prune。收工纪律写进 DISPATCH/cli-fleet。
- **2026-07-09** 派力工:**禁止** Cursor 内置 Task/subagent;只走本机 CLI。便宜重复活用 claude/kimi;**主力多用 `codex -m gpt-5.5 -c 'model_reasoning_effort="xhigh"'`**(实测通,可干活可顾问)。默认 `gpt-5.3-codex-spark` 偏笨且曾 tool 400,不派重活。见 `docs/reference/cli-fleet.md`。
- **2026-07-09/10** 重编译走中国 Linux 编译机；当前实测 SSH Host=`compiler`（旧 `huawei` 名称未配置）。embed `.so` 必须 `-nostdlib -nodefaultlibs`(裸 gcc 链 glibc → 板上 LD_PRELOAD 失败)。见 `docs/reference/host-build.md`。

## 战略裁决 (值不值得打)

- **A1 pthread operate-in-place**：技术三级 SOUND,但**不在 uptodown/Unity 上屏关键路径**(备用不启用)。防止在非关键路径过度投入。来源:A1 方法报告。
- **过早平台化**：明确规避。先把板子稳住+伪并行停掉+证据门钉死,三件都不需要写平台。
