W-004 / 5ce isolated color APK lane — WIP handoff — 2026-07-12

User direction:
- Use 5ce2dcee00000000000000000923012c exclusively for this lane.
- Do not depend on or touch 5583.

Observed 5ce baseline before writes:
- board online, aarch64, no probe/dalvik process found
- substrate ART  = 3ea7b69d07fb9591a1ac1420e4444b658d2cb6c83794f41f07d0bd5a858f6d09
- substrate dex  = 696dd3cb9f3d9c8739c06d01dc9065be2fd4cddf293e5539855731c7f3e61773
- substrate sidecar = a311b0ad63f12f85cd6433acae8a008017c260d23dd2674ce600b80200e0e848

Device isolation already created:
- private root: /data/local/tmp/wl-color-codex
- android/compat/expand/storage plus baseline apks/probes/art entries are read-only symlinks
- 53 symlinks created
- original /data/local/tmp/westlake-dayu600-substrate was not overwritten
- no new ART/probe/color run was deployed or executed before handoff

Independent host build:
- worktree: /Users/yao/Desktop/code/westlake-piercing.worktrees/W-003-color
- detached baseline: 2854df46 (W-003 clean-relink delivery)
- output: local-build-adapters/art-latest/build-ohos-arm64/lib/libart.so
- sha256: dc1d5e8239dc6fb4aeff6d70e21c12985fe7f85e6e02efcfa7f433cb3b257a83
- size: 28462512 bytes
- status: build/link PASS; artifact NOT deployed
- local-only build fix still dirty: stubs/dlmalloc.c Linux absolute include changed to the Mac AOSP path

Repo work added in canonical checkout:
- tasks/doing/W-004.md
- oracle/verify/color-smoke-5ce.sh

Oracle contract (not yet run):
WCOLOR_ART_SHA=<sha> WCOLOR_DEX_SHA=<sha> \
  oracle/verify/color-smoke-5ce.sh 5ce2dcee00000000000000000923012c

PASS requires all of:
- locked ART/dex/color APK hashes under /data/local/tmp/wl-color-codex
- color-smoke=ok and changed=yes
- pixel0=ffff0000 and pixel1=ff00ff00

Next executable work:
1. Create/checkout worker/color-smoke-5ce in W-003-color and make the dlmalloc include portable.
2. Add the colorSmoke probe stage that loads the APK-owned ColorView and re-records after nextColor().
3. Wire g214bb_raw_read_pixel through the renderer/JNI and write two frame pixels.
4. Build probe dex; deploy ART/dex/sidecar/color-smoke.apk only into the private root.
5. Run the W-004 oracle. Do not claim panel rendering before it prints PASS.

Claim boundary:
- W-003/#49 has a separate clean-relink oracle PASS on 5583 in worker commit 2854df46.
- This handoff does NOT claim R2, color recording, panel pixels, or W-004 PASS on 5ce.

================================================================================
SESSION UPDATE — 2026-07-12 (canonical checkout, branch dayu600-hwui-gate1-2)
Shared-infra portion of the W-004 front, done device-free. Still WIP; not PASS.

Progress against the "Next executable work" list above:
- [DONE] item 2 — colorApk probe stage. runColorApk() in Dayu600ApkStageProbe.java:
  loads com.westlake.colorapk.ColorView from color-smoke.apk's OWN dex (DexClassLoader,
  parent=probe loader so its superclass android.view.View resolves to the substrate View),
  unsafe-allocs it (mirrors firstFrame53's device-proven prep: skip View(Context)/measure,
  seed mColor=PALETTE[0]=red, wlEnsureRenderNodes, wlForceViewBounds), records red -> stable
  native RenderNode ptr -> nativeInit==2 -> frame0 push+read, then the APP's own nextColor()
  (red->green) -> re-record same root -> frame1 push+read. Routed via WESTLAKE_SUBSTAGE=colorApk.
  Commit b849d3eb.
- [DONE] item 3 — two-frame pixel readback, BUT wired via egl_interposer, NOT g214bb.
  DEVIATION (deliberate, an improvement): the readback is NOT hwui_oh_abi_patch's
  g214bb_raw_read_pixel (a main-thread glReadPixels — reads nothing because the EGL context
  is bound to the RenderThread, and its 0xAABBGGRR pack fails the red check). Instead
  egl_interposer.cpp captures the center pixel INSIDE eglSwapBuffers[WithDamageKHR] on the
  RenderThread (EGL-current, pre-swap = the only place the just-drawn frame is readable),
  packs ARGB = (A<<24)|(R<<16)|(G<<8)|B into volatile g_wl_last_swap_argb, and the renderer's
  new nativeLastSwapArgb() (JNI) hands it to the Java driver AFTER g_proxy->fence() makes the
  swap synchronous. ARGB pack verified against the oracle: red app-color 0xFFFF0000 -> px
  R,G,B,A=FF,00,00,FF -> 0xffff0000; green 0xFF00FF00 -> 00,FF,00,FF -> 0xff00ff00. Exactly
  the oracle's pixel0=ffff0000 / pixel1=ff00ff00. Commit b74e39d7.
- [DONE] item 4 (build half) — probe dex builds. build-probe-mac.sh: javac+d8 PASS,
  dayu600-apk-probe.dex 240664 bytes. Deploy half (into private root) still pending (no board).
- [BONUS] firstFrame53 hardened: "ok" now requires a real captured swap pixel
  (nativeLastSwapArgb>=0), not just ir==2. Kills the "JNI returned but nothing swapped"
  false positive. Commit 6707b283.
- [BONUS] color APK build determinism VERIFIED this session: build-color-apk.sh built TWICE,
  both sha256 = 082559aeb0e87bb9076a707e3f014938e78534fdbb6ec172c3211830275ae9c4 = the oracle's
  pinned hash. The oracle's hard APK-hash gate is de-risked; the on-device APK will match.

RESOLVED — .so builds device-free (earlier "blocker" was a wrong A2OH path, not missing source):
- The gfx-smoke build scripts default A2OH to /Users/yao/a2oh-source-audit, but THAT dir
  (created Jul 11) holds only ohos-sdk — a stale/partial copy. The COMPLETE AOSP+OHOS source
  tree is at /Users/yao/Desktop/code/a2oh-source-audit (all 10 referenced subtrees present:
  aosp-frameworks-base-15-r9/libs/hwui, aosp-15-headers, openharmony-third-party-skia,
  graphic_graphic_2d, graphic_graphic_surface, device-libs, ohos-6.0-headers, aosp-android-11,
  ohos-sdk, commonlibrary_c_utils). >>> Build with A2OH=/Users/yao/Desktop/code/a2oh-source-audit. <<<
- With that A2OH, build-upscreen-renderer.sh (after refreshing egl_interposer.o) VERIFIED:
    * ohos_display_surface.pic.o + rs_abi_shims.pic.o recompile clean (the transaction/
      rs_interfaces.h gap was the same wrong-A2OH artifact — gone with full source).
    * westlake_upscreen_renderer.cpp COMPILES against real hwui headers — the renderer B2
      edits (g_proxy->fence(); extern volatile g_wl_last_swap_argb/g_wl_swap_count;
      jlong nativeLastSwapArgb(); kMethods entry; sizeof count) are now COMPILE-verified,
      not merely code-reviewed.
    * libwestlake_upscreen_renderer.so LINKS: 2940176 bytes,
      sha256 a194f9e652070b22fa7e2f025583d244dac5792f266e4c5c716eeb83ffa679aa.
- .so symbol self-consistency (llvm-nm):
    * JNI_OnLoad = T (exported registrar); nativeLastSwapArgb defined (t, anon-namespace,
      registered by function pointer in kMethods).
    * g_wl_last_swap_argb + g_wl_swap_count = B (BSS, defined by egl_interposer.o, linked in);
      ZERO undefined g_wl_ symbols -> the renderer's extern refs resolve INSIDE the .so.
    * Remaining undefined = only RenderProxy/RenderNode methods incl. RenderProxy::fence()
      (my new call) — correct: they resolve from libhwui.so at runtime.
- Artifact is a gitignored build output (ports/dayu600/out/), rebuilt on the deploy host;
  provenance recorded here (WIP, device-未验). NOTE: still needs the #49 pointer-publish
  (W-003 2854df46) + R2 same-source libhwui folded into the SAME substrate before deploy.

Remaining path to W-004 PASS (unchanged gates, now with the wiring in place):
1. [.so BUILT this session — a194f9e6, 2940176B] libwestlake_upscreen_renderer.so already
   carries the egl_interposer capture + nativeLastSwapArgb. Rebuild on the deploy host with
   A2OH=/Users/yao/Desktop/code/a2oh-source-audit if needed. STILL REQUIRED before deploy:
   fold in the #49 pointer-publish (W-003 2854df46) + R2 same-source libhwui into the SAME
   substrate the .so links/loads against (the R2 "libhwui mapped once" gate is not yet proven).
2. Rebuild dayu600-apk-probe.dex (already reproducible here) and re-pin WCOLOR_DEX_SHA.
3. Deploy ART/dex/sidecar/.so/color-smoke.apk ONLY into /data/local/tmp/wl-color-codex on 5ce
   (WESTLAKE_ROOT=/data/local/tmp/wl-color-codex so apkPath() resolves color-smoke.apk there).
   Do NOT overwrite the original substrate.
4. Run the probe with WESTLAKE_STAGE=uptodownProbe WESTLAKE_SUBSTAGE=colorApk; it writes
   /data/local/tmp/color-smoke-{result,pixels}.txt.
5. WCOLOR_ART_SHA=<sha> WCOLOR_DEX_SHA=<sha> oracle/verify/color-smoke-5ce.sh 5ce2dcee...0923012c
   Do not claim panel rendering before it prints PASS.

Commits this session (canonical, branch dayu600-hwui-gate1-2):
  b74e39d7 pre-swap pixel readback (egl_interposer -> nativeLastSwapArgb)
  b849d3eb colorApk probe stage
  6707b283 firstFrame53 "ok" requires real swap pixel
  (earlier same line: 4ef98820 AndroidRuntime boot-gate, 6585f1c2 WestlakeUpscreen into build)
No board touched this session; no deploy; no oracle run.
