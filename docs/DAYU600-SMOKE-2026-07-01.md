# DAYU600 ART Smoke Test - 2026-07-01

This records the first non-mutating Westlake ART smoke test on the live HH-SCDAYU600 / uis7885 board.

## Target

- Board: HH-SCDAYU600 / DAYU600
- Hardware: `uis7885`
- OpenHarmony: `OpenHarmony-6.1.0.31`
- Kernel architecture: `aarch64`
- HDC target: `5ce2dcee00000000000000000923012c`
- HDC shell user during test: `uid=0(root)`
- SELinux: `Permissive`

Nothing was written to `/system`. Test files were staged only under:

```text
/data/local/tmp/westlake-dayu600
```

## Source Inputs

The test used A2OH source audit clones outside this repo:

```text
C:\Users\ufop\a2oh-source-audit\art-latest
C:\Users\ufop\a2oh-source-audit\art-universal
C:\Users\ufop\a2oh-source-audit\openharmony-arm64
```

Relevant source findings:

- `art-latest` contains `Makefile.ohos-arm64` and `Makefile.bionic-arm64`.
- `art-universal` contains an OHOS ARM64 runtime makefile and README describing static ARM64 `dalvikvm`.
- `openharmony-arm64` is a QEMU/minimal-initramfs environment, not a DAYU600 board integration.

## Tested Binaries

Both binaries are ELF64 AArch64 (`e_machine=0xB7`).

```text
C:\Users\ufop\a2oh-source-audit\art-latest\build-ohos-arm64\bin\dalvikvm
SHA256: 083D5F66A69BDB29D33A8A4F8E57FE8BB6185FFF803C4261C4CB2145790E263A
Size:   18145904

C:\Users\ufop\a2oh-source-audit\art-latest\build-bionic-arm64\bin\dalvikvm
SHA256: 36BB78C08C018E2E07CE39195ACC9C7E74CDE8102A085A75A2F4A7FB0D3F7474
Size:   25810216
```

The test class and boot jars came from `art-latest`:

```text
test-java/dyntest.dex
core-jars/core-oj.jar
core-jars/core-libart.jar
core-jars/core-icu4j.jar
core-jars/bouncycastle.jar
core-jars/apache-xml.jar
core-jars/okhttp.jar
```

## Commands

Staging:

```powershell
hdc shell "rm -rf /data/local/tmp/westlake-dayu600; mkdir -p /data/local/tmp/westlake-dayu600/art /data/local/tmp/westlake-dayu600/core-jars"
hdc file send C:\Users\ufop\a2oh-source-audit\art-latest\build-ohos-arm64\bin\dalvikvm /data/local/tmp/westlake-dayu600/art/dalvikvm
hdc file send C:\Users\ufop\a2oh-source-audit\art-latest\build-bionic-arm64\bin\dalvikvm /data/local/tmp/westlake-dayu600/art/dalvikvm-bionic
hdc shell "chmod 755 /data/local/tmp/westlake-dayu600/art/dalvikvm /data/local/tmp/westlake-dayu600/art/dalvikvm-bionic"
```

Runtime smoke:

```sh
cd /data/local/tmp/westlake-dayu600
timeout 5 ./art/dalvikvm -help
timeout 5 ./art/dalvikvm-bionic -help
timeout 5 ./art/dalvikvm -Xbootclasspath:./core-jars/core-oj.jar:./core-jars/core-libart.jar:./core-jars/core-icu4j.jar:./core-jars/bouncycastle.jar:./core-jars/apache-xml.jar:./core-jars/okhttp.jar -Xverify:none -classpath ./art/dyntest.dex DynTest
timeout 5 ./art/dalvikvm-bionic -Xbootclasspath:./core-jars/core-oj.jar:./core-jars/core-libart.jar:./core-jars/core-icu4j.jar:./core-jars/bouncycastle.jar:./core-jars/apache-xml.jar:./core-jars/okhttp.jar -Xverify:none -classpath ./art/dyntest.dex DynTest
```

## Results

Both `dalvikvm -help` commands succeeded on the DAYU600 board. This proves the staged AArch64 runtime binaries can execute on the stock uis7885 OpenHarmony userspace from `/data/local/tmp`.

The OHOS ARM64 runtime starts but times out before Java `main`:

```text
[RT] Runtime::Start() ENTERED
[icu_jni] NativeConverter registration done
[RT]   Loaded libicu_jni.so OK
[RT]   Loaded libjavacore.so OK
nullptr E ... java_vm_ext.cc:1068] Unexpected exception:
```

The bionic ARM64 runtime reaches a deeper point:

```text
[RT] Runtime::Start() ENTERED
[RT] InitNativeMethods done
[RT] JIT created successfully
[dalvikvm] Looking for class 'DynTest'
nullptr W ... Tolerating clinit failure for Ljava/io/File;: java.lang.StackOverflowError: stack size 8188KB
  at boolean java.lang.reflect.Field.isSynthetic() (Field.java:138)
```

## Interpretation

The bionic ARM64 build is currently the better DAYU600 bringup candidate despite the target being OpenHarmony/musl: it reaches runtime startup, native stub initialization, JIT creation, and class lookup before failing in a known Java/reflection initialization path.

The immediate failure to debug is not USB, flashing, fastboot, or appspawn. It is ART/core-library compatibility:

- OHOS build: `java_vm_ext.cc:1068 Unexpected exception` after `libopenjdk` native registration.
- Bionic build: `java.io.File.<clinit>` stack overflow through repeated `Field.isSynthetic()`.

## Next Step

Continue from `art-latest` rather than the old DAYU200 binary substrate:

1. Reproduce the bionic ARM64 `DynTest` failure locally or on-device with reduced logging.
2. Apply `patches/dayu600-art/0001-field-is-synthetic-native.patch` to A2OH `art-latest`.
3. Rebuild `build-bionic-arm64/bin/dalvikvm` or port the relevant patches to `Makefile.ohos-arm64`.
4. Keep testing from `/data/local/tmp/westlake-dayu600`; do not touch `/system/bin/appspawn`.

## Candidate Fix

The first candidate fix makes `java.lang.reflect.Field.isSynthetic()Z` side-effect free:

- `Runtime::Start()` rewrites `Field.isSynthetic()` to a native stub.
- `UnstartedRuntime::Jni()` gets a fallback handler for the same method.
- Both paths return the `ACC_SYNTHETIC` bit (`0x1000`) from the backing `ArtField`.

This is intended to cut the observed recursion:

```text
java.io.File.<clinit>
  -> java.lang.reflect.Field.isSynthetic()
  -> java.lang.reflect.Field.isSynthetic()
  -> StackOverflowError
```

The patch has been checked with `git apply --check` against a clean `A2OH/art-latest` clone. It has not yet been rebuilt into a new DAYU600-tested `dalvikvm` because this Windows/WSL machine is missing the AOSP/OHOS paths hard-coded by the current A2OH makefiles:

```text
/home/dspfac/aosp-android-11
/home/dspfac/openharmony
/home/dspfac/art-universal-build
/home/dspfac/android-to-openharmony-migration/dalvik-port/ohos-sysroot
```

## 2026-07-01 Update: Passing DynTest Smoke

After reconstructing the A2OH build inputs under WSL, a full `build-bionic-arm64`
rebuild produced a `dalvikvm` that still ran `-help` but crashed with `SIGBUS`
during native registration. The passing path is narrower:

- Keep the known-booting bionic ARM64 object cache.
- Replace only `runtime/mirror/class.o`, which contains the existing
  `CheckSetStatus()` identical-status no-op guard.
- Patch `core-oj.jar` so `java.lang.reflect.Field.isSynthetic()Z` returns
  `false` directly and does not re-enter reflection.
- Put `art/dyntest.dex` on `-Xbootclasspath`, avoiding the broken standalone
  `PathClassLoader` creation path during this bringup smoke.

Artifacts staged on the board:

```text
/data/local/tmp/westlake-dayu600/art/dalvikvm-pristine-plus-classfix
SHA256: A549054AC0A85C56E320AF9668D38AAFDEF96740EB28BDDA98CA9BED3C8B04B5
Size:   25808120

/data/local/tmp/westlake-dayu600/core-jars/core-oj-fieldfix.jar
SHA256: C62CA6A1E7FDA88A15A8D96FE5D13E258FE1BE1A1D75445B9896D293DA4C3D33
Size:   5836938
```

Host-side reproduction helpers:

```text
patches/dayu600-art/patch_core_oj_field_is_synthetic.py
libart-build/link_dayu600_pristine_classfix.sh
```

Successful command:

```sh
cd /data/local/tmp/westlake-dayu600
timeout 25 ./art/dalvikvm-pristine-plus-classfix \
  -Xbootclasspath:./core-jars/core-oj-fieldfix.jar:./core-jars/core-libart.jar:./core-jars/core-icu4j.jar:./core-jars/bouncycastle.jar:./core-jars/apache-xml.jar:./core-jars/okhttp.jar:./art/dyntest.dex \
  -Xverify:none \
  DynTest > classfix-coreoj-boot-dyntest.log 2>&1
echo rc:$?
```

Observed result on HH-SCDAYU600 / uis7885 over HDC:

```text
rc:0
[RT] Runtime::Start() ENTERED
[RT] InitNativeMethods entering
[RT] InitNativeMethods done
[RT] JIT created successfully
[dalvikvm] Looking for class 'DynTest'
[dalvikvm] Class found: 0x7fba83f009
[dalvikvm] main() method found: 0x7fb5c351a0
[dalvikvm] Calling main()...
[dalvikvm] main() returned (elapsed: 5 ms)
[dalvikvm] main() completed successfully
```

This is still a `/data/local/tmp` smoke test, not a system integration. It proves
that the DAYU600 board can run the patched A2OH ART runtime far enough to execute
a Java `main()` method without flashing, replacing `appspawn`, or modifying
system partitions.

## 2026-07-01 Update: Next Probe

A second probe was added under `test-fixtures/dayu600-art-probe/ProbeMain.java`
and compiled with the local Android SDK `d8`:

```text
C:\Users\ufop\AppData\Local\Android\Sdk\build-tools\36.0.0\d8.bat
```

The probe checks `Properties`, reflection, `Field.isSynthetic()`, thread
creation, and current-classloader lookup. It reports failures as an exit-code
bitmask so the result does not depend on Java-level stderr/file logging.

Bootclasspath mode:

```sh
cd /data/local/tmp/westlake-dayu600
timeout 25 ./art/dalvikvm-pristine-plus-classfix \
  -Xbootclasspath:./core-jars/core-oj-fieldfix.jar:./core-jars/core-libart.jar:./core-jars/core-icu4j.jar:./core-jars/bouncycastle.jar:./core-jars/apache-xml.jar:./core-jars/okhttp.jar:./art/probe.dex \
  -Xverify:none \
  ProbeMain > probe-bootclasspath-v5.log 2>&1
echo rc:$?
```

Observed result:

```text
rc:68
```

The bitmask means:

```text
4  field-isSynthetic missing the expected synthetic inner-class field
64 thread start/join threw
```

The thread failure is a real runtime stub gap:

```text
[InterpJni] void java.lang.Thread.nativeCreate(java.lang.Thread, long, boolean) shorty='VLJZ'
InterpreterJni: unhandled static shorty 'VLJZ' for void java.lang.Thread.nativeCreate(java.lang.Thread, long, boolean)
```

Normal classpath mode still fails before `ProbeMain` loads:

```text
Failed to create PathClassLoader
Unable to locate class 'ProbeMain'
```

So the next DAYU600 ART tasks are:

1. Implement or bypass `java.lang.Thread.nativeCreate(Thread,long,boolean)` for
   standalone smoke tests.
2. Fix standalone `PathClassLoader` creation so app dex can be loaded through
   `-classpath` instead of being appended to `-Xbootclasspath`.

## 2026-07-01 Update: Thread NativeCreate Fixed

`patches/dayu600-art/0002-interpreter-thread-nativecreate-vljz.patch` adds an
`InterpreterJni` dispatch arm for:

```text
java.lang.Thread.nativeCreate(Thread, long, boolean)
shorty: VLJZ
```

The patched object was relinked with the known-good pristine/classfix cache as:

```text
/data/local/tmp/westlake-dayu600/art/dalvikvm-pristine-plus-classfix-threadjni
SHA256: 4490B1853FE56629CD3111F75FFD1913026AEA16E675C23F4CA18BB2E4F33C7E
Size:   25835504
```

Probe result in bootclasspath mode:

```text
probe_rc:0
```

This confirms the standalone runtime can now create a Java thread, run its
`Runnable`, sleep briefly, and complete the probe without the previous
`unhandled static shorty 'VLJZ'` warning.

Normal classpath mode still fails at the same loader boundary:

```text
[dalvikvm] Looking for class 'ProbeMain'
Failed to create PathClassLoader
Unable to locate class 'ProbeMain'
```

The next remaining blocker is therefore standalone `PathClassLoader` creation.

## 2026-07-01 Update: Native PathClassLoader Fixed

`patches/dayu600-art/0003-dalvikvm-native-pathclassloader.patch` adds a
standalone `-classpath` loading path for the DAYU600 ART smoke runtime:

- open the `-classpath` dex entries with `ArtDexFileLoader`;
- keep the opened `DexFile` objects alive for the runtime process lifetime;
- create the loader through `ClassLinker::CreatePathClassLoader`;
- resolve `main(String[])` by scanning direct `ArtMethod`s before falling back
  to JNI `GetStaticMethodID`.

The cleaned runtime relinked from the known-good pristine/classfix cache was
staged on the board as:

```text
/data/local/tmp/westlake-dayu600/art/dalvikvm-pristine-plus-classfix-threadjni-pathloader-clean2
SHA256: 6907E5344B6EE618A7C62676522533250B9632BDAB6DAC9FA7296657A96AFBD6
Size:   25912248
```

The updated probe dex was rebuilt from
`test-fixtures/dayu600-art-probe/ProbeMain.java` and staged as:

```text
/data/local/tmp/westlake-dayu600/art/probe.dex
SHA256: D0A03CF09107BC48DB7E46AC08C00857B419679B1D2BDA79FDC3B286C5BA7354
```

Normal classpath mode now loads `ProbeMain` through the native PathClassLoader:

```sh
cd /data/local/tmp/westlake-dayu600
timeout 20 ./art/dalvikvm-pristine-plus-classfix-threadjni-pathloader-clean2 \
  -Xbootclasspath:./core-jars/core-oj-fieldfix.jar:./core-jars/core-libart.jar:./core-jars/core-icu4j.jar:./core-jars/bouncycastle.jar:./core-jars/apache-xml.jar:./core-jars/okhttp.jar \
  -Xverify:none \
  -classpath ./art/probe.dex \
  ProbeMain > probe-pathloader-clean2.log 2>&1
```

Observed log:

```text
[dalvikvm] Created native PathClassLoader for 1 dex files
[dalvikvm] Loaded class 'ProbeMain' via native PathClassLoader
[dalvikvm] main() returned (elapsed: 327 ms)
[dalvikvm] main() completed successfully
```

Bootclasspath mode also completes:

```text
[dalvikvm] main() returned (elapsed: 325 ms)
[dalvikvm] main() completed successfully
```

The probe was adjusted so `Field.isSynthetic()` verifies the method call on
whatever fields D8 emits instead of requiring a compiler-specific `this$0`
field. A failure still exits through `System.exit(failures)`; successful runs
return normally.
