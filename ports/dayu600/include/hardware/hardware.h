#pragma once

// Stub of libhardware's hardware/hardware.h for the OpenHarmony bringup
// build. The HAL_PIXEL_FORMAT_* constants hwui needs come from the AOSP
// libsystem system/graphics.h that is already on the include path.

#include <system/graphics.h>
