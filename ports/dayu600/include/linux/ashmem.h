#pragma once

// Minimal host-build compatibility header for OpenHarmony ashmem declarations.
// The DAYU600 smoke compile only needs class and method declarations, not
// ashmem ioctl values. A real OHOS sysroot should provide the platform header.
