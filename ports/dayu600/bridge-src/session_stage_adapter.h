#pragma once

// Legacy private adapter header missing from the public source copy.
// The DAYU600 port keeps this as a placeholder until the full bridge-build
// source tree is recovered or the unused V7 path is removed.
