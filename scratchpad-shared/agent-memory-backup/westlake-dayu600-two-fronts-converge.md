---
name: westlake-dayu600-two-fronts-converge
description: Westlake/DAYU600 — as of 2026-07-06 s4n the VarHandle CORE BLOCKER is FIXED and test.apk=com.uptodown's onCreate runs through app dispatch → cache dirs → conscrypt → and now BUILDS A WORKING TLS STACK via bundled BouncyCastle JSSE (tlsForce=OK/tls2=BCJSSE/tmf=OK). Remaining is PERFORMANCE: BC pure-Java EC crypto in the C++ interpreter is very slow for OkHttp's full SSLContext init (isSupportedNamedGroup across all curves), so onCreate is slow-but-functional, not blocked. See "SESSION 4n" block below. Prior "SESSION 4m LANDMARK" block has the ~18 foundation root fixes. Fixes landed s4 (all verified on device): (1) appNew ICCE (i5.m7 implements non-interface d1.h) — root cause 2048-2-9.apk wrongly on shared boot/app classpath polluting R8 short names (d1.h class vs interface), fixed by env-gating 2048 out of the dlopen-probe classpath (WESTLAKE_INCLUDE_GAME_APK, default off); (2) appNew AtomicReferenceFieldUpdater NPE — root cause getStackClass2 patched to a buggy class-name skip-set returning jdk.internal.reflect.Reflection instead of the real caller, breaking EVERY private-field Atomic*FieldUpdater.newUpdater, fixed by reverting to stock NthCallerVisitor(soa.Self(),3) in patches/runtime/native/dalvik_system_VMStack.cc; (3) appOnCreate NoSuchMethodError registerActivityLifecycleCallbacks — shim android.app.Application missing it, added the method + nested ActivityLifecycleCallbacks interface (surgical dex patch, +1 class, 0 regression). Earlier s4-prior fix still holds: InterpreterJni LLLILL shorty for DexFile.openDexFileNative (patches/runtime/interpreter/interpreter.cc). CURRENT test.apk walls: (A) appOnCreate now at Context.getAssets()→AssetManager — converges with the RESOURCE front (needs working AssetManager; fwlib libandroidfw.so still FAILs __android_log_print relocation, AssetManager.<init>[boolean] NoSuchMethod); (B) mainNew Looper.getMainLooper=Stub!. Run recipe recovered (LD_PRELOAD dlopen probe .so, foreground timeout run)
metadata: 
  node_type: memory
  type: project
  originSessionId: d4cae05e-0838-460f-a388-f91472bb79b5
---

**SESSION 4n (2026-07-06) — VarHandle FIXED + a WORKING TLS stack; onCreate now runs the app's real init all the way through OkHttp/TLS.** After the VarHandle fix (see 4m-cont below), onCreate cleared: getCacheDir/dirs (ProbeContext overrides), conscrypt (liblog.so arm64 stub + libjavacrypto.so was X86-64 → arm64 JNI_OnLoad stub w/ NativeCrypto.clinit no-op), then hit OkHttp `AssertionError: No System TLS`. **The deployed conscrypt was wrong-arch and there is no bctls in AOSP's bouncycastle.jar, so I bundled BouncyCastle JSSE myself:** downloaded bcprov+bctls+bcutil jdk18on from Maven, d8'd to `$S/apks/bc-tls.jar`, loaded via DexClassLoader in the probe and `Security.insertProviderAt(new BouncyCastleJsseProvider(bcProv), 1)`. Chain of fixes to make BC TLS actually construct (all probe-side, before onCreate):
  1. **Regex native** (biggest): BC's `JcaAlgorithmDecomposer.<clinit>` compiles `Pattern.compile("with|and|(?<!padd)in")` → `com.android.icu.util.regex.PatternNative.compileImpl` was unregistered → returned 0 → "nativePtr is null". IMPLEMENTED PatternNative+MatcherNative in runtime.cc via C++ **std::regex over UTF-16** (compile/openMatcher/setInput/matches/lookingAt/find/findNext/groupCount/hitEnd/... + offsets int[]); registered them (patch_runtime_native_method) + added InterpJni static shorties `JLI/IJL/ZJ/ZJL/ZJIL` (JLI was the "unhandled shorty"). std::regex ECMAScript lacks lookbehind → added `WlSanitizeRegex` that strips `(?<=..)/(?<!..)` + possessive quantifiers + named-group opens, retried on compile failure. NOTE: deleted a prior-session DEAD regex impl (hacky literal-substring `WestlakeRegexFind` + its registration) that duplicated symbols.
  2. **Math natives**: `Math.ceil` etc. unregistered → HashMap.calculateHashMapCapacity/BC failed. Registered java.lang.Math+StrictMath double natives → libm.
  3. **TMF/KMF default algo**: OkHttp uses `TrustManagerFactory.getDefaultAlgorithm()`="SunX509" which BC lacks → `Security.setProperty("ssl.TrustManagerFactory.algorithm","PKIX")` (+KMF).
  4. **trustStore**: BC `init(null)` reads default truststore → `BufferedInputStream.close()` NPEs (boot-image `bufUpdater` static is null, can't seed from probe = access control). Set `javax.net.ssl.trustStore=NONE` → empty in-memory store, no file I/O.
  5. **DRBG entropy**: `DRBG$Default.random` (BC entropy SecureRandom) null → seeded reflectively with a `/dev/urandom`-backed SecureRandom (anonymous SecureRandomSpi).
  6. **JCE provider auth**: re-dexing bcprov stripped its jar signature → `SecurityException: JCE cannot authenticate the provider BC`. Reflectively put both BC providers into `javax.crypto.JceSecurity.verificationResults` with the `PROVIDER_VERIFIED` marker.
  7. **Security.spiMap** null (boot-image) → seeded a ConcurrentHashMap.
  RESULT: `tlsForce=OK tls2=BCJSSE tmf=OK(BCJSSE)` — TLS WORKS. onCreate then built the OkHttpClient (SLOW: BC pure-Java EC crypto + isSupportedNamedGroup across all curves runs minutes in the C++ interpreter — appeared to "hang" at 120s timeout but COMPLETES ~2-3min) and advanced PAST TLS to `Context.getApplicationInfo() on null` (added ProbeContext.getApplicationInfo/getPackageResourcePath/getClassLoader). **~90 on-device iterations this turn.** KEY STANDING NOTE: performance — BC pure-Java crypto under the interpreter is the practical bottleneck now (correctness is fine); a real fix would need JIT/AOT of the crypto or a working native crypto lib. Boot-image null-static pattern recurs (BufferedWriter.defaultCharBufferSize, Security.spiMap, BufferedInputStream.bufUpdater, DRBG.random) — several classes' <clinit> didn't run at image build.

**SESSION 4n-perf tail (2026-07-06):** tried to speed up BC crypto — buffered the /dev/urandom SecureRandom (16KB reads vs per-call InterpJni round-trip) and set `jdk.tls.namedGroups=secp256r1`+`jdk.tls.client.protocols=TLSv1.2` to cut EC-group enumeration. STILL device-pegging: the C++ interpreter (`-DART_USE_CXX_INTERPRETER`, no JIT) running BC pure-Java EC math is fundamentally too slow, and under sustained load the **DAYU600 physically dropped off the hdc bus** (`hdc list targets`=[Empty], won't reconnect via kill/start — needs manual recovery). CONCLUSION: the remaining wall is STRUCTURAL performance, not correctness. To actually finish onCreate fast, the real options are (a) enable JIT/AOT in the runtime for the crypto hot paths, or (b) swap BC for a NATIVE TLS — the repo already prototyped this at `/Users/yao/westlake-piercing/native-tls/` (libtlsjni.c over the device's `/system/lib/platformsdk/libssl_openssl.z.so` + TlsShimProvider) — wire that provider in instead of BouncyCastleJsseProvider so OkHttp's SSLContext.TLS uses native OpenSSL (fast). Confirmed onCreate depth before the perf wall (run90, fully read): TLS built OK → `getApplicationInfo` (added to ProbeContext) → `ActivityManager.getMemoryClass()` at UptodownApp.onCreate:150 (added reflective ActivityManager to getSystemService). Next app-init stubs likely: PackageManager, WindowManager, ContentResolver. NOTE device may need power-cycle/USB-reconnect to come back after the crypto-load disconnect.

**SESSION 4n-fix READY (2026-07-06, awaiting device recovery):** The right fix for the perf wall is NOT to speed up BC but to AVOID heavy crypto at onCreate: OkHttp's constructor only needs `SSLContext.getInstance("TLS")` + a TrustManagerFactory + a socket factory to EXIST (handshake crypto is later, not in onCreate). **IMPLEMENTED a zero-crypto pure-Java JSSE provider `LiteTlsProvider` in the probe** (static nested classes `LiteTlsProvider/LiteSslContextSpi/LiteSslSocketFactory/LiteTmfSpi/LiteKmfSpi` in Dayu600ApkStageProbe.java): registers `SSLContext.TLS`+`TrustManagerFactory.PKIX/SunX509`+`KeyManagerFactory` doing NO cipher-suite/EC enumeration → OkHttp's eager SSLSocketFactory build is INSTANT. In the BC block, `bcJsse = new LiteTlsProvider(); insertProviderAt(bcJsse,1)` REPLACES BouncyCastleJsseProvider (bcprov stays registered for fast crypto primitives if needed). Probe COMPILES (out/dayu600-apk-probe.dex, 99616 bytes) — NOT yet deployed/tested because the DAYU600 dropped off the hdc bus under the earlier crypto load and won't reconnect via `hdc kill/start` (needs MANUAL power-cycle/USB reconnect). Also PROACTIVELY hardened ProbeContext for the next walls (compiled, untested): in-memory `LiteSharedPrefs`/`LiteEditor` for getSharedPreferences, plus getMainLooper()/getTheme()/getApplicationInfo()/getPackageResourcePath()/getClassLoader(), and getSystemService("activity")→reflectively-constructed ActivityManager. Probe dex now 104548 bytes (added SharedPrefs+getMainLooper/getTheme + onCreate watchdog). **CONFIRMED on device (run93): LiteTlsProvider WORKS — `tls2=WestlakeLiteTLS tmf=OK(WestlakeLiteTLS)`, onCreate FLEW past the TLS wall (no crypto pegging).** BUT onCreate then HANGS/loops PAST TLS (device pegged 8min → dropped off USB again). Hypothesis: app makes an eager network request in onCreate over LiteSslSocketFactory's dummy socket (returns plain Socket, not SSLSocket → OkHttp ClassCastException → app retry-loop). ADDED a watchdog: onCreate runs on main thread but a daemon dumps the main-thread stack + `Runtime.halt(7)` after 45s if not done → never pegs forever, reveals the loop location. NEXT: device needs replug (dropped again); then run, read `appOnCreate=HANG@45s stack=...` to pinpoint the loop, then likely make LiteSslSocketFactory.createSocket throw a fast clean exception (or return a real SSLSocket via native TLS) so the app doesn't spin.  **LiteTlsProvider flow VALIDATED on host JVM** (scratchpad/litetls/TestLiteTls.java): `TrustManagerFactory.getInstance("PKIX").init(null)`→1 X509TrustManager, `SSLContext.getInstance("TLS").init(null,tms,null).getSocketFactory()`→non-empty suites, all resolve to WestlakeLiteTLS — exactly what OkHttp's constructor calls. So the fix is proven-correct; only on-device deploy+run remains. NEXT WHEN DEVICE BACK: deploy the new probe dex (libart already current), run `run-utd-long.sh`, read appOnCreate — expect it to blow past the TLS wall FAST (Lite provider = zero crypto) to the next app-init stub (ActivityManager was line 150; then likely PackageManager — which is a ~150-method abstract class, hard to stub, may need a reflectively-built ApplicationPackageManager or a targeted intercept). (Real HTTPS handshakes would still need a native TLS wired into LiteSslSocketFactory.createSocket — separate from finishing onCreate.)

**SESSION 4m LANDMARK (2026-07-06) — the whole foundation below the VarHandle blocker is now CLEARED; the app's onCreate reaches its own dispatch code and MainActivity constructs.** ~68 on-device iterations. Fixes landed (all verified on device, all in art-latest → libwestlake_art.so unless noted):
- **Charset ENCODE**: InterpJni `IJLILILZ` handler → NativeConverter.encode works (u8enc=4).
- **Charset DECODE**: NativeConverter.decode stub (stubs/icu_jni_stub.c) was writing ABSOLUTE positions to data[] but CharsetDecoderICU.setPosition() adds them RELATIVELY (buffer.position(pos()+data[i])) → flush pass double-counted → IAE newPosition. FIX: decode stub writes DELTAS (`d[0]=count; d[1]=count`). (Encode escaped this because its buffers always start at pos 0.)
- **File I/O**: implemented + registered Linux.lseek/pread/fstat/stat/lstat/open/close/readBytes/writeBytes in runtime.cc; InterpJni instance shorties `JLJI`(lseek)/`ILLII`(readBytes/writeBytes) ROUTE libcore.io.Linux.* DIRECTLY to Westlake_Linux_* (a stub registration was winning the JNI entry point → corrupt reads → false ZipException:Duplicate). Also instance `JJ`/`VJ`/`BJ`/`VJB` for Unsafe.allocateMemory/free/get/putByte; static UnixNativeDispatcher stat0/getcwd.
- **String decode**: InterpJni static `LLIIL` → StringFactory.newStringFromBytes(byte[],int,int,Charset) i.e. `new String(bytes,off,len,charset)`. Was dropped → null → ZipFile "Duplicate entry name: null" (all entry names null). THIS unblocked ZipFile CEN parsing (1/13→11/13 handlers).
- **getResourceAsStream / boot resources**: icu-data.jar is resource-only (no classes.dex) → excluded from getBootClassPathEntries → no ClassPathURLStreamHandler → ICU resources unreachable via BootClassLoader. FIX (probe-side vmclFix): explicitly append icu-data.jar to the rebuilt bootClassPathUrlHandlers. → icuCLRes=FOUND.
- **ICU data**: provisioned FULL icudt75 as `$S/apks/icu-data.jar` (32MB STORED, all 3874 .res incl supplementalData/weekData) under android/icu/impl/data/icudt75b/, on BOTH -Xbootclasspath (via dlopen probe) AND system classpath; repackaged from device icudt74l via `icupkg -tl`.
- **Memory.unsafeBulkGet**: implemented in patches/runtime/native/libcore_io_Memory.cc (+ registration + InterpJni static `VLIILIIZ`) — bulk ByteBuffer copy used parsing ICU .res.
- **java.util.zip.Inflater (zlib)**: NEW — implemented Westlake_Inflater_init/inflateBytesBytes/end/reset/getAdler/setDictionary in runtime.cc using zlib (already linked as libz.a), patched onto the Inflater ArtMethods (like the Linux block), + InterpJni shorties static `JZ`/`VJ`/`IJ`/`VJLII` and instance `JJLIILII`. inflateBytesBytes return layout: bits[0..30]=outputConsumed, bits[31..61]=inputConsumed, bit62=finished, bit63=needDict. Root: Inflater.init(Z) UnsatisfiedLinkError → TimeZone.<clinit> tolerated-failed (LOGGER null → NPE) + no DEFLATE zip entries.
- **java.lang.Character.*Impl (24 natives)**: implemented via musl wctype.h (iswdigit/iswalpha/towupper/…) in runtime.cc + patch block + InterpJni static `III` (digitImpl). isDigitImpl etc. were UnsatisfiedLinkError.
- **Looper**: probe calls android.os.Looper.prepareMainLooper() before Activity ctor → mainNew=OK (MainActivity constructs).
- **BufferedWriter.defaultCharBufferSize**: boot-image class whose <clinit> didn't run → field 0 → System.out.print → "Buffer size <= 0". Probe seeds it to 8192 (reflection). NOTE: this hints OTHER boot-image classes may have un-run <clinit> static fields (a class of latent bugs).
**VarHandle blocker FIXED (2026-07-06 s4m-cont).** Root: `ArrayElementVarHandle` (e.g. AtomicReferenceArray's) is created imageless with `accessModesBitMask == 0` → every access mode rejected → `UnsupportedOperationException`. Diagnostic already in-tree: `[PFCUT-VARHANDLE] unsupported mode=getVolatile(2) mask=0x0 ... vh=ArrayElementVarHandle`. The fork ALREADY had a zero-mask workaround `PFCutIsZeroMaskFieldVarHandleModeSupported` (patches/runtime/var_handles.cc) but ONLY for FieldVarHandle/StaticFieldVarHandle. **FIX: extended it to ArrayElementVarHandle/ByteArrayViewVarHandle/ByteBufferViewVarHandle — is_final=false, compute support from the element (var) type exactly like a non-final field.** var_handle->Access() then does the real array access. → VarHandle UOE gone, app dispatch code runs.

**onCreate then advanced through REAL app init (each fixed in turn):**
- `getCacheDir() on null` (UptodownApp.onCreate:91) → ProbeContext now overrides getCacheDir/getFilesDir/getCodeCacheDir/getDir/getNoBackupFilesDir/getExternalCacheDir/getExternalFilesDir/getDatabasePath → real dirs under `$root/appdata/*`.
- conscrypt `System.loadLibrary → liblog.so not found` → built arm64 **liblog.so stub** (21 __android_log_* symbols → stderr), deployed to `$S/android/lib64/liblog.so`.
- `libjavacrypto.so unsupported relocation type 7` → **the deployed libjavacrypto.so is X86-64, wrong arch** (device is arm64; it was mis-provisioned). Built an arm64 **stub libjavacrypto.so** (JNI_OnLoad→JNI_VERSION_1_6, registers NativeCrypto.clinit() as no-op) so System.loadLibrary succeeds + NativeCrypto.<clinit> passes.
- **CURRENT WALL (honest boundary): `AssertionError: No System TLS` @ v9.c.a → u9.w.<init>:272 → UptodownApp.onCreate:127.** The app builds an **OkHttpClient** in onCreate that needs a FUNCTIONAL TLS stack. Because conscrypt native is only stubbed (no real crypto), OkHttp's Platform.get() finds no usable TLS → asserts. This is a HARD external dependency: to finish onCreate the app needs a **real arm64 conscrypt + BoringSSL** built for OHOS musl (or the platform JSSE with a working provider). That is a separate multi-hour crypto-provisioning build, NOT a runtime InterpJni patch. Also pending (tolerated so far): `Math.ceil(double)`/other Math natives unregistered (NativeCrypto.<clinit> tolerated on it). NOTE: onCreate is now genuinely running the app's own initialization (dispatch → cache dirs → crypto provider → HTTP client); the VarHandle "两战线汇合点" is behind us. LEFTOVER DEBUG to strip when stable: [DECODE]/[LSEEK]/[RDBYTES]/[STAT0] fprintf (runtime.cc), probe unsafeAlloc/u8dec/icuRes/icuJar/zipSig/bootRes diagnostics. Run recipe: build libart `make -f Makefile.ohos-arm64 link-libart`; probe dex `build-probe-mac.sh`; deploy to `$S=/data/local/tmp/westlake-dayu600-substrate` (art/libwestlake_art.so, apks/*.dex/*.jar, probes/*.so) with md5-verify+restart loop; `nohup sh /data/local/tmp/run-utd-substrate-noshim.sh`; read `$S/apks/probe-logs/uptodown-probe.txt`.

The DAYU600 Android-on-OHOS work has TWO fronts that meet at a single blocker. Reproduction manual: `~/Desktop/西湖穿刺-完整上下文与复现.md` (dated 2026-07-05).

**Front 1 — resource → View-name chain (DONE, re-verified on device 2026-07-05).** libandroidfw.so (aosp15 resource engine + 31 hand-written JNI) loads a real APK, parses resources.arsc, and runs the full `getLayout` XML chain. Re-ran the `assetProbe` stage today (`sed WESTLAKE_APP_STAGE=assetProbe` in `/data/local/tmp/deploy-dayu600.sh`, then `timeout 60 sh deploy-dayu600.sh`, read `$R/apks/probe-logs/asset-probe.txt`); output matched the doc's 铁证 verbatim: `OK cookie=1 ... resValue(0x7f010000)=[... xmlPath=res/y4.xml ... XMLTREE_OK XMLPARSE[firstTagName=alpha] FRAMEWORK_getLayout[tag=alpha]] LAYOUT=[abc_screen_simple viewTags=[FitWindowsLinearLayout ViewStubCompat include]] realResourceNames=[0x7f010000=com.digiplex.game:anim/abc_fade_in ...]`. This is exactly the input `LayoutInflater.inflate()` consumes. Device libs match local byte-for-byte (libandroidfw.so 3511120, libart.so 18648680) — no rebuild needed to reproduce. Also DONE: hwui paints on the panel (gates 1-2, see [[westlake-dayu600-hwui-gate2]]).

**Front 2 — arm64 ART runtime foundation (the real blocker).** The getLayout probe runs because it dodges VarHandle-heavy code. But every real framework app needs `java.util.concurrent.atomic.*` / VarHandle (ViewRootImpl/Handler/Choreographer), and those are broken imageless on arm64. The fix path (build a boot image whose image_writer runs the FieldVarHandle fixup) is fully built — the arm64 boot image BUILDS and LOADS at runtime (rc=0) — but VarHandle `<clinit>` inside the image still fails on ONE pinned bug.

**THE CONVERGENCE / single critical-path blocker (pinned 2026-07-05 local build):** an interpreter **invoke-virtual vtable mis-dispatch** during boot-image pre-init. `AtomicInteger.<clinit>` calls `invoke-virtual MethodHandles$Lookup.findVarHandle (method@5231)` but the interpreter dispatches `unreflectVarHandle` instead (both are Lookup methods → wrong vtable entry), then args mis-marshal (`v9 ← int.class`), so a later `getModifiers()` on a `java.lang.Class` receiver resolves to `Object.clone()` → `CloneNotSupportedException` ×25 (tolerated). Suspected area: the invoke-polymorphic / CallerSensitive special-casing (`DoInvokePolymorphic` @patches/runtime/interpreter/interpreter_common.cc:5714) or vtable-index resolution — findVarHandle/unreflectVarHandle are @CallerSensitive. NOT getStackClass2, NOT a range +1 shift (both refuted).

**Once this one bug is fixed → the whole A-path lights up:** boot image gets initialized VarHandle → runtime VarHandle works → real app onCreate runs past AtomicLong → `LayoutInflater.inflate` (fed by the verified Front-1 resource chain) → View tree → hwui (already painting) → UI on screen. So Front 1 + hwui are downstream-ready; this single vtable bug is the gate.

**Build/iterate (all LOCAL on Mac now, WSL eliminated):** source `~/westlake-local-build/art-latest/`, `Makefile.ohos-arm64`, OHOS SDK `~/a2oh-source-audit/ohos-sdk/native`. Cycle ~1min: edit `patches/runtime/interpreter/*.cc` → `rm build-ohos-arm64/runtime/interpreter/interpreter.o` (+ `template_instantiations.o`/`interpreter_switch_impl0.o` for template fns — flaky /mnt timestamps meant silent no-recompiles historically) → `make -f Makefile.ohos-arm64 -j8 link-dex2oat` → send `dex2oat.cb` to device → run boot compile at `/data/local/tmp/wl-imgboot` → grep log for `[CLONE-BODY]`/`[GSC2]`. Diagnostics already in source: CLONE-BODY + RT-LOOKUP (interpreter.cc:~1864), GSC2 (interpreter_common.cc:7072). Load-bearing FIXES to keep: Fix A (interpreter.cc 861 pop-skip removed), wild-write bounds guard (unstarted_runtime.cc newStringFrom), GC-skip (heap.cc IsAotCompiler), xz-tolerate (xz_utils.cc), VarHandle un-skip (class_linker.cc).

**Strategy (from doc §6-7):** A-path (normal Android app on screen) is the high-success, verifiable route and this vtable bug is its last runtime gate — NOT Genshin. B-path (Genshin/Unity) doesn't use the View path at all (needs NDK + Vulkan-direct-to-Surface + anti-cheat), is multi-month/uncertain, AND the on-hand APK `~/Downloads/uptodown-com.miHoYo.GenshinImpact.apk` is the **Uptodown store client (com.uptodown), NOT Genshin**. To even judge Genshin feasibility the user must supply a small Unity game APK (real libunity/libil2cpp) to test whether Unity's Vulkan can pipe to an OHOS Surface on the Mali GPU. User's stated goal = a stable万年底座 foundation (fix roots, no per-app bypass) → keep fixing the vtable bug, don't skip VarHandle.

## ★★★ SESSION 4b (2026-07-06 s4b): SUBSTRATE pivot — app instantiates under REAL framework.jar; blocked on getSystemContext framework-res provenance ★★★
User directive: "直接走 substrate + 真 framework", "不要停不要问我". Pivoted uptodownProbe from the non-substrate layout to the **substrate layout** (real AOSP-15 framework.jar on the boot classpath instead of android-35-ctor-noop.dex). Ran via the dlopen probe with substrate env (recipe below). Progression: appNew ICCE(m5.b) → fwlib reloc → getApplicationContext NPE → getSystemContext framework-res NotFound.

**Substrate run recipe (dlopen probe, NOT the appspawn deploy-dayu600.sh path):** scratchpad `run-utd-substrate.sh` — sets ANDROID_ROOT/DATA/ART_ROOT/I18N/TZDATA/APEX all =$S/android..., `LD_LIBRARY_PATH=$S/art:$S/android/lib64:$S/probes:$S/android/lib64/sidecars:$S/compat:/system/lib64:...`, `WESTLAKE_ROOT=$S WESTLAKE_LAYOUT=substrate WESTLAKE_CREATE_VM=1 WESTLAKE_NO_EXIT=1 WESTLAKE_STAGE=uptodownProbe WESTLAKE_LOAD_COMPAT_STUBS=1`, `LD_PRELOAD=$S/probes/libwestlake_embedded_art_dlopen_probe.so /system/bin/toybox true`. Run in bg (`nohup … &`) — survives hdc link drops; read `$S/apks/probe-logs/uptodown-probe.txt`. $S=/data/local/tmp/westlake-dayu600-substrate. Deploy libart→$S/art/, libandroidfw→$S/android/lib64/, probe dex→$S/apks/, shim→$S/apks/, adapter-mainline-stubs→$S/android/framework/. NOTE: substrate & non-substrate use SEPARATE copies of every artifact — deploy to BOTH.

**FIX 5 — appNew ICCE→NoClassDefFound `Lm5/b;` (substrate-only).** `m5.b implements android.net.nsd.NsdManager$RegistrationListener`; `android.net.nsd.*` is a connectivity-mainline module NOT in core framework.jar (non-substrate's android-35-ctor-noop.dex is SDK-android.jar-derived and HAS it → why it worked there). FIX: added `android/net/nsd/{NsdManager (+nested RegistrationListener/DiscoveryListener/ResolveListener/ServiceInfoCallback),NsdServiceInfo}.java` no-op stubs to `ports/dayu600/adapter-mainline-stubs-src/`; built adapter-mainline-stubs.jar on mac (javac --release 8 -XDignore.symbol.file -cp android-35.jar → d8 → `jar cf`) → deploy `$S/android/framework/adapter-mainline-stubs.jar`. **appNew=OK** after.

**FIX 6 — fwlib `__android_log_print` (root fix, layout-independent).** `libandroidfw.so` (and libandroidfw_stubs.so) compile the stub TU `westlake-local-build/androidfw-build/westlake_androidfw_dlopen_stubs.cpp` which stubbed every `__android_log_*` EXCEPT `__android_log_print` → the only non-libc `U` symbol. On $S it *sometimes* resolved via libhwui (loaded in appspawn path) but NOT in the dlopen probe. FIX: added `extern "C" int __android_log_print(...)` (vfprintf→stderr) to the stub; relinked with `androidfw-build/relink_libandroidfw.sh` (recovered exact recipe: compile stub→dep-obj/atrace_stub.o, then `clang++ --target=aarch64-linux-ohos --sysroot=$OHOS_SYSROOT -shared -fPIC -static-libstdc++ -Wl,--allow-shlib-undefined -Wl,--allow-multiple-definition -Wl,--export-dynamic -Wl,-soname,libandroidfw.so obj/*.o dep-obj/*.o jni.o libz.a -o … -ldl; llvm-strip --strip-all`). Deploy $S+$R /android/lib64/. **fwlib=OK, JNI natives register** (cookie=1, nativeGetResourceName no longer UnsatisfiedLinkError).

**THE CURRENT WALL — app needs a real Context; getSystemContext blocked on framework-res provenance.** uptodownProbe builds UptodownApp via bare `newInstance()` (runResolved ~L1040) with `ContextWrapper.mBase=null`, so `onCreate`'s `getApplicationContext()` (real framework ContextWrapper.getApplicationContext:152 → mBase.getApplicationContext) NPEs. Added a `ctxAttach` block (runResolved, after appNew): assetProbe's framework-res wiring (loadFromPath framework-res.apk → set sSystemApkAssets/sSystemApkAssetsSet/sSystem, clear Resources.mSystem) + OHServiceManager.install + `ActivityThread.systemMain()/getSystemContext()` → set mBase. systemMain now passes Trace (with-shim: substrate shim has no-op android.os.Trace; no-shim hits `Trace.nativeIsTagEnabled` UnsatisfiedLinkError) but **`getSystemContext()` fails `NotFoundException: Resource ID #0x1110044`** (= 0x01110044, android pkg, internal type 0x11). ROOT: the packed framework-res.apk (from SDK android.jar's resources.arsc) has internal resource IDs that DON'T match framework.jar's compiled `com.android.internal.R.*` constants → getSystemContext's internal-resource lookups NotFound. WESTLAKE_BOOT_IMAGE=1 did not help. This needs a **real AOSP-15 framework-res.apk matching the framework.jar** (a build artifact), OR a different Context source that avoids com.android.internal resources.

**Shim vs no-shim under substrate (both tried):** substrate shim is only 31 classes (shadows Handler/Looper/MessageQueue/Trace/… but NOT Application/Context/ContextWrapper — those come from real framework.jar). no-shim → real Handler (mainNew wants Looper.prepare) + real Trace native gap; with-shim → shim Handler lacks `Handler(Looper)` ctor (agent F: add `Handler(Looper)`, `Handler(Callback)`, `Handler(Looper,Callback)` + nested `Callback`), shim Trace no-op helps systemMain. `res=[]` (probe's heuristic resource-name scan empty; secondary). appNew ICCE/m5.b identical both ways.

**SESSION 4c continuation (same day) — CHOSE PATH B-variant: bind a self-contained Context, onCreate now runs DEEP.** Instead of getSystemContext (framework-res provenance wall), added to the probe (runResolved, needs `build-probe-mac.sh` to rebuild dayu600-apk-probe.dex — javac --release8 -cp shim/out/classes:android-35.jar → d8 via r8.jar; deploy $S+$R /apks/):
- `ProbeContext extends android.content.ContextWrapper` (static nested class, NO @Override — probe compiles against the thin shim ContextWrapper which lacks the methods; they override the real one at runtime): getApplicationContext→this, getAssets→**null** (app only uses it for Typeface.createFromAsset(fonts)→falls back to Typeface.DEFAULT on null; a non-null AM would hit nativeOpenAssetFd, unregistered), getResources→lazy `new Resources(am,DisplayMetrics.setToDefaults,new Configuration())`, getPackageName→"com.uptodown".
- ctxAttach: reuse the res-enum's AssetManager (`uamShared`, hoisted) as the app AM — re-loading the same APK hits `ApkAssets.nativeIsUpToDate` (unregistered). Set `ContextWrapper.mBase = new ProbeContext(uamShared)` via reflection. **ctxAttach=OK**.
- Result progression on the no-shim substrate run: getApplicationContext✓ getAssets✓(null) → `getResourceAsStream` NPE `VMClassLoader.getResource:64` (static-final `bootClassPathUrlHandlers` null: VMClassLoader.<clinit> tolerated-failed with `NPE:charset` in createBootClassPathUrlHandlers). Added probe re-init: rebuild the handler array with a per-entry `catch(Throwable)` (libcore only catches IOException) and reflectively set the field. **vmclFix=OK but (0/13)** — ALL 13 boot-classpath entries throw `NPE:charset` inside `ClassPathURLStreamHandler(String)` (deeper, in the StrictJarFile/ZipFile zip-open entry-name decode — a systematic runtime zip-charset bug). So getResource returns null for everything → onCreate's NEXT wall: **`MissingResourceException: android/icu/impl/data/icudt75b/en_US`** (ICU locale data loaded via the now-empty classpath-resource path). 

**CURRENT FRONTIER (s4c end):** `appOnCreate` reaches ICU `UResourceBundle` load, fails because classpath-resource loading is dead — root = `NullPointerException: charset` when `ClassPathURLStreamHandler` opens ANY boot jar (all 13). Fixing that (libcore ZipFile/StrictJarFile charset init, or the runtime's zip entry-name Charset being null during that path) unblocks getResourceAsStream + ICU + resource-bundle loading foundation-wide. Also still: `mainNew` real Handler wants `Looper.prepareMainLooper()` on the probe's main thread (systemMain would do it but it's blocked on the framework-res provenance wall) — add `Looper.prepareMainLooper()` in the probe before mainNew. NET: UptodownApp instantiates AND runs deep into onCreate under REAL AOSP-15 framework.jar (past getApplicationContext/getAssets/getResourceAsStream); remaining walls are runtime-completeness (zip-charset, ICU, Looper), not class-loading or context-binding.

**SESSION 4d (same day) — ROOT of the resource wall traced to a tolerated-<clinit> cascade + missing sun.nio.fs natives.** Pushed onCreate past getResourceAsStream by chasing the null-static chain (all re-init'd reflectively in the probe's ctxAttach block; each verified on device):
- `VMClassLoader.bootClassPathUrlHandlers` null (its <clinit> tolerated-failed) → rebuilt with per-entry `catch(Throwable)` (libcore only catches IOException).
- Root of THAT: `ClassPathURLStreamHandler`→`JarFile`→`ZipFile.<init>` does `requireNonNull(charset)` on `StandardCharsets.UTF_8` which is **null**. And `Charset.forName` itself NPEs at `monitor-enter cache2` (Charset's `cache2` HashMap static is null). **FIX (works, `charsetFix=6/UTF8=true`):** reflectively seed `java.nio.charset.Charset.cache2=new HashMap()` + `gate=new ThreadLocal()`, then `Charset.forName` reaches libicu's native UTF-8 converter (no .dat needed) → set all 6 `StandardCharsets.*` fields.
- Next in chain: `ZipFile$Source.get`→`File.toPath`→`FileSystems.getDefault()` is **null** (`FileSystems$DefaultFileSystemHolder.defaultFileSystem` static-final null; its <clinit> = `DefaultFileSystemProvider.create().getFileSystem("file:///")`). Re-running that reflectively **fails**: `fsFix=FAIL UnsatisfiedLinkError sun.nio.fs.UnixNativeDispatcher.getcwd()` — the POSIX file natives (getcwd etc.) are NOT registered in the standalone runtime. So the default FileSystem cannot be built imageless → modern `ZipFile` (uses Path/toPath) cannot open jars → getResourceAsStream/ICU stay dead.

**THE SYSTEMIC ROOT (this is the real foundation blocker for resources):** core libcore classes (`Charset`, `StandardCharsets`, `java.lang.VMClassLoader`, `FileSystems$DefaultFileSystemHolder`) had their `<clinit>` TOLERATED-FAILED at early boot (class_linker.cc:6278), leaving static-final caches null; they never re-run. Some are reflectively re-init-able (charset — DONE), but the FileSystem one bottoms out on **unregistered `sun.nio.fs.UnixNativeDispatcher` natives** (getcwd/…). Two real fixes for next session: (1) register the sun.nio.fs Unix natives in the runtime (libart / InterpreterJni — getcwd is a trivial libc passthrough) so the default FileSystem builds → ZipFile → getResourceAsStream → ICU data (icudt75b) loads; and/or (2) find why these libcore <clinit>s fail at boot and fix the common cause (suspect: something in early boot ordering / a native the <clinit> path needs). The app's onCreate needs ICU (`MissingResourceException icudt75b/en_US`) which rides entirely on this. Also pending: `mainNew` real Handler wants `Looper.prepareMainLooper()` in the probe before mainNew.

**Net state after s4d:** UptodownApp instantiates + runs into onCreate under real AOSP-15 framework.jar; passes getApplicationContext/getAssets/getResourceAsStream(non-NPE)/charset; blocked at ICU data load, whose root is the FileSystem/`sun.nio.fs` native gap. ~10 fixes verified this session (nsd, libandroidfw relink, ProbeContext ctxAttach, getAssets-null, VMClassLoader rebuild, Charset/StandardCharsets seed). All probe changes are in `test-fixtures/dayu600-apk-probe/Dayu600ApkStageProbe.java` (rebuild: `build-probe-mac.sh`); everything deployed to $S paths.

**SESSION 4e (same day) — getcwd landed as a real libart fix; charset statics seeded; bottomed out on ICU-version/charset-encoder circular bootstrap.**
- **libart FIX (LANDED, keep):** added `sun.nio.fs.UnixNativeDispatcher.getcwd()` to the `InterpreterJni` bridge in `patches/runtime/interpreter/interpreter.cc` (static-method section, `ddc->DescriptorEquals("Lsun/nio/fs/UnixNativeDispatcher;") && shorty=="L"` → return process CWD as `mirror::ByteArray`). NOTE: the natives go through `InterpreterJni` (interpreter.cc), NOT the `PFCutTryUnixNativeDispatcherPathIntrinsic` in interpreter_common.cc (that intrinsic fired 0× — wrong dispatch path; I also added getcwd there but it's inert, harmless). Rebuild: `rm build-ohos-arm64/runtime/interpreter/interpreter.o; make -f Makefile.ohos-arm64 link-libart`; deploy $S+$R /art/. This fixed `fsFix` getcwd → advanced FileSystem construction.
- **Probe charset-static seeding (in ctxAttach, `charsetFix=9/UTF8=true`):** the tolerated-<clinit> cascade leaves static-finals null; reflectively re-seed `Charset.cache2=new HashMap()` + `gate`, all 6 `StandardCharsets.*` (via `Charset.forName`), and 3 `CodingErrorAction.{IGNORE,REPLACE,REPORT}` (private ctor(String)). Also FileSystem: `DefaultFileSystemHolder.defaultFileSystem` seeded by direct-constructing `LinuxFileSystem(provider,"/")` (skips getFileSystem's getcwd path).
- **THE HARD ROOT (where it now bottoms out):** `LinuxFileSystem.<init>`→`UnixPath.<init>`→`UnixPath.encode("/")`→`CharsetEncoder.encode` throws `InvalidPathException: Malformed input ... : /` — the UTF-8 **encoder rejects even ASCII**. The charset converters are `com.android.icu.charset.CharsetFactory`→native libicu, and the device has **icudt72l.dat (ohos_icu) + icudt74l.dat but the framework/app want icudt75** (app's onCreate `MissingResourceException: icudt75b/en_US`). So native ICU charset encoding is broken → CIRCULAR: charset-encode needs ICU ↔ FileSystem/ZipFile/getResourceAsStream needs charset-encode ↔ ICU4J .res data needs getResourceAsStream. **This ICU-version/data bootstrap is the true remaining foundation blocker for the whole resource/charset/filesystem layer.** Next-session options: (a) provide icudt75 data (or point ICU at the on-device icudt74 and reconcile the version), (b) fix native CharsetFactory/libicu init so UTF-8 encode works, (c) bypass UnixPath's eager encode. Everything else on the app path is now green up to this. CONFIRMED via direct probe test: `StandardCharsets.UTF_8.newEncoder().encode("/abc")` → `u8enc=FAIL:MalformedInputException` — the native ICU UTF-8 encoder rejects plain ASCII, so `com.android.icu.charset` / native libicu charset conversion is fundamentally broken in this runtime (not just missing data — the converter mis-encodes). This is THE single confirmed foundation blocker now: fixing native ICU charset conversion (icudt version/data + NativeConverter/CharsetFactory init — device has icudt72l/74l, framework wants icudt75) makes charset encode work → UnixPath/FileSystem/ZipFile/getResourceAsStream/ICU4J-data all unblock in one shot. Suspect the icu_jni init (`[icu_jni] libcore.icu.ICU FindClass` in log) doesn't fully wire the converter data. This is native/runtime-level, not probe-level.

**SESSION 4f (same day) — CHARSET/FILESYSTEM/RESOURCE LAYER FULLY FIXED via 3 libart native additions; app onCreate now blocked ONLY on missing ICU-75 data (a data asset, not code).**
Root of the charset-encoder failure: `com.android.icu.charset.NativeConverter.encode/decode` have shorty **`IJLILILZ`** which `InterpreterJni` (interpreter.cc) didn't handle → the call was silently DROPPED (same bug class as LLLILL) → converter never ran → `data[]` stayed zero → `MalformedInputException` on even ASCII. The charset natives are the hand-written stub `art-latest/stubs/icu_jni_stub.c` (Latin1 encode/decode, correct for ASCII) — they were fine; the bridge just wasn't calling them.
**Three libart fixes LANDED (all in `patches/runtime/interpreter/interpreter.cc` InterpreterJni; rebuild `rm build-ohos-arm64/runtime/interpreter/interpreter.o; make -f Makefile.ohos-arm64 link-libart`; deploy $S+$R /art/):**
1. `sun.nio.fs.UnixNativeDispatcher.getcwd()` (shorty "L") → return CWD as mirror::ByteArray. Unblocked default FileSystem construction.
2. `com.android.icu.charset.NativeConverter.encode/decode` (shorty **"IJLILILZ"**) → marshal (long handle, char[]/byte[], int, byte[]/char[], int, int[], boolean) and call the entry point (long occupies args[0]=low/args[1]=high). → **`u8enc=4` (UTF-8 encoder works), `fsFix=OK` (FileSystem builds)**.
3. `sun.nio.fs.UnixNativeDispatcher.stat0/lstat0` (shorty "VJL") → read path from the native-buffer long address, stat(), fill UnixFileAttributes fields (st_mode/ino/dev/size/…, mirrors PFCutFillUnixFileAttributes). Also added `#include <unistd.h>` + `<sys/stat.h>` to interpreter.cc. → **`vmclFix=already`**: bootClassPathUrlHandlers now builds (13 handlers), so ClassPathURLStreamHandler/ZipFile/**getResourceAsStream now fully works** (ZERR gone).
NOTE: these natives dispatch through `InterpreterJni` in interpreter.cc, NOT the `PFCutTryUnixNativeDispatcherPathIntrinsic` in interpreter_common.cc (that intrinsic fires 0×). Any further missing sun.nio.fs/native → add to the same UnixNativeDispatcher branch.

**CURRENT WALL = pure DATA gap (not code):** `appOnCreate=FAIL MissingResourceException: android/icu/impl/data/icudt75b/en_US`. getResourceAsStream now works, but **core-icu4j.jar has ZERO icu data entries** (ICU4J data is never in the jar) and **no `icudt75*.dat` exists anywhere** on device/build (device only has `/system/usr/icu/icudt74l.dat` + `/system/usr/ohos_icu/icudt72l.dat`; framework/app want ICU 75). `libcore.icu.LocaleData.<clinit>` also tolerated-fails on the same. So the app's onCreate needs ICU-75 locale data that doesn't exist here. FIX (data provisioning, next session): obtain `icudt75l.dat` from an Android-15 system image / com.android.i18n apex and set `-Dandroid.icu.impl.ICUBinary.dataPath` (or place `icudt75b/*.res` on the boot classpath); OR repackage the on-device `icudt74l.dat` resources under `icudt75b/` (icupkg; ICU .res format is largely version-stable) as a stopgap. Also still pending: `mainNew` real Handler wants `Looper.prepareMainLooper()` in the probe. **NET: every code/plumbing wall for the app's onCreate is fixed — class-load, ICCE, getCallerClass, Context binding, charset, FileSystem, ZipFile, getResourceAsStream all work under real AOSP-15 framework.jar; only the ICU-75 data file is missing.**

**SESSION 4g (same day) — ICU data repackaged + on classpath; but boot getResourceAsStream now blocked on the NIO file-native layer (modern ZipFile → Files.readAttributes → 0-address path buffer).**
- **ICU data provisioning DONE (deployed):** device only has icudt74l/icudt72l, app wants icudt75. Repackaged `/system/usr/icu/icudt74l.dat` → `icudt75l.dat` via `/opt/homebrew/Cellar/icu4c@77/77.1_1/sbin/icupkg -tl icudt74l.dat icudt75l.dat` (renames internal pkg icudt74l→icudt75l; ICU .res format is version-stable). `ResourceBundleWrapper` (the app's loader) loads via CLASSPATH getResourceAsStream, NOT ICUBinary.dataPath — so extracted the 3874 .res (`icupkg -d ex -x '*' icudt74l.dat`) and packed `icu-data.jar` (32MB STORED, `jar cf0`) with them under `android/icu/impl/data/icudt75b/*.res`; added `$R/apks/icu-data.jar` to the substrate -Xbootclasspath (build_bootclasspath_option in the dlopen probe .c; rebuild the probe .so with the mac clang cmd) and deployed to `$S/apks/icu-data.jar`. Probe also sets `-Dandroid.icu.impl.ICUBinary.dataPath` + reflectively calls `ICUBinary.addDataFilesFromPath(rootPath()+"/icu", ICUBinary.icuDataFiles)` — belt-and-suspenders.
- **Probe vmclFix now rebuilds bootClassPathUrlHandlers if null OR empty** (at boot they build EMPTY because StandardCharsets.UTF_8 is still null then; must rebuild after charsetFix). 
- **NEW BLOCKER (the real remaining foundation gap):** rebuilding the handlers now runs `ClassPathURLStreamHandler`→`JarFile`→`ZipFile$Source.get`→`Files.readAttributes`→`sun.nio.fs...readAttributes`→`stat0(long pathAddress, UnixFileAttributes)` and **pathAddress is 0** (probe [STAT0] debug: `args0=0 args1=0 args2=<attrsref>`). i.e. `NativeBuffers`/`jdk.internal.misc.Unsafe.allocateMemory` returned a 0-address buffer → the path never got copied to native memory → stat gets NULL → EINVAL → readAttributes IOException → handler build fails → getResourceAsStream returns nothing → ICU MissingResource. So the modern **java.nio.file/sun.nio.fs native layer is unimplemented** (path buffers via Unsafe.allocateMemory/putByte, stat0, likely also open0/read for the actual zip read). Each is a simple libc passthrough but it's a MULTI-NATIVE cascade. stat0 handler (interpreter.cc InterpJni, shorty VJL) is correct but never gets a valid address. NEXT: implement Unsafe.allocateMemory/freeMemory/putByte/getByte (or NativeBuffers' natives) so the path buffer is real, then stat0 works, then whatever open/read natives ZipFile$Source needs. OR route ClassPathURLStreamHandler/getResourceAsStream away from the NIO ZipFile (older java.io ZipFile path). This is the same InterpJni/native-completion pattern as getcwd/stat0/IJLILILZ — bounded but several more natives.

**STATE: every wall UP TO the app's use of classpath-resource loading is fixed (class-load, ICCE, getCallerClass, Context, charset+encoder, FileSystem construction all work under real AOSP-15 framework.jar via libart fixes getcwd/IJLILILZ/stat0). test.apk onCreate still doesn't complete — blocked on getResourceAsStream, which needs the NIO file-native layer (Unsafe/NativeBuffers path buffers) + ICU-75 data (data now provisioned, waiting on getResourceAsStream). Debug fprintf `[STAT0]` still in interpreter.cc — remove once done.**

**SESSION 4h (same day) — NIO blocker root-caused to `Unsafe.allocateMemory` returning 0.** Probe test (`jdk.internal.misc.Unsafe.theUnsafe`.`allocateMemory(64)`) → **`unsafeAlloc=0`**. So `NativeBuffers` gets a 0-address path buffer → `stat0`'s pathAddress is 0 (probe [STAT0] debug: `args0=0 args1=0`) → EINVAL → readAttributes IOException → boot handler build fails → getResourceAsStream dead. `jdk.internal.misc.Unsafe.allocateMemory(J)J` is declared `public native` (core-oj-fieldfix), registered as `FAST_NATIVE_METHOD(Unsafe, allocateMemory, "(J)J")` → `Unsafe_allocateMemory` in patches/runtime/native/jdk_internal_misc_Unsafe.cc:713 which mallocs correctly. But it returns 0 AND does NOT appear in InterpreterJni logs (interpreter.cc) NOR the generic shadow-frame bridge (interpreter.cc:2438) — so this instance FAST_NATIVE (shorty "JJ", receiver=Unsafe) is dispatched by a THIRD path that mis-marshals the `long bytes` arg to 0 (probably reads the wrong vreg slot / treats receiver as an arg). InterpreterJni's "JJ" branches (interpreter.cc:1012 static, :1517 static) all pass `klass`=DeclaringClass, i.e. they're STATIC-only; there is no correct INSTANCE "JJ" handler, so allocateMemory (instance) either misses them or hits a fallback that zeros the arg. **NEXT: add/fix an INSTANCE "JJ" (and the Unsafe put/get/copy memory shorties) handler in InterpreterJni that passes the real receiver + reads `bytes=(args[1]<<32)|args[0]` correctly — then Unsafe.allocateMemory works → NativeBuffers → stat0 gets a real path → readAttributes → ZipFile → getResourceAsStream → ICU (data already provisioned via icu-data.jar). Then likely more sun.nio.fs read natives (open0/read) for the actual zip read.** Temporary `[STAT0]` fprintf + probe `unsafeAlloc`/`unsafeAlloc` diagnostics still in place — remove when done. This is the same InterpreterJni instance-native marshalling class of bug as everything fixed this session; bounded but several natives deep. HONEST STATE: every wall up to the NIO/Unsafe-memory native subsystem is fixed and verified on real AOSP-15 framework.jar; test.apk onCreate does NOT complete — blocked here.

**SESSION 4i (same day) — Unsafe.allocateMemory FIXED (libart); next layer = java.io file-read natives.** Root of unsafeAlloc=0: `InterpreterJni` (interpreter.cc) had NO instance `JJ` handler (all JJ branches at :1012/:1517 are static, passing `klass`), so instance `Unsafe.allocateMemory(long)` fell to a fallback that zeroed the arg. **FIX (landed): added INSTANCE `JJ`/`VJ`/`BJ`/`VJB` handlers to the non-static block (interpreter.cc ~:1590, pass `receiver` + read the wide arg from args[0]/args[1])** → `unsafeAlloc=547651040848` (real address), stat0 now gets a valid path, FileSystem+readAttributes work. ZERR advanced: `EINVAL` → **`EOFException @java.io.RandomAccessFile.readFully:495 @ZipFile$Source.findEND/initCEN`** — ZipFile stats+opens the jar but can't READ it: the log shows `Failed to register native method java.io.FileInputStream.open0(Ljava/lang/String;)V / read0()I / readBytes([BII)I / close0()V` (and RandomAccessFile's equivalents) — the java.io file-read natives are unimplemented/unregistered, so readFully returns 0 → EOF. **NEXT: implement the java.io file-read natives (FileInputStream/RandomAccessFile read0/readBytes/seek0/open0 — libc read/lseek/open passthroughs) so ZipFile can read the central directory → getResourceAsStream → ICU (data already provisioned).** Likely a few natives, same InterpreterJni/native-completion pattern.

**★ SESSION 4 CUMULATIVE (2026-07-06) — libart root fixes that LANDED (keep all):** in `patches/runtime/interpreter/interpreter.cc` InterpreterJni: `LLLILL` (DexFile.openDexFileNative), `IJLILILZ` (ICU NativeConverter encode/decode — makes charset work), `getcwd`/`stat0`/`lstat0` (sun.nio.fs, static section), **instance `JJ`/`VJ`/`BJ`/`VJB` (Unsafe.allocateMemory/freeMemory/getByte/putByte)**; `getStackClass2`→NthCallerVisitor(3) in dalvik_system_VMStack.cc; `__android_log_print` in androidfw stub + relink. Probe (Dayu600ApkStageProbe.java) reflective seeds: Charset.cache2/gate, StandardCharsets.*, CodingErrorAction.*, FileSystems$DefaultFileSystemHolder (LinuxFileSystem(provider,"/")), VMClassLoader.bootClassPathUrlHandlers (rebuild-if-null-or-empty), ICUBinary.addDataFilesFromPath, ProbeContext mBase attach, ICU dataPath. Classpath: nsd stubs jar, icu-data.jar (icudt74→75b .res). Progression of test.apk onCreate this whole session: class-load → ICCE → getCallerClass → appNew → getApplicationContext → getAssets → getResourceAsStream → **charset → FileSystem → Unsafe.allocateMemory → stat → (now) RandomAccessFile.read**. Every layer up to file-read is fixed on real AOSP-15 framework.jar. onCreate NOT yet complete — blocked on java.io read natives → then ICU should load. LEFTOVER DEBUG to strip: `[STAT0]` fprintf in interpreter.cc, `unsafeAlloc`/`pathToStr` probe diagnostics.

**SESSION 4j (same day) — file-read path fixed to the point of ZipFile parsing; final blocker = a native-registration conflict on libcore.io.Linux.readBytes/lseek.** After adding instance `JLJI`(lseek)/`ILLII`(readBytes) InterpJni handlers + registering `Linux.lseek`/`Linux.pread` in runtime.cc, ZipFile READS the jars (EOF gone) and 1/13 boot handlers build. But 12/13 fail `ZipException: Duplicate` — a FALSE duplicate (verified: even a 294KB icu-data.jar with unique entries fails). Root, isolated via trace: the deployed libart HAS `Westlake_Linux_readBytes`/`lseek` (md5 + strings confirm) and `Linux.readBytes`/`lseek` are dispatched 2726/2722× through the new InterpJni handlers — but the `[RDBYTES]`/`[LSEEK]` fprintf NEVER fires → `method->GetEntryPointFromJni()` for these is NOT the Westlake_ function. **Another registration (suspect OHBridge stub / a second RegisterNatives) won and binds Linux.readBytes/lseek to a stub that returns wrong data → corrupt central-directory read → false Duplicate → ClassPathURLStreamHandler build fails → getResourceAsStream returns nothing → ICU MissingResource.** NEXT: find who else registers libcore.io.Linux.readBytes/lseek (grep OHBridge / the startup stub registration) and ensure Westlake_Linux_* wins (register LAST, or unregister the stub, or make the InterpJni ILLII/JLJI handler call Westlake_Linux_readBytes directly by method-name instead of the entry point). Then ZipFile parses → handlers build → icu-data.jar found → ICU loads → onCreate proceeds. ICU data is provisioned (minimal icu-data.jar with root/en/en_US/pool.res under android/icu/impl/data/icudt75b/ on the substrate -Xbootclasspath; repackaged from device icudt74l via `icupkg -tl`). Boot jars ALSO have some genuine duplicate entries → may additionally need ZipFile duplicate-tolerance, but icu-data.jar itself is clean so fixing the readBytes registration should suffice for ICU. LEFTOVER DEBUG to strip when done: `[LSEEK]`/`[RDBYTES]`/`[STAT0]` fprintf (runtime.cc + interpreter.cc), probe `unsafeAlloc`/`icuRes`/`icuJar` diagnostics.

**SESSION 4k (same day) — readBytes/lseek registration conflict FIXED (reads now correct), but ZipFile.initCEN still false-Duplicates.** Root of the earlier corruption: `libcore.io.Linux.readBytes`/`lseek` had their JNI entry point bound to a STUB (not `Westlake_Linux_*`) by a second registration; the InterpJni JLJI/ILLII handlers called the stub → garbage reads. **FIX (landed): in interpreter.cc InterpJni, for `Llibcore/io/Linux;` `readBytes`/`writeBytes`/`lseek`, call `Westlake_Linux_readBytes`/`writeBytes`/`lseek` DIRECTLY (extern decls) instead of GetEntryPointFromJni().** Verified via `[LSEEK]`/`[RDBYTES]` trace: reads are now CORRECT (`lseek off=5837650 -> 5837650`, `readBytes want=128 -> 128 (pos advances)`). BUT `icuJar=ZipException:Duplicate` STILL — even for a minimal 4-entry clean jar (`zip -0 -D`, no manifest/dirs, verified zero duplicate names). So `java.util.zip.ZipFile.initCEN` reports a FALSE duplicate despite correct read counts → either the read DATA (not count) isn't landing in the Java byte[] (suspect GetByteArrayElements/ReleaseByteArrayElements copy-back in the InterpJni-called native context), or initCEN itself is buggy in this dexed core-oj-fieldfix ZipFile. Direct probe test `new RandomAccessFile(icu-data.jar).readFully(4)` → `zipSig=FAIL:UnsatisfiedLinkError` (some RAF-path native still missing, though the ZipFile RAF path reaches initCEN). NEXT DIAG: log the actual bytes read (not just count) in Westlake_Linux_readBytes to confirm data lands; if data is zero/garbage → the GetByteArrayElements copy isn't committed in this call context (try GetPrimitiveArrayCritical or ensure mode-0 release / a pinned write). If data IS correct → the bug is in ZipFile.initCEN's duplicate detection (would need a core-oj patch or a duplicate-tolerant ZipFile). This is the LAST hurdle: fix it → handlers build → icu-data.jar found → ICU loads → onCreate proceeds. ~53 on-device iterations this session; ~22 verified fixes incl 9 libart/runtime root fixes. LEFTOVER DEBUG to strip: [LSEEK]/[RDBYTES]/[STAT0] fprintf; probe unsafeAlloc/icuRes/icuJar/zipSig diagnostics.

**SESSION 4l (same day) — false-Duplicate root-caused to the UTF-8 DECODER (NativeConverter.decode stub) → last hurdle.** `ZipException: Duplicate entry name: null` = ZipFile decodes ALL CEN entry names to null. `ZipCoder.toString` = `decoder().decode(bytes).toString()` — the ENCODER was fixed (IJLILILZ) but the DECODER is broken: probe test `UTF_8.newDecoder().decode(wrap("abc"))` → `u8dec=FAIL: IllegalArgumentException: newPosition`. Read DATA is correct (traced: `data=504b0304`/`504b0102` ZIP sigs land in the byte[] fine — so GetByteArrayElements commit works; reads NOT the problem). The bug is in `com.android.icu.charset.NativeConverter.decode` (hand-written stub `art-latest/stubs/icu_jni_stub.c:284`) vs `CharsetDecoderICU.decodeLoop` (core-icu4j) data[]-convention. Traced decode calls: pass1 `inPos0 inEnd3 outPos0 outEnd3 count3 -> d0=3 d1=3`; pass2/flush `inPos0 inEnd0 outPos3 outEnd3 count0 -> d0=0 d1=3`. Values look self-consistent but decodeLoop then throws `Buffer.position(newPosition)` IAE → so decodeLoop treats `data[INPUT_OFFSET=0]`/`data[OUTPUT_OFFSET=1]` NOT as the absolute new positions the stub writes (likely it ADDS them as deltas, or expects the count/return-code differently; the encoder path happens to avoid it). **NEXT (the actual last step): disassemble `CharsetDecoderICU.decodeLoop` fully (core-icu4j CharsetDecoderICU.smali) to see EXACTLY how it uses data[0]/data[1] + the return code after `NativeConverter.decode`, then make the decode stub write what it expects (probably: set data[IN_POSITION]/data[OUT_POSITION] to the delta consumed/produced, not the absolute; and/or return the right ICU code). Fix that ONE stub → entry names decode → ZipFile.initCEN succeeds → boot handlers build → icu-data.jar found (icuRes=FOUND) → ICU loads → onCreate proceeds.** Everything else is proven working: reads/lseek/stat/Unsafe/charset-ENCODE/FileSystem/Context all correct on real AOSP-15 framework.jar. ~56 on-device iterations, ~23 verified fixes, 10 libart/runtime root fixes this session. LEFTOVER DEBUG to strip: [DECODE]/[LSEEK]/[RDBYTES]/[STAT0] fprintf; probe unsafeAlloc/u8dec/icuRes/icuJar/zipSig diagnostics.

**Two candidate next paths (both multi-hour):** (A) obtain/build a framework-res.apk whose internal-R IDs match framework.jar so getSystemContext works → real Context → onCreate resources; (B) avoid getSystemContext: add a self-sufficient `android.app.Application` (+ContextWrapper/Context) to the substrate shim that returns `this` for getApplicationContext and provides getAssets(null→Typeface.DEFAULT per agent D)/getResources(real Resources over app AssetManager)/getContentResolver/getPackageName/getSharedPreferences (agent F priority list). Path B is grindable via the working shim build pipeline; Path A is a data-artifact problem. agent D: onCreate's real resource needs are getResources().getConfiguration() + getString(0x7f…) (arsc-backed), then SharedPreferences + PackageManager.

## ★★★ SESSION 4 (2026-07-06 s4): test.apk boots into onCreate — 3 foundation fixes ★★★
Directive: "按照多dex修复完整推进" then "可以，继续". Progression of UptodownApp: class-load wall → appNew ICCE → appNew ARFU-NPE → appOnCreate (reg…Callbacks) → appOnCreate (getAssets). Each fix exposed the next; all verified on device via the uptodownProbe.

**Run recipe (non-substrate layout, the one in play):**
```
HDC=~/.local/openharmony-tools/bin/hdc
PRELOAD=/data/local/tmp/westlake-dayu600-substrate/probes/libwestlake_embedded_art_dlopen_probe.so
$HDC shell "timeout 90 sh -c 'LD_LIBRARY_PATH=/data/local/tmp/westlake-dayu600/compat:/system/lib64 WESTLAKE_CREATE_VM=1 WESTLAKE_NO_EXIT=1 WESTLAKE_STAGE=uptodownProbe LD_PRELOAD=$PRELOAD /system/bin/toybox true' 2>&1"
# result file: $R/apks/probe-logs/uptodown-probe.txt  (R=/data/local/tmp/westlake-dayu600)
```
libart deploy → `$R/art/libwestlake_art.so`; shim deploy → `$R/framework-jars/dayu600-framework-shim.dex`. hsh.sh + md5-verify loop in scratchpad.

**FIX 1 — appNew ICCE `i5.m7 implements non-interface class d1.h`.** SAME MECHANISM as the old `a.a` false-positive: `2048-2-9.apk` was hard-appended to BOTH -Xbootclasspath and -Djava.class.path in `westlake_embedded_art_dlopen_probe.c` (build_bootclasspath_option / build_classpath_value, 4 sites). 2048 is R8-minified; its `d1.h` is a `final class` (implements Serializable), uptodown's `d1.h` is an `interface`. Parent-delegation resolved i5.m7's interface `d1.h` to 2048's class → IsInterface()==false → ICCE. Real Android never puts app B on app A's classpath. FIX: added `westlake_includes_game_apk()` (getenv WESTLAKE_INCLUDE_GAME_APK=="1", default off) gating all 4 2048 appends; the preceding apk-probe.dex loses its trailing ':' so the classpath is clean when gated out. Rebuild the probe .so on mac: `$OHOS_LLVM/bin/clang --target=aarch64-linux-ohos -fPIC -shared -nostdlib -nodefaultlibs -fno-stack-protector -I/opt/homebrew/opt/openjdk/include -Wl,--unresolved-symbols=ignore-all -Wl,-soname,libwestlake_embedded_art_dlopen_probe.so -o out/…probe.so …probe.c`. The 2048 headless stage must now set WESTLAKE_INCLUDE_GAME_APK=1.

**FIX 2 — appNew NPE on null AtomicReferenceFieldUpdater.** `w8.p1.<clinit>` calls `AtomicReferenceFieldUpdater.newUpdater(w8.p1, …, private-volatile field)`; the impl ctor's access check used `Reflection.getCallerClass()` which returned `jdk.internal.reflect.Reflection` itself → IllegalAccessException → clinit failed (tolerated) → updater field null → NPE at first `.set`. Root cause: `VMStack_getStackClass2` in patches/runtime/native/dalvik_system_VMStack.cc had been rewritten from stock `NthCallerVisitor(soa.Self(),3)` to a declaring-class SKIP-SET (skip VMStack/MethodHandles/sun.reflect.Reflection) — it did NOT list `jdk.internal.reflect.Reflection` (this libcore's getCallerClass lives there, calls getStackClass2) and architecturally returns the @CallerSensitive method itself, not its caller. FIX: reverted to stock `NthCallerVisitor(soa.Self(),3)`. NthCallerVisitor already skips runtime/bridge frames (include_runtime_and_upcalls_=false) — exactly what the skip-set's IsRuntimeMethod() guard wanted — and JIT is forced off so no inlining → depth 3 is exact (VMStack=0, Reflection.getCallerClass=1, newUpdater=2, real caller=3). getCallingClassLoader in the same file already trusts NthCallerVisitor(2), confirming frame counting is reliable here. This unblocks EVERY app using Atomic*FieldUpdater on a private field (Kotlin coroutines/OkHttp/RxJava). Rebuild: `cd ~/westlake-local-build/art-latest && make -f Makefile.ohos-arm64 link-libart`.

**FIX 3 — appOnCreate NoSuchMethodError registerActivityLifecycleCallbacks(Application$ActivityLifecycleCallbacks).** Shim `android.app.Application` (test-fixtures/dayu600-framework-shim/src/android/app/Application.java) lacked it. Added register+unregister (no-op — nothing dispatches lifecycle events yet) + nested `ActivityLifecycleCallbacks` interface (real 7-method signature over shim Activity/Bundle). Windows build (d8.bat + framework.jar) unavailable on mac → built SURGICALLY: baksmali the DEPLOYED shim dex, hand-add the 2 methods + write the interface .smali… BUT no smali assembler on mac (only baksmali-3.0.9). So instead: recompiled shim from source with mac d8 (`~/Library/Android/sdk/cmdline-tools/latest/bin/d8`, needs JAVA_HOME=/opt/homebrew/opt/openjdk), providing compile-only stubs for framework.jar's ClientTransactionHandler/ActivityThread(+ActivityClientRecord)/servertransaction.ClientTransaction + com.digiplex.game.CustomButton, deleting those 5 .class before d8 (matches build-framework-shim.ps1). The deployed dex was STALE (41 classes) vs current source (67) → to stay surgical, restricted d8 input to the old 41 class files + the new interface = 42 classes; diff vs old = ONLY `Application$ActivityLifecycleCallbacks` added, 0 removed.

**CURRENT WALL (s4 end):** `appOnCreate=FAIL NoSuchMethodError Context.getAssets()Landroid/content/res/AssetManager;` at `m5.c.onCreate:13` — onCreate now needs a working AssetManager → CONVERGES WITH THE RESOURCE FRONT (task #1). fwlib still FAILs `libandroidfw.so __android_log_print symbol not found` (must load from substrate path where liblog resolves) and `AssetManager.<init>[boolean]` NoSuchMethod. Plus mainNew still `Looper.getMainLooper=Stub!`. Leftover diagnostics (WESTLAKE_MULTIDEX/MDLOOP/OPENFILE/INMEM) still in dalvik_system_DexFile.cc + libdexfile/dex/dex_file_loader.cc — strip once stable.

## ★★★ SESSION 3 (2026-07-05 s3): FRAMEWORK-RES SERVES REAL RESOURCES ON DEVICE ★★★
NOTE: the "single vtable blocker" above is FIXED (see [[westlake-dayu600-hwui-gate2]] — volatile bit-vector zero in class_linker.cc + libart dlopen symbol fixes; runtime clone=0 stable). Current frontier = real-framework inflate + core-native completeness.

**framework-res.apk brought up end-to-end (this session):**
1. Device had NO framework-res.apk anywhere → packed one from SDK `~/Library/Android/sdk/platforms/android-35/android.jar` (it contains the full android-package resources.arsc 17MB + res/ with COMPILED binary XMLs, magic 0300 0800): `unzip resources.arsc res/*` then `zip -0 -X fw.apk resources.arsc; zip -rX fw.apk res` → arsc STORED at data offset 44 (4-aligned, no zipalign needed; no build-tools/zipalign on this Mac). 34074662 bytes → `$R/android/framework/framework-res.apk`.
2. Probe (assetProbe stage, VIEWINFLATE block) wires it into the system: reflection — MUST set `AssetManager.sSystemApkAssets` + `sSystemApkAssetsSet` (ArraySet) FIRST (setApkAssets() internally prepends sSystemApkAssets and calls sSystemApkAssetsSet.contains → NPE if null), then sentinel `new AssetManager(true)` + `setApkAssets(new ApkAssets[0], false)` + overwrite `sSystem`, then reset `Resources.mSystem=null` (rebuilds on our AssetManager). Startup sSystem was built by OHBridge STUB natives (incompatible fake ptrs) — never reuse it, rebuild wholesale.
3. **JNI cookie convention bug (was blocking ALL framework Java-path lookups):** AOSP JavaCookie is 1-BASED; `AssetManager.getResourceValue()` treats native return <=0 as NOT FOUND. My JNI returned raw 0-based cookie → every framework lookup NotFound'd (probe's own reflection tests read TypedValue fields directly and ignored the return, which hid this for weeks — the old `block=0` output was the tell). FIX in westlake_assetmanager_jni.cpp: nativeGetResourceValue returns cookie+1 + sets assetCookie=cookie+1; nativeOpenXmlAsset converts back (JavaCookie>0 → cookie-1, else kInvalidCookie).
4. **Cookie fix unlocked the NEXT crash:** Java getResourceValue on TYPE_STRING proceeds to getPooledStringForCookie → ApkAssets.getStringFromPool → `ApkAssets.nativeGetStringBlock` — NOT in my JNI (only 3 ApkAssets natives), so the OHBridge stub (registered at ApkAssets clinit) won and returned a fake handle → my StringBlock.nativeGetSize deref'd it → SIGSEGV addr 0x20 (fatal-bt in stderr names Java_android_content_res_StringBlock_nativeGetSize). FIX: added `ApkAssets_nativeGetStringBlock` (returns `(*WApk)->GetLoadedArsc()->GetStringPool()`), kApkAssets count 3→4.
5. **VERIFIED ON DEVICE:** `SYSRES[getSystem.getString(ok)=OK]` — Resources.getSystem().getString(android.R.string.ok) returns the real string through the full framework Java path (ResourcesImpl→AssetManager.getResourceValue→my native→pooled string).
6. inflate(android.R.layout.simple_list_item_1) now fails at `UnsatisfiedLinkError: SystemClock.uptimeMillis` → added Landroid/os/SystemClock; shorty-J intercepts (uptimeMillis/elapsedRealtime→CLOCK_MONOTONIC ms, uptimeNanos/elapsedRealtimeNanos→ns, currentThreadTimeMillis→CLOCK_THREAD_CPUTIME_ID) next to the System.currentTimeMillis intercept in patches/runtime/interpreter/interpreter.cc (~line 148). libart rebuilt 28209768 → stripped 18649192; deployed to $R/android/lib64/libart.so (OK); $R/art/libwestlake_art.so send PENDING (link died mid-send — MUST complete, runtime dlopens BOTH paths).
7. Known unhandled shorty (safe-void-defaulted, implement later): `AssetManager.nativeSetConfiguration` 'VJIILLIIIIIIIIIIIIIIIIZ'.

**test.apk (user request "run it"):** `~/Downloads/test.apk` = com.uptodown 7.33 store client (UptodownApp app class, com.uptodown.activities.MainActivity, AndroidX/Kotlin, real UI, arm64 libs only libuptodown-native/libandroidx.graphics.path/libdatastore_shared_counter). Sent to `$R/apks/test-uptodown.apk` (15182274, verified). Probe stage `uptodownProbe` ADDED to Dayu600ApkStageProbe.java (resource-engine sanity on its arsc + PathClassLoader + headless UptodownApp clinit/new/onCreate + MainActivity clinit/new, incremental writeText to uptodown-probe.txt); dex 44008 deployed. NOT yet run — run with `sed WESTLAKE_APP_STAGE=uptodownProbe` in deploy-dayu600.sh.

**Ops notes this session:** hdc link degrades to every-other-command [Fail] — wrap EVERY shell op in a retry helper (`hsh`: run, on [Fail] `hdc kill; sleep 4; hdc start; sleep 8`, retry ≤10×); sends need the wc-c verify loop (34MB went through on attempt 3). Kill list before each run: `ps -A | grep -aE 'appspawn-x|westlake.appspawnx|digiplex'` kill by PID (NEVER the system appspawn). Desktop doc `~/Desktop/OHOS-Android运行时兼容层-复现文档.md` reviewed: accurate for §1-5 but §6 next-step (canonicalize0) is stale — real current walls are the ones above.

## SESSION 3 continued — inflate wall = SHIM SHADOWING; test.apk multi-dex wall (2026-07-05/06)
**assetProbe VIEWINFLATE progression (each fix moved the wall):** NotFound 0x1110044 (no fw-res) → setApkAssets ArraySet NPE (set statics first) → SYSRES getString(ok)=OK ✅ + SystemClock.uptimeMillis UnsatisfiedLinkError → added SystemClock intercepts → **NoSuchMethodError Context.getOpPackageName in dayu600-framework-shim.dex @ContentResolver.<init>:820** = the SHIM-SHADOWING wall (gate-3a). Shim (66 classes, listed via baksmali of $R/framework-jars/dayu600-framework-shim.dex) stubs Context/ContextWrapper/ContextThemeWrapper/LayoutInflater/View/ViewGroup/Window/Activity/Application/TextView/Button/FrameLayout/LinearLayout/Drawable/AttributeSet/MotionEvent/Bundle/Handler/Looper/MessageQueue/animation + keeps OHOS-glue (FrameworkInitializers/ServiceManagers/servertransaction/Environment/Trace/conscrypt/digiplex.e). Real ActivityThread/ContextImpl/ContentResolver ARE real (not shimmed) but link against shim Context → method missing. NEXT = incremental shim strip (dexlib2 StripClasses; start Context+ContextWrapper+ContextThemeWrapper, expect native/completeness cascade with real Looper/MessageQueue/View).
**uptodownProbe results:** resource engine WORKS on test.apk (cookie=1, real com.uptodown:anim/* names). PathClassLoader walls: (1) DexFile.verifyInBackgroundNative ABORTS → FIXED (no-op'd in patches/runtime/native/dalvik_system_DexFile.cc). (2) **CLASS-LOADING WALL — FIXED 2026-07-06. Root cause was NOT multi-dex.** The earlier "fork exposes only classes.dex (c1=OK c2/c3/c4=CNFE)" conclusion was WRONG — an artifact of parent-delegation leakage: `a.a` IS a defined class in 2048-2-9.apk (baksmali-confirmed `La/a;`), 2048 is on the VM classpath = uloader's parent, so `Class.forName("a.a",false,uloader)` resolved from the PARENT, giving a false c1=OK; c2/c3/c4 are unique to uptodown so genuinely missed. **The REAL bug:** `DexFile.openDexFileNative` (shorty `LLLILL` = ret L, args L,L,I,L,L) was dispatched through the interpreter's hand-written generic JNI bridge `InterpreterJni` (patches/runtime/interpreter/interpreter.cc), which had NO branch for `LLLILL` → logged `interpreter.cc:1409 InterpreterJni: unhandled static shorty 'LLLILL'` and SILENTLY DROPPED the call → the C++ `DexFile_openDexFileNative` was never entered → PathClassLoader got a **null cookie** (`class_loader_utils.h:127 Null DexFile::mCookie`) → every self-dex class lookup missed (CNFE). 2048 worked only because it's opened at VM-init via the boot/classpath DexFileLoader (native, not this JNI bridge). **THE FIX (applied):** added the `LLLILL` branch to InterpreterJni (marshals obj,obj,int,obj,obj → jobject, using ObjArg(args[i]) + result->SetL(soa.Decode<mirror::Object>(jresult)); inserted right after the `LL` case). After fix, on-device proof: `WESTLAKE_OPENFILE enter path=.../test-uptodown.apk`, `WESTLAKE_MULTIDEX ndex=4` (classes.dex + classes2/3/4.dex all opened), probe now reports `dex1=OK appCls=OK mainCls=OK appClinit=OK mainClinit=OK` — **UptodownApp + MainActivity RESOLVE from classes4.dex**. Multi-dex enumeration was never broken (it correctly returns 4). NOTE the `new DexFile(raw).loadClass()` diagnostic path was ALSO a victim of the same missing-shorty bug (df4open=OK/df4load=NULL was the null-cookie symptom), now moot. Remaining NON-fatal dropped shorty seen: `ZL` for `DexFile.isBackedByOatFile` (returns default, harmless; add if needed). Instrumentation diagnostics (WESTLAKE_MULTIDEX/MDLOOP/OPENFILE/INMEM LOG(ERROR) lines) are still in patches/runtime/native/dalvik_system_DexFile.cc + aosp-art-15/libdexfile/dex/dex_file_loader.cc — consider removing once stable. **Remaining test.apk walls are DOWNSTREAM (not class loading):** `appNew=FAIL IncompatibleClassChangeError: Class i5.m7 implements non-interface class d1.h (in classes4.dex)`; `mainNew=FAIL RuntimeException:Stub! @android.os.Looper.getMainLooper` (needs real framework Looper/MessageQueue). UptodownApp.<clinit> tolerated a Stub! (class_linker.cc:6278 "Tolerating clinit failure").
**RUN RECIPE (recovered 2026-07-06 — there is NO deploy-dayu600.sh; it was inline each session):** launcher = LD_PRELOAD .so `/data/local/tmp/westlake-dayu600-substrate/probes/libwestlake_embedded_art_dlopen_probe.so` on `/system/bin/toybox true`. Source = test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c. It dlopens libwestlake_art.so, creates the VM (bootclasspath core-jars + framework-jars/dayu600-framework-shim.dex + apks/dayu600-apk-probe.dex + 2048-2-9.apk), then calls Dayu600ApkStageProbe.embeddedMainNoExit(target,stage,"0"). Env: WESTLAKE_CREATE_VM=1, WESTLAKE_NO_EXIT=1, WESTLAKE_STAGE=<stage> (→arg1), WESTLAKE_TARGET (opt), WESTLAKE_ROOT (default /data/local/tmp/westlake-dayu600), **WESTLAKE_OMIT_FRAMEWORK_SHIM=1 drops the shim from bootclasspath+classpath (line 505/612/647) — the lever for the inflate/shim-strip front!**. Full FOREGROUND run (most reliable vs flaky link; abort/exception comes straight back): `timeout 75 sh -c 'LD_LIBRARY_PATH=/data/local/tmp/westlake-dayu600/compat:/system/lib64 WESTLAKE_CREATE_VM=1 WESTLAKE_NO_EXIT=1 WESTLAKE_STAGE=uptodownProbe LD_PRELOAD=<preload> /system/bin/toybox true'`. Probe writes to `$R/apks/probe-logs/<name>.txt` (probeLogPath), args read from `$R/apks/probe-args.txt` (line0 target/1 stage/2 dir) when WESTLAKE_STAGE unset. libandroidfw MUST be loaded from the SUBSTRATE path — the probe's `System.load(rootPath()+"/android/lib64/libandroidfw.so")` at default root FAILS `__android_log_print: symbol not found` relocation (liblog not resolvable there); resource stage only works when libandroidfw resolves liblog (substrate compat has the ld-musl symlinks). Files needed at default root for uptodownProbe: apks/test-uptodown.apk, android/lib64/libandroidfw.so, apks/test-uptodown.classes*.dex — copy from substrate/apks + substrate/android/lib64.
**Ops:** hsh retry wrapper saved at scratchpad/hsh.sh (hdc kill/start loop on [Fail]/[Empty], ≤25×). Device recovers from hard-down via power-cycle (user did one 2026-07-06, back after 8 hdc cycles). DETACHED nohup launches proved UNRELIABLE this session (0 procs spawned repeatedly) — prefer FOREGROUND `timeout` runs inside hsh. Probe dex ~45k with uptodownProbe; c4 fallback now runs the c1..c4 PathClassLoader matrix.
