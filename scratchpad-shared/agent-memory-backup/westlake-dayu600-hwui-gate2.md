---
name: westlake-dayu600-hwui-gate2
description: "DAYU600 hwui-on-OHOS port — libhwui.so built from AOSP15 and hwui's own render pipeline verified painting on the real panel; the exact walls and fixes"
metadata: 
  node_type: memory
  type: project
  originSessionId: 970210e5-03bf-40cf-82db-a7d7b3f71a83
---

Port of westlake-piercing (Android-apps-on-OpenHarmony) to the **DAYU600 / uis7885 / arm64 / OHOS 6.1** board. Work done from a Mac (`~/westlake-piercing`, sources under `~/a2oh-source-audit`, OHOS SDK `~/a2oh-source-audit/ohos-sdk/native`, device via `~/.local/openharmony-tools/bin/hdc`, serial 5ce2dcee...).

**Gate 1 DONE — `libhwui.so` built from AOSP 15 (android-15.0.0_r9) for aarch64-linux-ohos.** 171 hwui sources compiled (minikin/text stubbed in `ports/dayu600/include/minikin/MinikinCompat.h`), linked against the board's own `libskia_canvaskit.z.so` (same m133 Skia). Build: `ports/dayu600/scripts/build-libhwui-smoke-macos.sh` + `build-support-objs.sh` (AOSP-15 libutils/RefBase/Looper real sources + `support-src/*.cpp` stubs) + `link-libhwui-macos.sh`. Must build with **default visibility** (not -fvisibility=hidden) so `register_android_graphics_*` export.

**Gate 2 DONE — hwui's OWN pipeline paints on the panel** (green bg + red rect, verified by snapshot). Native harness `ports/dayu600/gfx-smoke/hwui_render_harness.cpp` drives RenderProxy→RenderThread→CanvasContext→SkiaOpenGLPipeline into a display-attached RSSurfaceNode's OHNativeWindow. Walls climbed, in order:
1. `ASurfaceControlFunctions` dlopen("libandroid.so")+dlsym 13 ASurfaceControl_* → **fake `libandroid.so`** (`gfx-smoke/fake_libandroid.c`) with no-op stubs, on LD_LIBRARY_PATH first.
2. `initializeChoreographer` (no AChoreographer on OHOS) → set `Properties::isolatedProcess = true` → DummyVsyncSource branch.
3. `setBufferCount` reads `window->query` at AOSP ANativeWindow struct offset → **wrap OHNativeWindow** via `bridge-src/oh_anativewindow_shim.cpp` `oh_anw_wrap()`; struct must match the PORT's simplified `include/android/native_window.h` layout (`query`@offset 88, NOT full AOSP @144).
4. hwui calls `eglCreateWindowSurface` on the wrapper → **EGL interposer** (`gfx-smoke/egl_interposer.cpp`, strong symbol in the exe, RTLD_NEXT to real libEGL) `oh_anw_get_oh()` unwraps to real OHNativeWindow.
5. Empty buffer despite swap=EGL_SUCCESS → **the real wall**: OHOS `libskia_canvaskit.z.so` added a gate (`GrDrawingManager.cpp:172`) that SKIPS the whole GPU submit when any onFlush callback's preFlush returns false. `AtlasPathRenderer::preFlush` fails (MSAA-alpha8 atlas won't instantiate on this Mali-G57 GL driver). Root cause: **ABI mismatch** — device libskia was built `-DGPU_TEST_UTILS` (GrContextOptions has trailing fields incl. `fGpuPathRenderers`); libhwui built without it passed a shorter struct → device read garbage `fGpuPathRenderers` with the kAtlas bit set → atlas registered. **Fix: interpose `GrDirectContexts::MakeGL` compiled `-DGPU_TEST_UTILS`, set `fGpuPathRenderers = GpuPathRenderers::kNone`** (`gfx-smoke/skia_interposer.cpp`) → atlas never registers → gate never armed → GPU submits. Also `eglSwapBuffersWithDamageKHR`→plain `eglSwapBuffers` interposer.

Run: push `hwui_render_harness`+`libhwui.so`+`libandroid.so` to one dir, `LD_LIBRARY_PATH=<dir>:/system/lib64:/system/lib64/platformsdk:... ./hwui_render_harness N`.

**Gate 3 registration wall BROKEN — 54/54 graphics registrars pass (2026-07-04 session 2).** The earlier "register_android_graphics_classes never returns / samgr Parcel::InjectOffsets crash" was a MISDIAGNOSIS (stale faultlog). Real story, pinned by a custom fatal-signal backtracer added to `liboh_android_runtime.so` (`android-runtime-src/AndroidRuntime.cpp`: `install_fatal_backtrace()` — sigaction on SIGTRAP/ILL/ABRT/FPE, and SIGSEGV/BUS when `WESTLAKE_FATAL_SEGV` set; walks the FAULTING ucontext pc/lr + aarch64 x29 frame-pointer chain, `dladdr` each → names lib+symbol; env `WESTLAKE_FATAL_BT=1`). Registration was CRASHING (SIGTRAP via `__android_log_assert`/`LOG_ALWAYS_FATAL`), not hanging. Per-registrar loop diagnostics: `WESTLAKE_HWUI_STOP_AT=<n>`, `WESTLAKE_HWUI_SKIP=,n,` (skip a fatal registrar to survey the rest), `WESTLAKE_HWUI_AGG=1`. The four walls, in order:
1. **reg[4] Bitmap → `HardwareBufferHelpers::init()`** dlopen("libandroid.so")+dlsym `AHardwareBuffer_fromHardwareBuffer`/`_toHardwareBuffer`, `LOG_ALWAYS_FATAL_IF` null. Fake `libandroid.so` (`gfx-smoke/fake_libandroid.c`) lacked them → **added non-null stubs**.
2. **reg[33] Typeface** — `RegisterNatives(nativeGetWeight/nativeCreateFromArray/...)` failed because `android.graphics.Typeface` resolved to a STUB in `dayu600-framework-shim.dex` (65-class headless shim, on `-Xbootclasspath` BEFORE real framework.jar) with no native methods → **stripped `Landroid/graphics/Typeface;` from the shim dex** (dexlib2 `StripClasses`; no smali assembler on host, only baksmali+dexlib2 via `/opt/homebrew/opt/openjdk`).
3. **reg[50] RenderNode → `register_android_view_RenderNode`** `GetStaticMethodIDOrDie("callPositionChanged2","(Ljava/lang/ref/WeakReference;JIIIIIIII)Z")` — r9 wants **8 ints**, this framework.jar had 10 → **added an 8-int overload stub** to `android.graphics.RenderNode$PositionUpdateListener` in framework.jar's classes.dex (dexlib2 `AddStub`, body `const/4 v0,0; return v0`; repack jar with `zip -0 -X` to keep classes.dex STORED like a bootclasspath jar).
4. **reg[53] ThreadedRenderer → `register_android_view_ThreadedRenderer`** `SharedLib::openSharedLib("libandroid")` = `dlopen("libandroid")` **without `.so`**; musl won't auto-append → handle null → `ANativeWindow_fromSurface` dlsym null → fatal. **Fix: symlink `$R/android/lib64/libandroid -> libandroid.so`** (no-suffix alias) + added `ANativeWindow_fromSurface`/`_toSurface` stubs to fake libandroid. (All 8 HardwareRenderer$* callback methods DID match framework.jar — not a version-skew here.)
Result: **`AndroidRuntime::startReg leave hardFailures=0`**. dexlib2 host tools in scratchpad: `StripClasses.java`, `AddStub.java` (classpath = smali-baksmali+smali-util+smali-dexlib2+guava jars under `~/Library/Android/sdk/cmdline-tools/latest/lib/external`).

**NEXT WALLS (gate 3 not done — no visible UI yet).** After registration, the **PROBE path** (`Dayu600ApkStageProbe.runResolved` → `Method.invoke` → `MainActivity.onCreate`) runs onCreate HEADLESS (no window/Surface) and reaches `com.digiplex.game.c.<init>` → `Random.<clinit>` → `Unsafe.objectFieldOffset` then a `java.lang.NoClassDefFoundError` (boot class loader). The **real OHOS-AMS `aa start` forked child** SIGSEGVs early (fault 0x13de0078, in an ArtMethod). VISIBLE UI needs: (a) strip the shim's shadowing android.* classes so real framework.jar Activity/View/Window/Context run; (b) drive the real AMS lifecycle (ActivityThread→ViewRootImpl) OR create our own display RSSurfaceNode and hand its OHNativeWindow to the app renderer (reuse gate-2 `ohos_display_surface.cpp`+`oh_anw_wrap`); (c) a REAL `ANativeWindow_fromSurface` (currently null stub) bridging the app's Surface → OHNativeWindow. **DEFINITIVE ROOT BLOCKER (2026-07-04 s2, empirically proven with build-host access):** the Westlake ART engine solved `java.lang.invoke`/VarHandle on **x86_64** (phone/OnePlus6 dev target) but NOT on **arm64/OHOS** (DAYU600). PROOF: on the WSL build host `ssh win` → `/mnt/c/Users/ufop/a2oh-source-audit/art-latest/prebuilt-boot/`, `x86_64-vh/boot.art` = **4.6 MB (valid, VarHandle pre-inited)** vs arm64 `boot.art` = **53 bytes (invalid stub)**. Building the arm64 boot image on-device with the westlake `dex2oat` (`build-ohos-arm64/bin/dex2oat`, pulled+runs on device) **SIGSEGVs at `Pre-init failed for Ljava/lang/invoke/MethodType;: ExceptionInInitializerError`**. So arm64 runs IMAGELESS (`-Xint`, no boot image) where VarHandle misdispatches (`unreflectVarHandle(Field)`→`Object.clone`→CloneNotSupportedException; `VarHandle.getAndAdd`→NPE). That kills `AtomicLong`/`AtomicInteger`/`ConcurrentHashMap`.<clinit> → `Random`.<clinit> → `UptodownApp.onCreate` at instr ~10 (and all Kotlin-coroutine/AndroidX apps). Tested all 3 ARTs (device 3235f2ea, OHOS-classfix 30653256, bionic 4a15603c) + image-backed dalvikvm — arm64 boot.art header unreadable (53B). THE fix = make arm64/OHOS `java.lang.invoke` boot-image pre-init work (debug why MethodType/VarHandle pre-init crashes on arm64 in westlake dex2oat) — the project's own unsolved frontier (cracked x86_64, not arm64). **PINNED EXACTLY (2026-07-04):** on-device dex2oat crash chain = `MethodType.<clinit>`→`ConcurrentWeakInternSet.<init>`→`ConcurrentHashMap.<clinit>` fails, then `AtomicInteger.<clinit>` pre-init → after `[UNSAFE-JNI] getStackClass2`/`getNameNative` → SIGSEGV signal 11 fault addr 0x4 (x0=0 null receiver) at **`art::FindMethodToCall<(InvokeType)2 /*kVirtual*/>`** (interpreter virtual dispatch; symbolized via `llvm-symbolizer -e dex2oat.westlake 0x4e903c`). I.e. arm64 mis-dispatches `unreflectVarHandle`→`Object.clone` → null VarHandle → `invoke-virtual getAndAdd` on null → FindMethodToCall null-deref. It's an arm64-specific vtable/IMT/method-resolution bug for `java.lang.invoke` classes that classfix/W-series does NOT cover. Fix lives in ART interpreter dispatch + vtable setup for VarHandle/MethodHandle classes (`patches/runtime/interpreter/*`, `class_linker.cc` vtable, `art_method.cc`). Repro: push `build-ohos-arm64/bin/dex2oat` to device, `dex2oat.westlake --android-root=/system --runtime-arg -Xverify:none --dex-file=core-oj.jar ... --instruction-set=arm64 --compiler-filter=verify --image=boot.art`. **CANDIDATE FIX (2026-07-04, in progress):** pinned a concrete arm64 bug in `art-latest/patches/runtime/interpreter/unstarted_runtime.cc` `UnstartedJNIVMStackGetStackClass2` — `NthCallerVisitor(self, 3); if (caller != nullptr) result->SetL(...)` leaves `result` UNSET (garbage) when the depth-3 caller is null (arm64 has a different interpreter frame count than x86_64) → downstream uses garbage as a Class receiver → `FindMethodToCall<kVirtual>` null-deref. Patched to always `result->SetL(nullptr)` + fall back depths 3→2→1 (backup `.pre_arm64fix`). Build infra: WSL host `ssh win` → `wsl bash -lc`; the REAL build tree is on Windows FS `/mnt/c/Users/ufop/a2oh-source-audit/art-latest/` (Makefile.ohos-arm64, `build-ohos-arm64/`). Original dspfac OHOS toolchain (`/home/dspfac/openharmony/...`) is GONE; use `/home/yao/ohos-sdk-6.0.0.47/linux/native` (clang `llvm/bin/clang++`, libc++ at `llvm/lib/aarch64-linux-ohos/libc++.a`). **Mixing toolchains crashes** (relinking my /home/yao-built .o with dspfac-built .o → binary SIGSEGVs pre-main, before "Dex2oat entered") → must do a FULL consistent rebuild with one toolchain. Robust file transfer: **`scp win:"C:/Users/ufop/.../file" local`** (base64-over-wsl-shell drops on >20MB / flaky conn). A full consistent rebuild was kicked off in background (`nohup make -f Makefile.ohos-arm64 -j4 link-dex2oat`, log `/tmp/d2o_rebuild.log`, Makefile toolchain repointed to /home/yao). NOTE: `libz.a` not found in /home/yao SDK — link may need it built from `aosp-android-11/external/zlib`. NEXT after a working dex2oat: rerun boot compile → if MethodType/VarHandle pre-init passes (boot.art > 53B), the getStackClass2 fix works; then image-backed run for VarHandle, then downstream. **RESULT (2026-07-04):** consistent-toolchain full rebuild of dex2oat with /home/yao SDK WORKS (26MB, starts clean). Build method that survives WSL idle-shutdown: `.o` live on `/mnt/c` (persist); run `make -f Makefile.ohos-arm64 -j8 link-dex2oat` SYNCHRONOUSLY in ≤8-min ssh chunks (incremental, resumes). libz.a built to `/mnt/c/temp/libz.a` (Makefile link patched to use it). getStackClass2 fix VERIFIED to advance pre-init (boot.oat 53→4928B; MethodType now runs, crash moved to StackStreamFactory). BUT: the arm64 boot pre-init has a SERIES of crashers rooted in a SYSTEMATIC arm64 interpreter bug — garbage receiver `0x0123e2e00123e268` (a 32-bit compressed heap ref read as a 64-bit raw pointer) → `FindMethodToCall<kVirtual>` SIGSEGV — hitting many boot classes' <clinit>. Added a per-class skip blocklist in `patches/dex2oat/driver/compiler_driver.cc` `TryInitializeClass` (`[DAYU600-SKIP-INIT]`, e.g. StackStreamFactory) — works but reveals the NEXT crasher (whack-a-mole). VarHandle is fixed by `image_writer.cc` FieldVarHandle fixup (NOT clinit — dex2oat deliberately skips VarHandle classes), so the goal is just to make the boot image BUILD to completion. Real fix = the systematic arm64 compressed-ref/64-bit-ptr interpreter bug (one fix clears all crashers) — deep, multi-session.

**STRATEGIC (critical, 2026-07-04):** the ORIGINAL upstream **A2OH/westlake-piercing** (vs the BH3GEI fork this repo is) is **32-bit ARM, DAYU200/RK3568 ONLY** — and it WORKS end-to-end: stock coroutine/AndroidX apps (Material Catalog 0-failures multi-hour, noice 9/10 pages) run via a PROPER 32-bit libart (ba40f173, W-series, real JNI, boot image OAT v230). Its remaining walls are UI-level (IME text-entry injection, WMS focus/compositing) — NOT the runtime; VarHandle is solved. A2OH has ZERO arm64 code. So the whole arm64/DAYU600 VarHandle fight is a problem that ONLY EXISTS in the fork's arm64 port (which uses a weak imageless/hand-stub ART); the original solved it on 32-bit with a real libart. **DAYU600 (uis7885) is 64-bit ONLY** — `/system/lib` has only `ld-musl-aarch64.so.1`, no 32-bit musl/libs — so A2OH's 32-bit stack CANNOT run on DAYU600 without a 32-bit OHOS userspace (absent). Fork = `~/westlake-piercing` git remote BH3GEI. Options: (A) DAYU200/RK3568 hardware → replicate A2OH, apps run today; (B) stay DAYU600 → build a PROPER arm64 libart+boot image (like A2OH did for 32-bit) incl fixing the systematic arm64 interpreter bug — the legitimate but deep multi-session path this session advanced. **USER: only has DAYU600 → path B.** DAYU600 is 64-bit only so A2OH's 32-bit stack can't run.

**BREAKTHROUGH (2026-07-04 s2, path B): root cause of the arm64 VarHandle crash FOUND + partially FIXED.** A workflow analysis of the interpreter source pinned it: the garbage receiver `0x0123e2e0_0123e268` is two neighboring 4-byte `References()` slots dumped 8-wide; real fault is a bogus 32-bit ref in `References()[vregC]`, deref'd at `entrypoint_utils-inl.h:594` `(*this_object)->GetClass()->GetVTableEntry`. ROOT = shadow-frame stack imbalance in the patched native-invoke path: `patches/runtime/interpreter/interpreter.cc` `EnterInterpreterFromInvoke` SKIPPED `PopShadowFrame` for `dexMethodIndex==861` (`SetTopOfShadowStack(link); return;`) → a later frame overlaps the live `<clinit>` frame's References region → ref store lands on the caller's receiver slot / corrupts heap object headers. **FIX A** = delete that 861 pop-skip, always `PopShadowFrame()`. VERIFIED: with Fix A, dex2oat boot pre-init advances from crashing at ~class 10 (MethodType) to **317 classes cleanly tolerated/inited — the ENTIRE VarHandle chain (MethodType, ConcurrentHashMap, AtomicInteger, atomics) now passes**. Fix A applied to BOTH dex2oat AND the runtime `libwestlake_art.so` (relink via `tools/relink-arm64-libwestlake-art-ohos.sh` with `OHOS_SDK_NATIVE=/home/yao/ohos-sdk-6.0.0.47/linux/native`, 28MB). NOTE: runtime imageless VarHandle is STILL broken (unreflectVarHandle→CloneNotSupportedException / getAndAdd NPE — a DIFFERENT symptom than the AOT garbage-receiver; runtime VarHandle needs the BOOT IMAGE's image_writer FieldVarHandle fixup, not just the interpreter fix). So MUST complete the boot image. **REMAINING (1 more bug):** with Fix A the boot compile reaches the END (root-class-init loop kMax=51 completes through ExpatParser$ClonedAttributes) then SIGSEGVs on a NULL function-pointer call (pc=0, x30=0, string-scan register patterns 0x7f7f/0x1f1f) right after an `Explicit concurrent mark sweep GC` — a separate post-loop/GC-safety bug in the boot-compile finalization. Fix B (disable the `nn_backup` vregs-only memcpy in interpreter_common.cc:7222) did NOT affect it (reverted). BUILD INFRA for path B: source on WSL `/mnt/c/Users/ufop/a2oh-source-audit/art-latest` (`Makefile.ohos-arm64`, patches under `patches/runtime/...`); toolchain `/home/yao/ohos-sdk-6.0.0.47/linux/native` (dspfac's gone); build survives WSL idle-shutdown via `.o` on /mnt/c + `make -f Makefile.ohos-arm64 -j8 link-dex2oat` in ≤8-min synchronous ssh chunks; libz.a at `/mnt/c/temp/libz.a`; transfer binaries via `scp win:"C:/Users/ufop/.../file"`. Local ART src copy for analysis at scratchpad/artsrc/. Next: root-cause the post-GC null-call. FINDINGS (2026-07-04 s2): the post-GC crash is a GC-safety bug — a GC during pre-init walks shadow frames as roots and trips on a frame imbalance Fix A left behind (the patched `DoCallCommon` native branch in `interpreter_common.cc:~7161-7194` builds `new_shadow_frame` but in the `use_interpreter_jni` path does NOT push it while `EnterInterpreterFromInvoke` builds its own frame — double-frame-building; the `dexMethodIndex==861` `SetTopOfShadowStack(link)` hack in interpreter.cc was papering over the resulting extra frame). Confirmed by heap-size experiment: with `-Xmx≥~1600m` NO GC happens during pre-init and dex2oat reaches the IMAGE-WRITE phase (`[IMG] LoadBootImage entered`), but then the large-object-space mmap (sized = -Xmx) fails: **ART 32-bit compressed refs force the heap into the low 4GB; base 0x70000000 + big heap + LOS exceeds it → `large_object_space.cc:378 mmap ENOMEM`** (device has 7.5GB RAM / addr-space unlimited / overcommit=1, so it's the 4GB compressed-ref budget, not real OOM). So heap-tuning is a dead end. The REAL fix = the GC-safety / frame-balance bug in the custom native branch (Fix C = replace with stock `PerformCall` is too risky — the branch has load-bearing PFCUT/getStackClass2/lastIndexOf customizations; instead need a targeted fix that makes the `use_interpreter_jni` path NOT orphan `new_shadow_frame`, so no extra frame and no 861 hack needed, and GC root-walk stays balanced). That single fix should let the boot image build to completion at the normal small heap. THEN: boot completes → image_writer FieldVarHandle fixup → deploy boot image + core-jars + run the probe image-backed (`-Ximage:boot.art` + `-Xbootclasspath-locations:/apex/com.android.art/javalib/...`) → runtime VarHandle works → uptodown onCreate past AtomicLong → drive the Wine adapter → UI. Fix-A dex2oat + libwestlake_art are deployed on device (`.pre_fixA` backups exist). This is the single remaining ART blocker; everything downstream (hwui, ServiceInterceptor, surface) is ready. DIAGNOSTIC UPDATE (2026-07-04 s2 end): a `[FRAME-IMBALANCE]` probe added before the PopShadowFrame in `EnterInterpreterFromInvoke` (interpreter.cc) did NOT fire → frames ARE balanced at that pop → the "861 leaves an extra frame" hypothesis is REFUTED. The real crash is `(*this_object)->GetClass()` reading a GARBAGE CLASS WORD (heap-object header corruption), not a bad receiver reference — so it's HEAP corruption (something writes a ref into a live object's memory), and it shifts with -Xmx (heap layout). The corruption source is elsewhere: DoCallCommon's inconsistent frame mgmt (`interpreter_common.cc`: `ScopedStackedShadowFramePusher` at 6857/7056 vs manual `PushShadowFrame` at 7023/7171+manual pop 7194) OR synthesis root #2 (union carry-over: a `SetJ` raw pointer surviving a shared `result_register` into a `move-result-object` L-consumer → `SetVRegReference` truncates a raw ptr into a bogus ref). NEXT SESSION: wire the workflow's §4 diagnostic `PFCutReportCorruptReceiver` (dump frame ranges + `vregs_[i]!=References()[i]` desync + last-native breadcrumb at the corrupt-receiver event) to pin #1-vs-#2-vs-#3 empirically, then fix. Full workflow synthesis saved at scratchpad/tasks/ws0qkd8n5.output. Board currently has Fix-A dex2oat variants + Fix-A libwestlake_art deployed; stage=randomCtorProbe; `.pre_fixA` backups on device. FINAL DIAGNOSTIC CONCLUSION (2026-07-04 s2): the corruption is SYSTEMIC and drifts across mechanisms with heap layout — at -Xmx1550m it surfaces as a FindMethodToCall garbage-RECEIVER SIGSEGV; at default -Xmx512m it surfaces as a post-GC NULL-FUNCTION-POINTER call (pc=0,x30=0, after "Explicit concurrent mark sweep GC"); both from one underlying heap/memory corruption. A targeted guard added at the FindMethodToCall kVirtual deref (`entrypoint_utils-inl.h:594`, `.pre_guard` backup, checks receiver+class via `heap->FindSpaceFromAddress`) did NOT fire at 512m (0 hits) because that run's crash is the null-CALL path, not the receiver deref — proving single-symptom guards can't catch a drifting systemic corruption. CONCLUSION: symptom-guards + config tweaks (heap size, `-Xgc:nonconcurrent`, heap verify) do NOT converge; the fix must be the ROOT — the custom `DoCallCommon` native branch's inconsistent frame mgmt (mixed `ScopedStackedShadowFramePusher` vs manual `PushShadowFrame`/`PopShadowFrame`, orphaned `new_shadow_frame` in the `use_interpreter_jni` path). Two viable next-session paths: (a) wire the workflow's §4 `PFCutReportCorruptReceiver` to fire at the FIRST corrupt event (the guard is the trigger hook — extend it to also dump frame ranges + `vregs_[i]!=References()[i]` desync + last-native breadcrumb) to empirically pin the exact corrupting op; (b) Fix C — replace the hand-rolled native branch with stock `PerformCall` (base_interpreter_common.cc:1348), accepting loss of the PFCUT perf fast-paths (they are optimizations, not correctness), which eliminates the custom-frame corruption wholesale. Recommend (b) then (a) to verify. Session 2 built the complete toolchain to iterate this (all dex2oat/art variants, backups, diagnostics in place).

**ROOT NARROWED — ALL 3 SYNTHESIS ROOTS EMPIRICALLY REFUTED (2026-07-04 s2 final):** ran targeted diagnostics for each: #1 frame-imbalance — a `[FRAME-IMBALANCE]` probe before EnterInterpreterFromInvoke's PopShadowFrame fired 0×; #2 bogus-ref-store — instrumented `ShadowFrame::SetVRegReference` (shadow_frame-inl.h + helper `DayuCheckBogusRefStore` in interpreter_common.cc, `.pre_src` backups) to flag any non-null out-of-heap ref being stored → fired 0× (so refs are VALID at store time); #3 memcpy invariant — re-applying Fix B (disable the `nn_backup` vregs-only memcpy) did NOT fix it. CONCLUSION: it is a **USE-AFTER-FREE**, not a bad store — the object is valid when stored into a vreg, then the GC FREES it (misses it as a root) and its memory is reused, so the ref now points to a valid heap ADDRESS with GARBAGE contents (a bogus class word). This is why (a) the store-time source-catcher never fires, (b) the FindMethodToCall receiver/class address-guard (`FindSpaceFromAddress`) never fires — a reused-memory address is still in a space; only the CONTENTS are garbage. The GC is missing a live root — prime suspect = the **orphaned `new_shadow_frame`** in DoCallCommon's `use_interpreter_jni` path (built via `CREATE_SHADOW_FRAME` alloca at interpreter_common.cc:6835, holds the call's object args, but is NOT pushed to the thread shadow-frame chain, so GC's root-walk skips it; a GC during the nested native/interpreter call frees those args). NEXT SESSION (do these, not the 3 refuted roots): (1) confirm via a GC-root-completeness check — before each GC during pre-init, verify every object arg in flight is reachable; or enable ART heap poisoning / `-Xcheck:jni`-style freed-object fill so the dangling read is caught with a distinctive pattern + stack; (2) FIX = ensure the `use_interpreter_jni` path keeps `new_shadow_frame` GC-visible for the duration of the nested call (wrap it in a `ScopedStackedShadowFramePusher` spanning the EnterInterpreterFromInvoke call, mirroring the arg-copy pusher at 6857) OR route natives through stock PerformCall (Fix C). Backups on WSL: interpreter.cc.pre_fixA, interpreter_common.cc.pre_fixB/.pre_src/.pre_fixD, shadow_frame.h/-inl.h.pre_src, entrypoint_utils-inl.h.pre_guard.

**DECISIVE NARROWING (2026-07-04 s2 final-2): it's a WILD WRITE, and PRE-INIT ACTUALLY COMPLETES.** Fix D (scoped pusher for new_shadow_frame GC-visibility) did NOT fix it either. Symbolized the crash: x5=`art::ImageFileGuard::WriteHeaderAndClose` and with `-verbose:compiler` the crash moves to the `image_classes` enumeration loop (dex2oat.cc:1447, iterating a **C++ `HashSet<std::string>`**) — crashing at fault addr 0xffffffffffffffe8 (=-24) AFTER logging "Image class Ljava/lang/Compiler;". This proves: (1) ART class PRE-INIT COMPLETES — all 318 boot classes incl. the ENTIRE VarHandle chain init/tolerate cleanly (Fix A fully solved the VarHandle-init blocker for the AOT path); the crash is now in dex2oat's IMAGE-BUILD/SETUP phase, past class init. (2) The corruption hits **NATIVE C++ memory** (a std::string HashSet) too, not only the Java heap — so it is a WILD WRITE (out-of-bounds/stray-pointer store) from the custom interpreter/native code, that lands on whatever memory the heap layout places there (Java object → garbage class word → FindMethodToCall SIGSEGV; C++ container → HashSet crash; a code/vtable ptr → pc=0 null-call). This unifies every symptom (shifts with -Xmx and with -verbose; all ref-store/frame diagnostics negative; both Java and native corruption). NEXT SESSION — stop source-level guessing, use a memory tool: build the runtime/dex2oat with **AddressSanitizer** (`-fsanitize=address`) to catch the wild write at its exact site, OR run the boot-compile under a watchpoint on a victim address, OR audit every raw `memcpy`/buffer/pointer-arith in `patched_interpreter_common.cc` (the PFCUT fast-paths, arg marshaling around 7080-7205, the getStackClass2/lastIndexOf custom code) for an out-of-bounds write. The wild write is almost certainly in the heavily-customized DoCallCommon native/arg path. Once fixed → boot image writes → runtime VarHandle → uptodown. NET: the VarHandle *init* frontier is CLEARED; the remaining blocker is a single out-of-bounds write in the custom interpreter, best found with ASan not static reading. More refutations added this session: a ShadowFrame OOB catcher (always-on `i>=number_of_vregs_` checks in SetVReg/SetVRegLong/SetVRegDouble + `DayuReportShadowOOB` helper, `shadow_frame.h.pre_oob`) fired 0× → the wild write is NOT via any ShadowFrame Set* API; `-Xgc:MS/SS/CMS` all crash identically → GC is a RED HERRING (not a concurrent-GC race); the raw `memcpy` at unstarted_runtime.cc:623 (resource loader) is bounded (array Alloc'd to map_size). So the wild write is a raw store in custom native/interpreter code not covered by any of the guards tried. ASAN FEASIBILITY (checked): aarch64-linux-ohos ASan runtime EXISTS (`.../llvm/lib/clang/15.0.4/lib/aarch64-linux-ohos/libclang_rt.asan.a`/`.so`/`asan_cxx.a`; HWASan too); `-fsanitize=address -static-libsan` COMPILES a test binary fine (2.5MB). REMAINING ASan setup hurdles for next session: (i) get the static-ASan test binary onto the device and confirm it actually reports a planted overflow (transfer via `scp win:"C:/Users/ufop/a2oh-source-audit/art-latest/<f>"` — /mnt/c/temp scp was flaky this session; and WSL /tmp does NOT persist across the idle-shutdown so build+copy to /mnt/c must be in ONE ssh session); (ii) then rebuild dex2oat (or at least interpreter_common.cc/unstarted_runtime.cc/interpreter.cc) with `-fsanitize=address` in CXXFLAGS + link `libclang_rt.asan.a`+`asan_cxx.a`+`asan-preinit.a`; (iii) WATCH FOR a shadow-memory-vs-ART-base conflict — ART maps its heap at the fixed `-DART_BASE_ADDRESS=0x70000000`; ASan's shadow (addr>>3 + offset) may overlap → if ASan init or the 0x70000000 reservation fails, use HWASan (`-fsanitize=hwaddress`, tag-based, no large shadow) instead. Once ASan/HWASan runs the boot-compile it pinpoints the wild write's exact file:line → fix → boot image writes → runtime VarHandle → uptodown. This session's device state: dex2oat.oob (Fix A + Fix B-disabled + Fix D + all diagnostics) at /data/local/tmp/westlake-boot-workdir/bin/; ts.asan test at /data/local/tmp (may be stale). App is NOT on screen; the blocker is precisely one instrumentable wild write.

**FINAL EXHAUSTIVE STATE (2026-07-04 s2 end-of-session, ~34 rebuild cycles):** narrowed the wild write to a **use-after-free at the RAW-POINTER/vtable level** (not ObjPtr, not ShadowFrame API). Additional refutations this session, ALL negative: (a) per-clinit heap-integrity scan added at TWO points — the 47-entry hardcoded pre_init loop (class_linker.cc ~1410, `.pre_heapscan` backup) AND the "Tolerating clinit failure" tolerance point (class_linker.cc:6278, `[DAYU600-HEAP-SCAN2]`) — both walk the whole heap via `heap->VisitObjects` checking every object's class-word is in-heap: fired 0× → NO corruption exists at any point BETWEEN clinits; the corrupting write + the crash happen inside ONE clinit's execution, tightly coupled, right after an "Explicit concurrent mark sweep GC". (b) Skipping `Lorg/apache/harmony/xml/ExpatParser;`+`$ClonedAttributes;` via the compiler_driver.cc:2262 `kDayu600SkipInit` blocklist just moved the crash (318→320 classes) → the corruption is GENERAL, not class-specific → a GC frees a still-live object (a MISSED GC ROOT). (c) Force-enabling `kObjPtrPoisoning=true` (obj_ptr.h:32, `.pre_poison` backup) did NOT fire a cookie CHECK → the dangling access is NOT an ObjPtr deref; it's a raw `mirror::Object*`/vtable read (`klass->GetVTableEntry` on reused freed memory → garbage ArtMethod → `blr 0` null-call, pc=0/x30=0; crash regs often show poison-ish 0x7f7f7f7f/0x1f1f... = reading freed/filled memory). So: **a GC during a boot-class clinit frees an object reachable only through a root the GC does not scan** (an unregistered raw `mirror::Object*` / `Handle`-less local in the custom UnstartedRuntime native handlers or transaction code), and a later raw read of that reused memory faults. TOOLS TRIED & EXHAUSTED (all in-tree, all negative): frame-imbalance probe, SetVRegReference source-catcher, ShadowFrame OOB catcher, Fix B (memcpy), Fix D (frame GC-visibility), `-Xgc:MS/SS/CMS`, heap verify flags, 2× per-clinit heap scans, ExpatParser skip, ObjPtr poisoning. DECISIVE NEXT STEP (only two options left, both dedicated efforts): (1) **ASan/HWASan** (runtime confirmed present; `-fsanitize=address -static-libsan` compiles) — catches the exact raw wild read/free site; watch ART-base-vs-shadow conflict (use HWASan if ASan shadow clashes with 0x70000000). (2) **GC-root instrumentation**: poison freed memory in the GC sweep path (heap.cc/space free) with a distinctive pattern so the use-after-free read is unmistakable, AND/OR add a pre-GC assertion that dumps the object about to be freed if it's still referenced by a shadow-frame/native-local — to identify the MISSED ROOT (prime suspect: a Handle-less `mirror::Object*` local in a custom UnstartedRuntime handler live across an allocation/GC point). Fixing the missed root → boot image writes → runtime VarHandle → uptodown. Device: dex2oat.po (poisoning+all diags) at /data/local/tmp/westlake-boot-workdir/bin/. WSL backups: obj_ptr.h.pre_poison, class_linker.cc.pre_heapscan, compiler_driver.cc (ExpatParser added — revert if undesired).

## ★★★ MILESTONE (2026-07-04 s2): ARM64 BOOT IMAGE BUILT ★★★
The GC-skip WORKAROUND cracked it: skip GC entirely during AOT compilation (heap.cc `Heap::CollectGarbageInternal`, `.pre_skipgc` backup — `if (runtime && runtime->IsAotCompiler()) return kGcTypeNone;` at the top; NOTE the working gate is **IsAotCompiler() ALONE** — `!IsStarted()` and `IsActiveTransaction()` both test FALSE at the crashing GC because it runs AFTER the transaction rollback with the runtime already started). No GC → no use-after-free → dex2oat runs to IMAGE WRITING. Heap stays tiny so no OOM. Second fix needed: the OHOS lzma is broken — `Xz_Encode` returns -1 → `libelffile/elf/xz_utils.cc:90 CHECK_EQ(res,SZ_OK)` aborted; patched (`.pre_xzfix` backup, base file NOT patches/ — Makefile compiles `$(ART)/libelffile/elf/*.cc` via wildcard) to tolerate failure: emit empty blob (mini-debug-info is optional). RESULT with `dex2oat.xzb` + `-Xmx768m/850m/1024m`: **writes a VALID 6.18 MB `boot.art` (magic `art\n`, version `118`) + boot.oat(54KB)+boot.vdex(82KB)+boot-core-libart.{art,oat,vdex}** — preserved at `/data/local/tmp/wl-imgboot/keep/`. The image_writer ran (11526 objects binned, `image_writer.cc:667 Post-fixup: scanned methods, fixed 0 invalid entry points`) so the **FieldVarHandle image fixup executed** — this is exactly what makes runtime VarHandle work. dex2oat still exits 139 with a `-24`(0xffffffffffffffe8) SIGSEGV AFTER the header write (a residual layout-dependent wild-write in the post-image-write cleanup) but the primary boot.art is complete (header written last = write finished). Build recipe that works: `dex2oat.xzb --android-root=/system --runtime-arg -Xverify:none --runtime-arg -Xmx850m --base=0x70000000 --dex-file=core-oj.jar --dex-file=core-libart.jar --instruction-set=arm64 --compiler-filter=verify --image=boot.art --oat-file=boot.oat`. NEXT: load this boot image in the runtime (libwestlake_art) via `-Ximage:.../boot.art` + matching BCP (core-oj:core-libart) → runtime VarHandle should finally work → uptodown onCreate past AtomicLong. (Optionally make the -24 post-write crash non-fatal so dex2oat exits 0, but the image is already usable.) All the diagnostic instrumentation (heap-scan, poisoning[reverted], OOB, source-catch) is still compiled into dex2oat.xzb but harmless.

**INTEGRATION GAP found (2026-07-04 s2): the built image doesn't match the runtime BCP.** The probe (`test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c`, `build_bootclasspath_option()` @542, options[] @1576) runs IMAGELESS (`-Xint`, 8 options, NO `-Ximage`). Runtime BCP order = `stringfactory.jar : core-oj-fieldfix.jar : core-libart.jar : core-icu4j.jar : conscrypt.jar : bouncycastle.jar : apache-xml.jar : adapter-mainline-stubs.jar : [framework-shim] : framework.jar : adapter-runtime-bcp.jar : oh-adapter-framework.jar : ...apks`. Jars live at `$R/android/framework/core-jars/` (substrate). To be loadable, the boot image must be built for a matching PREFIX of THIS list (image covers a BCP prefix; rest load imageless). BUT: building the image for the runtime's real jars (`stringfactory:core-oj-fieldfix:core-libart`) **crashes deterministically at ~class 268 for ALL heap sizes 512m–1152m** — the residual direct wild-write (non-GC) fires on `core-oj-fieldfix` (the runtime's modified core-oj) every time. The 6MB image that DID build used plain `core-oj:core-libart` (lucky layout) — those jars are NOT the runtime's, so that image is a proof-of-mechanism, not runtime-usable. So the REMAINING blocker is unchanged: **the direct wild-write must be fixed** (ASan/HWASan — runtime present; or GC-sweep poison to find the missed root/bad write) so the runtime-BCP image (`stringfactory:core-oj-fieldfix:core-libart`) builds. THEN: add a 9th probe option `-Ximage:$R/arm64/boot.art` (expand `JavaVMOption options[8]`→[9], `args.nOptions=9`), place boot.art+boot.oat at `$R/arm64/`, run randomCtorProbe → VarHandle works → uptodown. NET this session: proved dex2oat CAN emit a valid arm64 boot image (GC-skip + xz-tolerance were the two unlocks); the FieldVarHandle image fixup runs; the last wall is the same wild-write, now needing a memory tool. Working image-build binary: `/data/local/tmp/westlake-boot-workdir/bin/dex2oat.xzb`.

## ★★★★ ROOT CAUSE of the WILD WRITE FOUND & FIXED (2026-07-04 s2) ★★★★
Symbolized the deterministic 3-jar crash: pc → `mirror::String::AllocFromByteArray<true>`, x30 → `UnstartedRuntime::Jni`. The wild write is in the CUSTOM `newStringFrom*` fast-path in `patches/runtime/interpreter/unstarted_runtime.cc:2850` (`.pre_wwfix` backup): it matches `strncmp(mn,"newStringFrom",13)==0 && StringFactory` — i.e. ALL `newStringFrom*` overloads — but blindly applies the `(byte[], int high, int offset, int count)` arg layout, reading `high=args[1]/offset=args[2]/count=args[3]`. For other overloads (e.g. `newStringFromBytes(byte[],int,int,String charset)`) args[3] is an OBJECT REFERENCE, read as `count` → a huge garbage count → `AllocFromByteArray` reads far past the byte array = the wild read/write that corrupts whatever is in the heap layout there (Java class-word / native HashSet / vtable — explains every earlier symptom & the -Xmx/layout dependence). **FIX**: before calling AllocFromByteArray, validate `offset>=0 && count>=0 && offset<=len && count<=len-offset` (len=byte_arr->GetLength()); bail to the empty-string fallback if out of bounds. **RESULT (`dex2oat.ww`)**: the runtime-BCP build (`stringfactory:core-oj-fieldfix:core-libart`) now advances PAST class 268 → 324 classes → image writing → Post-fixup (FieldVarHandle fixup) → writes a VALID MULTI-IMAGE: `boot.art`(1.39MB, magic art\n118)+`boot-core-oj-fieldfix.art`(4.8MB)+`boot-core-libart.art`(428KB)+all .oat/.vdex — preserved at `/data/local/tmp/wl-imgboot/keep-rt/`. dex2oat still exits 139 on a RESIDUAL, HARMLESS post-write crash: a `std::ostream::operator<<` (LOG statement) AFTER `image_writer.cc:667 Post-fixup` and after all image files are flushed (primary boot.art header written last = complete). So the runtime-usable arm64 boot image is BUILT. GC-skip (heap.cc) is still needed & active. NEXT (final integration): (1) optionally silence the post-write LOG crash for a clean exit 0; (2) place keep-rt images at the runtime's expected image dir; (3) add a 9th probe option `-Ximage:<root>/.../boot.art` in `westlake_embedded_art_dlopen_probe.c` (options[8], nOptions=9) — BCP already starts stringfactory:core-oj-fieldfix:core-libart so the image is a valid prefix; (4) run randomCtorProbe image-backed → VarHandle finally works → uptodown onCreate past AtomicLong → Wine adapter → UI. This session cracked the core months-long blocker end to end: arm64 boot image with VarHandle fixup now builds for the runtime's real BCP.

## FINAL-INTEGRATION state (2026-07-04 s2 end): -Ximage wiring started
Edited the DLOPEN probe on the MAC copy `/Users/yao/westlake-piercing/test-fixtures/dayu600-embedded-art-probe/westlake_embedded_art_dlopen_probe.c`: added `build_image_option()` (returns `-Ximage:<WESTLAKE_ROOT>/arm64/boot.art`) after `build_art_path()`, and in `run_stage_probe()` expanded `JavaVMOption options[8]`→`[9]`, added a conditional (env `WESTLAKE_BOOT_IMAGE=1`) `options[8]=-Ximage` with `dayu_nopt` 8→9. CAVEATS for next session: (a) the build is CROSS-PLATFORM — `build-dlopen-probe.ps1` compiles with `aarch64-linux-gnu-gcc` in WSL reading a WINDOWS repo copy (`/mnt/<drive>/.../westlake-piercing`), NOT the Mac copy — so either apply the same 3 edits to the WSL/Windows copy or build the .c on Mac with OHOS clang (`--target=aarch64-linux-ohos`, `-fPIC -shared -nostdlib -nodefaultlibs -Wl,--unresolved-symbols=ignore-all`, needs a jni.h include dir). (b) The DEPLOYED runtime uses the EXEC probe `libwestlake_appspawn_exec_probe.so` (appspawn path), while the VM options live in the DLOPEN probe — confirm which probe actually creates the VM for `WESTLAKE_APP_STAGE=randomCtorProbe`; may need to wire `-Ximage` into whichever path runs, or test via `run-dlopen-probe.ps1` directly. (c) Place the keep-rt multi-image (`boot.art`+`boot-core-oj-fieldfix.art`+`boot-core-libart.art`+.oat+.vdex from `/data/local/tmp/wl-imgboot/keep-rt/`) at `<WESTLAKE_ROOT>/arm64/`; OAT dex-locations were built with the `$CJ` (core-jars) paths = the runtime BCP paths, so checksums should match. (d) ART image loading is finicky (BCP order/checksums/location) — expect a couple iterations. Then set `WESTLAKE_BOOT_IMAGE=1` in deploy-dayu600.sh + run randomCtorProbe: if the image loads, VarHandle classes are pre-initialized → AtomicLong/VarHandle work → uptodown onCreate proceeds. THE CORE ART BLOCKER IS SOLVED; this is the last mile of wiring, fully unblocked.

## ★ IMAGE LOADS AT RUNTIME (rc=0) — end-to-end pipeline validated (2026-07-04 s2) ★
Built the modified dlopen probe on the MAC with OHOS clang (`~/a2oh-source-audit/ohos-sdk/native/llvm/bin/clang --target=aarch64-linux-ohos --sysroot=.../native/sysroot -fPIC -shared -nostdlib -nodefaultlibs -Wl,--unresolved-symbols=ignore-all -I~/a2oh-source-audit/aosp-android-11/libnativehelper/include_jni -o out.so westlake_embedded_art_dlopen_probe.c`, 49KB, exports `westlake_embedded_art_run_stage`). CONFIRMED the deploy chain: exec probe `libwestlake_appspawn_exec_probe.so` dlopens `libwestlake_embedded_art_dlopen_probe.so` and calls `westlake_embedded_art_run_stage` — so the DLOPEN probe IS the VM creator. Deployed it (`.pre_image` backup on device), placed keep-rt multi-image at `$R/arm64/`, added `WESTLAKE_BOOT_IMAGE=1` to deploy-dayu600.sh (`.pre_image` backup). RESULT: `JNI_CreateJavaVM rc=0` WITH `-Ximage:$R/arm64/boot.art` — **the arm64 boot image LOADS successfully** (BCP prefix matches, checksums OK, no load error). Full pipeline build→load works. **BUT VarHandle still fails identically** (AtomicLong→CloneNotSupportedException via unreflectVarHandle; ColorSpace/Matrix→VarHandle.getAndAdd NPE). ROOT: the image build (dex2oat) SKIPS VarHandle pre-init (`class_linker.cc` pre_init loop `if (strstr(desc,"VarHandle")) continue;` + Root-class-init-loop skips `kJavaLangInvoke{,Field,StaticField}VarHandle`) — added originally to dodge the clinit crash — so the image contains VarHandle classes UNINITIALIZED, and the runtime re-runs their clinit (with the OLD libwestlake_art interpreter, which still has the wild write + the unreflectVarHandle→clone misdispatch) → same failure. NEXT (2 parts): (1) REMOVE the VarHandle skips in class_linker.cc and rebuild dex2oat + the runtime-BCP image — with the wild-write fix, test whether dex2oat can now pre-initialize VarHandle INTO the image (if the CloneNotSupportedException was a wild-write symptom it may now succeed; if it's a separate clone-dispatch bug, symbolize it like the wild write and fix). (2) Rebuild libwestlake_art.so WITH the wild-write fix (+GC-skip not needed at runtime) so the runtime interpreter is also correct. Then image-backed VarHandle should work → uptodown. NOTE: WSL connection went flaky (idle-shutdown) at session end; `ssh win "wsl ..."` may need a wake retry (WSL distro Ubuntu-24.04 STOPPED — `ssh win "wsl -l -v"` then a `wsl bash -lc` call restarts it; ignore the WSL-NAT/localhost warning).

## VarHandle un-skipped → next bug: clone MISDISPATCH (2026-07-04 s2)
Removed both VarHandle skips in `class_linker.cc` (`.pre_unskipvh` backup): pre_init-loop `if (strstr(desc,"VarHandle"))continue;`→gated `false &&`, and Root-class-init-loop `kJavaLangInvoke{,Field,StaticField}VarHandle` continue→gated `false &&`. Rebuilt `dex2oat.vh`; the runtime-BCP image still builds (boot.art 1.4MB, Post-fixup ran) and NO LONGER skips VarHandle — but VarHandle `<clinit>` now FAILS 25× (tolerated), so the wild-write fix did NOT fix this; it is a SEPARATE bug. PRECISE TRACE: `AtomicLong/AtomicInteger/AtomicBoolean.<clinit>` → `MethodHandles$Lookup.unreflectVarHandle` (MethodHandles.java:2044) → `Object.clone()` (Object.java:276) → `CloneNotSupportedException: Class java.lang.Class doesn't implement Cloneable`. i.e. `Object::Clone` (native `internalClone`, handler `UnstartedJNIObjectInternalClone` @unstarted_runtime.cc:2030 → `mirror::Object::Clone`) receives a **java.lang.Class** as the receiver where an ARRAY/Cloneable was intended — another receiver-misdispatch (same family as the newStringFrom wild write, but a distinct site). ALSO in the chain: `MethodType.makeImpl` (MethodType.java:309) → `MethodType$ConcurrentWeakInternSet.get` NPE (null intern set) hitting `arrayElementGetter`/`ObjectMethods`. So VarHandle `<clinit>` is a CHAIN of arm64 interpreter/UnstartedRuntime bugs; the clone-misdispatch is the first. NEXT: instrument `UnstartedJNIObjectInternalClone` to log the caller ArtMethod + receiver class when receiver is a Class → find why the wrong receiver (Class vs array) is passed to clone at unreflectVarHandle:2044 (likely an `array.clone()` whose receiver vreg is misread, or a Class[] element vs the array), fix it like the wild write; then handle the MethodType ConcurrentWeakInternSet NPE; then VarHandle clinit succeeds → image has initialized VarHandle → runtime works. The image-load pipeline (rc=0) and image build are DONE; VarHandle is a bounded chain of receiver/dispatch bugs to clear, each precisely traceable by symbolizing. dex2oat.vh (un-skip + wild-write fix) on device at westlake-boot-workdir/bin/.

## clone-on-Class refined (2026-07-04 s2): thrown in JAVA, not native
Instrumented `UnstartedJNIObjectInternalClone` (dex2oat.cl/.cl2): every clone that REACHES the native handler has a correct ARRAY receiver (enum `values()`→`$VALUES.clone()`, e.g. `VarHandle$AccessMode.values()`, all `recv_is_class=0 isArray=1` — these SUCCEED). ZERO `Class`-receiver clones reach internalClone. YET `CloneNotSupportedException: Class java.lang.Class doesn't implement Cloneable` still fires 25×. CONCLUSION: `Object.clone()` (Object.java:276) does the `this instanceof Cloneable` check in JAVA and throws BEFORE calling native `internalClone` — so the failing clone has a **java.lang.Class receiver** and is thrown at the Java level, invisible to the internalClone handler. Call site: `MethodHandles$Lookup.unreflectVarHandle` (MethodHandles.java:2044) → `Object.clone()`. So some code in unreflectVarHandle calls `.clone()` on a `java.lang.Class` instance where an array/Cloneable was intended = a receiver misread at the invoke-virtual of clone() (interpreter reads e.g. a `Class[]` element or the array-class instead of the array instance). NEXT-SESSION TACTIC: either (a) baksmali/dexdump `unreflectVarHandle` in core-oj-fieldfix.jar to see the exact bytecode at :2044 and which local it clones, or (b) instrument DoInvoke/FindMethodToCall in interpreter_common.h to log when the invoked method is `Object.clone`/`clone` and the receiver's class is `java.lang.Class`, with caller+vreg — then fix the receiver read (like the newStringFrom wild write). AFTER clone: the `MethodType.makeImpl`→`MethodType$ConcurrentWeakInternSet.get` NPE (null intern set during AOT) is the next link. VarHandle `<clinit>` = a bounded CHAIN of such arm64 receiver/dispatch bugs; each is precisely traceable by symbolize+instrument (the method that cracked the wild write). Instrumented dex2oat.cl2 on device. Backups: unstarted_runtime.cc.pre_wwfix (has the wild-write fix; the clone-diag instrumentation is extra, remove before final).

## STRATEGIC (2026-07-05): user wants a STABLE FOUNDATION (万年底座), NOT a per-app bypass. Deep-dived the clone.
User decided: build a rock-solid arm64 ART foundation, fix roots not symptoms. So NO VarHandle bypass. Continued root-causing the VarHandle-clinit clone. Findings: (a) `getDeclaredField` custom handler returns a CORRECT `java.lang.reflect.Field` (instrumented, isField=1) — not the corruption source. (b) `unreflectVarHandle`'s FIRST bytecode is `invoke-virtual {p1}, Field.getModifiers()` (p1=the Field param); crash = clone on a Class receiver → p1 is a Class at entry (caller passed wrong), OR method-attribution is bogus. (c) Instrumented DoInvoke (interpreter_common.h) to log ALL `clone` invokes + caller chain → fired 0× even though CloneNotSupported fires 25× → **the failing clone does NOT go through the standard `DoInvoke` invoke-virtual path**. (d) The tolerated-failure stack is only 3 frames `AtomicLong.<clinit>:84 → unreflectVarHandle:2044 → Object.clone` but AtomicLong.<clinit>:84 actually calls `findVarHandle` (disasm-confirmed), NOT unreflectVarHandle — so **the AOT method/line attribution for these boot classes is UNRELIABLE**. CONCLUSION: the failing clone is inside the **MethodHandle/VarHandle machinery via invoke-POLYMORPHIC** (MethodHandle.invoke), which bypasses DoInvoke — the deepest, hardest part of the JVM to get right on a hand-patched interpreter. This is why the fork originally SKIPPED VarHandle. HONEST SCOPE for the foundation: making MethodHandle/VarHandle fully correct on this hand-patched arm64 interpreter is a MAJOR multi-session effort (invoke-polymorphic, MethodType interning, caller-sensitive resolution, clone/array semantics all fragile); the truly-stable alternative is porting a PROPER complete arm64 libart (huge). NEXT if continuing the fix: instrument the invoke-polymorphic path (DoInvokePolymorphic / MethodHandle invoke handling) not DoInvoke, to catch the clone; expect this to be one of several deep MethodHandle bugs. Instrumented dex2oat.mdb on device. interpreter_common.h has the [DAYU600-MISDISPATCH] diag (remove before final).

## CONFIRMED (2026-07-05): the failing clone bypasses the interpreter entirely — it's in NATIVE MethodHandle machinery.
Instrumented THREE levels, all fired 0× on the failing clone while CloneNotSupported fires 25×: (1) DoInvoke (interpreter_common.h) clone check — 0×; (2) DoCall (interpreter_common.cc:7337, `[DAYU600-CLONE-CATCH]`, the entry EVERY interpreter method call passes through) — 0×. So the failing `Object.clone()` is invoked via `ArtMethod::Invoke` DIRECTLY from native C++ (the MethodHandle/VarHandle machinery in `runtime/method_handles.cc` — MethodHandleInvoke/MethodHandleInvokeExact/DoMethodHandleInvokeCommon), NOT through the interpreter's DoInvoke/DoCall. This is the deepest, hardest JVM layer. The enum `values()` clones DO go through the interpreter with correct ARRAY receivers and succeed; only the MethodHandle-machinery clone fails with a Class receiver. NEXT SESSION: instrument `runtime/method_handles.cc` (where the target ArtMethod is Invoked in the MethodHandle call path) to catch the clone target + receiver + the MethodType/coordinateTypes being cloned; expect the corruption to be in how the arm64 MethodHandle machinery builds/passes the receiver or a MethodType array. This is a genuine multi-session effort in the MethodHandle subsystem (matches the "万年底座 = weeks/months" honest scope told to the user). Backups on WSL for all the diag instrumentation; the load-bearing FIXES are: Fix A (interpreter.cc 861 pop-skip removed), wild-write bounds guard (unstarted_runtime.cc newStringFrom), GC-skip (heap.cc), xz-tolerate (xz_utils.cc), VarHandle un-skip (class_linker.cc).

## ★★ WORKFLOW CRACKED THE CLONE ROOT (2026-07-05) — plus a BUILD-RELIABILITY lesson ★★
TWO critical findings:
(A) BUILD RELIABILITY: WSL /mnt/c `touch <file>; make` does NOT reliably recompile — timestamps are flaky on /mnt/c, AND `DoCall`/`DoInvoke` are TEMPLATES instantiated in `build-ohos-arm64/stubs/template_instantiations.o` + `interpreter_switch_impl0.o`. So instrumentation added to those was silently NOT compiled in (verified: `strings dex2oat | grep DAYU600-CLONE-CATCH` = 0 despite source having it). This INVALIDATED several "0-hit" conclusions. FIX: to reliably compile a change, `rm build-ohos-arm64/.../<obj>.o` (and template_instantiations.o + interpreter_switch_impl0.o for template fns) before `make`. NOTE: the MISDISPATCH probe in DoInvoke, once actually compiled, made dex2oat crash at ~19 classes (per-invoke GetName overhead/instability) — DISABLE it. Prefer instrumenting NON-template files (interpreter.cc Execute, unstarted_runtime.cc) which recompile reliably.
(B) CLONE ROOT (workflow w0xmx47xo, source-verified): The failing `Object.clone()` runs inside `Execute()` (patched_interpreter.cc:1675, method @:1682) via TWO UN-PROBED wrappers — `EnterInterpreterFromEntryPoint` (:2229) and `ArtInterpreterToInterpreterBridge` (:2270) — driven by the polymorphic/caller-sensitive machinery (DoInvokePolymorphic @interpreter_common.cc:5714 → DoMethodHandleInvokeCommon/DoVarHandleInvokeCommon → MethodHandleInvoke in method_handles.cc → PerformCall → bridges → Execute). That is why probes on DoInvoke/DoCall/EnterInterpreterFromInvoke all miss. ROOT of the Class receiver (RANK 1): **`patched_interpreter_common.cc:7069` `result->SetL(caller_class)`** — the `VMStack.getStackClass2` handler in DoCallCommon's native branch returns the CALLER Class via a fragile arm64 `GetLink()×2` walk (:7048-7050). RANK 2: `patched_unstarted_runtime.cc:1958` (`UnstartedJNIVMStackGetStackClass2`) — the author's OWN comment @:1948-1951 names this exact symptom ("...used as a Class receiver -> FindMethodToCall<kVirtual>"). During `AtomicLong.<clinit> → findVarHandle` (@CallerSensitive), getStackClass2 returns a real Class; it's MOVE_RESULT_OBJECT'd into a vreg and then consumed as a virtual-call receiver → clone() (fixed vtable slot) → CloneNotSupportedException. The getStackClass2 "fix" (returning non-null Class instead of null) converted the old null-deref into the 25× CNSE. THE FINAL off-by-one (which vreg mis-selects the Class as the clone receiver) is in `method_handles.cc`/`var_handles.cc` VarHandle operand→receiver marshaling (analogous to the +1 operand shifts at base_interpreter_common.cc:351/372-374) — NOT in the local source set, so INSTRUMENT to pin. NEXT: add [CLONE-BODY] probe in Execute() after patched_interpreter.cc:1682 (recv via `accessor.RegistersSize()-accessor.InsSize()` = p0; walk `shadow_frame.GetLink()` chain) + [GSC2] probe before interpreter_common.cc:7069 (log caller_class ptr); if [CLONE-BODY] recv ptr == latest [GSC2] ptr → confirmed → fix the receiver index in method_handles.cc (pull it from WSL — it's NOT in scratchpad/artsrc). Full synthesis at scratchpad/tasks/w0xmx47xo.output. BLOCKER right now: WSL idle-shut-down and /mnt/c is not remounting (poll shows NOTYET for 90s+) — build host down; needs `wsl --shutdown` + cold restart to recover. **RECOVERY TECHNIQUE (works!):** repeated idle-shutdowns corrupt the drvfs /mnt/c mount so the source dir (`/mnt/c/Users/ufop/a2oh-source-audit/...`) becomes invisible from WSL while `/mnt/c/temp` still works and Windows/scp sees the source fine (confirmed: `scp win:"C:/Users/ufop/.../Makefile.ohos-arm64"` pulled 39705 bytes). `wsl --shutdown` piped through the Windows shell FAILS (系统找不到指定的路径). **The fix that WORKS: `ssh win "wsl -t Ubuntu-24.04"` (terminate the specific distro), sleep ~8s, then `ssh win "wsl bash -lc 'sleep 20; <check>'"` — the cold-start re-mounts drvfs cleanly (verified: deep source path RECOVERED).** Then do build work IMMEDIATELY (WSL idles fast); run long builds via `run_in_background` with a mount-wait loop. wfp.py (the GSC2+CLONE-BODY probe patcher) and patient.sh are staged at C:/temp/. Everything else (heap-scan, poisoning, OOB, clone-diag, misdispatch-diag) is instrumentation to strip before a clean build.

## ★ "clone bug" is really a VIRTUAL-DISPATCH/vreg corruption at MethodHandles.java:2044 (2026-07-04 s2) ★
Baksmali'd core-oj-fieldfix.jar (tools: `~/Library/Android/sdk/cmdline-tools/latest/lib/**` smali-3.0.9 jars on classpath, java at `/opt/homebrew/Cellar/openjdk/25.0.2/.../bin/java`; output at scratchpad/coj-smali/). MethodHandles$Lookup.unreflectVarHandle `.line 2044` bytecode = **`invoke-virtual {p1}, Ljava/lang/reflect/Field;->getModifiers()I`** — NOT clone! Yet the runtime executed `Object.clone()` on a receiver whose class is `java.lang.Class` → CloneNotSupportedException. So `Field.getModifiers()` MISDISPATCHED to `Object.clone()` AND the receiver read was a `java.lang.Class` instead of the Field `p1`. Chain: `AtomicLong.<clinit>` (line 84) → `MethodHandles$Lookup.findVarHandle(Class,String,Class)` → internally getDeclaredField → `unreflectVarHandle(Field)` → the misdispatched getModifiers. This is the SAME family as the newStringFrom wild write: a native/UnstartedRuntime handler in the reflection path corrupts the invoke's receiver vreg + method resolution, so a later invoke-virtual reads a Class receiver and resolves the wrong vtable method. NEXT: the reflection chain (findVarHandle→getDeclaredField→getModifiers) runs custom UnstartedRuntime handlers (Field.getArtField/getModifiers/getDeclaredField, Class.getDeclaredField*, etc., around unstarted_runtime.cc:2560-2640) — audit each like newStringFrom for an out-of-bounds/wrong-arg write that corrupts the receiver vreg, OR instrument DoInvoke (interpreter_common.h) to log when method==Object.clone && receiver->IsClassClass with caller+dexpc, then trace which prior reflection call corrupted vreg p1. This is the SAME symbolize→baksmali→instrument→fix method that cracked the wild write; the tools are all now proven on-hand (baksmali works). VarHandle clinit = this dispatch-corruption link, then the MethodType intern-set NPE. Still a multi-link chain + runtime rebuild before uptodown. Baksmali disasm cached at scratchpad/coj-smali/. Everything downstream (hwui 54/54, ServiceInterceptor, oh_window_manager_client surface path, hwui EGL hijack) is READY; VarHandle is the sole gate. Build host: `ssh win` → `wsl bash -lc`; AOSP-15 art source at `/home/dspfac/aosp-art-15`; build tree `/mnt/c/Users/ufop/a2oh-source-audit/art-latest/` (Makefile.ohos-arm64, patches/, link_pristine_classfix.sh, WESTLAKE_STATUS.md v114). Historical detail below (superseded framing):

**Earlier framing (superseded by the above): the embedded ART `libwestlake_art.so` + hand-patched `core-oj-fieldfix.jar` has BROKEN VarHandle/MethodHandles support.** `java.util.Random.<clinit>` → `AtomicLong.<clinit>` → `MethodHandles$Lookup.unreflectVarHandle(Field)` mis-dispatches to `Object.clone()` on a Class → `CloneNotSupportedException`; plus `java.lang.reflect.Field.toString()` infinitely self-recurses → StackOverflowError. ART "tolerates" the clinit failures so headless onCreate limps on, but this breaks ALL `java.util.concurrent.atomic.*` and VarHandle-using OJ17 code — which real `ViewRootImpl`/`Handler`/`Choreographer`/framework execution depends on. Root = core-oj/core-libart/libart version INCONSISTENCY, not a per-class bug; needs a coherent matched ART+core set (not on host). Fixing the missing-superinterface NCDFE first: built a dexlib2 stub dex (`java.util.random.RandomGenerator` empty iface + `jdk.internal.util.random.RandomSupport` no-op statics, `scratchpad/BuildStubs.java`) added as classes2.dex into core-oj-fieldfix.jar — that removed the NCDFE and REVEALED the deeper VarHandle wall. So the true remaining Gate-3 wall is a working runtime, then the real-ActivityThread+ViewRootImpl+surface-wiring cluster. Road-map workflow output saved: expert verdict = Gate 3 (visible 2048)+Gate 4 (touch) achievable with weeks of the above; Gate 5 (real text) is WEEKS (harfbuzz absent everywhere, must vendor+cross-compile); Gate 6 open-ended. Surface plan = Approach B: real `ANativeWindow_fromSurface` → `oh_anw_wrap(westlake_ohos_make_display_window(...))` (`gfx-smoke/fake_libandroid.c:91` forwarder). Shim strip = 36 UI classes (Activity/View/Window/…), keep 29 OHOS-glue.

Gates 4 (OHOS MMI touch→MotionEvent — mostly built in `bridge-src/oh_input_bridge.cpp`), 5 (real minikin+harfbuzz+FreeType; board has ICU; current text is header stub `include/minikin/MinikinCompat.h`), 6 (more games incl target `com.uptodown`) remain. Target APK `~/Downloads/uptodown-com.miHoYo.GenshinImpact.apk` is actually the **uptodown store client** (com.uptodown), NOT Genshin. Baseline backups on device: `framework.jar.pre_rendernode`, `liboh_android_runtime.so.pre_fatalbt`/`.pre_hwui_20260704`, `libandroid.so.pre_ahb`, `dayu600-framework-shim.dex.pre_typefacestrip`. See [[westlake-piercing-repro-repo]] if present.

## ★★★ WSL ELIMINATED + LOCAL MAC BUILD WORKS + CLONE ROOT PINNED (2026-07-05) ★★★
BREAKTHROUGH session. Three huge wins:
(1) **WSL is GONE.** The whole build env now runs LOCALLY on the Mac. Source at `/Users/yao/westlake-local-build/` (aosp-art-15, aosp-android-11, art-latest — transferred from Windows C: as a 656MB tar.gz via the user's own tool after scp proved too slow at 0.14MB/s over the public link; symlinks excluded via PowerShell reparse-point filter + `tar -T list`, 38749 files). Mac OHOS SDK at `/Users/yao/a2oh-source-audit/ohos-sdk/native` is IDENTICAL version (6.0.0.47, clang 15.0.4) to WSL's. Makefile adapted: 3 sed prefix swaps (`/home/dspfac`→`/Users/yao/westlake-local-build`, `/home/yao/ohos-sdk-6.0.0.47/linux/native`→Mac SDK, `/mnt/c/temp/libz.a`→local). Original saved as art-latest/Makefile.ohos-arm64.wsl-orig. `make -f Makefile.ohos-arm64 -j8 link-dex2oat` produces the SAME 26MB arm64 static dex2oat, RUNS on device. Build cycle now ~1min (rm the .o + make). libz.a + Makefile also copied to /Users/yao/westlake-local-build/. Boot jars pulled to bootjars/, baksmali'd with `java -cp <smali-baksmali+dexlib2+guava jars under ~/Library/Android/sdk/cmdline-tools/latest/lib/external/com/{android/tools/smali,google/guava}> com.android.tools.smali.baksmali.Main disassemble`.
(2) **CLONE-BODY probe (in Execute() @interpreter.cc, after `pfc_execute_method = shadow_frame.GetMethod()`) WORKS** — catches Object.clone entry, prints receiver + shadow-frame GetLink chain + NthCallerVisitor real-stack (needs `#include "nth_caller_visitor.h"`; NthCallerVisitor has `.caller` (ArtMethod*) + `.caller_pc` but NO `.dex_pc`). GSC2 probe (interpreter_common.cc before `result->SetL(caller_class)`) does NOT compile into the binary (it's in a template → template_instantiations.o didn't pick it up; not needed anyway).
(3) **CLONE ROOT PINNED (supersedes the getStackClass2 theory):** real stack = `Object.clone()` ← `MethodHandles$Lookup.unreflectVarHandle(Field)` ← `AtomicInteger.<clinit>()`. NO intermediate frames. unreflectVarHandle's shadow frame dex_pc=**0** (method entry). unreflectVarHandle's FIRST bytecode is `invoke-virtual {p1}, Ljava/lang/reflect/Field;->getModifiers()I`. Its bytecode has NO clone() call anywhere (verified via baksmali: getModifiers/isStatic/getDeclaringClass/getType→v4/commonFieldChecks{v1..v6}invoke-direct-range/FieldVarHandle.create/StaticFieldVarHandle.create). So **the FIRST invoke-virtual (getModifiers on the Field p1) is MIS-DISPATCHED to Object.clone() with receiver = a stray `int.class`** (the field TYPE — but getType hasn't run yet at dex_pc 0, so the int.class is GARBAGE left in a vreg by the caller frame / register overlap). recv is `java.lang.Class<int>` for AtomicInteger, `<long>` for AtomicLong. This is a C++ interpreter invoke-virtual register/receiver or vtable-index bug, NOT getStackClass2. The normal enum `values()`→`$VALUES.clone()` (AccessMode[], DirectMethodHandleDesc$Kind[]) clones SUCCEED (isClass=0, arrays are Cloneable). NEXT: instrument the caller frame's current instruction (decode opcode + method_idx at caller dex_pc) to confirm getModifiers→clone mis-dispatch, then find the interpreter invoke-virtual receiver/vtable bug. Substrate device 5ce2dcee...; boot compile = `dex2oat --android-root=/system --runtime-arg -Xverify:none --runtime-arg -Xmx850m --base=0x70000000 --dex-file=stringfactory.jar --dex-file=core-oj-fieldfix.jar --dex-file=core-libart.jar --instruction-set=arm64 --compiler-filter=verify` at /data/local/tmp/wl-imgboot; probed binary deployed as dex2oat.cb.

## CLONE ROOT REFINED → invoke-virtual VTABLE MIS-DISPATCH (2026-07-05, local build)
Definitive via caller-inst decode probe (interpreter.cc CLONE-BODY, decode caller ShadowFrame instr via `CodeItemInstructionAccessor fa(fm->DexInstructions()); fa.InstructionAt(fpc).DumpString(fm->GetDexFile())`):
- `AtomicInteger.<clinit> @dex10` = `invoke-virtual {v0,v1,v2,v3}, MethodHandles$Lookup.findVarHandle(Class,String,Class) // method@5231` (v3=int.class=Integer.TYPE).
- The frame that then RUNS is `unreflectVarHandle @dex0` = `invoke-virtual {v9}, Field.getModifiers() // method@6001`, with **v9 = int.class** (garbage/mis-marshaled arg).
- getModifiers on receiver int.class (a Class) → vtable slot on Class → `Object.clone()` → CloneNotSupportedException.
**ROOT (single):** `invoke-virtual findVarHandle (method@5231)` on a MethodHandles$Lookup receiver is VTABLE-MIS-DISPATCHED to `unreflectVarHandle` (both are Lookup methods) — wrong vtable entry selected. Args mis-marshal (v9←int.class) as a consequence; the getModifiers→clone is a secondary effect of the garbage receiver. So the real bug is the interpreter's invoke-virtual vtable-index resolution OR the vtable construction (class_linker) during boot-image pre-init. findVarHandle/unreflectVarHandle are likely @CallerSensitive — the interpreter may special-case caller-sensitive/polymorphic methods and substitute wrong. Investigate: entrypoint_utils-inl.h FindMethodToCall(~516)/kVirtual GetVTableEntry(~594), interpreter_switch invoke-virtual, class_linker vtable build, and any CallerSensitive/PolymorphicSignature interpreter special-casing. NOT getStackClass2, NOT a range-invoke +1 shift. Fast local iterate: edit patches/runtime/interpreter/*.cc, `cd art-latest; rm build-ohos-arm64/runtime/interpreter/interpreter.o; make -f Makefile.ohos-arm64 -j8 link-dex2oat`, `hdc file send ... dex2oat.cb`, run boot compile, grep cb.log for CLONE-BODY/caller-chain/real-frame.

## ★ SMOKING GUN: MethodHandles$Lookup VTABLE IS MALFORMED (2026-07-05) ★
NTERP-VT probe (patches nterp.cc kVirtual branch, logs resolved method + GetMethodIndex + declaring-class vtable): 
`method_idx=5231 resolved=findVarHandle vtable_idx=0 decl=MethodHandles$Lookup vtable_len=11 at_slot=unreflectVarHandle`.
So: method@5231 resolves CORRECTLY to findVarHandle, but findVarHandle->GetMethodIndex()==**0** (WRONG — a class's own new virtual methods must get indices >= superclass vtable length ~11), and MethodHandles$Lookup's vtable_len==**11** (≈ just Object's inherited methods — Lookup's ~30+ own public virtual methods are NOT properly appended), and vtable[0]==unreflectVarHandle. So invoke-virtual findVarHandle → GetVTableEntry(0) → unreflectVarHandle → garbage args (v9=int.class field type) → getModifiers on int.class → Object.clone → CloneNotSupportedException. **THE ROOT IS THE VTABLE/method-index CONSTRUCTION for MethodHandles$Lookup (and likely other java.lang.invoke classes) in class_linker.cc LinkVirtualMethods** — Lookup's own virtual methods are being mis-assigned index 0 (as if overriding Object slot 0) instead of appended, truncating the vtable to 11. FIX target: class_linker.cc LinkVirtualMethods / vtable build (patches/runtime/class_linker.cc first, base second). NOT interpreter dispatch (FindMethodToCall/NterpGetMethod are correct — they faithfully use the broken GetMethodIndex). This likely ties to the earlier VarHandle-skip un-skip in class_linker (line ~1414/1464) leaving java.lang.invoke class-linking half-broken.

## ★★ COMPLETE PICTURE: Lookup vtable override-detection BROKEN (2026-07-05) ★★
Full vtable dump (NTERP-VT probe): MethodHandles$Lookup has num_virt=27, num_direct=21, but vtable_len=**11** (= Object's vtable only). Dump:
vtable[0]=unreflectVarHandle(Field) [WRONG—should be Object.clone], [1]=Object.equals [2]=Object.finalize [3]=Object.getClass [4]=Object.hashCode [5]=Object.notify [6]=Object.notifyAll [7]=Lookup.toString(legit override) [8]=Object.wait [9]=Object.wait(long) [10]=Object.wait(long,int).
findVarHandle is public, non-direct, non-final, non-copied, non-miranda → a NORMAL virtual method, yet GetMethodIndex()==0. So **ALL 27 of Lookup's own virtual methods are being mis-detected as OVERRIDES of Object's ~11 methods → assigned indices in [0,11) instead of appended → vtable stays at 11, clone(slot0) overwritten by unreflectVarHandle, findVarHandle also lands at 0**. invoke-virtual findVarHandle → GetVTableEntry(0) → unreflectVarHandle. ROOT = `AssignVTableIndexes` override detection (signature-hash / name+sig compare against the super-class vtable hash set) is FALSE-MATCHING every Lookup method to an Object slot, during boot-image creation with core-oj-fieldfix.jar. Definition: patches/runtime/class_linker.cc LinkMethodsHelper::AssignVTableIndexes (decl ~8129) — need its body + ComputeMethodHash/VTableSignatureHash. FIX = correct the override detection so new methods (not matching any super method's name+signature) get APPENDED with fresh indices >= super_vtable_length. Verify by re-dumping Lookup vtable (should be ~38 with findVarHandle at a high index and clone restored at 0). This single fix should unblock the whole VarHandle clinit chain → boot image completes.

## ★★★ CLONE/VTABLE BUG FIXED! (2026-07-05) — real latent AOSP bug ★★★
ROOT CAUSE (definitive): in `ClassLinker::LinkMethodsHelper<>::AssignVTableIndexes` (class_linker.cc ~9157), the `initialized_methods` BitVector is constructed over `bit_vector_buffer_ptr` which is a slice of an **un-zeroed `alloca` buffer** (BitVector ctor with external storage does NOT zero it — verified bit_vector.cc). On arm64 on-device boot-image creation the stack holds garbage (0xFE from ClobberOldMethods reuse), so initialized_methods starts with bits SET → in the "add non-overridden methods at the end" loop, `!initialized_methods.IsBitSet(i)` is false for all → ALL of a class's own virtual methods are dropped, vtable truncated to super length. For MethodHandles$Lookup (27 virtuals): final_vtable_length=11 instead of 37, overriding=27 instead of 1, findVarHandle got index 0 (collided with unreflectVarHandle) → invoke-virtual findVarHandle dispatched to unreflectVarHandle → int.class as receiver → getModifiers→clone → 25× CloneNotSupportedException. Real Android never hits this because the boot image is built on x86_64 HOST where that alloca stack slot happens to be zero; the fork builds the image ON the arm64 device via the C++ interpreter, exposing the latent bug.
**FIX (applied, 1 line):** in patches/runtime/class_linker.cc, right before the `BitVector initialized_methods(...)` construction, add `std::fill_n(bit_vector_buffer_ptr, bit_vector_size, 0u);`.
**VERIFIED:** [AVT] Lookup final_vtable_length=37 overriding=1; findVarHandle vtable_idx=24; **CloneNotSupported count = 0** (was 25). The whole VarHandle/AtomicInteger/AtomicLong clinit chain now links correctly. Boot pre-init now advances to 274 classes then hits a DIFFERENT crash (exit 139) — the next blocker, NOT clone. Diagnostic probes still in tree (interpreter.cc CLONE-BODY + caller-inst/real-frame, nterp.cc NTERP-VT vtable dump, class_linker.cc AVT) — REMOVE them for the clean build once done debugging. This vtable fix is LOAD-BEARING and CORRECT (not a workaround) — keep it.

## ★★★ BOOT IMAGE CREATED! (2026-07-05) — vtable fix unblocked the whole pipeline ★★★
With the BitVector-zero vtable fix, dex2oat now runs the FULL boot-image pipeline: pre-init (47+ Pre-init classes, many benign "Tolerating clinit failure" for classes needing runtime natives) → image_writer bin assignment (image_writer.cc:2429 "Found 12243 objects without assigned bin slots") → xz compression (tolerated empty, OHOS lzma broken) → post-fixup entry points (image_writer.cc:667) → **multi-image FILES WRITTEN**: boot.art(1.4MB)+boot.oat+boot.vdex, boot-core-libart.art(437KB)+oat+vdex, boot-core-oj-fieldfix.art(4.9MB)+oat+vdex at /data/local/tmp/wl-imgboot/out/. CloneNotSupported=0. BUT dex2oat then SIGSEGVs (exit 139) at the very end in std::ostream::sentry / __put_character_sequence (pc~0xee2170, x30~0x4997cc) writing a string containing "arena...loc=0B (" (register x6=0x6c6120616e657261="arena la", x7=0x282042303d636f6c="loc=0B ("), fault addr 0xffffffffffffffe8(-24, bad ostream rdbuf). This is a MemStats/arena-allocator stats dump LOG at shutdown crashing on a bad ostream (other LOG(WARNING) work fine, so it's a specific ostream/ostringstream, not std::cerr). NEXT: find + suppress/fix this stats-dump LOG so dex2oat exits 0 and the image is properly finalized (checksums), then LOAD the new image (correct vtables) in a runtime and verify VarHandle/AtomicInteger work at runtime → then downstream to APK. Clean binary (probes disabled) deployed as dex2oat.clean; boot compile cmd in memory above.

## ★★★ CLEAN BOOT IMAGE BUILD — exit 0! (2026-07-05) ★★★
Fixed the cosmetic shutdown SIGSEGV: in aosp-art-15/dex2oat/dex2oat.cc `Dex2Oat::LogCompletionTime()` (~2870, called from ~Dex2Oat() destructor), dropped the `driver_->GetMemoryUsageString(...)` arg — it dumps arena MemStats via a std::ostream that faults at shutdown on this standalone arm64 build (AFTER the image is already finalized). dex2oat.cc compiles from BASE ($(ART)/dex2oat/*.cc wildcard; no patches/dex2oat), edited the local base copy. NOW: **`dex2oat.clean` runs the full boot-image build in ~1.36s, SIGSEGV=0, exit code 0**, producing valid multi-image (boot.art/oat/vdex + boot-core-libart.* + boot-core-oj-fieldfix.*) at /data/local/tmp/wl-imgboot/out/. The FOUNDATION ("万年底座") is now solid: a clean, reproducible, correct-vtable arm64 boot image built on-device. NEXT PHASE toward "run any APK": (1) load this image in a runtime (embedded ART dlopen probe / JNI_CreateJavaVM -Ximage) and verify VarHandle/AtomicInteger + general classes work at runtime; (2) load an APK's dex atop the boot image and run its code; (3) wire the app UI (ThreadedRenderer/hwui already paints per gate-1/2) to screen. All diagnostic probes disabled (if(false)) but present; vtable fix (std::fill_n) + dex2oat exit fix are LOAD-BEARING keepers.

## ★★★ RUNTIME LOADS IMAGE + APP LAUNCHES (2026-07-05) — next: libart.so needs the same vtable fix ★★★
Replaced the on-device boot image at $R/arm64/ (R=/data/local/tmp/westlake-dayu600-substrate; backed old up to $R/arm64.pre-vtablefix/) with the NEW clean image from wl-imgboot/out/. Ran deploy-dayu600.sh (appspawn hybridspawn → aa start com.digiplex.game). RESULTS: embedded_art_dlopen_probe log shows **JNI_CreateJavaVM rc=0** loading -Ximage:$R/arm64/boot.art, register_system_natives rc=0, startReg rc=0, Dayu600ApkStageProbe.embeddedMainNoExit rc=24 OK. App process `m.digiplex.game` LAUNCHES ("start ability successfully"), hwui graphics registrars run (view_ThreadedRenderer etc., hardFailures=0). BUT at RUNTIME, AtomicInteger/AtomicLong.<clinit> STILL throw CloneNotSupportedException (tolerated now, not fatal) via unreflectVarHandle — because the RUNTIME **libart.so** (/data/local/tmp/westlake-dayu600-substrate/android/lib64/libart.so, 28MB, fork-built with DAYU600 markers, dlopen'd by the embedded probe) was NOT rebuilt with the BitVector-zero fix, so its class-linker re-links MethodHandles$Lookup with the same truncated vtable. THE LOCAL MAKEFILE BUILDS A STATIC `dalvikvm` (target `link-runtime`), NOT a shared libart.so — no `-shared`/`libart.so` target exists. NEXT: either (a) add a libart.so shared-lib target to the Makefile linking the runtime .o (which now include the fixed class_linker.o) and redeploy it, or (b) find the fork's original libart.so build recipe, or (c) determine why the runtime re-links Lookup instead of using the image's correct (vtable_len=37) copy and make it use the image. The app is CURRENTLY progressing (clinit failures tolerated); need to check if it reaches MainActivity.onCreate + renders. Target app is **com.digiplex.game.MainActivity** (a game), not com.uptodown. deploy cmd: `hdc shell "cd /data/local/tmp && sh deploy-dayu600.sh"`; app stderr=/data/local/tmp/westlake-appspawnx-2048-ams-exec.stderr; embedded log=/data/local/tmp/westlake-embedded-art-dlopen-probe.log.

## ★★★★ VTABLE/CLONE BUG ROBUSTLY FIXED END-TO-END (2026-07-05) ★★★★
The std::fill_n(bit_vector_buffer_ptr,...) fix was NON-ROBUST: -O2 ELIMINATED it in clean builds (it only "worked" when unrelated diagnostic probe code perturbed the stack frame — that's why probed builds gave clone=0 but clean builds gave clone=2, which wasted an iteration). **ROBUST FIX (final): in patches/runtime/class_linker.cc AssignVTableIndexes, construct `BitVector initialized_methods(...)` FIRST, then call `initialized_methods.ClearAllBits();`** — ClearAllBits() is an out-of-line function (bit_vector.cc) the optimizer cannot drop, so it deterministically zeroes the storage. This is the LOAD-BEARING keeper fix (replaces std::fill_n).
**VERIFIED with CLEAN production binaries (all diagnostic probes disabled: CLONE-BODY interpreter.cc, NTERP-VT nterp.cc, AVT class_linker.cc all `if(false)`):** dex2oat.v2 (image) + libart.so both rebuilt clean, deployed, `CloneNotSupported=0` STABLE. 
BOTH SIDES needed the fix + BOTH deployed: (1) dex2oat builds the boot image (Lookup vtable_len=37) → copy boot.art/oat/vdex + boot-core-libart.* + boot-core-oj-fieldfix.* to $R/arm64/; (2) **libart.so is the RUNTIME** — built via a NEW Makefile target I added to Makefile.ohos-arm64: `link-libart` (shared lib from RUNTIME_OBJS incl. fixed class_linker.o + westlake_jni.o + version-script stubs/westlake_exports.map, OHOS libc++.a/libc++abi.a/libz.a, -soname libwestlake_art.so, output build-ohos-arm64/lib/libart.so ~28MB, exports JNI_CreateJavaVM) → deploy to $R/android/lib64/libart.so. NOTE the LD_LIBRARY_PATH order in deploy-dayu600.sh puts $R/art before $R/android/lib64 (but $R/art has no libart.so so android/lib64 wins). Backups on device: $R/arm64.pre-vtablefix/, $R/android/lib64/libart.so.pre-vtablefix. **IMPORTANT gotcha:** deploy-dayu600.sh APPENDS to the app stderr log; `rm -f $ERR` before each run or stale CloneNotSupported lines mislead.
**RESULT:** com.digiplex.game.MainActivity app process launches (JNI_CreateJavaVM rc=0), hwui 54/54 graphics registrars OK, AtomicInteger/AtomicLong now init PAST findVarHandle (clone gone). 
**NEW FRONTIER (next):** app not yet at onCreate/rendering; now blocked by STANDALONE-RUNTIME-COMPLETENESS tolerated clinit NPEs: AtomicInteger/AtomicLong.<clinit> now fail at `GenericDeclaration.getAnnotationsByType(Class) on a null object`; ColorSpace/Matrix fail at `VarHandle.getAndAdd(Object[]) on null` (their static VarHandle is null because the annotation NPE aborted the clinit that would set it); FontVariationAxis `nativePtr is null`; various `UnsatisfiedLinkError`/missing-native. These are the real-framework-init completeness issues (gates 4-6): need annotation reflection + more native method stubs working in the standalone runtime so these clinits succeed → then ActivityThread/onCreate → ViewRootImpl/ThreadedRenderer → on-screen. Target app = com.digiplex.game.

## NEXT-SESSION ROADMAP (2026-07-05, after vtable fix)
Deployed state (clean, fixed, backed up): dex2oat.v2 + libart.so (ClearAllBits fix, no probes) on device; image at $R/arm64/; backups at $R/arm64.pre-vtablefix/ + $R/android/lib64/libart.so.pre-vtablefix. Local clean source: all 3 probes `if(false)`, ClearAllBits fix live. To rebuild: `make -f Makefile.ohos-arm64 -j8 link-dex2oat link-libart`.
KEY REALIZATION: the current deploy runs `WESTLAKE_APP_STAGE=randomCtorProbe` → `Dayu600ApkStageProbe.embeddedMainNoExit` (a TEST STAGE that ctors Random→AtomicLong etc.), NOT the real com.digiplex.game.MainActivity.onCreate. The process runs the stage then exits. So "on screen" needs BOTH: (a) standalone-runtime completeness so framework clinits succeed, (b) driving the real Activity lifecycle (change WESTLAKE_APP_STAGE / the appspawn target flow to actually call ActivityThread → handleLaunchActivity → onCreate → setContentView → ViewRootImpl → ThreadedRenderer draw).
NEXT BLOCKERS (standalone runtime completeness, all currently tolerated clinit NPEs): 
1. findVarHandle internally NPEs at `GenericDeclaration.getAnnotationsByType(Class) on null` — some reflection/annotation/hidden-api native returns null inside Lookup.findVarHandle→commonFieldChecks field-annotation processing. Trace which native/reflection returns null; stub/fix it so AtomicInteger/AtomicLong VALUE VarHandle gets set (else all atomics are null → cascade).
2. FontVariationAxis `nativePtr is null`, ColorSpace/Matrix VarHandle null (downstream of #1), many `UnsatisfiedLinkError` for framework natives.
Approach: pick the highest-fanout clinit failure (atomics via #1), fix its root native/reflection gap, rerun, watch the tolerated-failure list shrink, until framework init completes → then switch the stage to drive real onCreate. Consider a workflow to triage the tolerated-clinit-failure list by root cause.

## NEXT DEEP BUG PINNED (2026-07-05): interface default-method / invoke-super resolves to null
After the vtable/clone fix, the highest-fanout remaining clinit failure is: System.<clinit> → initUnchangeableSystemProperties → VMRuntime.<clinit> → VMRuntime.<init> → AtomicInteger.<clinit>:70 → findVarHandle → **Class.getAnnotationsByType (Class.java:4299)** throws `NullPointerException: Attempt to invoke InvokeType(3) [=kSuper] method GenericDeclaration.getAnnotationsByType(Class) on a null object reference`. Class.getAnnotationsByType:4299 bytecode = `invoke-super {p0, p1}, Ljava/lang/reflect/GenericDeclaration;->getAnnotationsByType(Ljava/lang/Class;)` — a super-call to the DEFAULT interface method AnnotatedElement/GenericDeclaration.getAnnotationsByType. The receiver p0 is `this` (a non-null Class), yet the runtime reports a null receiver → this is a DEFAULT/COPIED-method or invoke-super resolution bug in the class linker/interpreter (analogous class of bug to the vtable one: something in interface-method-table / copied-default-method linking resolves wrong, likely another un-initialized buffer or the FinalizeIfTable/ReallocMethods/imt path in class_linker.cc LinkMethodsHelper). NOTE: AOT/line attribution here is historically unreliable in this fork, so INSTRUMENT empirically (the vtable bug taught this — static analysis got it wrong). Suggested probe: in the interpreter invoke-super / FindSuperMethodToCall path, log when the referenced method name is "getAnnotationsByType" (or when a super/default-method resolution yields null/runtime-method), dump resolved method + receiver + declaring class + the class's copied-methods/iftable. Check class_linker.cc copied-default-method setup (num_new_copied_methods_, ReallocMethods ~8839, FinalizeIfTable) for another un-zeroed/miscopied buffer. Fixing this unblocks getAnnotationsByType → findVarHandle field-annotation check → atomics init → System/VMRuntime clinit → framework. This is gate-4+ standalone-runtime completeness. Build/deploy loop: edit patches/, `make -f Makefile.ohos-arm64 -j8 link-libart` (runtime) [and link-dex2oat if image classes affected], push libart.so to $R/android/lib64/, `rm -f $ERR; sh /data/local/tmp/deploy-dayu600.sh`, grep $ERR.

## CONTRADICTIONS TO RESOLVE NEXT (2026-07-05) — dex2oat-build vs runtime divergence
Confusing signals found while chasing the getAnnotationsByType NPE, need fresh systematic investigation:
1. **dex2oat.v2 IMAGE BUILD (v2.log) STILL shows AtomicInteger clinit fail = CloneNotSupportedException** (findVarHandle→unreflectVarHandle mis-dispatch), i.e. the ClearAllBits fix appears NOT to take effect during dex2oat's own pre-init — YET the RUNTIME (fixed libart.so loading that same image) shows clone=0 and AtomicInteger gets PAST clone to getAnnotationsByType. So either (a) the image's persisted Lookup vtable is correct (37) even though AtomicInteger.<clinit> transiently failed clone earlier in the build (ordering: clinit ran before Lookup relinked?), or (b) dex2oat and libart.so somehow get different class_linker behavior despite linking the same CL_PATCH_OBJ. MUST verify: rebuild dex2oat WITH the AVT probe + ClearAllBits and check whether [AVT] Lookup prints overriding=1/vtable_len=37 during the v2-equivalent build; if it still prints 27/11, ClearAllBits is ALSO being defeated in dex2oat's compile (then the fix needs to be even more robust, e.g. `volatile` writes or memset via a noinline helper). If it prints 37, the build-time clone is a harmless ordering artifact and only the runtime getAnnotationsByType matters.
2. **The runtime getAnnotationsByType null-receiver NPE does NOT go through FindMethodToCall** — added [DAYU600-NULLRECV] probe to entrypoint_utils-inl.h:552 (the `if (this_object==nullptr) ThrowNullPointerExceptionForMethodAccess`), rebuilt libart.so (probe confirmed in binary via strings), but it did NOT fire. So the NPE is thrown elsewhere: other ThrowNullPointerExceptionForMethodAccess call sites are in interpreter_common.cc + unstarted_runtime.cc + common_throws.cc. Next: instrument THOSE (esp. interpreter_common.cc DoInvoke/DoInvokePolymorphic null checks) or grep for the exact null-receiver throw for invoke-super/kSuper. Also confirm the probed libart.so was actually the loaded one (verify strings on-device + that the app didn't fall back to a cached lib).
The NULLRECV probe is currently in entrypoint_utils-inl.h — DISABLE/remove it for clean builds. Deployed libart.so may currently carry it (harmless, capped at 20).

## ★ ROBUST FIX FOUND (volatile) + RUNTIME-vs-IMAGE PUZZLE (2026-07-05) ★
**THE fix (final, robust): in class_linker.cc AssignVTableIndexes, BEFORE constructing `initialized_methods`, zero bit_vector_buffer_ptr via a VOLATILE pointer loop:** `{ volatile uint32_t* v = bit_vector_buffer_ptr; for (size_t i=0;i<bit_vector_size;++i) v[i]=0u; }`. Reason: reading the un-zeroed alloca buffer is UB, and -O2 EXPLOITS that to delete plain std::fill_n/memset AND ClearAllBits (proven: with AVT probe, std::fill_n→vtable37 but ClearAllBits→vtable11; the AVT read was accidentally keeping fill_n alive). Only volatile writes (observable side effects) survive the optimizer. **VERIFIED: dex2oat image build now shows [AVT] Lookup vtable_len=37 overriding=1, CloneNotSupported=0 during build.** This is the LOAD-BEARING keeper (replaces std::fill_n and ClearAllBits, both non-robust).
**UNRESOLVED PUZZLE (next session, fresh/careful):** even with the correct image (Lookup=37, build clone=0) AND libart.so rebuilt with the volatile fix, the RUNTIME still shows AtomicInteger/AtomicLong clone=2 (findVarHandle→unreflectVarHandle). At runtime: AVT does NOT fire (runtime does NOT re-link Lookup → should use image's 37) and NTERP-VT does NOT fire (findVarHandle not resolved via NterpGetMethod → cached/pre-resolved). So the runtime dispatches findVarHandle via index 0 from SOME cached/image-persisted source, despite the image linking it to 24 and having vtable_len=37. Hypotheses to check: (1) does the image WRITER persist findVarHandle's ArtMethod.method_index_ (=24) and the embedded vtable correctly, or does it store stale 0? (oatdump/image inspection, or a probe in image_space load); (2) is the interpreter-cache/DexCache resolved-methods for AtomicInteger's findVarHandle invoke pre-populated with a stale index 0 in the image; (3) is the runtime actually loading the v3c image + volatile libart.so (verify on-device). DEFINITIVE next probe: in libart.so, in Execute() CLONE-BODY (fires on the runtime clone), resolve MethodHandles$Lookup and dump its vtable_len + findVarHandle.GetMethodIndex() AT RUNTIME — if 11/0 the image persisted a broken Lookup (image-writer bug); if 37/24 the mis-dispatch is a cache issue. Clean state left on device: dex2oat.v3c + volatile-fixed clean libart.so (0 probes) deployed; v3c image (build clone=0) at $R/arm64/.

## ★★★★★ PUZZLE RESOLVED: it was ZYGOTE CACHING, not an image bug (2026-07-05) ★★★★★
The "runtime clone=2 despite correct image" puzzle was NOT an image-writer bug. ROOT: the deploy uses a KEEPALIVE appspawn zygote (KEEPALIVE_PROTO_PID) that loaded the OLD (buggy) libart.so ONCE; app processes fork from it and inherit the stale libart.so, so redeploying libart.so had NO effect (and CLONE-BODY/RT-LOOKUP/AVT/NTERP-VT probes never fired = my new interpreter wasn't running). **FIX/GOTCHA: after replacing libart.so, force-kill ALL appspawn/zygote/app processes before re-deploying** (`hdc shell "for p in \$(ps -A | grep -E 'appspawn-x|digiplex|appspawn' | ...); do kill -9 \$p; done"`), else the fresh zygote won't load the new lib. The deploy script's own `kill appspawn-x-prot` was insufficient. 
**AFTER killing the zygote + fresh deploy with the volatile-fixed libart.so: runtime CloneNotSupported=0, STABLE.** The volatile-write vtable fix (in class_linker.cc AssignVTableIndexes) is CONFIRMED working END-TO-END: dex2oat image build clone=0 (Lookup vtable_len=37) AND runtime clone=0. VTABLE/CLONE BUG FULLY, ROBUSTLY RESOLVED. Disregard the earlier "image-writer persistence" / "37 vs 24" hypotheses — they were artifacts of the stale zygote.
**CURRENT REAL BLOCKER (app progresses past clone now):** AtomicInteger/AtomicLong.<clinit> now fail at `Class.getDeclaredAnnotation(Class) on a null object reference` (InvokeType(2)=kVirtual, null receiver) — inside findVarHandle's field-annotation/hidden-api processing, some reflection returns a null Class that's then used as a getDeclaredAnnotation receiver. Downstream: ColorSpace/Matrix `VarHandle.getAndAdd on null` (their static VarHandle is null b/c the annotation NPE aborted the clinit), FontVariationAxis `nativePtr is null`. These are standalone-runtime reflection/native completeness (gate-4+). Next: trace the null Class in the findVarHandle→getDeclaredAnnotation path (empirically — instrument the null-receiver throw for getDeclaredAnnotation, but note it did NOT go through entrypoint_utils FindMethodToCall earlier — check interpreter_common.cc/switch-interp null checks and remember to KILL THE ZYGOTE before each test). All probes currently ON in the deployed libart.so (CLONE-BODY re-enabled + RT-LOOKUP) — rebuild clean (disable them) once done.

## HONEST STATE + FLAKINESS (2026-07-05, end of marathon session)
The volatile-write vtable fix is SOLID in dex2oat: image build ALWAYS shows [AVT] Lookup vtable_len=37, overriding=1, CloneNotSupported=0 (verified repeatedly). BUT the RUNTIME is FLAKY: with the clean volatile-fixed libart.so + fresh zygote (killed first), some runs give clone=0 (AtomicInteger progresses to getDeclaredAnnotation NPE) and some give clone=2 (findVarHandle→unreflectVarHandle mis-dispatch returns). This flakiness means the runtime's Lookup vtable linking is NON-deterministic despite the volatile fix + a correct image. Since AVT/NTERP-VT did NOT fire at runtime in earlier tests (though those were confounded by the zygote-cache bug), it's UNCONFIRMED whether the runtime (a) re-links Lookup itself (flaky → another un-zeroed buffer in AssignVTableIndexes/LinkMethodsHelper that I did NOT fix: declared_virtuals_buffer, super_vtable_buffer, same_signature_vtable_lists — but same_signature is std::fill_n'd at 9220 and could ALSO be -O2-eliminated like the bit_vector was; declared_virtuals is the hash-set buffer) or (b) loads an inconsistent Lookup from the image. NEXT (fresh, systematic): (1) re-add AVT to libart.so, kill zygote, run MULTIPLE times — does AVT fire at runtime? what vtable_len each time? This tells re-link vs image-load. (2) If re-link: apply the SAME volatile-zero treatment to ALL alloca-derived buffers in AssignVTableIndexes (esp. change the `std::fill_n(same_signature_vtable_lists.data(), super_vtable_length, dex::kDexNoIndex)` at ~9220 to a volatile loop, since -O2 may eliminate it too). (3) The getDeclaredAnnotation NPE (the blocker AFTER clone, when clone=0): thrown NOT from entrypoint_utils FindMethodToCall (NULLRECV probe never fired) but likely from **interpreter_common.cc:5521 (patched) / :322 (base) ThrowNullPointerExceptionForMethodAccess** (a different invoke path — DoInvokePolymorphic/DoCall) — instrument THERE next. Class.getAnnotation:4245 `invoke-virtual {p0=this}, Class.getDeclaredAnnotation` (a NATIVE method) reports null receiver despite this being non-null → same wrong-receiver symptom class as clone.
CRITICAL OPERATIONAL NOTE: after replacing libart.so, ALWAYS `kill -9` all appspawn-x/digiplex processes before re-deploy (the keepalive zygote caches the old lib). And clear $ERR (deploy appends). Clean binaries currently deployed (0 probes) but runtime is flaky clone 0/2 — NOT a finished state; the runtime Lookup linking needs the fresh investigation above.

## ★★★ DEFINITIVE LOCALIZATION: IMAGE-WRITER does not persist Lookup's fixed vtable (2026-07-05) ★★★
Ran AVT-probed libart.so at runtime with robust `pkill -9 -f appspawn-x; pkill -9 -f digiplex` before deploy. RESULT: **AVT does NOT fire at runtime** (runtime does NOT re-link MethodHandles$Lookup) YET **clone=2**. Combined with: dex2oat LINKING shows [AVT] Lookup vtable_len=37/overriding=1 and build CloneNotSupported=0. CONCLUSION (precise): the class LINKER is correctly fixed (volatile bit-vector zero → 37 in dex2oat), the runtime does NOT re-link (uses the image copy), but **the IMAGE the runtime loads has a BROKEN Lookup (vtable_len=11, findVarHandle at index 0)** → the image_writer/image-loading does NOT preserve the corrected vtable/method-index that AssignVTableIndexes produced. So the remaining bug is in **image_writer.cc (patches has IW_PATCH_OBJ) — the vtable / ArtMethod.method_index_ persistence, or a post-link fixup that re-truncates**, NOT the linker and NOT the runtime linker. The earlier confusing flakiness (clone 0 vs 2) was: (a) zygote caching of old libart.so, AND (b) the ONE clone=0/getDeclaredAnnotation run likely used a transiently-good state; the STEADY state is clone=2 because the persisted image Lookup is 11.
NEXT (fresh, focused): (1) Instrument image_writer.cc where it copies/relocates classes+vtables+ArtMethods — dump MethodHandles$Lookup's vtable_len + findVarHandle.method_index BEFORE vs AFTER the image write / post-fixup (image_writer.cc:667 "Post-fixup: scanned methods" is a suspect; also the bin-assignment / CopyAndFixupObjects / FixupClass vtable copy). (2) Check whether image_writer PRUNES vtable entries or recomputes method_index from a stale source. (3) Alternatively add a runtime probe in ClassLinker image-load (InitFromBootImage / VisitClassesFromImage) to dump Lookup vtable_len right after load. Compare 11 (persisted) vs 37 (linked). Fix the writer to preserve the linked vtable. THEN clone truly gone at runtime → getDeclaredAnnotation blocker → framework init → onCreate → render.
CLEAN STATE: restored clean libart.so (0 probes, volatile linker fix) to $R/android/lib64/. dex2oat.final2 (volatile fix) + its image at $R/arm64/. Linker fix (volatile bit-vector zero in class_linker.cc AssignVTableIndexes) is a LOAD-BEARING keeper regardless. Remember pkill zygote + rm $ERR before each runtime test.

## ★★★ CRITICAL CLUE: runtime probes DON'T FIRE despite my libart.so running (2026-07-05) ★★★
DEFINITIVELY established this session: (a) LINKER is correct — dex2oat build clone=0, [AVT] Lookup vtable_len=37. (b) IMAGE-WRITE input is correct — [IW-LOOKUP] in image_writer.cc FixupClass shows orig Lookup vtable_len=37, findVarHandle@24 (idx=24, reloc=1, inboot=0) — so the class the writer copies is CORRECT and findVarHandle IS relocation-assigned (will be copied). (c) YET runtime STILL clone=2 even with the AVT-verified-37 image copied to $R/arm64/ + clean/volatile libart.so + zygote killed (pkill -9 appspawn-x/digiplex, verified). 
**THE KEY UNEXPLAINED CLUE:** with ALL probes ENABLED in the deployed libart.so (CLONE-BODY in interpreter.cc Execute + AVT in class_linker AssignVTableIndexes + NTERP-VT in nterp.cc + RT-LOOKUP) and the zygote freshly killed, NONE of them fire at runtime, yet clone=2 and the tolerate-clinit log IS emitted by my libart.so's class_linker.cc:6255. So my libart.so IS loaded/running (JNI_CreateJavaVM rc=0 + tolerate-clinit are mine), but the findVarHandle→unreflectVarHandle→Object.clone at runtime does NOT go through my interpreter.cc Execute() NOR my AssignVTableIndexes. HYPOTHESES for next session (needs different tooling): (1) the boot image's OAT has COMPILED code for AtomicInteger.<clinit>/findVarHandle/Object.clone being executed instead of the switch interpreter (check: compiler-filter=verify should mean no compiled code, but confirm; the clone may run via artQuickToInterpreterBridge → a path not through interpreter.cc Execute, OR via nterp asm despite IsNterpSupported=false). (2) The clinit failure is RE-REPORTED from the image's stored erroneous-class state (the class was marked erroneous during SOME build) without re-running — but build clone=0 argues against this; verify by checking if the tolerate happens at image-LOAD vs during a real clinit run (add a probe in ClassLinker::EnsureInitialized / the erroneous-class rethrow path). (3) oatdump the $R/arm64/boot.art image to directly inspect MethodHandles$Lookup's PERSISTED vtable_len + findVarHandle.method_index (is it 37/24 or 11/0?) — this settles image-content vs runtime-use definitively. **START HERE NEXT: oatdump the image's Lookup vtable, AND add a probe in the ClassLinker path that ACTUALLY emits the tolerate-clinit (class_linker.cc:6255) to dump the interpreter path + whether the clinit is re-run or re-reported.** 
LINKER FIX (volatile bit-vector zero in class_linker.cc AssignVTableIndexes) + image_writer IW-LOOKUP probe are in the tree; disable IW-LOOKUP + all interpreter probes for clean builds. The linker fix is a LOAD-BEARING keeper. This whole runtime-clone chase (fill_n→ClearAllBits→volatile→zygote→image-writer→probes-dont-fire) shows the clone at runtime is a SEPARATE issue from the linker bug — likely image-OAT/interpreter-path related, not the vtable linking (which is proven correct end of pipeline).

## RULED OUT re-report; clinit IS re-run at runtime (2026-07-05)
The tolerate-clinit at class_linker.cc:6278 (my patched ClassLinker) is inside InitializeClass, guarded by `ObjectLock<Class> lock; if (self->IsExceptionPending())` — i.e. it runs AFTER the class's <clinit> actually EXECUTED and left a pending exception. So AtomicInteger.<clinit> IS genuinely re-run at runtime (not re-reported from a stored erroneous image state), it calls findVarHandle, findVarHandle mis-dispatches to unreflectVarHandle, Object.clone runs and throws — all while my CLONE-BODY (interpreter.cc Execute) + AVT (AssignVTableIndexes) + NTERP-VT (NterpGetMethod) probes stay silent. CONCLUSION: at runtime the findVarHandle invoke-virtual and Object.clone execute via a path OTHER than my patched interpreter.cc Execute()/switch-interp method-resolution — strongly implicating **image-OAT compiled code** (invoke-virtual with a vtable index baked in at dex2oat compile time — if compiled when Lookup was momentarily broken, index=0→unreflectVarHandle) OR the mterp/nterp asm handlers (arm64ng/main.S etc. exist in tree even though IsNterpSupported=false — verify they're truly unused) OR artQuickToInterpreterBridge dispatching to something other than interpreter.cc. NEXT DEFINITIVE STEP: (1) oatdump $R/arm64/boot.art (and boot-core-oj-fieldfix.oat) — check MethodHandles$Lookup.findVarHandle's PERSISTED method_index AND whether AtomicInteger.<clinit> has COMPILED code (quick_code) with an embedded vtable index; (2) confirm the boot image was built with --compiler-filter=verify (no compiled code) — if some methods ARE compiled, that's the culprit; rebuild with a filter that forces interpret-only, or ensure the vtable index in compiled code matches. This is the crux for the runtime clone.

## ★★★★★ BREAKTHROUGH: runtime clone=0, app runs! Root cause was SYMBOL-LOADING, not vtable (2026-07-05) ★★★★★
THE ENTIRE "runtime clone=2 mystery" was NOT a vtable/image bug — the vtable/clone LINKER fix (volatile bit-vector zero) was correct all along. The real problem: my freshly-built libwestlake_art.so FAILED to dlopen (embedded probe uses dlopen RTLD_NOW at $WESTLAKE_ROOT/art/libwestlake_art.so), because my hand-written `link-libart` Makefile target was MISSING objects/symbols that dex2oat's static link tolerated. Each missing symbol failed dlopen one at a time; the app then FELL BACK to an OLD buggy libwestlake_art.so (pre-vtable-fix) → clone=2. My probes never fired because MY lib was never loaded. Diagnosis breakthrough: embedded-art-dlopen-probe.log showed "dlopen libwestlake_art.so failed / Error relocating ...: <SYM>: symbol not found". Fixed FOUR missing symbols (all in art-latest/Makefile.ohos-arm64 + stubs/disassembler_stub.cc):
1. create_disassembler — disassembler/ not built for libart; added a returns-null stub (stubs/disassembler_stub.cc, compiled with -I$(ART)/disassembler).
2. android::base::SendFileDescriptorVector/ReceiveFileDescriptorVector — pulled in by android-base/abi_compatibility.cpp referencing cmsg.cpp (which is in ABASE_EXCLUDE). Fix: added %abi_compatibility.cpp to ABASE_EXCLUDE (matches how the old working lib had NO such reference).
3. artCriticalNativeOutArgsSize — defined in stubs/thread_cpu_stub.o but link-libart's object list omitted it. Fix: added $(BUILDDIR)/stubs/thread_cpu_stub.o + westlake_bionic_compat_stubs.o to the link-libart recipe.
4. x86_cpu_enable_simd — leaked UNDEFINED ref from the static libz.a (x86 SIMD CRC helper, never called on arm64). Fix: no-op `extern "C" void x86_cpu_enable_simd(){}` stub.
KEY DIAGNOSTIC TECHNIQUE: `llvm-nm -D -u mylib.so | sort > my; nm -D -u old.so | sort > old; comm -23 my old` → the symbols MY lib references that the OLD (loadable) lib didn't = exactly the ones to stub/exclude. After all 4: "dlopen libwestlake_art.so ok", JNI_CreateJavaVM rc=0, stderr 9456 lines (stage runs!), **clone=0**. Remaining extras (_Unwind_*, __emutls_get_address) resolve fine from system libs at runtime (not blockers).
DEPLOY GOTCHA CONFIRMED: app loads $WESTLAKE_ROOT/art/libwestlake_art.so (WESTLAKE_ROOT=/data/local/tmp/westlake-dayu600-substrate). Deploy my build to ALL THREE: westlake-dayu600/art/, westlake-dayu600-substrate/art/, and substrate/android/lib64/libart.so (verify sizes match — device disconnects mid-scp silently leave stale libs). Then run deploy WITHOUT aggressive pre-kill (deploy's own line-15 kills appspawn-x-prot + starts fresh $PROTO that dlopens the current disk lib). `pkill -f proto` BREAKS the launch (stage won't run, stderr=2).
NEW FRONTIER (post-clone): stage=randomCtorProbe now runs deep — reaches hwui reg[53/54] view_ThreadedRenderer, tons of InterpJni reflection/string calls, Dayu600ApkStageProbe.nativeWriteText. Tolerated clinit failures now DOWNSTREAM graphics: android/graphics/ColorSpace, android/graphics/fonts/FontVariationAxis (VarHandle/nativePtr issues). 1 FATAL in log (investigate). Path to on-screen: stabilize graphics clinits, then switch WESTLAKE_APP_STAGE from randomCtorProbe to real ActivityThread→onCreate→ViewRootImpl→ThreadedRenderer→draw.

## ★★★★★ setContentView WORKS — Activity 110/118 constructed! (2026-07-05) ★★★★★
With clone fixed + lib loading fixed, switched deploy stage from randomCtorProbe → onCreateManual (Dayu600ApkStageProbe.java:1317 — hand-crafted MainActivity bring-up for com.digiplex.game). RESULT: embedded-exit-code.txt = **110** (written to $R/apks/probe-logs/embedded-exit-code.txt). Step map: 72=setContentView, 85-101=find+assign 16 game buttons, 102-108=board view+touch listener, 110=`new com.digiplex.game.c(32768)` (game model). So the app SUCCESSFULLY: created MainActivity instance, **setContentView(R.layout) INFLATED the full view hierarchy** (resource/asset/XML inflation WORKS!), found+wired all 16 buttons + the board View, attached touch listeners — reached 110/118 of full Activity construction. The tolerated ColorSpace/FontVariationAxis clinit failures are NON-fatal (inflate still works).
CURRENT BLOCKER (step 110 + graphics clinits): missing CORE native methods surface as UnsatisfiedLinkError — `java.lang.Math.pow(double,double)`, `java.lang.System.currentTimeMillis()`, plus the ICU regex PatternNative.compileImpl (shorty 'JLI' unhandled in InterpreterJni), Typeface.createFromResources, AssetManager.nativeGetResourceStringArray. These are ART intrinsics/natives not registered in standalone mode → fall through to JNI lookup → fail. The app gets far because MOST natives ARE bridged via interpreter.cc InterpreterJni, but these specific ones aren't. NEXT: (1) wire the missing core natives (Math.pow=shorty DDD static, System.currentTimeMillis=shorty J static — easy; add to the InterpreterJni bridge or register real impls) so ColorSpace/FontVariationAxis clinit + step-110 succeed → onCreateManual completes to 118. (2) THEN the real on-screen step: onCreateManual only CONSTRUCTS the Activity+views in memory — it does NOT create a ViewRootImpl/attach to an OHOS Surface. For pixels on panel need to wire the inflated DecorView → ViewRootImpl → ThreadedRenderer.draw → OHOS Surface (gate-3 rendering; hwui reg reaches 53/54 view_ThreadedRenderer already). Deploy stage currently = onCreateManual (deploy-dayu600.sh; .bak has randomCtorProbe). SESSION SUMMARY: clone bug was THE months-long blocker — now fixed end-to-end AND the app runs deep enough to inflate a real APK's layout. Remaining to on-screen: core-native completeness (small, iterative) + ViewRootImpl→Surface render wiring (gate 3).

## ★★★★★★ onCreateManual EXIT 0 — full Activity constructed! Core-native intercepts work (2026-07-05) ★★★★★★
Added inline intercepts at the TOP of InterpreterJni (interpreter.cc, after the function opening brace) for unregistered core natives: java.lang.Math/StrictMath (pow/atan2/hypot/IEEEremainder for shorty "DDD"; sqrt/cbrt/exp/log/sin/cos/tan/rint/etc for "DD") via std::* , and java.lang.System.currentTimeMillis/nanoTime (shorty "J") via clock_gettime. 64-bit args read as `*reinterpret_cast<double*>(&args[0])` (args[0..1]=first double, args[2..3]=second). RESULT: embedded-exit-code.txt = **0** (was 110) — onCreateManual runs to completion! ColorSpace.<clinit> now SUCCEEDS (Math.pow was its only blocker; no longer in the tolerated list). Only FontVariationAxis still tolerated (ICU regex PatternNative — non-fatal). So com.digiplex.game's MainActivity is now FULLY constructed on the standalone OHOS ART runtime: instance + setContentView(layout inflated) + 16 buttons + board View + touch listeners + game model (com.digiplex.game.c) + callbacks + system-UI — all 118 steps, finishOrExit(0).
FINAL STEP TO PIXELS (gate 3): onCreateManual builds the Activity+DecorView in memory but never renders. Need: DecorView → measure/layout → ThreadedRenderer.draw onto an OHOS Surface (hwui reg already 53/54 view_ThreadedRenderer; gates 1-2 already proved hwui CAN paint to the DAYU600 panel via the ANativeWindow→OHOS window bridge). To wire: either add a render step to Dayu600ApkStageProbe (get decorView via activity.getWindow().getDecorView(), attach to a ViewRootImpl or manually measure(EXACTLY w/h)+layout+draw(Canvas from ThreadedRenderer bound to the OHOS Surface)), OR drive via WindowManager.addView. The OHOS Surface/window bridge from gate-2 hwui smoke is the target surface. libart.so size now 28204896 (with math intercepts), deployed to all 3 paths; deploy stage=onCreateManual.

## Render-path analysis for gate-3 (2026-07-05)
The pixel path infrastructure PARTIALLY EXISTS: ports/dayu600/bridge-src/oh_window_manager_client.cpp (~2900 lines) already implements OHOS window creation (RSSurfaceNode-backed), CreateNativeWindowFromSurface → OHNativeWindow (AOSP-ABI ANativeWindow), SurfaceControl cross-.so C wrappers for liboh_android_runtime.so's android_view_SurfaceControl.cpp, and CanvasContext/ASurfaceControl_registerSurfaceStatsListener wiring. gfx-smoke/westlake_rs_smoke.cpp (135 lines) proved the LOW-LEVEL path: OHOS Surface → OHNativeWindow → SET_BUFFER_GEOMETRY → eglCreateWindowSurface → GL draw → panel (gate 2). MISSING for a real View on screen (gate 3): wire the Java-side pipeline — wrap the OHNativeWindow as a Java android.view.Surface, create an android.view.ThreadedRenderer/HardwareRenderer bound to it, and drive the inflated DecorView through measure(EXACTLY w,h)→layout→ThreadedRenderer.draw(rootNode). This will surface more missing natives (Canvas/RenderNode/Paint/Surface JNI). TWO approaches: (A) full hwui ThreadedRenderer pipeline (matches Android, heavier — needs Surface↔OHNativeWindow Java bridge + ThreadedRenderer nativeSetSurface). (B) SOFTWARE shortcut for first-pixels: DecorView.draw(new Canvas(bitmap)) into a Bitmap (software Skia, no hwui) then upload the bitmap as a GL texture and blit via the proven EGL smoke path — likely fewer moving parts to get SOMETHING visible. Start B for a fast first-pixel proof, then A for real interactive rendering.
SESSION-END STATE (2026-07-05): runtime fully working — clone=0, all 4 dlopen symbols fixed, core Math/System natives intercepted, onCreateManual exit=0 (full MainActivity construction + layout inflation). Deploy stage=onCreateManual, libart.so (28204896) on all 3 paths. NOT yet on-screen: gate-3 view→surface render wiring is the last mile, infrastructure ~half built in oh_window_manager_client.cpp.

## ★★★★★★★ PIXELS ON SCREEN — 2048 game visible on DAYU600 panel! (2026-07-05) ★★★★★★★
VERIFIED via screenshot (snapshot_display -f, pulled + viewed): a full playable 2048 board is rendering on the DAYU600 panel — 4x4 grid, score box "40", tiles 2/4/8 in classic 2048 colors, pixel-font digits (drawRect-based since minikin is stubbed). Rendered by OUR libhwui.so on Mali-G57 via the full gate-1..3 pipeline: RenderProxy→RenderThread→CanvasContext→Skia GrDirectContext(GL)→eglCreateWindowSurface→eglSwapBuffers(ret=1). 
HOW: the harness is ports/dayu600/gfx-smoke/hwui_2048.cpp (444 lines) — a self-contained native 2048 (game model + Canvas drawRect board/digits) built by gfx-smoke/build-hwui-2048.sh into out/gfx-smoke/hwui_2048 (2901880 bytes). RUN RECIPE: deploy hwui_2048 + out/libhwui-build/libhwui.so → /data/local/tmp/; `cd /data/local/tmp && LD_LIBRARY_PATH=/data/local/tmp:/system/lib64:/system/lib64/platformsdk:/system/lib64/chipset-sdk-sp ./hwui_2048` (autoplay; WESTLAKE_2048_INPUT=1 for stdin u/d/l/r moves). NEEDED .so all present on device (librender_service_client/base, libsurface, libskia_canvaskit, libutils, libipc_single, libsamgr_proxy, libeventhandler, libhilog). Screenshot: snapshot_display -f /data/local/tmp/x.jpeg (default writes /data/local/tmp/snapshot_<ts>.jpeg); MUST screenshot WHILE hwui_2048 runs (surface detaches on exit).
WHAT THIS IS vs THE FULL GOAL: hwui_2048 is a NATIVE C++ reimpl of 2048's game logic rendered by our libhwui — it proves the graphics stack renders the game UI to the panel (pixels confirmed). SEPARATELY, the ART runtime now runs the REAL com.digiplex.game APK bytecode (onCreateManual exit=0: MainActivity constructed, layout inflated, 16 buttons+board found). The REMAINING connection (true gate-3) is wiring the REAL APK's inflated DecorView → hwui ThreadedRenderer (instead of the native hwui_2048 game model) so the actual APK bytecode drives the on-screen pixels. Both halves now proven independently; the bridge between them is the last mile for "arbitrary APK bytecode visibly on screen".

## ★★★ PIVOTAL: current framework is STUBS, not real — render bridge built but nothing real to render (2026-07-05) ★★★
Building the in-process view-render bridge exposed the crux. The framework-shim (dayu600-framework-shim.dex) is a HAND-WRITTEN STUB, and it sits BEFORE framework.jar in the bootclasspath (`...adapter-mainline-stubs.jar : dayu600-framework-shim.dex : android/framework/framework.jar : ...`), so its stub classes OVERRIDE the real framework. Proof: src/android/app/Activity.java (75 lines) — `setContentView()` just sets an int, `findViewById()` returns NEW empty stub views each call (`new ViewGroup`/`new TextView`/`new CustomButton`), `getWindow()` returns an empty `new Window()`. So onCreateManual's "exit 0 / setContentView worked / 16 buttons found" only means the app's onCreate BYTECODE ran against stubs — there is NO real DecorView, NO inflated layout, NOTHING to render. The stub approach = per-app hand-writing (can't scale to arbitrary APKs / Genshin, exactly the user's intuition).
WHAT WAS BUILT THIS PASS (valid, reusable, saved to github BH3GEI/westlake-piercing):
- macOS dex toolchain: test-fixtures/dayu600-apk-probe/build-probe-mac.sh (brew openjdk + android-35 android.jar + d8 via cmdline-tools r8.jar). Removes WSL/Windows dep. Verified: mac-built probe.dex → onCreateManual exit=0 on device.
- In-process JNI render bridge: ports/dayu600/gfx-smoke/westlake_view_renderer.cpp + build-view-renderer.sh → libwestlake_view_renderer.so (2.9MB). JNI nativeRenderInit(rootNodePtr,w,h)/nativeDrawFrame()/nativeTeardown(); reuses hwui_2048's RenderProxy+OHOS window; takes an android.graphics.RenderNode native ptr, renders it via libhwui to the panel. LINKS + builds clean. NOT yet wired (no real RenderNode to feed it because the framework is stubbed). Note: had to recompile ohos_display_surface.cpp + rs_abi_shims.cpp with -fPIC (the 2048 build made them non-PIC for an executable; a .so needs all-PIC).
NEXT (the real gate-3, big): run the REAL framework.jar's view stack on our ART so a real DecorView exists. Path A — reorder/drop the shim so framework.jar's real Activity/Window/PhoneWindow/DecorView/LayoutInflater/Resources are used, then make the real framework's init actually work standalone (real Context, Resources from the APK, the system-service Binder shims the framework calls). Then setContentView really inflates the app's layout XML into a real DecorView, decorView.draw() records into a RenderNode, and the ALREADY-BUILT render bridge puts it on screen. This is the "run real framework.jar, shim only the OS boundary" Wine architecture — the actual scalable path to arbitrary APKs. The render bridge + hwui + dex toolchain are the foundation; the real-framework init is the remaining mountain.

## ★★★ REAL-FRAMEWORK PATH fully mapped with device evidence (2026-07-05) ★★★
Probed the real-framework foundation on device (assetProbe stage, mac-built dex). Real android.content.res.AssetManager/Resources are NOT stubbed (not in the shim), so they resolve to framework.jar's REAL classes — but calling `new AssetManager()` + `addAssetPath(apk)` HARD-CRASHES (50-frame FATAL, not a catchable exception): the class dump shows addAssetPath/nativeAssetCreate/nativeAssetGetLength/etc. have NO native implementation. libandroidfw is NOT built anywhere (device or repo). So real resource loading needs a libandroidfw bring-up (frameworks/base/libs/androidfw + core/jni/android_util_AssetManager.cpp + JNI registration) — a gate-1-2-libhwui-sized native effort.
COMPLETE MAP of the real-framework mountain (what "arbitrary APK" actually needs), in dependency order:
1. **libandroidfw (AssetManager/Resources)** — resource/asset loading from the APK. Foundational for EVERY real app (2048's layout XML + Genshin's assets). NOT built. ← next stone.
2. **Un-stub the view/graphics stack** — the shim (dayu600-framework-shim, ahead of framework.jar in BCP) stubs ~56 classes incl. View/ViewGroup/Canvas/LayoutInflater/Window/Context/Looper/Handler/MessageQueue/Bundle/TextView/Typeface/Drawable/MotionEvent. Real versions need their native layers (libhwui — HAVE; libminikin/text — stubbed; input).
3. **Real ActivityThread + Context + Instrumentation** — attach the Activity properly (mWindow/mDecor/Resources), run onCreate/onResume generically for ANY app (replaces hand-written onCreateManual).
4. **System-service Binder shims** — the framework calls AMS/WMS/PMS/InputManager via Binder; provide minimal in-process shims translating to OHOS (window from our OHOS bridge, package info from parsing the APK, input from OHOS input).
5. Then: real DecorView → decorView.draw() → RenderNode → the ALREADY-BUILT libwestlake_view_renderer.so → panel. Normal apps render. For GENSHIN specifically add: NDK native-lib loading (Unity/IL2CPP libunity/libil2cpp), SurfaceView→OHOS Surface, native GLES/Vulkan (Genshin renders itself, bypassing #2's View draw), + anti-cheat/integrity — Genshin is the far extreme (hardest mobile target).
HONEST SCOPE: this is a multi-month Wine-style effort (real framework.jar on our ART + narrow OHOS-boundary shims; NOT a container/subsystem). Foundation now in place + saved to github: ART runtime (clone/vtable fixed, boots real dex), libhwui→panel (2048 rendered), macOS dex toolchain, in-process render bridge. Each remaining stone (starting libandroidfw) is a gate-sized bring-up. Genshin playable = the summit, many stones away.

## libandroidfw stone: source fetched, build STARTED (2026-07-05)
UNBLOCKED the libandroidfw stone. github.com IS reachable (googlesource is NOT) — fetched the source via aosp-mirror: `git clone --filter=blob:none --sparse --depth 1 --branch android-15.0.0_r9 https://github.com/aosp-mirror/platform_frameworks_base` then `git sparse-checkout set libs/androidfw core/jni` → /Users/yao/westlake-local-build/aosp-fb-androidfw (85 .cpp + android_util_AssetManager.cpp JNI + full include/androidfw headers). Deps mostly LOCAL: libbase(aosp11), libutils(aosp-15-headers), libcutils(aosp11), libziparchive(art build has .o), libz.a, fmtlib. Core resource TUs (Android.bp libandroidfw srcs): ApkAssets/Asset/AssetDir/AssetManager/AssetManager2/AssetsProvider/AttributeResolution/ConfigDescription/Idmap/LoadedArsc/Locale/misc/ResourceTypes/StringPool/ZipFileRO/ZipUtils/ChunkIterator/ApkParsing/FileStream/BigBuffer(Stream). Build scaffold: westlake-piercing/westlake-art-runtime/build-libandroidfw.sh.
FIRST dep gap (build started, first compile error): androidfw is AOSP-15 but our libbase is aosp-android-11 → missing android-base/function_ref.h. FIX: fetch AOSP-15 libbase (aosp-mirror/platform_system_libbase android-15.0.0_r9, sparse include) — pre-fetching to $L/aosp-system-libbase. Then continue the OHOS compat whack-a-mole (like libhwui gate-1-2), build libandroidfw.so + the JNI, register the AssetManager natives in the runtime, re-run the assetProbe stage (should stop hard-crashing, real Resources.getLayout should parse the APK). Then real LayoutInflater → real DecorView → the ALREADY-BUILT libwestlake_view_renderer.so → on-screen real APK. This is stone 1 of ~5; a multi-session native bring-up.

## libandroidfw build IN PROGRESS: AssetManager2 headers resolved, at semantic-compat layer (2026-07-05)
KEY: googlesource GIT blocked, but googlesource HTTP `?format=TEXT` (base64) WORKS + github works. aospget helper (build-libandroidfw.sh) fetches any AOSP file. Resolved AssetManager2.cpp's full header dep tree by fetching: incfs util/map_ptr.h (system/incremental_delivery), android/configuration.h (frameworks/native), a minimal android/asset_manager.h stub (opaque fwd types), AOSP-15 fmtlib (13 headers, external/fmtlib — aosp11 fmtlib too old, missing fmt/std.h), AOSP-15 ftl (frameworks/native include/ftl small_vector/static_vector/initializer_list/small_map + details/array_traits+type_traits), system/graphics.h + libsystem (from aosp-android-11). Dep source roots: androidfw=$L/aosp-fb-androidfw (github aosp-mirror sparse), libbase/libutils/libcutils/liblog=$A2OH/aosp-15-src (has only system/). Fetched header stubs in $L/{incfs-stub,ndk-stub,fmt-15,ftl-15}. NOW: AssetManager2.cpp headers ALL resolve; blocked on SEMANTIC compat under OHOS libc++: `ftl::SmallVector<ResTable_config,1>` won't convert to `std::span<const ResTable_config>` (AssetManager2.h:167), plus construct_at / std::pair operator< template errors — need code-level compat (patch androidfw/ftl or the OHOS libc++ span, like libhwui's compat layer). REMAINING for the stone: compat-fix the ~21 androidfw TUs, BUILD the dep libs (libbase/libutils/libziparchive — real .a, not just headers) to link libandroidfw.so, build core/jni/android_util_AssetManager.cpp, register the AssetManager natives in the runtime, re-run assetProbe (should stop crashing). Build scaffold + all fetch commands: westlake-piercing/westlake-art-runtime/build-libandroidfw.sh. This stone is a multi-session native bring-up; real steady progress made (fetch method + header tree solved).

## libandroidfw AssetManager2.cpp: span compat fixed, now at libc++ template compat (2026-07-05, cont.)
Patched (in working copy $L/aosp-fb-androidfw): AssetManager2.h GetConfigurations() — return std::span explicitly (data,size) since OHOS libc++ std::span lacks the C++20 contiguous_range ctor (ftl::SmallVector wouldn't implicitly convert). REMAINING compile errors in AssetManager2.cpp are OHOS-libc++ template internals: (a) `construct_at` no-matching-function (allocator_traits.h:298), (b) `std::pair<uint32_t, Theme::Entry>` operator< invalid (comp.h:73 — pair spaceship/operator< under stricter OHOS libc++; sort/lower_bound sites at AssetManager2.cpp:262/1286/1415/1686/1736/1908). These need per-error compat patches (add operator<=>/operator< to Theme::Entry or fix the comparator; provide a construct_at-compatible ctor) — same nature as libhwui's OHOS-libc++ compat layer, careful C++ work. This is the DEEP semantic-compat layer of stone-1; a careful multi-session grind (then 20 more TUs, dep-lib builds, JNI, wiring, device test). Fetch method + full header tree are solved and saved; the compat patches are the next careful pass. Progress is real and steady but libandroidfw is a multi-session native bring-up, and it is stone 1 of ~5 before ANY real APK renders, with Genshin (Unity/Vulkan/anti-cheat) the far summit.

## ★★★ libandroidfw: ALL 21 core TUs COMPILE for OHOS (2026-07-05) ★★★
Got all 21 androidfw resource-loading TUs to compile aarch64-linux-ohos. Deps fetched via googlesource-HTTP+base64 (git blocked): incfs map_ptr.h (+ IncFsFileMap), android/configuration.h, AOSP-15 fmtlib (13 hdrs), AOSP-15 ftl (small_vector/static_vector/details), AOSP-15 libziparchive (zip_archive.h — aosp11's lacks extra_field_size), system/graphics.h (aosp11), androidfw include_pathutils. Authored stubs: android/asset_manager.h (opaque), binder/TextOutput.h (no-op; androidfw only debug-dumps via it). OHOS-libc++ COMPAT PATCHES (in $L/aosp-fb-androidfw working copy; all reproduced by westlake-art-runtime/build-libandroidfw.sh step [1]): (a) AssetManager2.h GetConfigurations returns std::span explicitly (OHOS libc++ std::span lacks the C++20 contiguous_range ctor); (b) Theme::Entry given a ctor (OHOS construct_at has no parenthesized-aggregate-init P0960) + operator< (default pair<> merge-sort in RebuildAttributes); (c) CombinedIterator.h added rvalue-ref swap overload (std::iter_swap passes operator* prvalue proxies, old lvalue swap wouldn't bind); (d) Asset.cpp `assert(dataMap != NULL)` -> unsafe_data()!=nullptr (OHOS NULL is 0L, IncFsFileMap has no !=long). Objects in $L/androidfw-build/obj/*.o (21).
REMAINING for libandroidfw.so + real Resources: (1) compile core/jni/android_util_AssetManager.cpp (the JNI — needs JNI headers + more framework internals, expect more compat); (2) compile dep libs to LINK: libbase, libutils, libcutils, libziparchive (have SOURCE in aosp-15-src/aosp11 — must build .o), plus a real libincfs or a fuller map_ptr/IncFsFileMap impl (the fetched map_ptr.h may need the incfs .cpp for IncFsFileMap methods); (3) link libandroidfw.so; (4) register the AssetManager natives in the runtime (interpreter JNI bridge or RegisterNatives); (5) re-run the assetProbe stage — should stop hard-crashing, res.getLayout should parse the APK. Then real LayoutInflater -> real DecorView -> the already-built libwestlake_view_renderer.so -> on-screen real APK. Build recipe fully reproducible: build-libandroidfw.sh. Big milestone within stone-1; the hardest compat layer is done.

## libandroidfw JNI: full-AOSP-JNI is a version-landmine chain → use MINIMAL JNI next (2026-07-05)
Tried compiling the full AOSP JNI core/jni/android_util_AssetManager.cpp (66KB). Its header chain is deep + version-inconsistent: android_runtime/android_util_AssetManager.h + AndroidRuntime.h + core_jni_helpers.h (fetched), nativehelper JNIPlatformHelp.h (AOSP-15, fetched to $L/nh-15) which needs android/file_descriptor_jni.h (stubbed) — but mixing aosp11 libnativehelper (JNIHelp.h) with AOSP-15 JNIPlatformHelp.h → REDEFINITION of jniCreateFileDescriptor/jniGetFDFromFileDescriptor. Fixing means bringing up a CONSISTENT all-AOSP-15 libnativehelper (+ then android_runtime + likely GraphicsJNI/android_content_res_ApkAssets JNI + their chains) — a deep bring-up. RECOMMENDATION for next pass: DON'T compile the full AOSP JNI. Write a MINIMAL hand-authored JNI (~30-40 methods) that implements only the natives the Java android.content.res.AssetManager declares (nativeCreate, nativeSetApkAssets/addAssetPath, nativeGetResourceValue, nativeGetResourceBagValue, openXmlAssetNative + XmlBlock natives for getLayout, etc.), calling the ALREADY-COMPILED AssetManager2/ApkAssets C++ API directly with just <jni.h> — bounded, avoids the android_runtime/GraphicsJNI/nativehelper-version hell. Register them via the interpreter JNI bridge or a RegisterNatives .so loaded by the app. For the assetProbe milestone only a SUBSET is needed (addAssetPath + getLayout path). The 21 androidfw core .o are done ($L/androidfw-build/obj); still need to build dep libs (libbase/libutils/libziparchive .o) to link, plus a real libincfs/IncFsFileMap impl (fetched map_ptr.h declares IncFsFileMap but the .cpp with its methods — IncFsFileMap::Create/unsafe_data — must be fetched/built from system/incremental_delivery for linking). Then link libandroidfw.so + the minimal JNI, register, re-run assetProbe. STATE: stone-1 (libandroidfw) core compiles; JNI + link + register + test remain; multi-session. This is the concrete, efficient next-pass plan.

## ★★★★★★ libandroidfw.so LINKS — 0 internal undefined (2026-07-05) ★★★★★★
BUILT libandroidfw.so (9.7MB) for aarch64-OHOS with ZERO internal undefined symbols — the C++ side of stone-1 is DONE. Link-guided closure (link with --allow-shlib-undefined, nm -D -u, build only the referenced files, repeat). Object set in $L/androidfw-build/{obj,dep-obj}:
- androidfw (28 TUs): the 21 core + PathUtils/StreamingZipInflater/LocaleData/LocaleDataTables/Util/ResourceUtils/TypeWrappers/ObbFile/ResourceTimer/PosixUtils.
- libbase (16): from aosp-15-src/system/libbase (logging needs program_invocation_short_name + android_set_abort_message → glibc_compat.o + westlake_glibc_compat.h decl; __NR_gettid=178).
- libutils: from aosp-15-src/system/core/libutils/binder/{RefBase,String8,String16,Unicode,VectorImpl,SharedBuffer,StrongPointer,Errors}.cpp (they live in the binder/ subdir!) + top-level files.
- libziparchive: AOSP-15 SOURCE fetched to $L/zip-15-src (zip_archive/zip_archive_stream_entry/zip_cd_entry_map.cc) + internal hdrs (entry_name_utils-inl, zip_archive_private/common, zip_cd_entry_map, zip_error, incfs_support/{signal_handling,util}) — aosp11's is too old. zlib const patch: zstream.next_in = const_cast<Bytef*>(...).
- AUTHORED stubs (saved to westlake-art-runtime/stubs/): IncFsFileMap_impl.cpp (real IncFsFileMap via android::FileMap plain mmap — no incremental-fs; + add_sysprop_change_callback no-op), CallStack_stub.cpp (no-op debug stacks), glibc_compat.o, ndk-stub/android/{configuration,asset_manager,set_abort_message,file_descriptor_jni}.h, binder/TextOutput.h.
- link: clang++ -shared -static-libstdc++ -Wl,--allow-shlib-undefined obj/*.o dep-obj/*.o libz.a -ldl.
EXPORTS the JNI-target API: android::AssetManager2::SetApkAssets/GetResource/OpenNonAsset/GetResourceId, ApkAssets::LoadFromPath. NEXT: the minimal JNI (MINIMAL_JNI_PLAN.md) — bridge Java AssetManager/ApkAssets/XmlBlock natives to these exports, link into/alongside libandroidfw.so, RegisterNatives, re-run assetProbe → real Resources.getLayout parses the APK. The whole compile+link mountain of stone-1 is behind us; only the JNI shim + registration + test remain for real resource loading.

## ★★★★★★★ libandroidfw.so + minimal JNI BUILT & DEPLOYED — addAssetPath no longer hard-crashes (2026-07-05) ★★★★★★★
Wrote westlake_assetmanager_jni.cpp (minimal JNI over the compiled AssetManager2/ApkAssets C++ API, <jni.h> only, saved to westlake-art-runtime/). Methods: ApkAssets nativeLoad/nativeDestroy/nativeGetAssetPath, AssetManager nativeCreate/nativeDestroy/nativeSetApkAssets/nativeGetResourceIdentifier, JNI_OnLoad RegisterNatives. Compiled + linked into libandroidfw.so (9.8MB, 0 internal undefined, JNI_OnLoad exported). CRITICAL signature fixes learned from device: nativeSetApkAssets is (J[Landroid/content/res/ApkAssets;ZZ)V (TWO booleans: invalidateCaches, preset), ApkAssets.nativeLoad is (ILjava/lang/String;ILandroid/content/res/loader/AssetsProvider;)J (4th arg is an AssetsProvider OBJECT not long). With correct sigs, the assetProbe (System.load(libandroidfw.so) at stage start) no longer HARD-CRASHES on addAssetPath (was a 50-frame FATAL; now FATAL=0). The whole compile+link mountain of stone-1 is DONE + the JNI works.
REMAINING integration debugging (not compile — integration): (1) an existing [OHBridge] already stubs ApkAssets(7/7)+AssetManager(10/11) natives and handles the runtime-startup system-asset load ([OHBridge] nativeLoad framework-res.apk); my JNI's RegisterNatives (from the assetProbe System.load) must WIN over OHBridge for the app's own addAssetPath — verify ordering / disable the OHBridge AssetManager stubs so MY libandroidfw handles resource loading. (2) assetProbe wrote no asset-probe.txt though exit=0 — verify the deployed dex has the latest assetProbe stage body + that it reaches new AssetManager()/addAssetPath with my natives (add step-by-step writeText markers). (3) getLayout needs nativeOpenXmlAsset + the XmlBlock natives (nativeCreate/nativeCreateParseState/nativeNext/nativeGetAttribute* etc.) — NOT yet in the minimal JNI; add them for the layout-XML path. Once these three resolve: Resources.getLayout parses the real APK -> real LayoutInflater -> real DecorView -> libwestlake_view_renderer.so -> real APK UI on the panel. Build: androidfw-build/ has libandroidfw.so; rebuild via westlake-art-runtime/build-libandroidfw.sh + the JNI. DEPLOY: libandroidfw.so -> $R/android/lib64/; assetProbe System.load's it.

## assetProbe end-to-end validation blocked at TEST-HARNESS layer, not libandroidfw (2026-07-05)
libandroidfw.so (9.8MB, JNI, JNI_OnLoad) is BUILT + deployed to $R/android/lib64/libandroidfw.so + does NOT destabilize the runtime (onCreateManual still exit=0, FATAL=0 with it present). BUT the assetProbe end-to-end test can't be validated because of a TEST-HARNESS quirk: writeText(probeLogPath("asset-probe.txt"),...) produces NO file even from the runResolved ENTRY marker (unconditional, runs for onCreateManual too), while writeText(probeLogPath("embedded-exit-code.txt"),...) from finishOrExit DOES write. Both go writeText -> nativeWriteText (embedded probe C). Likely cause: nativeWriteText opens the path WITHOUT O_CREAT (so it silently no-ops for a freshly-deleted asset-probe.txt but works for an existing embedded-exit-code.txt), and it returns success so writeText's FileOutputStream fallback never fires. FIX next pass: either pre-create asset-probe.txt (touch) before the run, OR change the probe's step markers to use java.io.FileOutputStream directly (bypass nativeWriteText), OR patch nativeWriteText to O_CREAT. THEN the assetProbe can actually report cookie/getLayout results. REMAINING for real resource loading (unchanged): (1) test-harness logging fix (above); (2) beat OHBridge's AssetManager/ApkAssets stubs (it handles startup framework-res.apk load via [OHBridge] nativeLoad — my JNI RegisterNatives from the assetProbe System.load must win for the app's own addAssetPath; verify ordering / disable OHBridge stubs); (3) add nativeOpenXmlAsset + XmlBlock natives for getLayout. The libandroidfw.so BUILD (the compile+link mountain) is done + saved; these three are integration/test-harness tasks. Device is clean: libandroidfw.so in place, stage=onCreateManual works (exit=0).

## ★★★★★★★★ CACHING ROOT CAUSE FOUND + libandroidfw.so LOADS ON DEVICE (2026-07-05) ★★★★★★★★
TWO massive unblocks this stretch:
(1) **KEEPALIVE-PROTO CACHING ROOT CAUSE** — the reason NONE of my probe-dex edits took effect for many turns: the keepalive proto process is named `westlake.appspawnx.ams.exec`, NOT `appspawn-x`. My `pkill -9 -f appspawn-x` never killed it, so it kept serving the OLD loaded Dayu600ApkStageProbe class (JVM caches the BCP class in the live proto). PROVEN via a runResolved-top `finishOrExit(99)` that only produced exit=99 AFTER killing by the real name. FIX (use every redeploy): `ps -A | grep -E 'appspawn-x|westlake.appspawnx|digiplex' | grep -v grep | awk '{print $1}' | xargs kill -9` (kill by PID list), then re-run deploy-dayu600.sh. Without this, probe-dex changes silently no-op.
(2) **libandroidfw.so now dlopens successfully on device** (assetProbe reached `STEP:loaded` after System.load). Cleared the whole dlopen symbol whack-a-mole by adding westlake_androidfw_dlopen_stubs.cpp (saved to westlake-art-runtime/): no-op ATRACE (atrace_begin/end_body, get_enabled_tags, update_tags, enabled_tags), liblog (__android_log_set_default_tag/is_loggable/assert/print/buf_print/error_write/logd_logger/set_aborter/set_logger/min_priority/write_log_message/bwrite/security_bwrite/stats_bwrite, LogdClose/Read, PmsgClose/Read), android_set_abort_message, posix_strerror_r, native_handle_close/delete, and C++ ErrorCodeString(int)+CallStack::print. KEY GOTCHA: match C vs C++ linkage — atrace/liblog/android_set_abort_message are `extern "C"` (unmangled); ErrorCodeString is a plain C++ fn (mangled _Z15ErrorCodeStringi). clang++ on a .c/.cpp defaults to C++ mangling, so wrap C ABI fns in extern "C". Link libandroidfw.so with these + `-Wl,--allow-multiple-definition`.
NEXT BARRIER (not libandroidfw): `new AssetManager()` (real framework) crashes with SIGSEGV(11)@0x2e via deep interpreter recursion — the real Resources/AssetManager path hits interpreter-JNI natives the bridge doesn't handle: `java.lang.String.doReplace(char,char)` shorty 'LCC' (unhandled non-static) and `android.graphics.ColorSpace$Rgb$Native.nativeCreate` shorty 'JFFFFFFFL' (unhandled static) -> return garbage -> crash. This is INTERPRETER NATIVE-COMPLETION work (patches/runtime/interpreter/interpreter.cc InterpreterJni): add handlers for these shorties (String intrinsics; ColorSpace can return a fake non-null ptr). Then the AssetManager constructor should complete -> then getLayout still needs the XmlBlock natives (nativeOpenXmlAsset etc., not yet in the JNI) + beating OHBridge. Progress this stretch is huge: caching root-caused (all validation unblocked) + libandroidfw.so proven loadable on device.

## ★ VALIDATION: my JNI ApkAssets.nativeLoad EXERCISED through real libandroidfw (2026-07-05) ★
With libandroidfw.so loading + the keepalive-proto-kill fix, the assetProbe's `new AssetManager()` now progresses INTO real resource loading before crashing: device log shows `[InterpJni] android.content.res.ApkAssets.nativeLoad(int,String,int,AssetsProvider) shorty='JILIL'` FIRED (my JNI ran -> ApkAssets::Load loaded framework-res.apk via the real libandroidfw C++ engine), then `ApkAssets.nativeGetStringBlock(long) shorty='JJ'`. So the libandroidfw C++ resource engine + my JNI genuinely execute on-device. Also added a SAFE-DEFAULT to interpreter.cc InterpreterJni unhandled-shorty branches (both static@~1239 and non-static@~1683): set result by return type (L->null, I/J/F/D->0) instead of leaving garbage — rebuilt libwestlake_art.so (make -f Makefile.ohos-arm64 link-libart; ~1min; deploy to $R/art/ + westlake-dayu600/art/). interpreter.cc backed up to westlake-art-runtime/src/.
REMAINING = interpreter native-completion TAIL for the full framework AssetManager/Resources path (each returns garbage/null->crash until handled): (1) `android.content.res.ApkAssets.nativeGetStringBlock(long) 'JJ'` -> return the ResStringPool ptr from the ApkAssets (add to JNI + interpreter routes to registered native); (2) `java.lang.String.doReplace(char,char) 'LCC'` (ART runtime String intrinsic — null return -> caller derefs -> SIGSEGV@0x2e; implement the char-replace or route to a real String impl); (3) `android.graphics.ColorSpace$Rgb$Native.nativeCreate 'JFFFFFFFL'` (return a fake non-null ptr); (4) `com.android.icu.util.regex.PatternNative.compileImpl 'JLI'`; then XmlBlock natives for getLayout + beat OHBridge. Path is proven (libandroidfw loads + nativeLoad runs); the wall is now the breadth of framework natives the imageless interpreter must service. Consider: a MORE DIRECT libandroidfw test that reflectively calls nativeCreate/nativeLoad/nativeSetApkAssets/nativeGetResourceIdentifier (bypassing the heavy AssetManager constructor + ensureSystemAssets) to exercise the C++ resource API cleanly while the String/ColorSpace intrinsics are still being filled in.

## ★★★★★★★★★★ REAL libandroidfw RESOURCE ENGINE RUNS ON DEVICE — 2048 APK LOADS (cookie=1) (2026-07-05) ★★★★★★★★★★
assetProbe now prints: `OK cookie=1 resId(main/layout)=0x0 resId(activity_main/layout)=0x0`. END TO END on the standalone OHOS runtime: libandroidfw.so loads -> my JNI RegisterNatives -> AssetManager sentinel ctor (nativeCreate -> new AssetManager2) -> addAssetPath('2048-2-9.apk') -> ApkAssets::Load PARSED the real APK and returned **cookie=1** (0/-1 = fail; 1 = loaded) -> getResourceIdentifier (-> AssetManager2::GetResourceId) executes WITHOUT crashing. The real Android resource C++ engine works on-device. The 0x0 is expected: 2048 is a Unity game (no res/layout/*, obfuscated res names res/-1.xml etc.), so name lookups miss — NOT an engine failure (cookie=1 proves the arsc parsed).
KEY TECHNIQUES that got here:
- BYPASS the heavy framework AssetManager path: `new AssetManager(true)` SENTINEL ctor (only nativeCreate, skips createSystemAssetsInZygoteLocked which needs OverlayConfig.getZygoteInstance() zygote env we lack). Then reflectively set mApkAssets = Array.newInstance(Class.forName("android.content.res.ApkAssets"), 0) (sentinel leaves it null; addAssetPathInternal reads .length). getResourceIdentifier is package-private -> getDeclaredMethod + setAccessible.
- interpreter.cc InterpreterJni now: (a) safe type-default for unhandled shorties; (b) String.doReplace(char,char) 'LCC' handled via mirror::String::DoReplace (was null->SIGSEGV). These two eliminated the deep-recursion crash on the real framework path. Rebuild libwestlake_art.so via `make -f Makefile.ohos-arm64 link-libart`.
- DEPLOY GOTCHA: the app loads libwestlake_art from $R/android/lib64/libart.so (verify by the interpreter.cc line number in 'unhandled shorty' logs shifting). hdc file send TRUNCATES on disconnect — send then verify `wc -c` == 28205752, retry with kill/start. Device RTC is frozen (~09:xx) so log timestamps are NOT a freshness signal; use line numbers / fresh markers.
NEXT (airtight non-zero proof + forward): implement nativeGetResourceName + nativeGetResourceValue in the JNI, query 0x7f0100xx IDs on the 2048 apk to show real names/values; OR load framework-res.apk (/system/framework/) into the sentinel AM + getResourceIdentifier("ok","string","android"). THEN nativeOpenXmlAsset + XmlBlock natives for getLayout on an apk that HAS layouts -> real LayoutInflater -> DecorView -> libwestlake_view_renderer.so on panel. The libandroidfw wall (stone-1) is essentially THROUGH: engine compiles, links, loads, parses a real APK, answers resource queries on-device.

## ★★★ libandroidfw AssetManager2::GetResourceName EXECUTES on device (2026-07-05) ★★★
Added 3 interpreter-JNI static shorty handlers to interpreter.cc InterpreterJni (the REGULAR static section, before the unhandled-else near the ZJ handler — NOT the @CriticalNative section ~288): "JILIL" (ApkAssets.nativeLoad: long fn(env,cls,int,String,int,Object)), "IJLLL" (nativeGetResourceIdentifier: int fn(env,cls,long,String,String,String)), "LJI" (nativeGetResourceName: String fn(env,cls,long,int)). WITHOUT these the natives were REGISTERED but the imageless interpreter couldn't MARSHAL the call -> hit the safe-default (null/0), so resource queries silently returned 0/null even though libandroidfw was loaded. "VJLZZ" (nativeSetApkAssets) was already handled. Now nativeGetResourceName is marshaled + reaches libandroidfw: assetProbe prints realResourceNames=[0x7f010000=GRN:notfound ...] — the "GRN:notfound" is MY diagnostic string returned from AssetManager2::GetResourceName, i.e. the C++ resource engine RUNS on device (not IOError). Added nativeGetResourceName to the JNI (GetResourceName -> package:type/entry) + forced SetApkAssets(invalidate=true).
OPEN (next): GetResourceName returns notfound for resids that DO exist in the arsc (verified via python: pkg 0x7f 'com.digiplex.game', real resids 0x7f010000/0x7f020000...). So the 2048 apk's resource table isn't indexed in the AssetManager2 -> suspect nativeLoad ('JILIL') returns a null/0 ApkAssets ptr (then nativeSetApkAssets skips it -> empty AssetManager2), OR SetApkAssets isn't registering the package group. Probe was extended to print amPtr/apkPtr/apkCount to diagnose, but a DEX-CACHE issue blocked seeing it (deployed dex = 33600 has 'apkCount' but runtime ran a cached ~32920 class even with NO proto process alive + both dex paths updated — investigate oat/vdex/app-image cache generated by deploy-dayu600.sh; the earlier keepalive-proto-kill trick did NOT clear this one). DEVICE VERY UNSTABLE: hdc file send of the 28MB libart TRUNCATES repeatedly (got 1.6MB/7.5MB partial); must loop send+`wc -c`==28206864 with kill/start between; device dropped to [Empty] targets once (recovered after ~8 kill/start cycles).
NET: libandroidfw stone is effectively THROUGH on device — engine loads, JNI marshaled, AssetManager2::GetResourceName/GetResourceId execute. Last mile = wire the app's own apk resource table into AssetManager2 (nativeLoad ptr) so lookups resolve, then XmlBlock/getLayout.

## ★ LAST-MILE BUG PINPOINTED: ApkAssets::Load fails on the valid 2048 APK (2026-07-05) ★
With the 3 shorty handlers + a fixed keepalive kill, the assetProbe now shows the REAL diagnostic:
`OK cookie=1 amPtr=548324168992 apkPtr=0 apkCount=1 resId=0x0 realResourceNames=[...GRN:notfound...]`.
amPtr!=0 (nativeCreate/AssetManager2 OK) but **apkPtr=0** -> ApkAssets.nativeLoad returned 0. Added an
fopen-diagnostic in the JNI nativeLoad: `path=[.../2048-2-9.apk] flags=0 exists=1 size=2109798 loaded=0`
— so the path is right, the file exists (2.1MB), flags=0, but **ApkAssets::Load(path, 0) returns NULL on a
valid existing APK**. That's the whole reason GetResourceName/GetResourceId return notfound/0: the apk's
resource table was never loaded into the AssetManager2. Added a finer diagnostic to aosp-fb-androidfw
ApkAssets.cpp::Load(path) logging `ZipAssetsProvider::Create(path) => OK|NULL` (backed up to
westlake-art-runtime/patches/ApkAssets.cpp.diag) to localize whether it's the zip-open (libziparchive/
IncFs mmap stub) or the resources.arsc parse — BUT the DAYU600 hdc link went severely unstable (28MB/9.8MB
sends truncate ~50% of the time -> 1.6MB/3.3MB partials; shell commands + `sh deploy-dayu600.sh` itself get
cut off mid-run; [Empty] targets recur), so the ZipAssetsProvider result couldn't be read yet. Prime suspects
for ApkAssets::Load NULL: (1) ZipAssetsProvider::Create -> libziparchive OpenArchive failing on this apk
(zlib/const Bytef build); (2) my IncFsFileMap plain-mmap stub failing Asset::Open of resources.arsc;
(3) LoadedArsc::Load. NEXT SESSION (device stable): read nativeload.txt's "ZipAssetsProvider::Create => "
line; if NULL, debug libziparchive/IncFs; if OK, the arsc/Asset::Open path. Fixing this one call makes
GetResourceName return real names -> then XmlBlock/getLayout.
## CACHING FIX (important): the WESTLAKE_NO_EXIT=1 stage leaves a LINGERING process renamed "appspawn"
(empty /proc/pid/cmdline) that keeps the OLD probe class + OLD libs; deploy-dayu600.sh only kills
'appspawn-x-prot', missing it. Before every redeploy: `ps -A | grep -i appspawn | grep -v grep | while
read pid rest; do kill -9 $pid; done` (kills ALL appspawn incl. the lingering fork) — WITHOUT this, probe/
lib changes silently no-op (ran the same stale class for many iterations until found via a finishOrExit(99)
canary). Note this also kills the system appspawn (recovers) — acceptable in this experimental setup.

## ★★★★★★★★★★★★ libandroidfw FULLY WORKS ON DEVICE — REAL APK RESOURCE NAMES (2026-07-05) ★★★★★★★★★★★★
THE LAST MILE IS CROSSED. assetProbe prints REAL resource names parsed from the 2048 APK's resources.arsc by the real libandroidfw C++ engine on DAYU600:
`OK cookie=1 apkPtr=545371221888 realResourceNames=[0x7f010000=com.digiplex.game:anim/abc_fade_in 0x7f010001=...abc_fade_out 0x7f020000=...animator/design_appbar_state_list_animator ...]` and nativeload.txt: `LoadedArsc::Load => OK  loaded=1`.
ROOT-CAUSE of the earlier apkPtr=0 / GRN:notfound: **ZipEntry struct-layout MISMATCH between aosp11 libziparchive and aosp15 androidfw.** The androidfw AssetsProvider.cpp (aosp15/zip-15 header) reads `entry.uncompressed_length`/`entry.offset`, but the linked libziparchive was compiled from aosp11 (different field ORDER: aosp11 = {method,mod_time,has_data_descriptor,crc32,compressed_length,uncompressed_length,offset}; aosp15 ZipEntryCommon = {method,mod_time,...,crc32,zip64_format_size,offset(off64_t)} + derived ZipEntry adds compressed/uncompressed_length AFTER). So uncompressed_length READ the offset's bytes (got 1078208 = the arsc data_offset instead of 965244), and offset read 0 -> mmap'd the APK from byte 0 (PK header) with wrong length -> LoadedArsc saw "header size larger than entire chunk" -> Load NULL.
FIX: compile BOTH libziparchive (from $L/zip-15-src/zip_archive.cc + zip_cd_entry_map.cc) AND the androidfw TUs that touch ZipEntry (AssetsProvider, ApkAssets, ZipFileRO, AssetManager2, LoadedArsc) with the SAME aosp15 zip-15 header (-I$L/zip-15 FIRST), and DELETE the old aosp11 zip .o (zip_zip_archive.o etc.) from dep-obj before linking. After that: FindEntry(resources.arsc) => offset=1078208 uncomp_len=965244 (correct), mmap bytes=02 00 0c 00 (RES_TABLE magic), LoadedArsc::Load OK, GetResourceName returns real names. sizeof(ZipEntry)=56.
KEY LESSON: keep the WHOLE androidfw+dep build on ONE AOSP version; mixing aosp11 libziparchive with aosp15 androidfw silently corrupts ZipEntry reads (no compile/link error — a struct-ABI trap). The libandroidfw stone (stone-1) is COMPLETE + proven on device: loads a real APK, parses the arsc, answers resource-name/id queries. Diagnostics (fprintf to nativeload.txt) are still in ApkAssets/LoadedArsc/AssetsProvider/IncFsFileMap/JNI — backed up to westlake-art-runtime/patches/*.diag; strip them for a clean production lib. stripped libandroidfw.so.stripped (~3.5MB) transfers over the flaky DAYU600 hdc link far more reliably than the 9.8MB unstripped (which truncates ~50%). NEXT: nativeGetResourceValue + nativeOpenXmlAsset + XmlBlock natives -> Resources.getLayout on an apk WITH layouts -> LayoutInflater -> DecorView -> libwestlake_view_renderer.so on panel.

## ★ nativeGetResourceValue WORKS on device — reads real resource VALUES (2026-07-05) ★
Beyond names: assetProbe now reads a resource's VALUE. `resValue(0x7f010000)=[type=0x3 data=0x234 cookie=0]` — type 0x3 = Res_value::TYPE_STRING, data=0x234 (564) = the string-pool index of the compiled-XML path for anim/abc_fade_in. So the AssetManager2::GetResource path works end-to-end on device. Added AssetManager.nativeGetResourceValue to the JNI (shorty "IJISLZ": int fn(long,int resId,short density,TypedValue,boolean) -> GetResource(resId,false,density) [+ResolveReference] -> fills TypedValue.type/data/assetCookie/resourceId/changingConfigurations/density via SetIntField -> returns cookie) + the matching interpreter.cc InterpreterJni static handler for "IJISLZ". This is getLayout step 1: getLayout(id) -> nativeGetResourceValue -> type=STRING+pool-index (the res/anim|layout/*.xml path) -> resolve string -> openXmlBlockAsset -> nativeOpenXmlAsset -> XmlBlock. NEXT: (1) resolve the string value (GetStringPoolForCookie -> the path text; coerceToString returned null since the Java string block isn't wired) OR just feed the cookie+path to (2) nativeOpenXmlAsset (AssetManager2::OpenNonAsset -> ResXMLTree) + the XmlBlock natives (nativeCreate/nativeCreateParseState/nativeNext/nativeGetAttribute*...) -> Resources.getLayout on an apk WITH layouts (the 2048 AppCompat has abc_*.xml layouts) -> LayoutInflater -> DecorView.
## DEVICE-RECOVERY note: after many kill/deploy cycles the DAYU600 degraded — the app dlopens libwestlake_art from BOTH $R/art/libwestlake_art.so AND $R/android/lib64/libart.so; a truncated send left $R/art/ at 10MB (of 28MB) and the runtime HUNG silently at "run_stage entered" (dlopen never returns, no error). Fix: send the full libart to BOTH paths (verify wc -c == full size), and use `timeout 45` (not 24) for deploy — the 28MB dlopen is slow on the degraded link. STRIP libart too (llvm-strip --strip-all -> ~18MB) for more reliable transfer; a mid-transfer disconnect silently truncates. macOS `wc -c` prints leading spaces — `tr -d ' '` before string-comparing sizes.

## ★ getLayout steps 1-2 WORK on device: resource id -> XML path -> ResXMLTree (2026-07-05) ★
assetProbe: `resValue(0x7f010000)=[type=0x3 data=0x234 xmlPath=res/y4.xml nativeOpenXmlAsset=0x7f2ecfd750 XMLTREE_OK]`.
(1) nativeGetResourceValue now RESOLVES the string: for TYPE_STRING it looks up GetStringPoolForCookie(cookie)->string8At(data) and SetObjectField's TypedValue.string -> got "res/y4.xml" (the obfuscated compiled-XML path for anim/abc_fade_in). (2) Added AssetManager.nativeOpenXmlAsset (shorty "JJIL": long fn(long,int cookie,String path) -> AssetManager2::OpenNonAsset(path,cookie,ACCESS_RANDOM) -> asset->getIncFsBuffer(true) + getLength() -> new ResXMLTree(GetDynamicRefTableForCookie(cookie)) -> setTo(buffer.unsafe_ptr(), length, true) -> return ptr) + the interpreter.cc "JJIL" static handler. On device it returned a NON-ZERO ResXMLTree ptr (0x7f2ecfd750) = the compiled binary XML opened + PARSED. GOTCHAS: incfs::map_ptr<void> has no operator bool and no unsafe_data() — use `.unsafe_ptr()` for both the null-check and to pass const void* to ResXMLTree::setTo (its declared map_ptr<void> param resolves to const void* in this build). NEXT (getLayout step 3): XmlBlock parse natives — register against android.content.res.XmlBlock: nativeCreateParseState(long tree,int resId)->ResXMLParser*, nativeNext(long parser)->int event, nativeGetName/nativeGetText/nativeGetAttributeCount/nativeGetAttributeName/nativeGetAttributeData/nativeGetAttributeResource... (+ their interpreter shorties) -> iterate the ResXMLTree -> Resources.getLayout returns a working XmlResourceParser -> LayoutInflater.inflate -> real DecorView -> libwestlake_view_renderer.so on panel. Every native so far (nativeCreate/nativeLoad/nativeSetApkAssets/nativeGetResourceIdentifier/nativeGetResourceName/nativeGetResourceValue/nativeOpenXmlAsset) needs BOTH a JNI impl AND an interpreter InterpreterJni static-shorty handler (register-only isn't enough — the imageless interpreter must marshal the call).

## getLayout step 3 CODE done (XmlBlock natives) — device too unstable to verify (2026-07-05)
Wrote the core XmlBlock parse natives in the JNI (registered against android.content.res.XmlBlock):
nativeCreateParseState(long tree,int) -> new ResXMLParser(*tree)+restart() (shorty JJI),
nativeNext(long)->int event mapped to XmlPullParser codes (2/3/4/0/1) (IJ),
nativeGetName/nativeGetText -> getElementNameID/getTextID (IJ),
nativeGetAttributeCount -> getAttributeCount (IJ), nativeDestroyParseState -> delete (VJ).
Added interpreter.cc InterpreterJni REGULAR-static handlers for JJI/IJ/VJ. All compiled + linked
(libandroidfw 3.5MB stripped; libwestlake_art rebuilt) + deployed. BUT the DAYU600 hdc link is now
so degraded (deploys get cut off mid-run, 'DEPLOYDONE' won't even print, sends truncate) that the
XmlBlock-iteration probe result couldn't be read; an earlier read showed resValue=[VAL_FAIL:
InvocationTargetException] but with NO '@line' marker -> that was a STALE cached class (lingering
NO_EXIT appspawn), not the new code. NEXT (when device recovers): read the probe's XMLPARSE[events=..
firstTagNameIdx=.. attrCount=..] — if events>0 the ResXMLTree iterates -> then wire Resources.getLayout
-> XmlBlock.Parser (add nativeGetStringBlock JJ + attribute getters IJI as getLayout/inflate needs them)
-> LayoutInflater.inflate -> real DecorView -> libwestlake_view_renderer.so on panel. VERIFIED on device
this session: getLayout steps 1-2 (resValue 0x7f010000 type=STRING xmlPath=res/y4.xml, nativeOpenXmlAsset
=> non-zero ResXMLTree). Device recovery recipe (repeat): kill ALL appspawn by PID x3, strip libs, send
with wc-c==size verify loop + tr-d-space, deploy with `timeout 45`, send full libart to BOTH
$R/art/libwestlake_art.so AND $R/android/lib64/libart.so.

## getLayout PARSER PATH code-complete (full XmlBlock native set) — compiles, awaiting device (2026-07-05)
Device went fully dead ([Empty] targets, needs physical power-cycle by user). Used the downtime to
COMPLETE the XmlBlock native set in the JNI (20 methods, registered vs android.content.res.XmlBlock):
nativeCreateParseState/DestroyParseState/Destroy, nativeNext, nativeGetName/Text/Namespace/LineNumber,
nativeGetStringBlock (returns &ResXMLTree::getStrings()), nativeGetAttributeCount, and the idx getters
nativeGetAttribute{Namespace,Name,Resource,DataType,Data,StringValue}, nativeGet{Id,Class,Style}Attribute
(via ResXMLParser indexOfID/Class/Style + getAttributeValue), nativeGetSourceResId. Added interpreter.cc
InterpreterJni static handlers for the two remaining shorties: "IJI" (int fn(long,int)) and "JJ"
(long fn(long)). BOTH libandroidfw (3.5MB) and libwestlake_art recompile+link CLEANLY. So the full
resource->XML->parser path is code-complete. NEXT when device recovers (user must power-cycle DAYU600):
(1) deploy + read the probe's XMLPARSE[events/firstTagNameIdx/attrCount] to confirm ResXMLTree iterates;
(2) StringBlock natives (android.content.res.StringBlock: nativeGetSize IJ, nativeGetString LJI already
handled shorty) so XmlBlock.Parser.getName()/getAttributeValue() resolve indices->strings; (3) then
Resources.getLayout(id) returns a real XmlResourceParser -> LayoutInflater.inflate -> real DecorView ->
libwestlake_view_renderer.so on panel. All shorties the imageless interpreter now handles: JILIL, IJLLL,
LJI, IJISLZ, JJIL, JJI, IJ, VJ, IJI, JJ (+ the pre-existing set). Build recipe unchanged; deploy full
libart to BOTH $R/art/libwestlake_art.so and $R/android/lib64/libart.so.

## StringBlock natives added — resource->XML->parser->STRING path now code-complete (2026-07-05)
Added StringBlock natives to the JNI (registered vs android.content.res.StringBlock; obj = the
ResStringPool* returned by XmlBlock.nativeGetStringBlock): nativeGetSize (IJ)->pool->size(),
nativeGetString (LJI)->pool->string8At/stringAt(idx) with inline UTF16->UTF8, nativeGetStyle
(LJI)->null (styles rare in layouts), nativeDestroy (VJ)->no-op (ResXMLTree owns the pool). ALL their
shorties (IJ/LJI/VJ) were already handled — no new interpreter changes. libandroidfw (3.5MB) + the probe
dex compile clean. So the WHOLE resource->XML->parser->string chain is code-complete: AssetManager
(create/load/setApkAssets/getResourceIdentifier/getResourceName/getResourceValue/openXmlAsset) + XmlBlock
(20 parse natives) + StringBlock (4). The probe now resolves the first tag-name INDEX -> real string via
nativeGetStringBlock + StringBlock.nativeGetString, so on device it should print XMLPARSE[events=..
firstTagName=<real tag> attrCount=..]. Device is STILL offline (needs user power-cycle) so unverified.
WHEN DEVICE BACK: deploy (libandroidfw + libwestlake_art to BOTH art/ + android/lib64 + dex, kill-all-appspawn,
timeout 45) -> read XMLPARSE -> if firstTagName is a real element name the parser fully works -> then
Resources.getLayout(id) returns a working XmlResourceParser -> LayoutInflater.inflate -> real DecorView
-> the already-built libwestlake_view_renderer.so on the panel. That is the remaining trunk to first real
Android UI on screen (stone-1 territory); Genshin (Unity/IL2CPP/Vulkan/anti-cheat) is a separate multi-month climb.

## ★★★★★★★★★★ getLayout XML CHAIN FULLY VERIFIED ON DEVICE — framework XmlResourceParser yields real tag "alpha" (2026-07-05) ★★★★★★★★★★
After the DAYU600 was power-cycled (my aggressive kill of the SYSTEM appspawn had tripped the OHOS watchdog -> reboot; LESSON: never kill the system appspawn, only our proto), the getLayout XML chain now works END TO END on device:
`XMLPARSE[events=2 firstTagName=alpha attrCount=4]` (direct natives) AND `FRAMEWORK_getLayout[events=3 tag=alpha]` (full framework path openXmlBlockAsset->new XmlBlock->new StringBlock->newParser->XmlResourceParser.next()/getName()). anim/abc_fade_in.xml IS an <alpha> tag with 4 attrs — REAL parse.
TWO bugs fixed to get here:
1) **OHBridge registration-order trap**: OHBridge registers XmlBlock(9/9)+StringBlock(4/4) native stubs on the class's <clinit>. AssetManager works because it clinit's at startup (OHBridge first, my System.load JNI_OnLoad second -> I win). XmlBlock clinit's LATE (after System.load) so OHBridge registered AFTER me -> OHBridge won (and only stubbed 9, leaving nativeNext etc. as "No implementation"). FIX: in the probe, force `Class.forName("android.content.res.XmlBlock"/"StringBlock", true, cl)` (initialize=true) BEFORE System.load(libandroidfw) -> OHBridge registers first, my JNI_OnLoad re-registers on top -> mine win. (Name-based JNI export Java_android_content_res_XmlBlock_* does NOT help — BCP-class native lookup only searches boot libs, not app-loaded libandroidfw.)
2) **nativeNext must loop past namespace events**: ResXMLParser::next() also returns START_NAMESPACE(0x0100=256)/END_NAMESPACE(0x0101=257) which the XmlPullParser interface skips; my switch hit default->1(END_DOCUMENT) on the first namespace event -> events=0. FIX: while-loop, `default: break` (skip) until a mappable event (START_TAG=2/END_TAG=3/TEXT=4/START_DOCUMENT=0/END/BAD_DOCUMENT=1).
So resource->value->XMLpath->ResXMLTree->ResXMLParser->tag/attr names ALL work on device via my JNI (AssetManager 7 + XmlBlock 20 + StringBlock 4 natives) + interpreter shorties. This is EXACTLY what LayoutInflater.inflate consumes. NEXT: find a LAYOUT-type resource (2048 AppCompat has abc_*.xml layouts, e.g. getResourceIdentifier("abc_screen_simple","layout","com.digiplex.game")) -> Resources.getLayout(id) -> LayoutInflater.inflate(parser,null) -> creates the View tree (needs View classes + their natives = the "un-stub View stack" stone) -> real DecorView -> libwestlake_view_renderer.so on panel. DEVICE OK now; do NOT kill system appspawn.

## ★★★★★★ PATH-B (Genshin/Unity) FEASIBILITY: Android arm64 .so LOADS on OHOS via bionic-shim (2026-07-05) ★★★★★★
The provided /Users/yao/Downloads/uptodown-com.miHoYo.GenshinImpact.apk is NOT Genshin — it's the Uptodown store client (package com.uptodown, only libuptodown-native.so + androidx libs, Geomanist fonts, NO libunity/libil2cpp). Real Genshin = Unity/IL2CPP, ~15GB downloaded at runtime. So THIS apk can't run Genshin.
BUT I validated the #1 Genshin prerequisite autonomously: **can Android native .so load on OHOS?** YES.
- DAYU600 GPU = Mali (libmali.so.0/1, mali_kbase.ko) + Vulkan supported (const.SystemCapability.Graphic.Vulkan=true, has libvulkan.so/libGLESv3.so/libEGL.so). GPU capability for Unity/Vulkan is THERE.
- Android arm64 .so (libdatastore_shared_counter.so from the apk) needs libc/libm/libdl.so (bionic sonames) + @LIBC-versioned symbols. OHOS = musl. Tested with a tiny OHOS dlopen tester (westlake-art-runtime/pathB-ndk/dltest.c): first fail was `Error relocating: __errno: symbol not found` (bionic __errno vs musl __errno_location) — so the OHOS linker ACCEPTS the Android ELF + resolves libc/libm/libdl, only bionic-specific symbols are missing.
- Built westlake-art-runtime/pathB-ndk/libbionic_compat.c (shim: `int* __errno(){return __errno_location();}` + `__system_property_get` stub). `LD_PRELOAD=libbionic_compat.so ./dltest ./and.so` -> **DLOPEN_OK**. The Android .so loaded on OHOS musl.
- Symbols this small .so needed: __cxa_atexit/__cxa_finalize/__errno/__stack_chk_fail/__system_property_get/ftruncate/getauxval/mmap/strerror/strncmp — musl has all except __errno(→__errno_location) and __system_property_get. Note: existing bionic shim art-latest/stubs/westlake_bionic_compat_stubs.c from the appspawn work can be reused/extended.
PATH-B mechanism PROVEN: Android .so -> OHOS via bionic-on-musl shim (whack-a-mole scales with lib size). Compile the tester: `clang --target=aarch64-linux-ohos --sysroot=.../native/sysroot -O0 dltest.c -o dltest -ldl`. NEXT for real Unity: (1) get a REAL Unity game apk (with libil2cpp.so/libunity.so) — small no-anticheat one first; (2) extend the bionic shim to all symbols libunity needs (pthread/JNI/Android liblog/libandroid/AAssetManager/ALooper/native window); (3) Vulkan passthrough: Unity's VkInstance/VkSurface -> OHOS libvulkan + a real Surface/ANativeWindow (the hwui/ANativeWindow bridge from gate-2 is a start); (4) UnityPlayerActivity + SurfaceView shell + Activity lifecycle; (5) anti-cheat = the real risk (may be intractable + legal concerns). Path A (LayoutInflater/Context/View stack for ordinary Android apps) barely overlaps with path B. Full reproduction doc: ~/Desktop/西湖穿刺-完整上下文与复现.md.

## ★★★ PATH-A (real Android UI app) major progress: framework-res + inflate runs deep (2026-07-05) ★★★
Goal became: run /Users/yao/Downloads/test.apk on the board. test.apk == the com.uptodown store app
(15182274 bytes, same as the "genshin" file; a REAL normal Android UI app: androidx/AppCompat,
4 dex, tiny native libs). Pushed the View path toward getting its UI on screen:
- framework-res.apk (Android framework resource package, 0x01xxxxxx styles/themes) is NOT on the
  OHOS board (correct — OHOS != Android). EXTRACTED it (35MB) from a LOCAL Android-35 system image
  ~/Library/Android/sdk/system-images/android-35/google_atd/arm64-v8a/system.img via a pure-python
  GPT->super(LP metadata)->system-ext4 parser + `pip install ext4` (in a venv /tmp/androidextract).
  Script saved: westlake-art-runtime/pathA-view/extract-framework-res.py. Deployed to the board
  (chunked 4MB transfer + device-side cat, since hdc truncates 20MB+). Path on device:
  $R/apks/framework-res.apk.
- VIEWINFLATE probe (in assetProbe/uptodownProbe stage of Dayu600ApkStageProbe.java): via reflection
  ActivityThread.systemMain() -> getSystemContext() -> add framework-res via AssetManager.addAssetPath
  -> LayoutInflater.from(ctx).inflate(0x01090003). VERIFIED: Resources.getSystem().getString(
  android.R.string.ok) == "OK" — the Android framework resource system RESOLVES on OHOS.
- Natives implemented this session to get inflate this far (all in westlake-art-runtime backups):
  (1) interpreter.cc UnixFileSystem.canonicalize0 (identity); (2) StringBlock.nativeCreate([BII)J in
  the JNI (builds a standalone ResStringPool via setTo(copyData) — framework makes these during
  inflate; was crashing nativeGetSize@0x20); (3) interpreter JLII shorty marshaller (for nativeCreate);
  (4) robust SystemClock uptimeMillis/elapsedRealtime catch in interpreter BEFORE the JNI-resolution
  block (shorty-independent — some clock natives are @CriticalNative and skipped the if-else chain).
- CURRENT WALL = "shim strip": inflate reaches ContentResolver.<init> -> Context.getOpPackageName(),
  NoSuchMethodError because the runtime resolves android.content.Context from dayu600-framework-shim.dex
  (an 8-method compile stub) which SHADOWS the real framework.jar Context. deploy-dayu600.sh has
  `WESTLAKE_OMIT_FRAMEWORK_SHIM=0/1` — setting 1 (strip whole shim) BREAKS runtime bootstrap (runtime
  needs some shim bootstrap classes). Need SURGICAL removal of only the shadowing classes (Context et al)
  from the shim while keeping bootstrap stubs, OR add the needed methods to the shim Context (whack-a-mole).
  Stage=uptodownProbe, WESTLAKE_TARGET still com.digiplex.game.MainActivity. Commits through 3765351.
- Remaining path A after shim-strip: finish inflate (more natives) -> View measure/layout/draw (Canvas/
  Paint/RenderNode/Typeface natives) -> ViewRootImpl -> ThreadedRenderer -> ANativeWindow->OHOS window
  (the gate-2 hwui bridge is the start) -> pixels on panel. Then input bridge for interaction.

## ★★★ SHIM-STRIP breakthrough + ServiceManager/Binder wall (2026-07-06) — the real crux ★★★
Goal: run /Users/yao/Downloads/test.apk (== com.uptodown, a real Android UI app) on the board, UI on screen.
The framework-shim.dex (device: $R/apks/dayu600-framework-shim.dex, 46088 B backup .bak) sits BEFORE
framework.jar in the BCP, so its ~51 stub classes SHADOW framework.jar's real Context/View/Activity/
LayoutInflater/widgets. WESTLAKE_OMIT_FRAMEWORK_SHIM=1 (strip all) BREAKS bootstrap.
CONSERVATIVE STRIP WORKS (pathA-view/shim-variants/dayu600-framework-shim-conservative.dex, 27032 B,
31 classes): keep bootstrap (*ServiceManager/*FrameworkInitializer/Flags) + os leaf (Bundle/Looper/
Handler/MessageQueue/Environment/Trace) + media + servertransaction; strip the Context/View/widget/
app-Activity/graphics shadow layer (40 classes). Also removed `westlakeFindViewStatus` trace-marker
refs from the KEPT shim Handler.java+SoundPool.java (they pointed at the now-stripped shim Activity).
WALL PROGRESSION (each proves real framework.jar classes now take over):
  getOpPackageName (shim Context) -> westlakeFindViewStatus (shim Handler coupling) [FIXED]
  -> "Unable to instantiate Application(): IServiceManager.getService2(String) on null @ActivityThread.
     initializeSystemThread:8732" == REAL framework.jar ActivityThread hitting the ServiceManager/Binder
     dependency the shim was designed to avoid. `getService2`->`android.os.Service` is a NON-standard
     (westlake/OHOS-patched framework.jar) signature; the patch source wasn't found in local trees.
THE CRUX: running a real Android app UI needs the Android system-service layer (ServiceManager -> Binder
-> WindowManagerService -> Surface). OHOS lacks it. The "Wine-style shim only the OS boundary" approach
hits this head-on (Waydroid solves it with a full Android container instead). This is the fundamental wall.
3 PATHS presented to user (away, no pick yet): (1) HYBRID = real inflate + draw View to panel via the
gate-2 hwui bridge, bypass ActivityThread/WindowManager — fastest to a VISIBLE real-layout-on-screen,
my recommendation; needs a minimal Context (real Resources/theme, no ActivityThread) that real Views
accept. (2) full system-service layer (weeks). (3) tiny self-made Hello-View APK first to prove the
render chain, then return to uptodown. DEVICE OPS: the aggressive strip crashed the board -> reboot ->
~15min to settle -> then usable with a robust retry wrapper (each hdc cmd retried up to ~12x with
kill/start). RUN probe DETACHED won't complete; run `timeout 55 sh deploy-dayu600.sh` synchronously
(connection drops mid-run, device finishes + writes $R/apks/probe-logs/asset-probe.txt), then robust-read.
uptodownProbe stage CRASHES at DexFile.entries/getClassNameList (JNI fatal, uncatchable) — use assetProbe
stage (has the VIEWINFLATE test). Device left at known-good baseline: original shim (46088), stage=assetProbe.
Commits through b90683a.

## ★★★★★ PIVOTAL (2026-07-06): framework.jar path = known dead-end; custom-engine path = PROVEN ★★★★★
Found docs/agent-memory/project_real_framework_jar_arch.md (1046 lines). It documents that the real
framework.jar path (which my shim-strip was pushing toward) hits the SAME getService2 NPE I hit, and
prior work spent 36+ PF-arch patches WITHOUT breaking through: framework.jar fundamentally needs real
Binder/ServiceManager IPC (ServiceManager.getService("display")->IServiceManager.getService2->NPE via
DisplayManagerGlobal during ContextImpl.createSystemContext), compounded by an ART long-jump NULL-vtable
bug that stops even catching the NPE. So the shim-strip -> real-ActivityThread direction is a KNOWN months-
stuck dead-end. DO NOT keep pushing framework.jar's real ActivityThread on DAYU600.
THE PROVEN PATH (doc's end, PF-arch-037..040): a CUSTOM engine that BYPASSES framework.jar entirely:
WestlakeInflater (parses the APK's binary AXML directly) + WestlakeLayout (custom ConstraintLayout multi-
pass solver) + WestlakeRenderer (walks a WestlakeNode tree -> OHBridge primitives -> SurfaceView). It
rendered a REAL app (noice) end-to-end: bottom-nav 5 tabs w/ real icons+labels, playback bar, fragments,
on an actual display. That engine's Kotlin/Java code is NOT on this machine (built on the com.westlake.host
Android-host-app side; only the memory doc + an early stub art-latest/stubs/westlake_render.cpp are here).
IMPLICATION for DAYU600 test.apk: rebuild a MINIMAL version of that engine ON the board. The hard INPUT
side is ALREADY done this session: libandroidfw parses AXML + getLayout returns the View-tag tree
(viewTags=[androidx.appcompat.widget.FitWindowsLinearLayout, ViewStubCompat, include]); the gate-2 hwui
bridge already lit 2048 on the panel. Plan: parse test.apk layout -> node tree (tag/attrs) -> simple
measure/layout -> draw each node as rect+text via the hwui/OHBridge bridge -> uptodown wireframe on the
DAYU600 panel = the first VISIBLE real result. This is path 2, validated by prior art. Device at known-good
baseline (orig shim 46088, assetProbe). Commits through a2eb796.

## ★★★★★★ VISIBLE RESULT (2026-07-06): custom-engine layout wireframe ON THE DAYU600 PANEL ★★★★★★
PIVOT executed + WORKING. hwui_layout.cpp (ports/dayu600/gfx-smoke/) renders a view-hierarchy node
tree as nested rounded rects via the gate-2 hwui pipeline (RenderProxy->RenderThread->OHOS Surface),
bypassing framework.jar entirely. VERIFIED ON DEVICE with a screenshot: a full store-app UI wireframe
(toolbar w/ nav+title+search, 6 list cards each w/ icon+name+subtitle+green install btn, 4-tab bottom
nav w/ first selected) rendered on the DAYU600 panel. Screenshot: westlake-art-runtime/screenshots/
dayu600-layout-wireframe-v1.jpeg. THIS is the first visible UI-on-panel of the whole test.apk effort.
BUILD: ports/dayu600/gfx-smoke/build-hwui-layout.sh (copy of build-hwui-2048.sh, SKIP_LIBHWUI=1 reuses
the gate-2 libhwui.so). Compiles hwui_layout.cpp + reuses ohos_display_surface.o/oh_anativewindow_shim.o/
egl_interposer.o/skia_interposer.o, links -l:libhwui.so + device-libs. Binary 2965800 B.
RUN: send to /data/local/tmp/hwui/hwui_layout; `cd /data/local/tmp/hwui && LD_LIBRARY_PATH=/data/local/
tmp/hwui:/system/lib64:/vendor/lib64:/system/lib64/platformsdk:/vendor/lib64/chipsetsdk ./hwui_layout 15
&`; capture with `snapshot_display -f /data/local/tmp/hwui/layout_shot.jpeg` (OHOS screencap, 1200x1920).
Draw primitives that work: canvas->drawColor/drawRoundRect (filled + kStroke_Style). Text still needs a
letter font (minikin stubbed; drawDigit does 0-9 via pixel rects — extend to A-Z for labels).
v1 = hand-laid representative com.uptodown tree. v2 (NEXT) = link libandroidfw into the native binary,
ApkAssets::Load(test.apk)->AssetManager2->OpenXmlAsset(main layout)->ResXMLTree walk -> node tree ->
render the REAL test.apk layout. Parse side already proven this session (getLayout via the same libandroidfw
C++ API in westlake_assetmanager_jni.cpp). Commit 54c4505. Device stable (robust retry wrapper needed).

## ★★★★★★★ GOAL-LEVEL (2026-07-06): test.apk's REAL layout parsed from APK + rendered on panel ★★★★★★★
v2 COMPLETE. The custom-engine path renders test.apk's OWN layout on the DAYU600 panel, end-to-end,
bypassing framework.jar/Binder/ActivityThread entirely:
- PARSE (native, links libandroidfw): westlake-art-runtime/pathA-view/native-parse/dump_layout.cpp
  ApkAssets::Load(/data/local/tmp/test.apk) -> AssetManager2 -> found 487 real layouts (home_fragment,
  app_detail_activity, installer_activity, app_detail_main_info...) -> walk app_detail_main_info
  (0x7f0e0020) ResXMLTree -> 32-node tree (RelativeLayout>ImageView/ProgressBar/TextViews...) emitted as
  "depth|tag|w|h" to /data/local/tmp/hwui/tree.txt. Runs on-device with LD_PRELOAD=libandroidfw_stubs.so
  (androidfw dlopen stubs + a log_addendum.cpp providing __android_log_print/write/vprint which OHOS musl
  lacks). test.apk pushed chunked (5x ~3.5MB). list_layouts.cpp = the 487-layout scanner.
- RENDER (native, links libhwui): hwui_layout.cpp (v2) reads tree.txt -> naive nested-stack layout ->
  renders each node as a typed rect (ProgressBar=green 0x3ea06a, ImageView=blue 0x4e8ea8, TextView=grey
  0x9fb7c0, containers=outline) via the gate-2 hwui pipeline. FILE-BRIDGE decouples parse(libandroidfw) from
  render(libhwui) — avoids co-linking risk. Build: build-hwui-layout.sh. Run: send to /data/local/tmp/hwui/,
  `LD_LIBRARY_PATH=/data/local/tmp/hwui:/system/lib64:... ./hwui_layout 15 tree.txt &`, snapshot_display -f.
VERIFIED: screenshots dayu600-testapk-real-layout-v2.jpeg (test.apk's real app-detail wireframe on panel)
+ dayu600-layout-wireframe-v1.jpeg. Commit b3cc53a.
HONEST CAVEATS / next: structural wireframe only — naive vertical-stack layout (no LinearLayout orientation/
weights, no RelativeLayout rules), NO text glyphs (minikin stubbed; bars not text — need A-Z pixel font like
drawDigit, or real minikin), NO image decode (blue box not the real icon). Next milestones: proper layout
engine (orientation/weight/gravity/match_parent-vs-wrap), text render, PNG/vector decode for icons. But the
core is proven: test.apk's own parsed layout renders on the board via the validated custom-engine route.
