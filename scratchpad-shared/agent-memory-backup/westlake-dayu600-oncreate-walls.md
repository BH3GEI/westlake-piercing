---
name: westlake-dayu600-oncreate-walls
description: "DAYU600 test.apk(com.uptodown)onCreate 攻坚:过了 TLS 墙后连破 6 道运行时墙(核心是 Object.wait 修复),onCreate 推进到第 164 行,当前卡在资源按名解析(L10);big board serial 5ce2dcee"
metadata: 
  node_type: memory
  type: project
  originSessionId: b3d658ee-33a9-4b16-9c9b-325d7c2886c6
---

自建 ART(libwestlake_art.so)在 DAYU600 大板(hdc serial `5ce2dcee00000000000000000923012c`,Rockchip VID 2207,substrate 在 `/data/local/tmp/westlake-dayu600-substrate/`)上跑 test.apk(com.uptodown 7.33)。2026-07-06 这个 session `UptodownApp.onCreate` 从"卡在 TLS 后"推进到**第 164 行**,连破 6 道墙,全部已修+上大板验证:

1. **`Object.wait(long,int)` 空转死锁**(核心 bug):shorty `VJI` 在 InterpJni 没处理→wait 空操作立即返回→`while(!cond)wait()` 忙等死转。修:interpreter.cc 实例段加 `VJI` handler,直接 `receiver->Wait(self,ms,ns)`。这个修复解锁所有线程同步(协程/HandlerThread/阻塞队列)。
2. **Perfetto 追踪 ~60 native**:`PerfettoTrackEventExtra*` 全未注册。修:runtime.cc 里脚本生成 62 条 no-op 桩(init/get_extra_ptr 返回假句柄 1,setter/emit 空操作)。
3. **PerfettoTrace / Category**:`MQ_CATEGORY` 为 null 导致 `isEnabled()` NPE。修:桩 `Category.native_init`→1、`native_is_enabled`→false;并加 InterpJni `JLLL` 静态 shorty。
4. **HandlerThread 集体死**:`Process.setThreadPriority` 未注册在 `HandlerThread.run` 抛异常。修:桩 android.os.Process 调度 native(setThreadPriority/ThreadGroup/Scheduler 等)。
5. **Binder/StrictMode**:`Binder.setThreadStrictModePolicy` 等未注册。修:桩 android.os.Binder 静态 native(StrictMode/calling-identity/work-source)。
6. **android.os.Trace**:真实名是 `nativeIsTagEnabled` 等(不是 `IsTagEnabled`,曾注册错名字)。修:按真实名桩全部 Trace native,`nativeIsTagEnabled`→false。
7. Context 补 `checkCallingOrSelfPermission`/`registerReceiver`/`sendBroadcast` 等;探针在 onCreate 前跑 `FirebaseInitProvider`(ContentProvider)。

**2026-07-06 续:onCreate 从 150 推进到第 342 行**,又破多道墙:
- **资源按名解析**:`getIdentifier` 返回 0(arsc 名索引空)→ 探针里 `WlResources` 子类硬编码桥接 5 个 Firebase string(值由 subagent 从 arsc 解出:google_app_id=0x7f1401d1=`1:171380306104:android:4e827fc7c388aeec79c44d` 等)+ getBoolean/getInteger 兜默认值。
- **`Unsafe.allocateInstance` 运行时根本没实现**(UnsatisfiedLinkError)→ runtime.cc 实现(AllocObject,允许抽象类)。很多框架靠它。
- **`java.lang.reflect.Proxy.proxyClassCache` 全局为 null**(其 clinit 容忍失败)→ 动态代理全坏(拖垮 Settings$Global/Retrofit/binder 桩)→ 探针反射重建 WeakCache(KeyFactory+ProxyClassFactory)清 final 后 set,`proxyFix=OK`。
- **conscrypt `NativeCrypto.RAND_bytes`** 未实现 → runtime.cc 从 /dev/urandom 实现(UUID.randomUUID/SecureRandom 用)。
- **具体 PackageManager**:抽象类 179 方法,javac 编不出(隐藏方法)→ subagent 生成 smali 具体子类(委托 getServiceInfo/getPackageInfo 给 WlPmHelper),d8 合并进探针 dex。
- **Firebase 组件发现**:t2.g 读 `getServiceInfo(ComponentDiscoveryService,GET_META_DATA).metaData` 找 registrar → WlPmHelper 提供 metadata(去掉硬编码的 Common/Executors 避免重复)。
- **主 looper 时序 bug**:FirebaseInitProvider 跑在 prepareMainLooper 之前 → GMS 测量 `new Handler(getMainLooper()=null)` 崩 → 把 looper 准备挪到 Firebase 前。
- **系统服务**:`getSystemService` 用 `wlService`(allocateInstance + binder 字段塞 no-op Proxy)返回 connectivity/wifi/notification/phone/jobscheduler/alarm 等非空实例。

**★ 转折点发现(subagent 根因分析,2026-07-06)**:WorkManager 墙的直接卡点(Proxy/Method 的 static final 字段全 null → 动态代理坏)**根因 = 这个 fork 的 "Tolerating clinit failure" 补丁(class_linker.cc:6267-6519)**:任何 `<clinit>` 抛异常就吞掉并强行 `MarkClassInitialized`,字段留 null。这**正是**贯穿整个项目的 boot-image null-static 病(Charset/StandardCharsets/FileSystem/Security.spiMap/BufferedWriter…全是它)。而最深的触发源 = **MethodHandles$Lookup/VarHandle invoke-virtual vtable mis-dispatch(findVarHandle→unreflectVarHandle)+ Unsafe.<clinit> 崩** —— 即 [[westlake-dayu600-two-fronts-converge]] 里那个"单一 vtable bug 就是总闸"。**不是 invoke-custom/LambdaMetafactory(那个完整存在、无关)。** 修复排序:A=修 vtable mis-dispatch + Unsafe clinit,重建 prebuilt-boot/*.art(根治,一次解决所有 null-static);B=class_linker.cc:6513 clinit 失败时别 MarkClassInitialized(止血);C=把 :6321/:6421 的逐类字段回填扩展到 Proxy/Method(band-aid)。我目前走的探针反射回填 = C 的探针版。

**2026-07-06 再续:onCreate 冲到第 623 行 —— WorkManager 完全打通!** 关键突破:
- **`Unsafe.allocateInstance` 实现后**(见上),用它给系统服务/AMS 造实例。
- **具体 IActivityManager stub(subagent 生成 269 方法 smali,getRunningAppProcesses→emptyList)** 塞进 `ActivityManager.IActivityManagerSingleton.mInstance` → `WorkManager.initialize()` 里 ForceStopRunnable 的 `getRunningAppProcesses()` 通了 → **wmInit=OK(真初始化,连 Room/SQLite 调度器全建)**。
- **Proxy/Method 静态字段探针回填**(proxyClassCache/ORDER_BY_SIGNATURE_AND_SUBTYPE/constructorParams/Method.ORDER_BY_SIGNATURE,wlSetStaticFinal 清 final)—— 但大接口 Proxy 脆弱,所以 AMS 走 smali concrete stub 而非 Proxy。
- runtime.cc 新增 native:`sun.nio.ch.NativeThread.current/signal/init`、`sun.nio.ch.FileKey.init`(fstat 取 dev+ino)、`sun.nio.ch.FileDispatcherImpl.lock0`(返回 LOCKED=0)/release0 + InterpJni 实例 shorty `ILZJJZ`。
- **`Resources.mSystem` 换成容错 WlResources**:`SQLiteGlobal.getWALConnectionPoolSize` 读框架资源 `Resources.getSystem().getInteger(0x10e015b)` NotFound → WlResources.getInteger 吞掉返 0。WlResources 也给 getString/getText/getStringArray/getIntArray 加 NotFound 容错。
- 探针 onCreate 错误现在解开 ExecutionException→root cause 看真实堆栈。

**2026-07-06 又续:SQLite JNI 打通,onCreate 深入 WorkManager 查询**:
- **libsqlite_jni.so 静态链接 SQLite 3.46.1 amalgamation**(subagent 建,over sqlite3.c 静态,不依赖平台 libsqlite.z.so —— OHOS 的 libsqlite.z.so 只导出符号表不导出函数)。含 android_database_SQLiteConnection/CursorWindow/SQLiteGlobal 的全部 JNI + JNI_OnLoad 自动注册。部署到 $S/android/lib64/,探针 `System.load` 它。1.4MB。
- **SQLite native 的 shorty 全批量加进 InterpJni**:脚本生成 28 个静态 shorty handler(JJJ/JJL/IJJ/ZJJ/LJJ/VJJZ/JLILZZII/VJJIL/VJJIJ/VJJID/JJJJIIZ/DJII/LJII/ZJDII/ZJJII/ZJLII 等,J/D 占 2 vreg),注入 interpreter.cc 静态段 JLLL 之后。
- runtime.cc 加 `libcore.io.Linux.statvfs`(statvfs 填 StructStatVfs,StatFs/Picasso 缓存用)。
- ProbeContext 加 deleteDatabase/databaseList/openOrCreateDatabase(Room DB 用)。
- **当前卡点**:SQLite 执行真 SQL,`REINDEX LOCALIZED` 报 SQLITE_ERROR —— Android 特有 collation "LOCALIZED"/"UNICODE" 没注册(SQLite JNI 的 register_localized_collators 当初桩成 no-op)。让 subagent 补基础 create_collation(UTF-16 memcmp)。
- **前向扫描(subagent)**:onCreate 623→结尾第 1362 行(还~740 行),依赖已全摸清:2 个同步阻塞点(getWorkInfosByTag().get() @623/786)、协程 Dispatchers(需主 Looper)、PackageManager 权限、root 检测(h3.t1.W 建议桩 false)、通知清理(q6.h active_notifications 表 SQLite)、InMobi CMP ChoiceCmp.startChoice(被 wizard_completed=false 门控,大概跳过,否则桩空)、NotificationManager.getActiveNotifications(需非空数组)、设置迁移(纯 SharedPreferences)。MainActivity 上屏=传统 View/AppCompat/setContentView/Toolbar/ViewPager2。

**(历史)当前卡点 = SQLite native JNI**:onCreate:623(WorkManager `getWorkInfosByTag` future)→ Room 打开 WorkDatabase → `android.database.sqlite.SQLiteConnection.nativeOpen` **未注册**。设备有 `/system/lib64/platformsdk/libsqlite.z.so`,但 SQLite JNI(android_database_SQLiteConnection.cpp/CursorWindow/SQLiteCommon,~30 native)不在 liboh_android_runtime 里 —— 要从 AOSP 源码移植编译一层 `libsqlite_jni.so`(over libsqlite.z.so,OHOS clang)并注册。这是当前唯一大墙。

**(历史)当前卡点 = Android 系统服务/binder 边界**:onCreate:342 `WorkManager.getInstance()` → 我补的 `WorkManager.initialize()` → `ActivityManager.getRunningAppProcesses()` → 静态 AMS binder → `ServiceManager.sServiceManager` 为 null → NPE。给 AMS 塞 no-op IActivityManager Proxy 时又撞到大接口 method 排序把 Method 当 Comparable 的 ClassCastException。WorkManager 还需 JobScheduler(binder)+ Room/SQLite。这是无头板子没有 Android system server 的根本限制,需要系统服务模拟(一大块新工作),不是零散运行时缺口。

**关键文件**:interpreter.cc(InterpJni shorty handler)、runtime.cc(native 桩+注册块,patch_runtime_native_method)、Dayu600ApkStageProbe.java(ProbeContext + onCreate 看门狗 45s)。构建:`make -f Makefile.ohos-arm64 link-libart`;探针 `build-probe-mac.sh`;都 md5-verify 循环推送到大板,`nohup sh /data/local/tmp/run-utd-long.sh`,读 `probe-logs/uptodown-probe.txt` 的 `appOnCreate=`。设备迭代串行(单板),只读分析可并行派 subagent。见 [[westlake-dayu600-two-fronts-converge]]。

## ★★★ 里程碑达成(2026-07-06 晚):appOnCreate=OK —— onCreate 彻底跑通!
最后一道墙 = `UiModeManager.getCurrentModeType` 读实例 IpcDataCache(allocateInstance 后 null)→ 生成具体 smali 子类 `WlUiModeManager`(覆盖 getCurrentModeType→1,<init> 调 super,allocateInstance 实例化),合并进探针 dex。**onCreate 从 150 → 1362(结尾)完整完成,MainActivity 类加载+构造也 OK。** 全程根因级修复无 per-app 绕过。
**我现在是三-agent 统筹(领导)**:Agent-A 资源半边(framework-res.apk 匹配的是大板上 md5 3cac774e 那个,和 framework.jar 8c377c13 同目录部署,已拉到 /Users/yao/westlake-piercing/scratchpad-shared/)、Agent-C 窗口半边(新大板 5583f5be 真机验证)。下一山头=MainActivity 上屏。见 [[westlake-agent-b-identity]]。
smali 汇编器正确 classpath:smali-3.0.9.jar + **正确的 antlr-runtime-3.5.2.jar(167KB,repo1.maven.org 下,wlpm 里原来那个是坏的)** + cmdline-tools/latest/lib/external/*.jar。具体服务 stub 套路:smali 子类 extends 真 manager,<init> 调 super,覆盖被调方法返回默认值,allocateInstance 实例化,dex 合并进探针(build glob wlsvc/*.dex)。

## ★★★ 里程碑 + 根因发现(2026-07-06 深夜):MainActivity.onCreate 进入 APP 业务代码 + 正则 native 是坏的
**驱动 MainActivity.onCreate 越过全部框架墙**(反射 attach 手法):mApplication→layout_inflater→窗口(allocateInstance PhoneWindow 绕 Settings 无底洞)→主题 native(libandroidfw 加 6 Theme JNI)→样式 native(nativeApplyStyle/ResolveAttrs/RetrieveAttributes)→ATMS+ActivityClient 两个 binder 具体 smali stub(WlActivityTaskManagerSvc 98方法 / WlActivityClientControllerSvc 70方法,塞进单例)→getActivityInfo 非空(WlPmHelper.activityInfo)→Activity.mActivityInfo 设非空→mFragments.attachHost(null)→Application 回调 ArrayList 字段回填(allocateInstance 跳过 field-init)→Firebase Crashlytics registrar(installations/datatransport/messaging/messaging-ktx/**crashlytics/crashlytics-ktx**;**注意:加 sessions/executors/analytics 会破坏 Firebase 组件图**——sessions 要 @Background/@Blocking CoroutineDispatcher,executors 重复,都别加)。**现已进入 app 自己的 MainActivity.onCreate 业务代码**(`i5.g0.onCreate`→`g2.y3.<init>`)。
**★ 硬 foundation 墙(新根因)**:`com.android.icu.util.regex.PatternNative.create` **对任意 pattern 返回 nativePtr=0** → `Pattern.compile`/`String.replaceAll` 全挂(`IllegalArgumentException: nativePtr is null`)。即本 imageless ART 的 **ICU 正则 native(uregex_open)彻底不工作**。这一直被 tolerate-clinit 掩盖(所有 `static Pattern X=compile(...)` 的 clinit 静默失败留 null,如 Crashlytics `e3.y.g/h`)。修好正则 = 解锁一大片 null-static + app 真正能跑 regex。**最可能根因:imageless ART 没初始化 ICU data(u_setDataDirectory/udata),uregex_open 无数据返 null**。subagent a41cd 在查修(scratchpad/regex-icu/FINDINGS.md)。这跟 [[westlake-dayu600-two-fronts-converge]] 的 boot-image null-static 病同源(clinit 失败被吞),但触发源之一明确是**正则 native**。
上屏渲染钩子已预置(actOnCreate=OK 后抓 DecorView 调 Agent-C 的 WestlakeUpscreen.show(decor,1200,1920),guarded)。

## ★★★ FOUNDATION 大胜(2026-07-06 深夜):正则 native 修好 —— null-static 疫情根治
根因(subagent a41cd 诊断):这个 fork **早把 ICU 正则换成了手写 C++ `std::regex`(ECMAScript 语法)** 实现(`Westlake_PatternNative_compileImpl` @ runtime.cc:1390)。ECMAScript **拒绝 Java 专有语法**(`\Q..\E` 字面量转义、`\p{Alnum}` POSIX 类等)→ assign() 抛 → compileImpl 返 0 → `PatternNative.<init>` 的 registerNativeAllocation(0) 抛 `nativePtr is null`。现有翻译器 `WlSanitizeRegex` 只处理 lookbehind/possessive/named-group,没管 `\Q\E`/`\p{}`。**修法:扩展 WlSanitizeRegex(runtime.cc,加 WlPosixClassFor map + `\Q\E`→转义字面量 + `\p{Name}`/`\pX`→context-aware `[[:name:]]`/`[^[:name:]]` + `\A\z\Z\G\h\H\R`)。** compileImpl 的 retry 路径已存在无需改。编译 `make -f Makefile.ohos-arm64 link-libart`,部署 libwestlake_art.so(md5 abbed775)。**实测:regex 全通,Crashlytics 从 a3.c.a:679(replaceAll)冲到 a3.c.a:1489。** 非 ICU 问题(OHOS NDK libicu.so 零 uregex 符号,根本没用 ICU)。FINDINGS 在 scratchpad/regex-icu/FINDINGS.md。
下一墙:`The Crashlytics build ID is missing`(a3.c.a:1489,Crashlytics 读构建 ID)。

## ★★★ 里程碑(2026-07-06 深夜):冲进 setContentView 的 View 构造(inflate 开始)
正则修好后连破:Crashlytics build-id(WlResources 加 CR_STR/CR_ARR 硬编码 dummy 非空:com.crashlytics.android.build_id 等)→FirebaseAnalytics(GMS j7 强转 Application → ProbeContext.getApplicationContext 改返真 `new android.app.Application()`+设 mBase,静态 WL_APP_CTX)→**ViewConfiguration/View.<init>/ViewGroup.<init>(=inflate!)**。ViewConfiguration 读框架 dimen 0x10500d7 NotFound → WlResources.getValue 容错兜 0。现卡 `WindowManagerGlobal.getWindowManagerService()`(View 构造要 IWindowManager binder)→ 生成 WlWindowManagerSvc 具体 stub 塞 sWindowManagerService(同 ATMS 套路)。**下一步=纯 inflate 铺开→DecorView→Agent-C 的 WestlakeUpscreen.show 上屏。** framework-res 用 A 的 remapped-99pct(md5 76a92b8f)。

## ★★★ 里程碑+根因(2026-07-07):冲到 DecorView 构造,图形 native 走 InterpJni 缺 shorty handler
正则修好后一路过 Firebase/Analytics/AccessibilityManager(ServiceManager 用 WlServiceManagerSvc concrete stub,**getService2 返回 `Service.serviceWithMetadata(new ServiceWithMetadata())`**=null binder→降级)/WindowManager(WlWindowManagerSvc 155方法)/getDisplayId→0,**冲到 `com.android.internal.policy.DecorView.<init>` → `Paint.<init>` → `Paint.nSetFlags`**。
**图形 native 注册的真相**:runtime.cc 启动时 `dlopen("libandroid_runtime.so")` 失败(本 substrate 库名是 **`liboh_android_runtime.so`**!)→ 图形注册整个跳过。修:runtime.cc:4845 改 dlopen `liboh_android_runtime.so`(+ 先 dlopen libhwui.so RTLD_GLOBAL)。改后 `AndroidRuntime::startReg` 跑了,`hwui reg[23/54] graphics_Paint rc=0`。
**但 Paint.nSetFlags 仍 UnsatisfiedLinkError** —— 因为 **InterpJni(interpreter.cc)是解释器→native 的桥,按 shorty 硬编码 marshalling**(每个 shorty 一个 handler,`method->GetEntryPointFromJni()` 拿注册指针 + 按签名调)。**图形 native 的 shorty(静态 VJI/VJF/VJII/VJFF/VJZ…)没 handler**。我现有 VJI 是实例段(Object.wait),Paint.nSetFlags 是静态。**解法=给图形 native 的 shorty 批量加 InterpJni 静态 handler(同 SQLite 28 个那批),或实现通用 shorty 派发器。** 这是上屏 View 层的关键闸。

## ★★★ 图形 native 墙:根因锁定 + 板子不稳(2026-07-07 深夜,极长 session 收尾)
**onCreate 后一路冲到 `DecorView.<init>`(setContentView 核心),卡 `Paint.nSetFlags` UnsatisfiedLinkError。根因终于查清**:
- **boot classpath = core-jars(stringfactory/core-oj/core-libart/core-icu4j/conscrypt/bouncycastle),不含 framework.jar!** 所以启动时 `register_android_graphics_Paint` 的 `FindClass("android/graphics/Paint")` 在 boot classpath 里找不到 app 用的 Paint(它在 `$S/android/framework/framework.jar`,由 app 的 uloader 后加载)→ 图形 native 注册到错的/空的 Paint 类 → **app 运行时的 Paint.nSetFlags 未被 RegisterNatives 绑上 → ULE**(走名字 fallback `Java_android_graphics_Paint_nSetFlags` 也没有)。**adapter libhwui 有 PaintGlue::setFlags 实现 + nSetFlags 注册项**(不是缺实现),nInit 能用(Paint 构造走到 nSetFlags),纯 classloader/时机 mismatch。
- **native 派发架构**:`-DART_USE_CXX_INTERPRETER`,ULE 消息只在 `java_vm_ext.cc:311 Libraries::FindNativeMethod` 生成,由 `FindCodeForNativeMethod(1179)` 调,3 个 caller 抛 ULE(interpreter.cc:596、quick_trampoline:2124、jni_entrypoints:141)。DecorView 是 JIT/编译执行,nSetFlags 走 quick/jni_entrypoints 路径。
- **修法(两条,择一)**:①**把 framework.jar 加进 boot classpath**(启动 FindClass 找到对的 Paint,注册就绑对)——最干净,我的域;②在 app classloader 上下文重跑 register_android_graphics_classes;③(试过并撤回)在 FindCodeForNativeMethod chokepoint 对 void 未注册 native 返回 no-op 函数 `WlCriticalNoop` 绕过 —— **这个 no-op 工作法我全撤了**(interpreter/quick/jni_entrypoints/java_vm_ext),因为没验证到收益且怀疑引入回归。
- **★板子状态坑(下次注意)**:出现过**卡死的 toybox 进程(D 状态,kill -9 杀不掉)一直用旧 libart mmap 重写 log 污染结果**;为清它重启板子 2 次;**重启后 run 稳定早停在 `icuEntry=FOUND`(1198字节,proxyFix 之前),到不了 app onCreate** —— 撤光我所有 no-op 后仍这样,**确认是重启后板子运行态问题(非我代码)**,可能缺某个 mount/setup。重启前能到 actOnCreate(2696字节 DecorView)。**下次:先修复重启后 run 停 icuEntry(查 proxyFix 前那步 / substrate 重启后 setup),恢复到 DecorView,再上 boot-classpath 图形注册修法。**
- 保留在 libart 里的好东西:通用 InterpJni shorty 派发器(interpreter.cc,arm64 asm trampoline，foundation)、dlopen 库名修复(liboh_android_runtime)、正则 native 修复、libandroidfw 主题+样式 native、ATMS/ActivityClient/WindowManager/ServiceManager 具体 stub、A 的 99% framework-res(76a92b8f)。

## ★重启后停 icuEntry 的真因(2026-07-07):板子文件系统损坏
`ls /data/local/tmp/westlake-dayu600-substrate/core-jars/` 报 **`Invalid argument`(EINVAL=FS 级损坏)**,`conscrypt.jar` 同样。**卡死的 D 状态 toybox 进程 + 为清它做的 2 次不干净重启 → 文件系统损坏**(inode/目录项级)。boot classpath 依赖 core-jars → ART 在 icuEntry 挂,到不了 app onCreate。从 `/data/local/tmp/westlake-dayu600/core-jars/`(备份完好)`cp -r` 恢复了文件,但**仍停 icuEntry → FS 损坏比文件缺失更深**。**下次:板子需要 fsck / 重刷 / 干净重启修复 FS**,再恢复 substrate,才能回到 DecorView 继续 boot-classpath 图形修法。这不是代码问题。备份源:westlake-dayu600/core-jars、westlake-arm64/core-jars、wl-imgboot/jars。deploy 脚本:/data/local/tmp/deploy-dayu600.sh。

## 板子退化崩溃点(2026-07-07):FindMethodToCall<invoke-virtual> SIGSEGV
faultlog `cppcrash`(/data/log/faultlog/temp/cppcrash-*)确认:重启后 ART 在 icuEntry 后**崩溃(不是干净退出)**:`SIGSEGV(SEGV_MAPERR)@0x42e01400426c`(两次同地址=确定性),栈顶 `art::FindMethodToCall<(InvokeType)2=invoke-virtual>` ← `INVOKE_VIRTUAL()` ← `ExecuteSwitchImplCpp`。即解释器解析某 invoke-virtual 时踩到**损坏的类/vtable 数据**(FS 损坏污染了 dex/class)。恢复 core-jars 文件后仍崩 → 损坏更广。**这印证了 [[westlake-dayu600-two-fronts-converge]] 的 invoke-virtual vtable 隐患区,但此处是 FS 损坏触发的。恢复:重新 provision substrate 的 jars/dex(备份 westlake-dayu600/、westlake-arm64/、wl-imgboot/)。** 重启前一切正常到 DecorView。

## ✅ 更正(2026-07-07):上面"FS损坏/proxyFix崩溃/板子退化"全是管道阻塞幻觉!
真相:用 `hdc shell "sh run.sh 2>&1 | tail" &`(regular bash 后台)跑 run,**管道没人消费 → toybox 卡在 pipe_read → run 提前停在 icuEntry/px5**,被我误读成 FS 损坏 + FindMethodToCall 崩溃 + 回归。那个 cppcrash(FindMethodToCall<invoke-virtual>)是更早带派发器时的旧崩溃,不是这些 run 的。**正确跑法:run_in_background=true(harness 消费管道)或设备端 setsid 重定向到设备文件**。这样一路通:px1-7→proxyFix=OK→sqliteJni=loaded→gfx OK→appOnCreate=OK→**actOnCreate=FAIL:Paint.nSetFlags(DecorView 里程碑,和重启前一样)**。板子从没坏过。core-jars 的 `Invalid argument` 是卡死进程瞬态,干净重启+恢复后正常。**教训:跑 run 绝不用 regular-bash 后台管道;core-jars md5 匹配备份=没损坏。** 我撤了派发器+dlopen 修复(其实没必要撤,但现在是干净的 pre-图形态,仍到 DecorView)。

## 🎉 图形 native 墙攻破(2026-07-07):@CriticalNative no-op 修复生效
**Paint.nSetFlags 墙过了!** 真因不是 classloader(Paint 就在 BootClassLoader,PaintCL=BootClassLoader),是 **adapter libhwui 对 framework-16 只做了部分注册**(nInit 绑了,nSetFlags 没绑)。修复(全部生效):
1. `java_vm_ext.cc FindCodeForNativeMethod`:未注册的 **android/graphics/ + void** native 返回 `WlCriticalNoop`(限定 graphics+void,不误伤别的)。
2. **关键 bug 修复**:`interpreter.cc` @CriticalNative 解析路径,`RegisterNative(WlCriticalNoop)` 后**对未 visibly-init 的 @CriticalNative,GetEntryPointFromJni() 仍返回旧 dlsym stub** → 第598行误判 "unresolved or stale native entry" 抛异常。改成解析后**直接用 native_code**(`jni_entry = native_code`),别重读 GetEntryPointFromJni。
→ onCreate 从 DecorView:272(Paint)推进到 **DecorView:298 AnimationUtils.loadInterpolator**。
**新墙**:`actOnCreate=FAIL:NotFoundException:Resource ID #0x10c000e @ResourcesImpl.getValue:285 @Resources.getAnimation @AnimationUtils.loadInterpolator:415 @DecorView.<init>:298 @PhoneWindow.generateDecor`。0x10c000e = android 框架 interpolator/anim 资源,Agent-A 的 framework-res 里缺/ID 没映射对。WlResources 的 tolerant getValue 没拦到(抛在 ResourcesImpl.getValue,不是 Resources wrapper)。**下一步:补 framework-res 的这个 interpolator,或让 loadInterpolator/getValue 容错。★ 跑法铁律:run_in_background 或 setsid,绝不 regular-bash 后台管道;每条命令别乱 pkill 上一个 run。**

## 进展(2026-07-07):interpolator 墙也过了,onCreate 深入 DecorView 构造
interpolator 资源墙(0x10c000e)绕过:①WlResources 加 `getAnimation` 容错 override(getIdentifier 按名字重定向到能解析的 interpolator);②**关键**:PhoneWindow 设 `mUseDecorContext=false`(反射,udc=false 标记),让 generateDecor 用 getContext()(=actBase=ProbeContext→WlResources 容错)而非 DecorContext(包真 application Resources)。→ onCreate 过 DecorView:298 interpolator,推进到 DecorView/setContentView 更深处,现在在 **app 的 MainActivity.onCreate 深处 hang/慢**(过 preOC=1,没到 actOnCreate)。加了看门狗线程(invoke onCreate 前起守护线程,每20s dump main 线程 Java 栈到 oc-hang.txt)定位。**★板子进程管理噩梦:setsid run 的 timeout 400 + NO_EXIT 让进程重叠/cycle,pid 乱跳;`tr` busybox 缺失;冷启动慢。诊断务必:重启清一切→单 setsid run→单条长轮询(不中途 pkill,否则杀掉正在跑的 run)。** libart md5=78ecbc2d(graphics no-op fix),probe dex 带看门狗。图形墙彻底解决是本 session 最大成果。

## 板子环境退化(2026-07-07 收尾):run 停窗口构造,非代码
撤回所有实验 probe 编辑(watchdog/udc/getAnimation)后,run **仍稳定停在 mainNew=OK**(窗口构造 wlAlloc PhoneWindow,~15s 后 toybox 干净退出,无 cppcrash)→ **确认是板子环境退化(5+ 次重启 + 重叠 setsid run 累积),非我代码**。健康时(本 session 早些)同 libart 能到 actOnCreate=FAIL:interpolator。probe dex 已回到干净态(189648 bytes,无实验编辑)。**下次:板子需要真正干净的状态(可能断电冷启动/换板子/等服务充分起来)才能复现到 DecorView。图形墙 libart 修复(md5=78ecbc2d)是扎实成果,与板子退化无关。** 可靠跑法 = run_in_background + `hdc "sh run.sh 2>&1|tail"`(harness 消费管道);setsid 设备端重定向不可靠。

## 🎯 SIGSEGV 崩溃根因修复(2026-07-07):@CriticalNative 分发用错 fn
之前 onCreate 到 nSetFlags/DecorView 时 **RC:139 SIGSEGV**(cppcrash: `InterpreterJni → art_jni_dlsym_lookup_critical_stub → artFindNativeMethodRunnable → GetCurrentMethod → WalkStack` NULL deref @0x4)。根因:interpreter.cc @CriticalNative 分发块第638行 `const void* fn = method->GetEntryPointFromJni();` **重读了旧 dlsym critical stub**(对未注册 @CriticalNative,RegisterNative 不更新 EntryPointFromJni),然后 handler 把 stub 当裸 C 函数直接调 → stub 去 walk 不存在的托管帧 → 崩。**修复:把 `jni_entry` 提升出块作用域(声明 `const void* jni_entry = nullptr;` 在 575 块前),解析后设为 native_code(WlCriticalNoop 或真 fn),638 行改 `const void* fn = jni_entry;`。** → onCreate 稳定过 nSetFlags,~5s 到 actOnCreate=FAIL:interpolator(0x10c000e)。libart 需重记 md5。
## Subagent 侦察成果(2026-07-07)
- **framework-res**:0x10c000e 在 remapped-99pct.apk 里解析正确(type 0x0c=interpolator,entry 0x0e=linear_out_slow_in,XML 在)!NotFound 是运行时 AssetManager 问题(on-device 可能旧/没当 package 0x01 加载)。getIdentifier("linear_out_slow_in","interpolator","android")→0x10c000e 可解析。
- **DecorView.<init> 墙序列**(framework.jar 8c377c13,classes5.dex)::298 loadInterpolator(0x10c000e)→ :300 第二个(0x10c000f)→ getInteger(0x10e0183)→ getBoolean(0x11101b2)→ getColor(0x1060375)→ setWindow → 完。**真正的墙在 PhoneWindow.generateLayout**:`obtainStyledAttributes(R.styleable.Window)` + `inflate(R.layout.screen_simple=0x1090133)`。DecorView.<init> 无 obtainStyledAttributes/getSystemService。
- **uptodown MainActivity**:extends AppCompatActivity(via i5.g0 base)。onCreate=super.onCreate(AppCompat)→ **setContentView(R.layout.main=0x7f0e0121)**→ ~20 findViewById + RecyclerView/ViewPager2/TabLayout。**无阻塞原语,跑到 return-void**(延迟工作是需 Looper.loop 的协程)。**门:h5.e.m(this)@L10200 若判为 TV → startActivity(TvMainActivity)+finish()+return**(headless 要小心别误判 TV)。

## 本轮净成果(2026-07-07 subagent 轮):SIGSEGV 修复 + 全路线图 + interpolator 诊断
**libart md5=5b517bd884ed720cbde17b6dc67e8856(SIGSEGV 修复版,已部署)**。核心:@CriticalNative 分发 SIGSEGV bug 修好(见上条)→ bowf95auw run 验证 onCreate 稳定到 actOnCreate=FAIL:interpolator(过 Paint.nSetFlags,无崩溃)。
- **interpolator(0x10c000e)诊断**:on-device framework-res=76a92b8f(remapped-99pct,非旧,已 addAssetPath 加载);subagent 证实 apk 里 0x10c000e 解析正确(type 0x0c=interpolator,XML 在)。**真因:DecorView 用 DecorContext→applicationContext 的 Resources,其 AssetManager 缺 framework-res**(所以 interpolator + 后续 getInteger/getBoolean/getColor 都 fail)。修法:让 DecorView 用探针的 framework-res-loaded Resources(udc=false 方向对,但 udc 反射编辑本身让窗口构造停 mainNew;getAnimation override 触发 vtable-bug 致 RC:1 —— 两个绕过都不稳,需换法:让 WL_APP_CTX/DecorContext 的 AssetManager 带 framework-res)。
- **★板子环境非确定性(确认)**:同一 libart+probe 跨 run 到达点不同(mainNew=OK / preOC=1 / actOnCreate:interpolator 都出现过),窗口构造(wlAlloc PhoneWindow 加载数百类)在 6+ 次重启+几十次 run 后的冷/退化板子上慢且不稳。需要真正干净的板子状态(fresh boot 充分 settle)才能稳定复现。probe dex 已回干净基线(189648 bytes,无 udc/getAnimation/watchdog)。
- **★看门狗教训**:probe 里起 `new Thread().start()` 会让 onCreate 处进程干净退出(此运行时线程支持问题),别在 probe 用原生线程抓栈。

## 突破:板子 settle 跑法 + getAnimation 是对的拦截点(2026-07-07)
- **板子跑法解决**:重启后 `sleep 75`(settle OHOS 服务/缓存)再跑 → 健康稳定到 actOnCreate(无 RC:1/崩溃/mainNew 停)。命令:`hdc shell "sleep 75; sh run-utd-long.sh 2>&1|tail"` run_in_background。之前的非确定性是没 settle 的冷启动。
- **getAnimation override 确认正确**:栈显示 `WlResources.getAnimation` 在 DecorView loadInterpolator 路径上(DecorContext.getResources()=activity context 的 WlResources,**不需要 udc 反射** —— udc 才是之前 RC:1/mainNew 停的祸首,已弃)。但 **getIdentifier("...","interpolator","android") 运行时返 0**(native 名字索引没填充)→ 兜底失败仍 NotFound。
- **下一步**:getAnimation 兜底改用 **XmlBlock**(从 framework-res 的 linear.xml 编译字节 `new XmlBlock(bytes).newParser()` 建 LinearInterpolator parser,不依赖 getIdentifier)。subagent 正设计。之后 DecorView 后续 getInteger/getBoolean/getColor 已被 WlResources 容错,真墙在 generateLayout 的 obtainStyledAttributes(Window)+inflate(screen_simple)(另一 subagent 正设计 stub)。

## interpolator 修法定案(2026-07-07):openXmlBlockAsset(非 XmlBlock byte[] ctor)
- **`XmlBlock.nativeCreate(byte[],int,int)`(block 构造 native)在 libandroidfw 里未注册** → `new XmlBlock(byte[]).newParser()` 走 nativeCreate → ULE → 失败。
- **但 `XmlBlock_nativeCreateParseState/nativeNext/nativeGetName/...` 都注册了**,且 **`AssetManager.openXmlBlockAsset(int cookie, String path)` 用这些已注册 native 从加载的 asset 读**(探针 1236-1241 已有工作例证:`openXBA.invoke(am, cookie, xmlPath) → xblock.newParser(0) → XmlResourceParser`)。
- **WlResources.getAnimation 兜底改用**:`getAssets().openXmlBlockAsset(ck, "res/interpolator/linear.xml")` 迭代 cookie 0..8,拿到 `<linearInterpolator/>` parser → createInterpolatorFromXml → `new LinearInterpolator()`(subagent A 证实无参、不碰 Resources/TypedArray)。诊断写 wl-xmlblock.txt(OK cookie=N / FAIL:原因)。
- **★还需验证**:板子非确定性在 DecorView 构造(onCreate 有时干净退出在 :298 前,有时到 actOnCreate=FAIL:interpolator)。reboot+80s settle 后首跑 ~30-50% 能到 DecorView:298。需抓一次到 :298 的 run 看 wl-xmlblock 是 OK 还是 FAIL。libart=5b517bd8(SIGSEGV 修复),probe 带 openXmlBlockAsset getAnimation + XmlBlock 诊断。

## ★根治:framework-res 没加进 uamShared(2026-07-07)+ generateLayout 设计
**真根因**:`uamShared`(DecorView 用的 AssetManager,probe 1516 行)**只 addAssetPath 了 app apk(0x7f),没加 framework-res(0x01)!** 所以所有 framework 资源(interpolator 0x10c000e、Window styleable、screen_simple 0x1090133)NotFound。诊断:openXmlBlockAsset 找 res/interpolator/linear.xml 报 FileNotFoundException(所有 cookie)。
**修法**:probe 1516 行后加 `addAssetPath(uam, ".../framework/framework-res.apk")` → `fwck=2`(成功,cookie=2)。这一下 interpolator + DecorView 后续 getInteger/getBoolean/getColor + generateLayout 的 obtainStyledAttributes(Window) + inflate(screen_simple) 可能全原生解析。getAnimation openXmlBlockAsset 兜底保留(framework-res 加载后应能找到文件)。
**generateLayout 墙设计(subagent,备用)**:PhoneWindow Unsafe.allocateInstance 后必设的 final 字段(用 **Unsafe.putObject,Field.set 对 final 会抛**):mContext=ProbeContext、mWindowAttributes=new WindowManager.LayoutParams()、mMinWidthMajor/Minor=new TypedValue()、mLayoutInflater=LayoutInflater.from(probe)、mDecor=decorView。**避开 native obtainStyledAttributes**:注入零化 TypedArray 到 `Window.mWindowStyle`(getWindowStyle 返回它不走 native)——`TypedArray.obtain(res,71)` 造 71-entry 全零(所有 attr TYPE_NULL→default)。**选 screen_simple**:seed `mFeatures=mLocalFeatures=0x2`(FEATURE_NO_TITLE)。危险点:idx50 cutout(hasValue 必 false 否则抛)、idx19/20 minWidth(写 final TypedValue)、idx16 actionModeOverlay/idx3 noTitle 影响选层。**再下墙**:inflate(screen_simple)需 WlResources.getLayout(0x1090133)返回 parser + 每 view 的 tolerant obtainStyledAttributes(AttributeSet) + @android:id/content(0x1020002)可解析。若 framework-res 加载后 native 路径能用,这些设计可能都不需要。

## interpolator/资源墙真根因(2026-07-07 深挖):OHOS 原生 AssetManager 不功能性解析
加了 framework-res 到 uamShared(fwck=2,cookie=2)仍不解析 0x10c000e。**更深诊断**:`res=[]`(连 app 0x7f 资源用 nativeGetResourceName 都枚举不出)+ openXmlBlockAsset(2,"res/interpolator/linear.xml") 报 FileNotFoundException。→ **probe 用 sentinel AssetManager(AssetManager(boolean) + mApkAssets 设空数组反射,1508-1515,故意避开未注册的 nativeIsUpToDate)+ addAssetPath 只返回 cookie,但原生资源表/asset 文件访问没真正建起来**。这是深层资源子系统问题(和 [[westlake-dayu600-two-fronts-converge]] resource front 同源)。app 能到 DecorView 是因为 DecorView:272(Paint)前没解析过任何资源,interpolator(:298)是第一个资源解析就撞这墙。
**修法方向(深层,下次)**:①让 sentinel AssetManager 真正加载 assets(可能要注册 nativeIsUpToDate 或用别的 AssetManager 构造);②或在 ART/adapter 层修 nativeGetResourceValue/openXmlAsset 对 addAssetPath 的 apk 生效;③getLayout 测试(probe 1239)用 `tval.assetCookie` 从已解析资源拿 cookie 时 openXmlBlockAsset 是 work 的 —— 说明当资源表建起来后能用,问题在资源表没建。这是 Agent-A/C 资源域 + ART native。

## interpolator/资源墙:6 角度攻坚后的精确根因(2026-07-07)
证据链(全部实测):`SYSRES[getSystem.getString(ok)=OK]`(framework arsc 解析✅)+ `viewTags=[FitWindowsLinearLayout...]`(app apk addAssetPath + openXmlBlockAsset 开文件✅)。但 interpolator 0x10c000e 6 种兜底全失败:
1. addAssetPath(framework-res) → nativeGetResourceValue 不解析(NotFound)—— **framework-res 是 package 0x01=system,addAssetPath 给动态 package id,0x10c000e(0x01)不匹配**。
2. loadFromPath(PROPERTY_SYSTEM)+setApkAssets → arsc 解析✅(getString ok)但 **asset 文件打不开**(openXmlBlockAsset FileNotFound)——loadFromPath 只建 arsc 不建可开文件索引。
3. getSystem().getAnimation → 同 loadFromPath,FileNotFound。
4. openXmlBlockAsset(cookie,"res/interpolator/linear.xml") → FileNotFound(所有 cookie)。
5. XmlBlock(byte[132]) → **nativeCreate(byte[] block 构造)未注册**(libandroidfw 只有 nativeCreateParseState/nativeNext/...)。
6. getLayout 复刻(新 sentinel+addAssetPath(fw)+nativeGetResourceValue+openXmlBlockAsset)→ NotFound(同 1)。
**精确根因**:framework-res(remapped-99pct)在 OHOS 原生 AssetManager 上,loadFromPath 能建 arsc(0x01 string/value 解析)但**建不了可 openXmlBlockAsset 的 asset 文件索引**;addAssetPath 能建可开文件索引但**当作动态 package(非 0x01)**。两条路各缺一半。**下次修法**:①ART/adapter native 层修 loadFromPath 建文件索引 或 addAssetPath 保持 0x01;②或用注册 nativeCreate(byte[]) 让 XmlBlock(132字节)直接建 parser(我有字节,subagent A 验证过 createInterpolatorFromXml 对 linearInterpolator 无参无依赖);③或把 132 字节写进一个最小 app-style apk 再 addAssetPath 开。probe getAnimation 现停在 getLayout 复刻版(WL_FW_AM)。libart=5b517bd8(SIGSEGV fix)。

## 资源墙第7角度 + 系统性根因锁定(2026-07-07 收尾)
第7角:addAssetPath(framework-res) 上**直接 openXmlBlockAsset by path 也 FileNotFound**。对比:addAssetPath(app test.apk) 能开文件(layoutTest viewTags✅)。→ **framework-res-remapped-99pct.apk 本身在 OHOS 原生 AssetManager 上加载不了文件索引**(loadFromPath 建 arsc/string 解析✅但文件索引✗;addAssetPath 文件索引✗且动态 package)。app apk(test.apk)两者都✅。系统性阻塞:framework 的所有 XML 文件资源(interpolator 0x10c000e/f、screen_simple layout 0x1090133、其依赖)全开不了 —— 即使修 interpolator 单个,inflate(screen_simple)墙一样过不了。
**下次系统性修法**:①**重打包 framework-res** 用和 test.apk 相同的 zip 结构/工具(remapped 版可能被重打包成 OHOS 读不了的结构 —— test.apk 能读)——最可能一次性解决所有 framework 文件;②或 ART/adapter native 层修 loadFromPath 建文件索引;③或 aapt 造含所有需要的 framework XML 的 app-style apk。**关键对比数据**:test.apk(addAssetPath 文件✅)vs framework-res-remapped-99pct.apk(两路文件✗但 loadFromPath arsc✅)。probe getAnimation 停在 WL_FW_AM openByPath 版。这是 Agent-A framework-res 打包域。本轮硬成果=SIGSEGV 崩溃修复(libart=5b517bd8)。

## 第8角度(重打包)也失败 —— 资源墙确认为深层 native 硬墙(2026-07-07)
标准 zip 重打包 framework-res(resources.arsc STORED 首位,fwres-rezip.apk)后 addAssetPath+openXmlBlockAsset **仍 FileNotFound** → **不是 zip 结构问题**。addAssetPath 对 test.apk 建文件索引✅但对 framework-res(原版/重打包都✗)不建。8 个角度全失败 = **OHOS 原生 AssetManager 对 framework-res.apk 的加载是深层 native 硬墙**,超出 Java 层能力。**下次必须 native 层**:①对比 test.apk vs framework-res 在 nativeAddAssetPath/ApkAssets.nativeLoad 的行为差异(为何一个建文件索引一个不建)——可能 framework-res 触发某未注册/stub native;②或修 adapter 的 asset 文件打开 native;③或换一个 OHOS 能读的 framework-res(不同来源/aapt 版本重新编译,不是重打包)。**关键实验数据全在记忆**。本轮硬成果=SIGSEGV 崩溃修复(libart 5b517bd8,onCreate 无崩溃到资源层)。probe getAnimation=WL_FW_AM openByPath(fwres-rezip)。

## carrier apk 方案(2026-07-07,已部署待验证)
最有希望的资源修法:**用 test.apk(OHOS 能加载文件索引的结构)当载体,把 framework 的 res/interpolator/linear.xml + fast_out_linear_in.xml 加进去** = `fwcarrier.apk`(已部署 /data/local/tmp/westlake-dayu600-substrate/android/framework/fwcarrier.apk,15MB)。probe getAnimation 的 WL_FW_AM 指向它,addAssetPath + openXmlBlockAsset("res/interpolator/linear.xml") 按路径开文件(不需资源 ID 解析,openXmlBlockAsset 只读 zip 文件)。**逻辑高置信**(test.apk 结构 addAssetPath 建文件索引✅=layoutTest 证明),但**被 board 非确定性(window 构造 wlAlloc PhoneWindow ~50-70% 停 mainNew,invoke-vtable bug)挡住,没抓到一次到 DecorView:298 的 run 验证**。**下次**:①多重试 reboot+settle+run 直到到 DecorView:298 看 wl-xmlblock(OK openByPath=成功);②成功后同法把 screen_simple.xml + 依赖加进 carrier 解 inflate 墙;③或先修 board 非确定性(window 构造)。scratchpad: carrier/(源),fwcarrier.apk。本轮硬成果=SIGSEGV 崩溃修复。

## ★决定性 native 隔离(2026-07-07):framework-res 文件索引 native 墙
第10角度诊断(a16-emulator 真 Android framework-res):`addAssetPath 返回 cookie=1(成功)`但 `openXmlBlockAsset(cookie, "res/anim/linear_interpolator.xml")`(一个已知在 apk 里的文件)= `animFAIL:FileNotFoundException`。→ **addAssetPath 加载了 apk 但 OHOS 原生 AssetManager 建不了可 openXmlBlockAsset 的文件索引**。对比:test.apk 的文件能开(layoutTest,用 nativeGetResourceValue 解析出的 assetCookie,不是 addAssetPath 返回的 cookie)。**精确 native 根因**:openXmlBlockAsset 需要"解析出的 assetCookie"(nativeGetResourceValue 返回的),test.apk app 资源能解析→有 assetCookie→能开文件;framework-res 用 addAssetPath 加载成动态 package→0x10c000e(0x01)不解析→拿不到 assetCookie;用 loadFromPath 加载成 0x01→arsc 解析但**文件索引 native 没建**。两条路各缺一半,openXmlBlockAsset 拿不到"能开文件的 assetCookie"。**下次 native 修法**:①修 loadFromPath 的 native 让它建文件索引(和 addAssetPath 一样);②或让 addAssetPath 保持 framework-res 的 0x01 package(不转动态)这样能解析拿 assetCookie;③或注册 XmlBlock.nativeCreate(byte[]) 直接用 132 字节建 parser(唯一不依赖文件索引的路)。**这是 libandroidfw/ART native 域,Java 层 10 个角度已证明不可行。** 本轮硬成果=SIGSEGV 崩溃修复(libart 5b517bd8)。

## 🎉🎉 interpolator 墙攻破(2026-07-07):纯 Java 合成 XmlResourceParser
攻了 11 个角度后,**纯 Java 路攻破**:自实现 `WlInterpolatorParser implements XmlResourceParser`,吐单个 `<linearInterpolator/>` 事件流(next()→START_TAG,getName()="linearInterpolator",无属性)。WlResources.getAnimation 兜底直接 `return new WlInterpolatorParser()` → AnimationUtils.createInterpolatorFromXml → `new LinearInterpolator()`(subagent A 证实无参无依赖)。**绕过整个 native 资源/文件墙,确定性。** 验证:`XB=OK synthLinear id=0x10c000f`(过了 0x10c000e@DecorView:298 AND 0x10c000f@:300 两个 interpolator)→ onCreate 深入 DecorView 构造/generateLayout。**关键教训:native 资源文件开不了时,对只需 tag 名的资源(interpolator),自造 XmlResourceParser 比修 native 快。** libart=5b517bd8。下一墙=generateLayout(obtainStyledAttributes(Window)+inflate(screen_simple))——subagent 已给 mWindowStyle 注入设计。

## 🎉 Window/generateLayout 墙攻破(2026-07-07):onCreate 冲到 inflate(screen_simple)
Window subagent 的设计生效(`winfix=OK`):PhoneWindow Unsafe.allocateInstance 后用 **Unsafe.putObject** 设 final 字段(mContext/mWindowAttributes/mMinWidthMajor/Minor/mLayoutInflater=getSystemService("layout_inflater"))+ **注入零化 TypedArray 到 Window.mWindowStyle**(TypedArray.obtain(res,71),getWindowStyle 返回它不走 native obtainStyledAttributes)+ **seed mFeatures=mLocalFeatures=2(FEATURE_NO_TITLE)** → generateLayout 选 screen_simple。辅助:wlUnsafeInit/wlSetF/wlSetI(Unsafe putObject/putInt,绕过 final 检查)。
→ onCreate 过了:两个 interpolator + obtainStyledAttributes(Window) + generateLayout,现在 **AC=NotFoundException:0x1090133 @getLayout @LayoutInflater.inflate @DecorView.onResourcesLoaded:2174 @generateLayout:2868**。**下一墙=getLayout(R.layout.screen_simple=0x1090133)**——同 native 文件墙,但比 interpolator 复杂(inflate 读属性)。screen_simple 结构=FrameLayout > ViewStub(action_mode_bar_stub) + FrameLayout(@android:id/content=0x1020002)。**修法:WlResources.getLayout 返回合成 layout parser(至少一个 FrameLayout id=0x1020002 match_parent)+ 可能需 tolerant obtainStyledAttributes(AttributeSet) 让 View 构造读到 id=0x1020002**。generateLayout 最后 findViewById(0x1020002)→contentParent,找不到就 RuntimeException。libart=5b517bd8,probe 带 synthLinear+winfix。

## screen_simple inflate 子墙(2026-07-07):LayoutInflater 需真 XmlBlock.Parser
WlResources.getLayout(0x1090133) 返回合成 WlScreenParser(FrameLayout id=0x1020002)→ **`InflateException:WlScreenParser cannot be cast to android.content.res.XmlBlock$Parser`**。→ **LayoutInflater.inflate 强转 parser 为 XmlBlock$Parser(拿 native XmlBlock 指针做属性解析),合成 parser 不行**(interpolator 的 createInterpolatorFromXml 不 cast,所以 synth 能用;layout 的 inflate 要 cast)。**修法**:①造真 XmlBlock.Parser —— 但 framework-res XML 文件开不了(native 墙)+ nativeCreate(byte[])未注册,死路;②**注册 XmlBlock.nativeCreate(byte[]) 在 ART/libandroidfw**(nativeOpenXmlAsset 内部已有"bytes→ResXMLTree"逻辑,只是 JNI wrapper 没注册)——最有希望;③或让 getLayout 用 openXmlBlockAsset 从 test.apk 开一个有 @android:id/content 的 layout(但 test.apk 无此 layout);④或 hook DecorView.onResourcesLoaded 跳过 inflate 手动建 content FrameLayout。**进展巨大:interpolator + Window/generateLayout 两大墙已过,onCreate 冲到 setContentView 的 layout inflate。** libart=5b517bd8。

## layout inflate 墙的接续路径(2026-07-07,具体)
需真 XmlBlock.Parser(含 @android:id/content=0x1020002)。**test.apk 的文件能 openXmlBlockAsset(layoutTest 证明),framework-res 不能。** AppCompat 的 `abc_screen_content_include.xml`(或 abc_screen_simple)在 test.apk 里,含 ContentFrameLayout id=@android:id/content。**getLayout(0x1090133) 实现**:①用 uamShared 的 `nativeGetResourceValue(amPtr, <abc_screen_content_include 的 0x7f layout id>, tval)` 拿 tval.string(path)+tval.assetCookie;②`openXmlBlockAsset(uamShared, tval.assetCookie, path)` → 真 XmlBlock.Parser;③返回它。找 layoutId:enum nativeGetResourceName 0x7f 的 layout type,或 getIdentifier("abc_screen_content_include","layout","com.uptodown")。风险:inflate 需 ContentFrameLayout/AppCompat 类(uloader 能加载)+ obtainStyledAttributes(styleable)(真 parser 能 cast,但 framework styleable 要解析——WlResources 可容错)。若 ContentFrameLayout 构造读 framework styleable 失败,再加 tolerant obtainStyledAttributes。**当前 probe**:getLayout 返回合成 WlScreenParser(cast 失败),需换成上述真 XmlBlock.Parser 法。合成 parser 类(WlInterpolatorParser=interpolator 用,已成功;WlScreenParser=layout 用,cast 失败弃)都在 probe 里。

## getLayout sub-wall(2026-07-07):getIdentifier 找不到 AppCompat content layout
真 XmlBlock.Parser 法实现了(getLayout 用 getIdentifier 找 abc_screen_content_include/abc_screen_simple → nativeGetResourceValue → openXmlBlockAsset),但 **`LAY=FAIL no-lid`**:`getIdentifier("abc_screen_content_include"/"abc_screen_simple","layout","com.uptodown")` 都返 0。layoutTest(2048/com.digiplex.game)能找到,uptodown 找不到 —— 可能 ①uptodown 的 arsc 包名≠"com.uptodown";②AppCompat layout 名不同;③getIdentifier 包名解析问题。**下次修法**:枚举 app 0x7f 的 layout-type 资源(nativeGetResourceName 遍历)找名字含 "content"/"screen_simple"/"screen_content" 的 → 拿 ID;或先 log 出 app 的实际包名 + layout 资源名列表。**注意 content id**:需要 layout 含 @android:id/content(0x1020002)——AppCompat 的 ContentFrameLayout 应该是这个 id,但要验证(否则 findViewById(0x1020002) 仍 fail,得找真有这个 id 的 layout)。**getLayout 确认到达(过了 winfix+两 interpolator+obtainStyledAttributes(Window)),这是 setContentView 的最后一步。** 备选:注册 XmlBlock.nativeCreate(byte[]) 用自造 FrameLayout AXML。probe getLayout=真 XmlBlock.Parser 法(WL_APP_CONTENT_LAYOUT 缓存),libart=5b517bd8。

## getLayout WL_APP_AM 法 + 潜在下层问题(2026-07-07)
uamShared 枚举不出 app layout(res=[]),故 getLayout 改用 **WL_APP_AM = fresh sentinel + addAssetPath(test.apk)**(像能解析的 asset-probe `am`)枚举 0x7f layout(名含 content/screen)+ nativeGetResourceValue + openXmlBlockAsset → 真 XmlBlock.Parser。**潜在下层问题**:①inflate 用 DecorView 的 context(uamShared)做 obtainStyledAttributes,若 app layout 引用 app 属性(@id/action_bar_activity_content)而 uamShared 不解析 app 资源→inflate 仍失败;②app layout 的 content id 可能是 @id/action_bar_activity_content 而非 @android:id/content(0x1020002)→findViewById(0x1020002) fail。**若这两个成立,正解是造 bare FrameLayout(仅 @android:id/content 这个 framework 属性)的真 XmlBlock.Parser**——需注册 nativeCreate(byte[]) 或从可加载 apk openXmlBlockAsset 提取的 framework screen_simple bytes。**board vtable 非确定性(mainNew 停 ~50%+)严重拖慢验证**,故用 multicycle.sh 多轮 reboot 抓过窗口的 run。

## ★★ 关键洞察(2026-07-07):XmlBlock.nativeCreate([B)J 其实已注册!
之前误判"nativeCreate 未注册"。源码 `/Users/yao/westlake-local-build/aosp-fb-androidfw/core/jni/android_util_XmlBlock.cpp` line 348 gXmlBlockMethods 表含 `{"nativeCreate","([BII)J",...}`,`register_android_content_XmlBlock`(line 397)用 RegisterMethodsOrDie 注册**整表**。nativeCreateParseState/nativeNext(同表)确定已注册(openXmlBlockAsset 能用)→ **nativeCreate 也已注册**。nativeCreate 实现:`new ResXMLTree(); setTo(bytes); return ptr`(纯字节→ResXMLTree,不依赖文件索引)。
**→ 最稳健的 layout 解**:提取 uamShared 加载的 framework-res.apk(/data/.../android/framework/framework-res.apk)里的 `res/layout/screen_simple.xml`(956字节编译AXML,含 FrameLayout/ViewStub/@android:id/content)→ 部署为 screen_simple.axml → getLayout 读字节 → `new XmlBlock(byte[])`(反射,ctor 调 nativeCreate)→ newParser(id) → **真 XmlBlock$Parser**(inflater 接受,纯 framework 属性 uamShared 能解析)。probe getLayout=WL_SCREEN_AXML nativeCreate 法。app-layout 法(WL_APP_AM 枚举)已弃(uptodown name index 没建,ENUM 空)。用 multicycle.sh 抓过窗口 run。

## ★★★ Agent-A 的 823e3c03 = 资源墙总解(2026-07-07,已集成大板)
Agent-A 找到 openXmlBlockAsset FileNotFound 的根因:**apktool 重打包的 framework-res 用了 data descriptor(local header general-purpose bit3),OHOS libziparchive 读不了(local header 里 size=0)→ 建不起文件索引**。验证坐实:当前部署的 remapped-99pct **2102/8361 条目有 data descriptor**;Agent-A 的 Info-ZIP rezip `framework-res-remapped-99pct-rezip.apk`(md5=**823e3c03**80a5a166df13a2ea28bfddd3)**0/8361**(同 test.apk 结构)。**已部署 823e3c03 到 `/data/local/tmp/westlake-dayu600-substrate/android/framework/framework-res.apk`(旧的备份为 framework-res.remapped-dd.apk.bak)**。→ 换上后 openByPath(framework XML)应该通,可能连 uamShared 的 layout 解析(getValue 0x1090133 之前 NotFound)都修好(文件索引是解析的前提)。
**我的 nativeCreate 路与之互补且保留**:getLayout 先 try super.getLayout(823e3c03 下可能直接通),失败回退 `new XmlBlock(screen_simple.axml bytes)`(nativeCreate 已注册,不依赖 zip 结构)。screen_simple.axml=从 framework-res 提取的 956B 编译 AXML(type=0x0003 合法,含 @android:id/content=0x1020002),部署在 android/framework/screen_simple.axml。
**接续**:multicycle.sh 抓过 mainNew(vtable ~50% 停)的 run 验证 screen_simple inflate 是否过 → findViewById(0x1020002) → setContentView(app R.layout.main from test.apk) → DecorView 有内容 → 接 Agent-C WestlakeUpscreen.show(decor,1200,1920)。Agent-A screen_simple 最小 View 版(程序化建 content FrameLayout id=0x1020002)是兜底(AGENT-COORD.md 1095段)。libart=5b517bd8。

## 更正(2026-07-07):nativeCreate 部署运行时实测 UnsatisfiedLinkError(源码注册 ≠ 部署注册)
`new XmlBlock(byte[])` → `UnsatisfiedLinkError: No implementation found for XmlBlock.nativeCreate(byte[],int,int) (tried Java_..._nativeCreate...)`。**部署的 libandroidfw 没注册 nativeCreate**(源码 gXmlBlockMethods 有,但部署的 lib 是不同 build 或没跑 register_android_content_XmlBlock 那条)。**我第一次判断"nativeCreate 未注册"是对的**,中间基于源码的"已注册"推断错了。**教训:符号是否注册要在设备实测,别信源码。** → nativeCreate 路死。**改走 Agent-A 的 openByPath 路**(823e3c03 已部署,data descriptor 去掉→文件索引能建→openXmlBlockAsset/nativeOpenXmlAsset[已注册] by path 能开)。getLayout 现=fresh addAssetPath(823e3c03)+ openXmlBlockAsset(ck 0-4, "res/layout/screen_simple.xml") by path。注:uamShared super.getLayout(0x1090133) 仍 NotFound(remapped arsc 缺 0x1090133 的 resolution,与文件打开分离),所以走 by-path 不走 by-id。libart=5b517bd8。

## ★★★ 补上缺失的 XmlBlock.nativeCreate:自建 helper .so(2026-07-07)
诊断链:①openByPath 换 823e3c03 仍 FileNotFound(data descriptor 不是根因;framework-res 走 addAssetPath 变动态 package,拿不到可 openXmlBlockAsset 的 cookie)②部署的 libandroidfw **导出了 XmlBlock 全部 natives 为 `Java_*` 符号(dlsym 自动链),独缺 `Java_android_content_res_XmlBlock_nativeCreate`**(strings 确认)③但 **ResXMLTree 的 ctor+setTo 符号导出了**:`_ZN7android10ResXMLTreeC1Ev` + `_ZN7android10ResXMLTree5setToEPKvmb`。
**解**:写 `wl_xmlblock_create.cpp`(scratchpad)→ `libwl_xmlblock_create.so`(14616B,OHOS clang++ `--target=aarch64-linux-ohos`,-I aosp-android-11 jni.h):`Java_..._XmlBlock_nativeCreate` = dlsym ResXMLTree ctor+setTo → malloc(8192)+ctor+setTo(bytes)→ 返回 ptr;**JNI_OnLoad 里 RegisterNatives(XmlBlock,nativeCreate)** 绕过 ART classloader 限制(XmlBlock=boot 类,.so=app load)。部署 `android/lib64/libwl_xmlblock_create.so`。getLayout:`System.load(该so)`→`new XmlBlock(screen_simple.axml bytes)`→newParser→真 XmlBlock$Parser。**这是补 libandroidfw 缺失 native 的通用模式(dlsym 已导出的 C++ 实现 + RegisterNatives 桩)。** 编译器:`/Users/yao/a2oh-source-audit/ohos-sdk/native/llvm/bin/clang++`,sysroot 同。multicycle 验证中。libart=5b517bd8。

## ★★★★ 重大突破链(2026-07-07)+ 部署路径致命 bug
**最关键发现:运行时加载的 libart 是 `$S/art/libwestlake_art.so`(LD_LIBRARY_PATH 里 $S/art 排最前 + dlopen probe 明确 `dlopen /art/libwestlake_art.so ok`),不是我一直部署的 `$S/android/lib64/libart.so`!** 我近期所有 libart 改动(interpreter/java_vm_ext)之前全部部署到错文件、根本没生效。**libart 改动必须部署到 `$S/art/libwestlake_art.so`。** ART 进程 comm=main,由 `run-utd-long.sh` 的 `timeout 400 /system/bin/toybox true` + LD_PRELOAD `libwestlake_embedded_art_dlopen_probe.so` dlopen 起来。
**XmlBlock native 层全解剖**:①`nativeCreate` libandroidfw 没导出→我写 helper `.so`(libwl_xmlblock_create,malloc+dlsym'd ResXMLTree ctor+setTo,setTo err=0 树正确)。②`nativeNext`/`nativeGet*` 等 @CriticalNative parser natives:libandroidfw **导出了** Java_ 符号,但 `IsCriticalNative()` 在本运行时返 **false**,且 `FindCodeForNativeMethod` classloader-scoped 找不到 libandroidfw→**java_vm_ext.cc FindCodeForNativeMethod 加 dlsym libandroidfw 兜底**(libart 上下文能 dlopen libandroidfw)。③**interpreter.cc @CriticalNative 分发白名单加 XmlBlock 且强制 raw(即使 IsCriticalNative false),但排除 nativeGetStringBlock(它是普通 native 有 env)**。④原本 XmlBlock.nativeNext 走普通 "IJ" 分发 `fn(JNIEnv,jclass,long)`→真 @CriticalNative 函数把 env 当 token 读→垃圾→END。
**已证明:native ResXMLParser 直接遍历树 = [256,258,258,259](START_NS/START_EL/START_EL/END_EL)完全正确。** 残留:Java 侧 nativeCreateParseState 建的 parse state 或 @CriticalNative token 传递仍有问题(Java next() 返回 ≠native 的事件,getName() SIGSEGV)。**下次:查 nativeCreateParseState 的 "JJI" 分发 args / parse state 指针是否正确传到 Java nativeNext。** libart=build-ohos-arm64/lib(需部署到 libwestlake_art.so)。helper 在 scratchpad/wl_xmlblock_create.cpp。

## ★ 残留精确定位(2026-07-07):XmlBlock native 逻辑 100% 对,bug 在解释器分发
决定性证据:helper 的 nativeCreate 里 native 直接建 ResXMLParser **完整镜像 nativeCreateParseState 逻辑(ctor `_ZN...ResXMLParserC1ERKNS_10ResXMLTreeE` → setSourceResourceId `_ZN...19setSourceResourceIdEj`(0x1090133) → restart → next)= `[256,258,258,259]`(START_NS/START_EL/START_EL/END_EL)完全正确**。→ **树+native ResXMLParser+setTo(err=0)全部 100% 正确**。
**残留 = 解释器 InterpreterJni 对 XmlBlock 三个 native 的分发之一给出坏 parse state**:①nativeCreate "JLII"(byte[],I,I→J,line 827)②nativeCreateParseState "JJI"(J,I→J,line 2104,`fn(env,clazz,tree,resId)`)③nativeNext 强制 raw "IJ"(line 683,`fn(token)`)。Java 路径:new XmlBlock→nativeCreate→mNative(tree);newParser→nativeCreateParseState→mParseState;next()→nativeNext(mParseState)→**返回错误事件(≠native 的),getName() hang/SIGSEGV**。**最可能:long 返回值(mNative 或 mParseState 指针 ~0x7f...)在解释器 SetJ/结果捕获里被 32 位截断 → nativeCreateParseState 拿到坏 tree 或 nativeNext 拿到坏 token**。**下次直接查:probe 反射读 XmlBlock.mNative / Parser.mParseState 的值,对比 helper nativeCreate 日志的 osb ptr(=0x7fa7a3d5c0),看是否截断。若截断→修解释器 JLII/JJI 的 SetJ 或 long 参数读取。** wrapper 诊断被 2nd-libart-copy(namespace 隔离)挡住,没用。

## ★ 指针 round-trip 排除截断(2026-07-07):残留=nativeNext 用有效 token 仍返回错事件
反射读:`mNative=0x7fbc5b44c0` **= helper 的 osb=0x7fbc5b44c0 完全一致(没截断!)**;`mParseState=0x7fbc4acac0`(有效指针)。→ **nativeCreate 返回 + 解释器 long 返回处理 + nativeCreateParseState 建 parse state 全部正确**。残留:`nativeNext(0x7fbc4acac0 有效 token)` 经解释器强制 raw 分发仍返回错误事件(≠native 直接 walk 的 [256,258,258,259]),Parser.next() hang/getName SIGSEGV。**排除了:截断、树、native 逻辑、指针传递。** 剩下极窄:①nativeNext 的 jni_entry 可能不是真 nativeNext(resolution 解析到别的);②或 Parser.next() 内部调的别的 native(如 doNext 里的 nativeGetName/nativeGetNamespace)被误分发;③或 @CriticalNative raw 调用约定在本运行时对 XmlBlock 有别的坑。**下次:在 java_vm_ext dlsym 兜底里对 nativeNext 加日志打印解析到的地址,对比 nm -D libandroidfw 的 Java_..._nativeNext 偏移(0x2ad72c),确认 jni_entry 是真 nativeNext。** wrapper 诊断被 2nd-libart-copy 挡住。libart 部署路径=$S/art/libwestlake_art.so(不是 android/lib64)。

## 最终状态(2026-07-07):两种 ABI 都 hang,残留极深
撤销 forced-raw 回普通 "IJ"(env,clazz,token)后 nativeNext 仍 hang;forced-raw(token)也 hang/错。**两种 @CriticalNative ABI 都不行**,但:mNative=osb(对)、mParseState=0x7fbc4acac0(有效)、树 setTo err=0、native 直接 ResXMLParser::next() walk=[256,258,258,259] 全对。→ 残留不是 ABI、不是指针、不是树。**极窄可能**:①nativeNext 的 jni_entry 解析到的不是真 nativeNext(java_vm_ext dlsym 兜底 vs libraries_ 竞争);②XmlBlock.Parser.next()(Java)内部循环调 nativeGetName/nativeGetNamespace,其中一个误分发导致死循环;③@CriticalNative 在本 C++ 解释器有第三种未知调用坑。**需要重启单步/在真 nativeNext 加日志(被 2nd-libart-copy namespace 挡住)才能继续。** **本轮净成果:发现致命部署路径 bug($S/art/libwestlake_art.so)、XmlBlock native 层从 0 推进到 95%(树+native+指针全验证正确)、残留精确隔离到 nativeNext 的 Java 解释器分发一个点。**

## ★★★★★ 决定性:nativeNext 是普通 JNI 且正确工作;残留=解释器建的 parse state (2026-07-07)
**关键实验(helper native 里两种 ABI 都试)**:对 native 直接建的有效 parse state(pctor+setSourceResourceId+restart)调真 `Java_..._nativeNext`:**`jRaw(jint(jlong))=[1,1,1] 错;jNorm(jint(JNIEnv*,jclass,jlong))=[2,2,3] 正确!`**(START_TAG/START_TAG/END_TAG,正是 screen_simple 的 LinearLayout/ViewStub)。→ **nativeNext 是普通 JNI(有 env),不是 @CriticalNative;IsCriticalNative()=false 是对的;普通 "IJ" 分发(env,clazz,token)才对。** 我之前强制 raw 是错的,已撤销。
**残留精确点**:自检 next0=0(START_DOCUMENT,Parser.next 的 mStarted 路径),**next1=nativeNext(mParseState)HANG**。但同一 osb 树,native 建的 ps3 的 nativeNext=[2,2,3] 工作,Java 的 mParseState(解释器 nativeCreateParseState 建)的 nativeNext hang。改 "IJ"/"JJI" 用 jni_entry 也没用。**→ 解释器的 nativeCreateParseState "JJI" 分发创建的 parse state 行为与 native 建的不同(subtly bad)。** mNative=osb 对、mParseState 是有效指针,但其上 nativeNext 首调即 hang。**下次:helper 里调真 Java_nativeCreateParseState(env,clazz,tree,resId)建 state 再 jNorm walk,对比解释器建的;或查 "JJI" 分发的 arg0/args[2] 是否真=osb/resid(加 native 日志打印 fn 收到的 tree/resid)。** libart 部署=$S/art/libwestlake_art.so。helper=scratchpad/wl_xmlblock_create.cpp。

## ★★★★★★ 终极定位(2026-07-07):XmlBlock native 层 100% 工作,残留=解释器 NORMAL 分发
**决定性实验(helper native 全链路)**:`jViaCps=[2,2,3]` —— 用真 `Java_nativeCreateParseState(env,clazz,osb,0x1090133)` 建 parse state(ps4)+ 真 `Java_nativeNext(env,clazz,ps4)` walk = **[2,2,3] 完全正确**(START_TAG LinearLayout / START_TAG ViewStub / END_TAG)。**→ 树 + nativeCreate + nativeCreateParseState + nativeNext 四个 native 全部 100% 工作(NORMAL JNI ABI,env,clazz,token)。**
**残留 = 解释器 InterpreterJni 的 NORMAL 静态分发("JJI" nativeCreateParseState / "IJ" nativeNext,line 2094/2102)**:同样的 fn(jni_entry=真函数)、同样的 arg(soa.Env/clazz/arg0=osb/resid),但解释器调出来的 mParseState 上 nativeNext 首调即 HANG,而 native 直调 [2,2,3]。改用 jni_entry 没用。**唯一剩下的差异 = 解释器的 `soa.Env()`/soa(ScopedObjectAccessUnchecked)状态转换,或 args 读取。下次直接查:①在 "JJI" 分发加 native 日志(需绕过 libart fopen SELinux——用 __android_log/hilog)打印 fn 收到的 env/tree/resid,对比 native jcps 的;②或试不同的 soa/env 获取方式。** **本轮净成果:发现致命部署路径 bug、XmlBlock native 层从 0→100% 验证工作、残留精确到解释器 NORMAL 分发一个点(非 native、非 ABI、非树、非指针)。** libart=$S/art/libwestlake_art.so;helper=scratchpad/wl_xmlblock_create.cpp(jViaCps/jNorm 测试可复现)。

## ★★★★★★★ 根本突破(2026-07-07):XmlBlock parser WALK 通了!根因=generic-JNI trampoline
**根因**:解释器 InterpreterJni 里,XmlBlock 的 native(nativeCreateParseState/nativeNext/nativeGetStringBlock/nativeGet*)的 `GetEntryPointFromJni()`/`jni_entry` 是 **libart 里的 generic-JNI trampoline(地址 0x7f30... 在 libart,而真 libandroidfw 函数在 0x7f22...)**,不是真函数。直接当 C 函数调 trampoline → 建出**坏的 parse state**(ResXMLParser::next 对它 hang)。**证据**:nativeCreate ret=nativeCreateParseState tree(树对)、resId=0x1090133(对),但 fn=0x7f30...(trampoline);WLRXPRE 打了但 WLRXPOST 没打(ResXMLParser::next(解释器 parse state)本身 hang)。
**根修复**(interpreter.cc 解析块 line ~625):对 XmlBlock(`DescriptorEquals("Landroid/content/res/XmlBlock;")`)**强制走 FindCodeForNativeMethod 重新解析 + RegisterNative**,使 jni_entry/GetEntryPointFromJni() 都变真 libandroidfw fn。配合 java_vm_ext.cc FindCodeForNativeMethod 顶部对 XmlBlock 直接 dlsym libandroidfw(nativeCreate 除外=null→回退我的 helper)。**libandroidfw 有全部 19 个 XmlBlock parser native,唯独没 nativeCreate(我的 helper 补)。**
**结果**:自检 `evs=[0,2]`=START_DOCUMENT+START_TAG,**nativeNext 不再 hang,parser 真的走了!** 残留:getName 抛 AIOOBE(StringBlock.get(id) 越界,可能 nativeGetName 的 id 或 StringBlock 层)。**注意反射 getLong 读 mNative/mParseState 返回错值(0x7fa5 vs 真 0x7f93),别信反射,信解释器 native arg 日志。** libart=$S/art/libwestlake_art.so。

## ★★★★★★★★ XmlBlock+StringBlock native 层完全打通(2026-07-07)tag=LinearLayout!
StringBlock 类的 natives(nativeGetSize/nativeGetString,libandroidfw 全有,偏移 0x2adf88 起)也走同样的 generic-JNI trampoline bug → StringBlock 空(mStrings length=0)→ getName 越界。**修复扩展到 StringBlock**:①java_vm_ext FindCodeForNativeMethod 对 XmlBlock+StringBlock 都 dlsym libandroidfw(符号 Java_android_content_res_<Cls>_<method>);②interpreter 解析块 wl_is_xmlblock 匹配两个类;③@CriticalNative 块的 NORMAL 分发也匹配两个类。**结果:自检 `evs=[0,2] tag=LinearLayout atc=4` —— parser walk + getName(真名)+ getAttributeCount 全通!** screen_simple 的 XmlBlock 解析层彻底打通。下一步=真实 onCreate→setContentView→getLayout(已改成 synthetic axml 绕过 openByPath)→LayoutInflater.inflate→DecorView。

## ★★★★★★★★★ 里程碑(2026-07-07):screen_simple 真实 INFLATE 了!卡点转移到资源层
XmlBlock+StringBlock native 层修复后,真实 onCreate→setContentView→installDecor→DecorView.onResourcesLoaded→LayoutInflater.inflate(screen_simple) **真的跑起来了**:LayoutInflater 读 parser、创建 LinearLayout、处理子 View 的 LayoutParams。**新卡点(InflateException line#4)**:`TypedArray.getLayoutDimension → "You must supply a layout_width attribute"`,`theme={InheritanceMap=[], Themes=[]}`(空主题)。即 obtainStyledAttributes(attrs, ViewGroup_Layout styleable)映射不出 layout_width —— **framework-res / R-styleable 匹配问题(Agent-A 的资源战线),不再是 XmlBlock 层**。窗口 setup 有 vtable lottery(~1/8 run 才到 actOnCreate)。**至此 XmlBlock 解析层从完全不通→parser walk→getName 真名→inflate 跑通,彻底解决;剩资源层。** wl-inflate.txt 存完整栈。

## ★ parser 属性读取也验证完美(2026-07-07)—— 卡点确定在 obtainStyledAttributes/资源层
自检读 root LinearLayout 属性:`orientation(0x10100c4)=1; fitsSystemWindows(0x10100dd)=true; layout_width(0x10100f4)=-1; layout_height(0x10100f5)=-1` —— **name/resId/value 全对(-1=match_parent)**。所以 XmlBlock parser 提供的 attr resource ID 完全正确(0x10100f4=layout_width)。inflate 时 `obtainStyledAttributes(attrs, ViewGroup_Layout styleable)` 却映射不出 layout_width(TypedArray TYPE_NULL)→ 这是 **AssetManager.applyStyle / framework-res styleable 层**(libandroidfw 零个 AssetManager native,applyStyle 在别处;空主题 theme={Themes=[]})。**我的域(XmlBlock/StringBlock parser 全层)已彻底完成;剩 framework-res/AssetManager 资源层 = Agent-A 战线 + 依赖 framework-res 来源(build-env blocker)。**

## ★★★ 最终 native 卡点(2026-07-07):AssetManager.nativeApplyStyle 未实现
自检直接 `getSystem().newTheme().obtainStyledAttributes(parser, {0x10100f4,0x10100f5})` → **`UnsatisfiedLinkError: No implementation found for AssetManager.nativeApplyStyle(JJIIJ[IJJ)`**。即 inflate 卡 layout_width 的真因=**nativeApplyStyle 这个 native 缺失**(libandroidfw 零个 AssetManager JNI wrapper),不是 framework-res 缺失、不是 parser(parser 完美提供 0x10100f4=-1)。newTheme 成功=nativeThemeCreate 已实现,只差 applyStyle。**libandroidfw 有 C++ `android::ApplyStyle(Theme*,ResXMLParser*,uint32 dsa,uint32 dsr,const uint32* attrs,size_t len,uint32* outValues,uint32* outIndices)` 符号 _ZN7android10ApplyStyleEPNS_5ThemeEPNS_12ResXMLParserEjjPKjmPjS6_** → 可像 XmlBlock helper 写 JNI wrapper 补。nativeApplyStyle 签名(JNI)=(long ptr,long theme,int dsa,int dsr,long xmlParser,int[] attrs,long outValues,long outIndices)→shorty "VJJIIJLJJ"(需加解释器 handler)。

## ★★★★ nativeApplyStyle 补上了(2026-07-07)OSA hasWidth=true lw=-1!
写了 JNI wrapper `Java_android_content_res_AssetManager_nativeApplyStyle`(helper wl_xmlblock_create.cpp)包 libandroidfw 的 C++ `android::ApplyStyle`;java_vm_ext FindCodeForNativeMethod 对 AssetManager.nativeApplyStyle 从 helper dlsym;interpreter 加 shorty "VJJIIJLJJ" handler(vreg:ptr[0,1]theme[2,3]dsa[4]dsr[5]xml[6,7]attrs[8]outv[9,10]outi[11,12])。**结果 obtainStyledAttributes 通:`hasWidth=true lw=-1 ic=2`(layout_width+height 都填出,-1=match_parent)。** 下一步=真实 inflate 应过 layout_width 墙。

## ★★★★★ inflate 深入到 FrameLayout(2026-07-07)—— 卡点=空主题解析 ?attr/windowContentOverlay
补 nativeApplyStyle 后 inflate 过 line#4(LinearLayout+layout_width),推进到 line#5 FrameLayout(@android:id/content)。新卡点:`View.<init>→getDrawable→UnsupportedOperationException: Failed to resolve attribute index 35: TypedValue{t=0x2/d=0x1010059}` —— t=0x2=TYPE_ATTRIBUTE=`?attr/0x1010059`(windowContentOverlay),`theme={Themes=[]}` 空主题解析不出。**即 Activity 没应用主题(不是缺 native)。下一步试 setTheme(框架主题)让主题非空;若 framework-res 加载正确则通,否则 = Agent-A framework-res 战线。** 至此 mine(XmlBlock/StringBlock parser + nativeApplyStyle)全部完成,inflate 从零推进到 view 树 line#5。

## ★★★★★★ 完整突破链总结(2026-07-07)——inflate 从零推进到主题层,根本卡点=framework-res
本轮我(Agent-B)彻底打通的层(全在 interpreter.cc + java_vm_ext.cc + helper wl_xmlblock_create.cpp):
1. **致命部署路径 bug**:libart 要部署到 $S/art/libwestlake_art.so(不是 android/lib64)。
2. **XmlBlock/StringBlock native 层**:根因=解释器里这些 native 的 jni_entry 是 libart 的 generic-JNI trampoline 不是真 libandroidfw 函数;修=解析块对这两类强制 FindCodeForNativeMethod + java_vm_ext dlsym 真 libandroidfw。→ parser walk、getName 真名(LinearLayout)、getAttribute*(0x10100f4=-1)全通。
3. **AssetManager.nativeApplyStyle 缺失**:写 JNI wrapper 包 libandroidfw C++ android::ApplyStyle + shorty "VJJIIJLJJ" handler。→ obtainStyledAttributes 通(hasWidth=true lw=-1)。
4. **AssetManager.nativeThemeApplyStyle 缺失**:写 wrapper 包 Theme::ApplyStyle + shorty "VJJIZ"。
**inflate 实际进度**:screen_simple 解析→LinearLayout inflate→过 layout_width(line#4)→FrameLayout(line#5)→卡 `View.<init> getDrawable ?attr/windowContentOverlay(0x1010059)` 空主题解析不出。试 setTheme(Theme_Material_Light 0x1030237)→**applyStyle HANG(在不匹配 framework-res 上解析主题父链循环)**。
**根本剩余卡点=正确的 A16 framework-res**(Agent-A 战线,依赖 Linux build host / dex2oat,Mac 做不了)。mine 全部完成。java_vm_ext AssetManager 分支已泛化(任意 method→helper dlsym),后续缺的 AssetManager style/theme native 只需往 helper 加 wrapper + interpreter 加对应 shorty。
