---
name: octos-web-ivory-obsidian-refactor
description: "octos-web frontend refactor state (Ivory Obsidian skin + / launcher + /studio workspace) and this Mac's dev-env quirks for that repo"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8690eba1-2957-40d3-83da-fbccfba7dc45
---

Canonical local checkout: `/Users/yao/Desktop/code/杂/work/mofa-org/octos-web` (many other stale copies exist under Desktop/code/杂 — ignore them). 2026-07-07 SHIPPED: PR #247 MERGED to main (merge commit eacf20e; 5 commits 7dcebc3 design system → 42b94ef launcher → 0eba730 studio → 59bc822 shell migration incl. editors/settings via API-compatible StudioTopbar → d2c2ef8 tests incl. voice-spec root-cause fix); Pages deploy success, feature branch deleted local+remote, local checkout back on main. Gates at HEAD: unit 805/805, tsc+vite build, offline e2e 135/135 (full green), live full-backend 2/2; repo-wide lint 163 pre-existing problems in old specs (baseline 166, none in touched files). The refactor was originally an UNCOMMITTED refactor implementing the Stitch "Ivory Obsidian" design (`~/Downloads/stitch_octos_home_project_launcher.zip`): new `ivory-obsidian` ui-style skin (new default, one-time warm→ivory migration marker `octos-ui-style-migrated-ivory`), self-hosted fonts in `src/assets/fonts/`, `.studio-*` CSS family, `/` rewritten as project launcher backed by `src/store/project-store.ts` (aggregates octos-slides-projects + octos-sites-projects + octos_session_titles, flags in `octos-project-flags`), and revived `/studio/:projectId` 3-pane workspace (`src/studio/`, hard-pinned SessionContext per slides-chat pattern; checked sources ride as turn media via beforeSend AND explicitly on skill-tile sends).

**Why:** the M9-β-2 Studio deprecation was deliberately reversed; legacy `studio-*` ids still redirect home (guard in StudioPage).

**How to apply:** verification gates that must stay green: `npm run test:unit` (800), `tsc -b && vite build` (only CI gate), eslint on changed files, offline Playwright (mock harness; frontend on port 5174, no backend needed). Pre-existing failures to NOT chase: e2e `ui-redesign-smoke:635` voice-tile + `home-wake-word-auto-listen` (fail on clean baseline too), ~166 eslint problems in tests/*.spec.ts, 2 set-state-in-effect errors in login-page.tsx.

Env quirks on this Mac: zsh npm/node shims broken non-interactively (`_nvm_lazy_load` missing) — use `export PATH="$HOME/.nvm/versions/node/v25.2.1/bin:/opt/homebrew/bin:$PATH"` and `command npm`; npm registry TLS broken (ERR_SSL_INVALID_AAD) but git/curl/jsdelivr work — vendor packages via curl instead of npm install.

Known accepted P3s: download anchors embed bearer token via buildAuthenticatedFileUrl (pre-existing pattern); studio project recency = creation time (no activity signal offline). Related: [[westlake-dayu600-hwui-gate2]] is a different project; the desk-camera assistant audit (`~/Desktop/octos-desk-camera-assistant-audit.md`) can later become a skill tile in `src/studio/skills.ts`.

2026-07-07 final round (offline e2e 135/135 FULL GREEN, unit 805): galleries (/slides /sites) migrated to shared StudioNav glass chrome (editors/chat/settings still old shells — different paradigms, next design task); launcher Recent uses `openedAt` stamps via `recordProjectOpened()` in octos-project-flags; studio rail downloads are header-authed blob fetches (no more ?token= in DOM); FIXED the two upstream pre-existing e2e failures — root cause: specs asserted "Voice engine ready" label that the Jun-30 product change deliberately hides (readiness surfaces only on problems) + missing `/api/voice/readiness` mock; wake-word ONNX flow actually works in 4s once ungated. Gotcha: `.studio-input` unlayered padding beats Tailwind layered `pl-*` — icon inputs use `data-with-icon`.

2026-07-07 later rounds: (a) Stitch strict-fidelity pass — shared glass StudioNav (src/components/studio-nav.tsx, launcher + /studio; other pages still on old workbench chrome = staged migration TODO), launcher restructured to design (3 tabs incl Shared-with-Me placeholder, more_horiz card menus, monochrome stars), design's exact 6 skills; home-nav.tsx deleted. (b) 36-agent critical review confirmed 30 findings → fixed 26 incl: uuid-v7 `web-{uuid}` session-id timestamp parsing in project-store (backend mints these; 48-bit hex ms prefix), mobile hamburger nav (links were dead-ends <md), drawer scrim z-30 below nav z-40, on-accent token for primary buttons/active chips (WCAG on warm skins), live spec step 8 real assertions, positive /studio route e2e coverage. Won't-fix: staged nav migration, studio blur inert, token-in-download-href, recency=creation-time.

LIVE full-backend E2E (2026-07-07, PROVEN): `tests/live-studio.spec.ts` (gated on OCTOS_LIVE_E2E=1) passes 2/2 against a real `octos serve` — real deepseek turn on /chat, and the full studio journey incl. grounding: uploaded md's codeword read back by the model via read_file. Recipe: backend repo `work/mofa-org/octos` (local branch dirty; build origin/main in a worktree, `cargo build --release --bin octos --features api` — WITHOUT `api` there is no serve subcommand); run isolated `octos serve --port 50080 --data-dir <fresh> --config <copy> --auth-token <tok>`; profile llm config shape is `{family_id, model_id}` (NOT provider/model — that 400s); enable profile + set llm via PUT /api/my/profile, then **do NOT start the gateway process** — a running gateway holds the profile's episodes.redb and serve's in-process runtime silently hangs every WS turn (the audit's lock landmine, still true on July main; POST /api/my/profile/stop fixes it). Raw admin token works as Bearer for HTTP+WS; /api/auth/verify static-code path rejects it. All 5 provider API keys are set in this Mac's env.
