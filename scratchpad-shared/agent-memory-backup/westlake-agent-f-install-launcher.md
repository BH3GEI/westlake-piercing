---
name: westlake-agent-f-install-launcher
description: "I am Agent-F (WestLake install/launcher/click-spawn research line); mechanism mapped, big-board routing already present, gated on B's arm64 ART substrate"
metadata: 
  node_type: memory
  type: project
  originSessionId: 947b0232-93cb-4211-852f-8504faa76b19
---

I am **Agent-F** in the WestLake DAYU600 project — the **"real install + launcher icon + click-to-launch"** research line, independent of B's headless runtime bring-up.

**⭐ MECHANISM VALIDATED (2026-07-07, hands-on, non-destructive):** on the claimed 2nd small board `…500404ac00` I ran the runbook end-to-end — appspawn-x→Phase4, `aa start noice.MainActivity`→"start ability successfully"→AMS→AppSpawnX→child forks→full 32-bit ART→`ActivityThread.main→handleBindApplication→NoiceApplication.onCreate RUNS`. Only failure = noice's OWN Hilt/Dagger DI crash in onCreate (app-specific, not the mechanism). ALSO validated launcher-icon path: deployed noice's entry.hap (pulled read-only from original board, same iconId 0x01000005/labelId 0x01000003) → cleared Launcher.db → restarted launcher → "Noice" icon+label rendered (evidence scratchpad-shared/F-newboard-noice-icon-validated.jpeg). So 2 of 3 north-star criteria PROVEN on 32-bit hardware: (1) launcher shows icon ✅, (2) click launches via appspawn-x→ART→onCreate ✅; (3) visible render downstream on B/C. Whole install→launcher-visible→click→spawn→ART→onCreate chain PROVEN; arm64 big-board = arch-swap. Remaining to visible-clickable-app: per-app runtime completeness (noice=Hilt crash; pick non-Hilt app) + B/C's first-frame render. Task md: `~/Desktop/AGENT-F-TASK.md`. I poll the whiteboard every 10min via cron `6f92b85f` (session-only) and append signed `## [Agent-F]` sections to `/Users/yao/westlake-piercing/AGENT-COORD.md` (2 entries posted ~15:4x/15:5x).

**Mechanism (small board dd011a41, working reference — verified on-device + CATALOG-REPRODUCE.md):** Android app → launcher-clickable = 3 things, no HAP-wraps-APK. (1) Registration: byte-patched `libappexecfwk_common.z.so` lets `bm install -p app.apk` take a raw APK → `bundleType 10 (APP_ANDROID)`, `codePath=…/<pkg>/android`, activities→OHOS abilities; `bm install` WIPES bundle dir. (2) Launcher icon: separate restool `entry.hap` at `…/<pkg>/entry.hap` supplies iconId 0x01000005/labelId 0x01000003, resolved client-side by resourceManager; clear `com.ohos.launcher/.../Launcher.db*`. (3) Click = `aa start -a <pkg>.MainActivity`(the `isLauncherAbility:true`+`entity.system.home` ability) → AMS sees bundleType 10 → routes to **AppSpawnX socket** → `appspawn-x` child_main → WestLake ART runs onCreate. `asx_boot.sh` brings appspawn-x up.

**Big board 5583f5be (target, arm64, Permissive) — CORRECTED via on-device probing (my 3rd whiteboard entry):** the install/spawn stack is **~90% deployed by an earlier "02A"/M0-M1 session** (NOT "residue" as [Agent-C L844] called it, NOT build-from-scratch as the workflow synth guessed). Verified on-device:
- Full arm64 payload at `/data/a64deploy/sysandroid/` (bind-mounted→/system/android; cold reboot drops the mount, `_m1_hw_bringup.sh` re-`mount --bind`s it — that's why /system/android looked empty): 9 bootclasspath jars + framework-res.apk + `arm64/` AOT boot image (boot-*.art/.oat/.vdex); lib64/ (51) full ART stack + libhwui/libandroidfw/skia + oh_adapter_bridge/android_runtime/net shims.
- Real arm64 appspawn-x binary at `/data/local/tmp/appspawn-x` (110KB PIE aarch64), `start_asx64.sh` + `_m1_hw_bringup.sh`. `asx_run.err` shows it reaches **"Phase 4: Ready to accept spawn requests"** (ART VM up + 9 jars preloaded + socket listening). So the appspawn-x child has its OWN working arm64 ART — F is NOT strictly gated on B's 5ce2dcee ART.
- Image services already patched: libappms `GetAndroidSpawnClient` routing, libbms `oh_adapter_install_apk_with_manifest` (v2), libappexecfwk_common `.apk` suffix gate (3-branch, no byte-patch needed). Socket relabel in script (MANDATORY per author even under Permissive).

**3 remaining gaps to close F's north star:** (1) `libapk_installer.so` NOT in deployed lib64 (EXHAUSTIVELY confirmed absent on big board all /data+/system paths + this Mac; small board copies are 32-bit only. Build spec reversed from small-board copy → in runbook: 2 C exports oh_adapter_install_apk[_with_manifest], self-contained 375KB leaf lib, deps = standard OHOS arm64 libs all present; arm64 build = straightforward recompile of 02A source. HARD-BLOCKED on 02A/bridge-build source tree, not on this Mac.) (libbms dlopens it to parse APK→bundleType-10; without it can't install an Android app) — small board has it at /system/android/lib/, need arm64 build; (2) adapter-bridge nativeStartAbility/OnScheduleLaunchAbility errors — DIAGNOSED (on-device static) as a PathClassLoader binding issue, NOT missing artifact/not simple skew: v2 AppSchedulerBridge (nativeOnScheduleLaunchAbility v2) + PackageInfoBuilder ARE on the board in oh-adapter-runtime.jar (44KB, loaded via PathClassLoader, NOT on BOOTCLASSPATH). Captured run: RegisterNatives(AppSchedulerBridge,3) OK yet .so logs "nativeOnScheduleLaunchAbility (new sig) not found". Outbound (startActivity) non-fatal (Java no-op). Inbound (initial launch) needs a bringup+runtime-logs to pin down (B/02A classloader domain, not statically resolvable, not fixed by swapping a jar); (3) zero bundleType-10 app installed → end-to-end APK→child→onCreate→render never closed (m1_hw*.jpeg screenshots = just OHOS launcher home). Child's RENDER half: arsc/AssetManager/theme wall is NOT shared — 02A appspawn-x's asx_run.err proves it calls AndroidRuntime::startReg (liboh_android_runtime.so, 32 modules) which binds real register_android_content_AssetManager/res_ApkAssets/StringBlock/XmlBlock (unlike B's headless probe's bare dlopen that misses JNI_OnLoad → the stub wall B retrofitted). So child inflates themed UI via real arsc; graphics-native + framework-res→Activity-context plumbing remain. NUANCE (from startReg logs): the SMALL-board appspawn-x startReg (26 modules) DOES register RenderNode via libhwui (`libhwui:register_RenderNode OK`) — so my small-board child gets graphics natives too; but the BIG-board 02A appspawn-x startReg (32 modules) does NOT register RenderNode (ends at SQLite) → my big-board child would hit the same RenderNode wall B hit (nGetElevation UnsatisfiedLinkError) and needs the libhwui RenderNode registration added (C's adapter libhwui domain, register-and-use). spawn+onCreate half F can push independently.

**Open:** the 02A workspace (`from_factory_plan.md`, `deploy_m0_from_factory.sh`, `build_net_shim.sh`, the arm64 build tree incl. libapk_installer.so) is NOT on this Mac's /Users/yao — likely another session/machine. Need it from user to finish the last 10%. Convergence links: [[westlake-dayu600-upscreen-render-window]], [[westlake-dayu600-two-fronts-converge]], [[westlake-build-env-blocker]].

**Executable runbook** written at `westlake-piercing/scratchpad-shared/agent-f-bigboard-install-runbook.md` — ordered big-board deploy steps (mount-bind→appspawn-x Phase4→bm install→entry.hap→clear Launcher.db→aa start), each tagged ready/blocked; only hard-blocked step = obtain arm64 libapk_installer.so. **framework-res:** A clarified (coord ~L1712) the CORRECT A16 one = `76a92b8f` (`scratchpad-shared/framework-res-remapped-99pct.apk`, 99%-aligned); old 64%/36%-mismatch = ffd402c0/3cac774e; big board currently deploys an even-older `121f478b` → runbook Step 2 swaps in 76a92b8f before running a real app. framework-res correctness is A's domain (A judges the applyStyle hang is native, not resource).

**Hard rules I follow:** never flash/wipe/install-on/kill-procs-on any board; read-only device probing only; small board dd011a41 (`…200971ac00`, unrebuildable blob 16e08711 — never wipe) ok; big board 5583f5be (`…0323012c`) read-only ps/bm/strings (B migrating in); **NEVER touch 5ce2dcee (`…0923012c`)=B exclusive**; append-only on whiteboard; @Agent-C to schedule 5583f5be time. **CLAIMED 2nd small board `…500404ac00` (full serial dd011a4144363141301012500404ac00; BOTH small boards share dd011a41 prefix → ALWAYS full serial) as install-runbook validation unit: full WestLake 32-bit deploy (appspawn-x + working 32-bit libapk_installer.so + noice installed). Plan = hands-on NON-destructive execute-validation of the whole runbook (bm install pure-Java APK → entry.hap → clear Launcher.db → aa start → verify icon+click), to upgrade runbook documented→32-bit-proven. Never wipe/flash it.**
