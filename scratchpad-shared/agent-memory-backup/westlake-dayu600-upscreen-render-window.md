---
name: westlake-dayu600-upscreen-render-window
description: "Agent-C's DAYU600 上屏 rendering/window half — strategy B (bypass ViewRootImpl) is THE path; native hwui->panel pipeline proven on a 2nd big board; drop-in renderer .so + Java host built"
metadata: 
  node_type: memory
  type: project
  originSessionId: f5946200-7e84-4ff5-82c6-3f4f572b0846
---

**I am Agent-C** in the 4-agent WestLake/DAYU600 effort (B=大板 5ce2dcee 主线 onCreate/MainActivity; A=上屏资源半边 framework-res.apk+AppCompat; **C=me=上屏渲染/窗口半边**; D=输入/触摸线). Coordinate via `/Users/yao/westlake-piercing/AGENT-COORD.md` (+ `AGENT-CHAT.md`); I have a 10-min cron reading it. Don't touch 5ce2dcee runtime / runtime.cc / interpreter.cc / Dayu600ApkStageProbe.java / SQLite / framework-res.apk. See [[westlake-dayu600-hwui-gate2]] (hwui gate1-2) and [[agent-a-multiagent-coordination]].

**MY BOARD: `5583f5be00000000000000000323012c`** — a 2nd DAYU600 (arm64, 1200x1920@60, OHOS 6.1.0.31, same as 5ce2dcee), assigned to me for on-device render verification. **(2026-07-07 user directive: BOTH big boards must be used — 5ce2dcee = B exclusive/critical-path; 5583f5be = C+D for the DECOUPLED engine proof (pureColorView+input). Re-confirmed 2026-07-07: full-panel solid blue ff3366cc rendered live via adapter pipeline, 11 clean eglSwapBuffers, no crash/early-stop — evidence `scratchpad-shared/upscreen-render/5583f5be-realpixels-bluecolor.jpeg`. That's the NATIVE color smoke (no ART) = render-pipeline proof only; the FULL Java pureColorView→show() engine proof needs an ART substrate on 5583f5be (libwestlake_art.so+core-jars+minimal Context probe = B's runtime artifacts, not staged to me → coordination pending). board-vs-code question needs an ART probe on 5583f5be, NOT the native render smoke.)** It's an appspawn-x/HelloWorld bring-up board (`/data/local/tmp` has appspawn-x + `libskia_canvaskit.z.so` + `_m1_hw_bringup.sh`), NO westlake substrate → clean for my hwui smokes. hdc = `~/.local/openharmony-tools/bin/hdc`.

**★ NATIVE hwui→OHOS-Surface→panel PIPELINE PROVEN ON 5583f5be (2026-07-06), zero ART/framework.jar.** Deployed the built `ports/dayu600/out/libhwui-build/libhwui.so` + `out/gfx-smoke/hwui_render_harness` (and `hwui_layout`) + fake `libandroid.so`; got green+red and a DecorView-shaped nested layout on the physical panel. Run recipe gotchas: `snapshot_display` ONLY accepts `.jpeg` suffix (not .png); fake `libandroid.so` MUST be first on LD_LIBRARY_PATH (`<dir>:/system/lib64:/system/lib64/platformsdk:/system/lib64/chipsetsdk`); `power-shell wakeup`+`display -o 230` to light the panel; host can't `sleep`, so run harness on-device `nohup &` then device-side `sleep N; snapshot_display`. skia_interposer's `fGpuPathRenderers=kNone` ABI fix is required and works on this board too (GPU gate `preFlushSuccessful=1`, `eglSwapBuffers ret=1`). Full recipe: `scratchpad/upscreen/notes/10-board-run-recipe.md`; shots `scratchpad/upscreen/shot-harness.jpeg` + `shot-decor.jpeg`.

**★ DECISIVE ARCHITECTURE (source-grounded, android-15.0.0_r9): use STRATEGY B (bypass ViewRootImpl). Strategy A is a dead end on headless OHOS.**
- **A dead**: android-15 `IWindowSession.relayout` fills a `WindowRelayoutResult` whose `surfaceControl` IS ViewRootImpl.mSurfaceControl; ViewRootImpl derives its Surface via `mSurface.transferFrom(new BLASTBufferQueue(mSurfaceControl, w,h,fmt).createSurface())`. That hard-requires a valid native SurfaceControl + BLASTBufferQueue = SurfaceFlinger/BufferQueue constructs that DON'T EXIST on OHOS. No IWindowSession stub can fake a valid SurfaceControl → A卡死. (The only session methods on the first-frame path are addToDisplayAsUser/relayout/finishDrawing; documented in the design doc but not worth implementing.)
- **B (the path)**: after the app builds its DecorView, `decor.measure(EXACTLY w, EXACTLY h)`+`layout(0,0,w,h)` → `RenderNode root=new RenderNode(name); root.setPosition(0,0,w,h); root.setClipToBounds(false); RecordingCanvas c=root.beginRecording(w,h); decor.draw(c); root.endRecording();` → hand `root`'s native ptr to our RenderProxy. Needs NO ViewRootImpl / IWindowSession / WMS / Choreographer / vsync. KEY: with `mAttachInfo==null` (we never dispatchAttachedToWindow), `View.draw()`/`ViewGroup.dispatchDraw()` take the SOFTWARE child-draw branch (drawingWithRenderNode=false) → whole tree inlines into ONE flat display list = exactly what hwui_layout already paints. MUST set node bounds (beginRecording only sizes the canvas) + verify `root.hasDisplayList()` (route-b `updateDisplayListIfDirty()` silently returns empty w/o mThreadedRenderer → black; don't use route-b).

**MY DELIVERABLES (scratchpad `f5946200-....../scratchpad/upscreen/`, handed to B for integration on 5ce2dcee):**
- `native/libwestlake_upscreen_renderer.so` (2.9MB, BUILT) — my own copy of the proven `westlake_view_renderer.cpp` pipeline, registers natives on class **`WestlakeUpscreen`** (NOT B's Dayu600ApkStageProbe). Natives: `nativeRenderNodePtr(RenderNode)J` (reads `RenderNode.mNativeRenderNode` via JNI GetFieldID/GetLongField — native access bypasses hidden-API), `nativeInit(JII)I` (make_display_window+oh_anw_wrap+RenderProxy(opaque,node*,factory).setSurface), `nativeDrawFrame()V`, `nativeTeardown()V`. Build: `native/build-upscreen-renderer.sh` (clone of `gfx-smoke/build-view-renderer.sh` with source stem swapped; source also copied into `ports/dayu600/gfx-smoke/westlake_upscreen_renderer.cpp`). Reuses prebuilt interposer objects in `ports/dayu600/out/gfx-smoke/obj/`.
- `java/WestlakeUpscreen.java` — strategy-B host (layout/record/show/drawFrame/pushFrame/teardown + `pureColorView(ctx,argb)`). COMPILE-VERIFIED against android-35 (openjdk at `/opt/homebrew/opt/openjdk/bin/javac`).
- `notes/30-design-and-standins.md` — the full deliverable: both strategies, IWindowSession stub table (备查), Surface source, Choreographer/vsync options, missing-native table, B integration recipe + risks R1/R2.

**Integration recipe for B**: `View decor=activity.getWindow().getDecorView(); int r=WestlakeUpscreen.show(decor,1200,1920); // r==2 => pixels on panel`. Deploy `libwestlake_upscreen_renderer.so`+our `libhwui.so`+fake `libandroid.so`(LD-first). Two risks B must check: **R1** the in-process egl/skia interposers may not win global-scope PLT when dlopen'd into ART; **R2** the Java RenderNode's native object must be from OUR libhwui.

**★ GRAPHICS-NATIVE BINDING — DEFINITIVE (2026-07-07, nm-verified):** my adapter libhwui (504386b1) EXPORTS the full graphics/view JNI registrars, so no hand-built helper is needed to bind RenderNode/Canvas/Paint natives. Key exports (nm -D): `register_android_graphics_classes` (**C linkage** — dlsym by plain name, @0x24bb70, the 54-registrar driver), `register_android_view_RenderNode` (registers `nGetElevation` etc.), `register_android_view_ThreadedRenderer`, `register_android_view_DisplayListCanvas`, `register_android_graphics_Canvas`/`CanvasProperty`/`Paint`/`animation_RenderNodeAnimator`. **To bind ALL graphics natives: `dlsym(adapter_libhwui, "register_android_graphics_classes")` then call `((int(*)(JNIEnv*))p)(env)`** — or `westlake_android_runtime_startReg(env)` → `register_hwui_if_present` (which calls the same). An `UnsatisfiedLinkError` on RenderNode/Canvas natives means the registrar didn't RUN (deferred because libhwui wasn't loadable when startReg first ran), NOT that the JNI is missing. R2: dlsym must target MY adapter (504386b1) so registration + my renderer share one libhwui → RenderNode ptr compatible. Second step after binding: crit-sig getters (nGetElevation etc., compiled -D__ANDROID__) need crit dispatch (force-raw/crit-flag) — but ULE is a registration problem, fix that first. (Context: B built the real DecorView View tree via A's 76a92b8f + resource root-fixes — resource half DONE — and hit RenderNode.nGetElevation; this is the last wall before WestlakeUpscreen.show().)

**★ CROSS-DOMAIN WIN (2026-07-07): I cracked the team's total blocker = the framework-res "openByPath FileNotFound" wall (B hit it 8-9 angles, nearly declared an "OHOS-native hard wall").** Root cause found via zip-forensic diff (my domain: packaging/toolchain): A's apktool-repacked `framework-res-remapped-99pct.apk` (76a92b8f) has **2102 deflated XML entries with the data-descriptor bit (0x08) set** — local file headers have zeroed CRC/sizes, real values trail after the data. OHOS's minimal `libziparchive`/`ZipFileRO` reads the LOCAL header when opening a file entry → sees size 0 → can't build the file index → FileNotFound. resources.arsc is STORED (no DD) so its VALUES parse via `loadFromPath` — exactly the observed "arsc parses but XML files won't open". Fix = re-zip WITHOUT data descriptors: A's Info-ZIP **`823e3c03`** (deployed to big-board uamShared) + my Python `ed5efd4c` (`scratchpad-shared/upscreen-render/`), both verified 0-DD. I deep-checked all three apks' LOCAL headers: 823e3c03 has **0 data descriptors / 0 extra fields / flags all 0** — cleaner than test.apk (which has zipalign-padding extra fields yet OHOS reads fine). So zip structure is comprehensively ruled out: B's `openByPath` test with 823e3c03 is now unambiguous — passes = whole framework resource wall breaks (screen_simple→DecorView content→my show()); still fails = provably NOT apk/zip → genuinely OHOS-native → pivot to D's programmatic-View decoupling (pureColorView/WestlakeInputTestView, no framework-res). B confirmed my diagnosis "完全正确" and dropped his nativeCreate backup (it's UnsatisfiedLinkError on the deployed libandroidfw — device-tested). One-line self-check any apk: `python3 -c "import zipfile;z=zipfile.ZipFile('X');print(sum(1 for i in z.infolist() if i.flag_bits&8))"` must be 0. Lesson reinforced (B's): symbol registration must be device-tested, not source-inferred.

**★ CRIT/NORMAL IS A COMPILE-TIME PROPERTY OF EACH .so (2026-07-07, verified):** whether a native has a crit signature (no env) or normal (env,jclass) depends on whether ITS .so was compiled with `-D__ANDROID__` (`graphics_jni_helpers.h`: `#ifdef __ANDROID__` → `CRITICAL_JNI_PARAMS_COMMA` empty=crit; `#else` → `JNIEnv*,jclass,`=normal). `--target=aarch64-linux-ohos` predefines only `__OHOS__`/`__linux__`, NOT `__ANDROID__` → default OHOS build = normal sigs. **This explains B's finding that the deployed libandroidfw's `nativeNext` is normal (has env, IsCriticalNative()=false is correct) — it wasn't built with `-D__ANDROID__`.** BUT **my libhwui build DOES force `-D__ANDROID__`** (`build-libhwui-smoke-macos.sh:63`, `build-hwui-2048.sh:55/141`) → my Canvas/Paint draw natives are **crit-sig (no env) → REQUIRE the crit-flag landed** → my render is aligned with A's `LoadMethod:4141` root fix (not in conflict with B's nativeNext reversal). Key principle for B: the crit-flag (landed by Java annotation) must MATCH each method's deployed-.so compiled signature — landing it on a normal-compiled lib's method crashes. My libhwui=crit(land), libandroidfw nativeNext=normal(don't). My `show()` guard unchanged: assert `Canvas.nDrawColor` `IsCriticalNative()==true` before drawing. NOTE: I originally (wrongly) folded `nativeNext` into a "3-way crit-flag convergence"; B reversed it (nativeNext=normal); corrected to 2-way (render+input crit) + nativeNext on its own normal track.

**★ DECOUPLING PATH (my original idea, D now co-champions) if framework-res stays blocked**: prove the render+input engine NOW on a PROGRAMMATIC View — `WestlakeUpscreen.pureColorView(ctx,argb)` (a `new View(ctx)`+setBackgroundColor, no inflate/obtainStyledAttributes, only needs a bare Resources w/ DisplayMetrics) — completely bypasses the framework-res XML file-index wall. Gets first-frame + tap milestone without solving framework-res. My `show()` renders EITHER a real DecorView OR a programmatic View unchanged. **Shared remaining gate for BOTH paths (render+input) = the @CriticalNative crit-flag** (`class_linker` LoadMethod:4141): my draw crit natives (`Canvas.nDrawColor`…) and D's input crit getters (`MotionEvent.nativeGetAction`…) must have `IsCriticalNative()==true` / not fall to B's `WlCriticalNoop` fallback — else my frame is BLANK (draw ops eaten, NOT a renderer bug) and D reads garbage touch. B will handle crit-flag at show() integration. Pre-show() self-check: dump `Canvas.nDrawColor` IsCriticalNative before calling show().

**★ R1 EMPIRICALLY TESTED on 5583f5be (2026-07-06)** — added a plain-C `westlake_upscreen_color_smoke(w,h,argb,secs)` to the .so (builds a color RenderNode natively, zero ART) + a `upscreen_smoke_launcher` (dlopen RTLD_GLOBAL). Two runs: (1) renderer-.so-loaded-FIRST (it pulls in libhwui/libEGL) → **blue paints, interposers all fire** (shot-r1-dlopen.jpeg). (2) `LD_PRELOAD=libhwui.so` so libhwui is resident BEFORE the renderer .so → **nothing paints, stalls before GL init, interposers lose** (shot-r1-preload.jpeg, home screen visible). Refined with 3 load configs: config1 (renderer .so is SOLE loader of libhwui) → paints; config2 (libhwui loaded first, then renderer — the NORMAL ART case) → hangs; config3 (renderer LD_PRELOADed first + libhwui also present) → interposers fire but EMPTY frames (n_rects=0). BOTH realistic ART configs (2,3) FAIL because a real ART process loads libhwui independently for graphics registration → **LD_PRELOAD-renderer is NOT a viable fallback**; the gfx-smoke link-scope-interposer renderer cannot work in a real ART process.

**★★ R1 SOLVED (built + verified on 5583f5be 2026-07-06) ★★** — built a **self-binding adapter libhwui**: folded the proven `egl_interposer.o` + `skia_interposer.o` + `oh_anativewindow_shim.o` INTO libhwui and relinked (libhwui is already `-Wl,-Bsymbolic-functions`, so libhwui's OWN eglCreateWindowSurface/GrDirectContexts::MakeGL calls bind to the internal interposer — load-order-independent). Then built a renderer variant linked against it WITH the interposers dropped. TEST (faithful config-2: `WL_DLOPEN_FIRST=libhwui-adapter.so` dlopen'd mid-main like ART registration, THEN the adapter-renderer): **full-screen orange paints** (n_rects=1, preFlushSuccessful=1) — the exact config that broke the gfx-smoke .so. **This is THE integration path for B; settles R1 AND R2 (one libhwui for both registration + renderer).** Artifacts staged in scratchpad upscreen/native/: `libhwui-adapter.so` (41MB), `libwestlake_upscreen_renderer_adapter.so` (2.9MB), `build-adapter-libhwui.sh` (recipe), `upscreen_smoke_launcher.c` (+ its plain-C `westlake_upscreen_color_smoke` entry in the .cpp). Deploy: use libhwui-adapter.so as the app process's libhwui (rename over android/lib64/libhwui.so), renderer = the adapter renderer (rename to libwestlake_upscreen_renderer.so to keep Java loadLibrary unchanged), fake libandroid.so on LD path; load order irrelevant. Evidence: shot-adapter-config2-faithful.jpeg. NOTE: the a2oh `hwui_oh_abi_patch.cpp` is a DIFFERENT (heavier, a2oh-entangled) self-binding approach; I did NOT use it — folding my own interposers in was cleaner and is what's verified.

**★ GRAPHICS NATIVE REGISTRATION (answered B's first-frame blocker 2026-07-07) ★** — B hit `DecorView.<init>→Paint.<init>→Paint.nSetFlags` UnsatisfiedLinkError: Java `android.graphics.*` natives (Paint/Canvas/RenderNode) were NOT registered. KEY: my green/red/etc. smokes used C++ `android::uirenderer` DIRECTLY and never went through Java `android.graphics.Paint`, so loading my renderer .so does NOT register them. Mechanism (verified in `ports/dayu600/android-runtime-src/AndroidRuntime.cpp`): the graphics natives are registered by `register_android_graphics_classes(env)` (54 registrars), invoked by `register_hwui_if_present(env)` inside `AndroidRuntime::startReg`, exported as **`extern "C" westlake_android_runtime_startReg(JNIEnv* env)`** in liboh_android_runtime.so. It is NOT a JNI_OnLoad (that's a no-op) — `System.load` does not trigger it; you must explicitly CALL `westlake_android_runtime_startReg(env)`. Common failure: startReg runs at VM init BEFORE libhwui is loadable → logs "libhwui.so not present yet; graphics native registration deferred" (AndroidRuntime.cpp:663) and skips graphics → re-call startReg after libhwui is loaded. `register_hwui_if_present` dlopens libhwui via paths `["libhwui.so", ".../substrate/android/lib64/libhwui.so", ...]` — **that libhwui MUST be `libhwui-adapter.so`** (rename over the substrate's android/lib64/libhwui.so BEFORE calling startReg) so the registered Paint/Canvas/RenderNode natives point into the SAME libhwui the renderer links (R2) and EGL/skia self-bind (R1). Debug per-registrar via WESTLAKE_HWUI_STOP_AT/SKIP/AGG (registrar list `g_hwui_reg_all[]`). gate-3 got 54/54 passing.

**★ FOLLOW-UP (2026-07-07): registrar runs (rc=0, native ptr IS set) but graphics natives still UNCALLABLE from the interpreter — root cause = @CriticalNative + InterpJni marshalling (B's runtime domain).** B hit `Paint.nSetFlags` UnsatisfiedLinkError after startReg. I diagnosed from the graphics side (baksmali framework.jar 8c377c13 + AOSP-15 hwui Paint.cpp): (1) graphics side is CLEAN — A16 Paint declares `nSetFlags(JI)V`, my adapter's `register_android_graphics_Paint` (Paint.cpp:1178) registers it, and AOSP-15's 83 registered Paint methods are a strict SUBSET of A16's 90 (no version-skew reg failure). (2) The distinguishing fact: `nInit()J` is a NORMAL native (no annotation) and binds+runs; `nSetFlags(JI)V` is **@CriticalNative** and fails. **66 of Paint's 90 natives are @CriticalNative** (nSet*/nGet*), and most Canvas/RenderNode/Matrix/ColorSpace natives too. The imageless ART's InterpJni (custom shorty-keyed native-call marshaller, like the 28 SQLite shorties) has handlers for normal-convention shorties but not the @CriticalNative convention (**@CriticalNative = args only, NO JNIEnv/jclass**; @FastNative = env+jclass, SAME signature as normal — only a faster transition. Corrected by Agent-A from AOSP art/test/004-JniTest; my earlier "@FastNative=env no jclass" was WRONG. Dispatcher branch is BINARY: IsCriticalNative ? params-only : env+jclass+params). B is implementing a general libffi shorty dispatcher in interpreter.cc that MUST branch on `ArtMethod::IsCriticalNative()`/kAccCriticalNative to pick the calling convention, not just the shorty string. **This gates MY show()'s record path too** — RenderNode/RecordingCanvas draw natives are largely @CriticalNative — so B's dispatcher fix unblocks graphics registration AND my show() recording in one shot. Diagnostic artifacts in scratchpad upscreen/rn-verify/. B deployed my adapter libhwui (md5 504386b1) over substrate android/lib64/libhwui.so per R1/R2.

**★ ROOT PINNED (2026-07-07): `kAccCriticalNative`-from-dex-annotation is the shared graphics+input root, and it gates BOTH RegisterNatives AND B's dispatcher.** After B built the AAPCS64 dispatcher, `Paint.nSetFlags` still ULE'd (ART name-fallback = never RegisterNatives-bound). I verified from source + my built .so: (1) my libhwui's Paint table has `{"nSetFlags","(JI)V",setFlags}` — PLAIN sig, ZERO `!` prefix (refutes the `!`-prefix hypothesis); (2) `setFlags` is compiled no-env/jclass (true @CriticalNative) because the build defines `-D__ANDROID__` (CRITICAL_JNI_PARAMS_COMMA→empty) — confirmed by mangled symbol `_ZN7android9PaintGlueL8setFlagsEli`=`setFlags(long,int)`; (3) not a wrong-Paint-class issue (nInit=normal bound on the same class). So the runtime MUST set `kAccCriticalNative` on framework.jar's @CriticalNative methods from the dex annotation `dalvik.annotation.optimization.CriticalNative` at class-load. CRITICAL connection: B's dispatcher branches on `ArtMethod::IsCriticalNative()`, whose truth IS the kAccCriticalNative flag — so if class_linker doesn't set it, IsCriticalNative()==false → the dispatcher ALSO mis-branches (pushes env+jclass for a no-env fn) → crash. So the class_linker dex-annotation fix is the common prerequisite for both binding and correct dispatch (B's runtime domain, class_linker.cc). Verify: assert `Paint.nSetFlags ArtMethod->IsCriticalNative()==true` after class load. My graphics side is fully clean (table/sig/convention/method-set all correct). Details: scratchpad-shared/CRITICALNATIVE-REGISTRATION-GROUNDTRUTH.md + my rn-verify/.

**★ ADJUDICATION (2026-07-07 03:2x): two competing roots — "wrong Paint class" (B's boot-classpath dump: framework.jar not on boot cp at register time → FindClass gets wrong/absent Paint) vs "@CriticalNative flag" — are distinguishable by nInit, because `register_android_graphics_Paint` registers nInit AND nSetFlags in ONE `RegisterNatives(FindClass("Paint"), gPaintMethods)` call (same class).** They predict OPPOSITE nInit behavior: wrong-class → nInit ALSO unbound → Paint.<init> ULEs at nInit → never reaches nSetFlags; @CriticalNative-flag → nInit (normal) binds → Paint constructs → reaches nSetFlags, only crit fails. So the CLEAN-run test (not stale/zombie logs): if Paint constructs (nInit binds) and only nSetFlags ULEs → it's the flag (A's LoadMethod fix); if nInit ALSO ULEs → it's wrong-class/classloader (register after framework.jar is on the classpath / in app-classloader context so FindClass gets the app's Paint). Both may be needed sequentially: bind to right class first, then flag for crit dispatch. A's shorty-regression isolates the flag mechanism (doesn't touch framework.jar). I owned that my earlier "nInit bound → right class" was inferred from B's possibly-stale symptom, not directly measured; the same-call LOGIC is the reliable discriminator.

**★ AttachInfo/onClick blocker (Agent-D found; I own + integrated) ★** — strategy B leaves `mAttachInfo==null`, so `View.post(mPerformClick)` queues into an un-drained RunQueue and returns true → onClick NEVER fires (touch dispatch returns handled but no click). Fix: install a fake AttachInfo with **mHardwareAccelerated=false** (keeps `drawingWithRenderNode` false → my software flatten UNCHANGED — verified safe) + mHandler=UI Looper, then dispatchAttachedToWindow. I integrated `attachForInput(View,Handler)` into `WestlakeUpscreen.show()` (after layout, before record; version-robust ctor introspection; guarded try/catch so it never breaks rendering; `sAutoAttach` flag). Compile-verified. So B's show() auto-attaches; D's pump needs no separate attach call. Complex-view fallback: reflectively set mAttachInfo field directly (skip onAttachedToWindow) — D has WestlakeInputAttach for that.

**Input threading (for Agent-D, added 2026-07-06)**: `WestlakeUpscreen` now also exposes `uiHandler()` (a Handler on the thread show() ran on) + `uiLooper()`. show() captures `Looper.myLooper()` of the calling thread = the UI thread (must be the probe's prepareMainLooper thread; View/RenderNode/dispatch are not thread-safe). D posts touches via `uiHandler().post(()->injectTouch(...))`.

**Vsync/Choreographer (only if ever needed; B doesn't need it for a static frame)**: real source = OHOS `OH_NativeVSync` (`libnative_vsync.so`, `OH_NativeVSync_Create`+`RequestFrame(cb)`), NOT wired to hwui (ndk_stubs AChoreographer is a no-op). To turn REAL Choreographer on: set `debug.choreographer.vsync=false` → Handler-timer fallback (no DisplayEventReceiver), or bind DisplayEventReceiver.nativeInit/nativeScheduleVsync to OH_NativeVSync. Choreographer needs a Looper on the thread (probe already `Looper.prepareMainLooper()`).

**For Agent-D (input line) — API FINALIZED (2026-07-06)**: strategy B has NO ViewRootImpl, so feed input to the root DecorView directly — NOT ViewRootImpl.enqueueInputEvent/InputChannel/InputEventReceiver. `WestlakeUpscreen.java` now holds the hosted `sDecor` and exposes: `injectTouch(int action, float x, float y, long eventTimeMs)` [eventTimeMs = SystemClock.uptimeMillis, MILLIS not nanos; downTime auto-latched on ACTION_DOWN; builds MotionEvent.obtain+dispatch+recycle], `dispatchTouch(MotionEvent)` [caller builds the event — use for multitouch/historical/custom downTime], `dispatchKey(KeyEvent)`, `decor()`, `requestRedraw()`. Strategy B has NO invalidate→Choreographer→traversal→draw loop, so every dispatch* re-records (decor.draw→RenderNode) + pushes a frame (redraw-always by default; a "only if handled" perf gate can be added). MotionEvent.obtain's native init is Agent-D's half. All compile-verified vs android-35.

**Heavier window path for later** (real WMS window with focus+MMI input): `ports/dayu600/bridge-src/oh_window_manager_client.cpp` `OHWindowManagerClient` is REAL+complete (connects OHOS SceneSessionManager SA 4606, CreateWindow+AddWindow+RequestFocus, `getOhNativeWindow(sid)` yields an oh_anw_wrap'd window). Overkill for the headless pure-color milestone; use the simple display-0 `westlake_ohos_make_display_window` for now.
