---
name: westlake-dayu600-input-line
description: "Agent-D's DAYU600 输入/触摸线 — Strategy B, feed decorView.dispatchTouchEvent via C's WestlakeUpscreen.injectTouch; only hard-new native = MotionEvent stub table + libohinput monitor shim"
metadata: 
  node_type: memory
  type: project
  originSessionId: b0445db8-b9e9-477f-a0d2-886d89ccd137
---

**I am Agent-D** in the 4-agent WestLake/DAYU600 effort (B=大板 5ce2dcee 主线 onCreate/MainActivity 打墙; A=上屏资源半边 framework-res.apk+AppCompat; C=上屏渲染/窗口半边 strategy-B; **D=me=输入/触摸/按键线**). Coordinate via `/Users/yao/westlake-piercing/AGENT-COORD.md` + `AGENT-CHAT.md`; I have a 10-min cron (job id may change per session; cron reads both files, responds to @Agent-D, else continues). B is the lead/integrator. Boundaries: never touch 5ce2dcee runtime / runtime.cc / interpreter.cc / Dayu600ApkStageProbe.java / SQLite / framework-res.apk; use board 5583f5be only after 错峰 with C. My deliverables go to MY scratchpad, B integrates. See [[westlake-dayu600-upscreen-render-window]] (C's half) and [[agent-a-multiagent-coordination]].

**INTERFACE LOCKED WITH C (strategy B, no ViewRootImpl):** feed input to `decorView.dispatchTouchEvent`, NOT ViewRootImpl/InputChannel. C's `WestlakeUpscreen.java` (compile-verified) exposes the entry: **`injectTouch(int action, float x, float y, long evMs)`** — evMs is MILLISECONDS = `SystemClock.uptimeMillis()` (not ns!); C auto-latches downTime into sDownTimeMs on ACTION_DOWN; C re-records+drawFrame after EVERY dispatch (so D does NO extra redraw). Also `dispatchTouch(MotionEvent)` | `dispatchKey(KeyEvent)` | `decor()` | `requestRedraw()`. THREAD contract: inject only on C's show()/drawFrame main-Looper thread (RenderNode not thread-safe); D marshals via Handler.post, DOWN→UP via postDelayed ~150ms (never sleep the main thread). ORDER: show() before any inject. C added `WestlakeUpscreen.uiHandler()`/`uiLooper()` (bound to the show()-thread Looper); WestlakeTouchPump self-fetches uiHandler() on every post so B needs NO manual bind (uiHandler() is null before show() → pump throws clear error). B MUST call show() on the prepareMainLooper thread.

**ARCHITECTURE (decided, from a 6-reader workflow synth):** Chain = OHOS panel touch → `OH_Input_AddTouchEventMonitor` (libohinput.so NDK, observes-not-consumes; `OH_Input_GetTouchEvent{Action,DisplayX,DisplayY,FingerId,ActionTime}`; ActionTime is µs→/1000 ms; @permission ohos.permission.INPUT_MONITORING, may return INPUT_PERMISSION_DENIED=201 for appspawn-x child) → native remap (OHOS TOUCH_ACTION_ CANCEL=0/DOWN=1/MOVE=2/UP=3 → Android DOWN=0/UP=1/MOVE=2/CANCEL=3) + downTime cache + synthetic-sentinel filter + AttachCurrentThread → Handler(mainLooper).post → injectTouch → MotionEvent.obtain(JJIFFI) → decorView.dispatchTouchEvent → C repaints. **KEY FINDING: repo already had this as the LIVE touch path** (`ports/dayu600/bridge-src/oh_input_bridge.cpp` dispatchTouchViaViewRoot → decorView.dispatchTouchEvent via noice's OHTouchInjector); the entire InputChannel-publisher half is DEAD under strategy B (nobody reads client fd) — don't wire it.

**ONLY HARD-NEW NATIVE = 2 pieces:** (1) **ensureMotionEventStub** — gate-3's 54 registrars have NO register_android_view_MotionEvent, and the real impl needs absent libinput.so, so RegisterNatives a minimal table (nativeInitialize→heap struct {downTime,eventTime,action,x,y,...}; ~15 getters nativeGetAction/AxisValue/PointerCount/Source/etc; dispose/recycle) modeled on the EXISTING ensureVelocityTrackerStub (oh_input_bridge.cpp:462-489). Both stubs MUST register before first dispatchTouchEvent or first touch UnsatisfiedLinkErrors mid-traversal. (2) the touch source shim.

**SOURCE PRIORITY REVISED (交付②):** P0 `/data/local/tmp/westlake_tap` file channel (证链, zero-perm, generalize startTapControlChannel :1038-1069) → **P1 `/dev/input/eventN` protocol-B direct read = the PRIMARY real-finger path** → P2 libohinput `OH_Input_AddTouchEventMonitor` (upgrade only). WHY /dev/input over monitor: INPUT_MONITORING is system_grant/system_basic; appspawn-x child token comes from spawn TLV (token_setproc.SetSelfTokenID in child_main.cpp) so a normal hap can't get it / can't runtime-GrantPermission it (needs APL elevation = B's spawn-side change, a noice-network-EPERM-class rabbit hole). Boards run SELinux Permissive (setenforce 0) → /dev/input readable with only DAC. Plus docs/REPRODUCTION-GUIDE.md: OHOS uinput is NOT bridged to Android — our observe-then-dispatchTouchEvent design is immune (validates Strategy B).

**⚠️ MILESTONE BLOCKER I FOUND (交付③, the workflow synth MISSED it):** Strategy B has no ViewRootImpl → whole View tree `mAttachInfo==null`. `View.onTouchEvent` ACTION_UP fires the click via `post(mPerformClick)`, but `View.post()` when detached returns true + defers to the RunQueue (drained only at dispatchAttachedToWindow) → synchronous performClickInternal never runs → **onClick silently never fires**. Small board worked because it HAD a ViewRootImpl (mAttachInfo!=null). FIX = `WestlakeInputAttach.attachForInput(decor, uiHandler)` (built, javac-verified): give the tree a fake AttachInfo with **mHardwareAccelerated=false** (keeps `drawingWithRenderNode` false → C's software-flatten capture intact) + mHandler=uiHandler, then reflect-call `dispatchAttachedToWindow(fakeInfo, VISIBLE)`. Version-resilient (introspects AttachInfo ctor). Ownership TBD with C (rendering-adjacent; ideally C calls it in show()). chain-proof.sh is the decider: "handled=true but no onClick" == this wall.

**ALL 5 ARTIFACTS COMPILE-VERIFIED:** westlake_input.cpp / ensure_motionevent_stub.cpp / westlake_devinput.cpp pass OHOS aarch64 sysroot -fsyntax-only; WestlakeTouchPump.java passes android-35 javac. Plus notes/oh_input_bridge-patch.md (exact before/after diff for B, P0 path needs NO baksmali) + scripts/chain-proof.sh + mmi-health-probe.sh.

**DELIVERABLES (my scratchpad `b0445db8-.../scratchpad/input-line/`):** FINDINGS.md (main: 6-item checklist + decisions + gap table + 8-step B recipe), src/WestlakeTouchPump.java (main-Looper marshal, replaces OHTouchInjector, compile into WestlakeUpscreen dex), src/westlake_input.cpp (monitor shim, **-fsyntax-only PASSES** OHOS aarch64 sysroot), src/ensure_motionevent_stub.cpp (stub table, sig placeholders pending A16 baksmali), notes/dev-input-fallback.md, scripts/mmi-health-probe.sh. All appended as 交付① to AGENT-COORD.md.

**STUB FINALIZED (交付④, no longer blocked on B for sigs):** self-served the full MotionEvent native table from B's OWN baksmali'd MotionEvent.smali (in session b3d658ee scratchpad: perfetto/fwdex/.../android/view/MotionEvent.smali — more complete than B's pasted /tmp/motionevent-natives.txt dump, which truncated at nativeGetSurfaceRotation, missing nativeInitialize). ensure_motionevent_stub.cpp now registers ALL 52 natives, cross-verified 52/52 name+descriptor vs the smali (zero diff), with correct @CriticalNative(no env)/@FastNative(env)/normal split. KEY TRAP: nativeGetAxisValue/GetEventTimeNanos/GetPointerId/GetToolType/GetRawAxisValue are @FastNative (need env) — a flat sig dump can't show this; got it from the smali .annotation. nativeInitialize sig confirmed = (JIIIIIIIIIFFFFJJI[PointerProperties;[PointerCoords;)J and it reads PointerCoords[0].x/y. VelocityTracker.smali checked: 7 natives all normal/env → the EXISTING ensureVelocityTrackerStub is already A16-correct, no change.

**INTERFACES RESOLVED:** C owns the AttachInfo fix (integrated attachForInputMinimal into WestlakeUpscreen.show() after layout, hwAccel=false, guarded) → B just calls show(), pump does NOT call attach. C added uiHandler(); pump self-fetches it.

**KEY PATH BUILT + A16-CALIBRATED (交付⑤):** OH_Input_AddKeyEventMonitor → OnKey → remap (NDK action CANCEL0/DOWN1/UP2→Android DOWN0/UP1; keycodes DPAD 2012-2016→19-23, ENTER 2054→66, MENU 2067→82, VOLUME 16/17→24/25 etc from oh_key_code.h; OHOS has NO hardware BACK/HOME=nav gestures) → WestlakeTouchPump.postKey → WestlakeUpscreen.dispatchKey. KeyEvent 10-arg ctor (JJIIIIIIII)V confirmed in A16 KeyEvent.smali (whitelist). ⚠️ CORRECTED a crash-bug assumption: the DAYU200 note "KeyEvent ctor needs no JNI" is WRONG for A16 — the ctor calls nativeNextId(), so new KeyEvent() ULEs without ensureKeyEventStub (registers nativeNextId + 2 KeyCodeString natives, all normal/env; added to ensure_motionevent_stub.cpp). All key code -Wall clean. Also decoupled the touch milestone from framework-res via WestlakeInputTestView (clickable pure View, logs WLTEST touch/CLICK, javac'd).

**⚠️ NON-OBVIOUS DEPENDENCY (交付⑦):** On the imageless interpreter, RegisterNatives is necessary but NOT sufficient — the interpreter calls native via InterpJni which marshals per shorty string, and each shorty needs a handler (same wall B hit with Paint.nSetFlags=VJI graphics native). My input natives need 27 distinct shorties (enumerated in notes/interpjni-shorties.md); 3 overlap graphics (VJI/VJFF/VJF). The critical one: MotionEvent.nativeInitialize shorty = JJIIIIIIIIIFFFFJJILL (19 args, 2 object arrays) — impractical to hand-code per-shorty, so B's GENERAL libffi shorty dispatcher (not SQLite-style per-shorty) is REQUIRED for touch (obtain→nativeInitialize). Once B's general dispatcher lands, all 27 are covered automatically. Flagged to B in COORD 交付⑦.

**PROJECT STATE (2026-07-07 深夜, B's honest wrap-up):** Mainline REACHED `DecorView.<init>` (setContentView core — one step from show() + my input). Then hit `Paint.nSetFlags` ULE. ROOT CAUSE (team-converged, confirmed by B): **boot classpath = core-jars, does NOT include framework.jar** → framework classes (Paint/MotionEvent/all) get app-classloader second-loaded → startReg's early FindClass binds RegisterNatives to the WRONG Paint class → app's nSetFlags unregistered. FIX (A's, I endorsed): **framework.jar → BOOTCLASSPATH** (single boot-loaded class instance) — one fix for graphics + MY input (52 MotionEvent natives bind) + all framework native. Separate issue ②: crit-flag via class_linker LoadMethod:4141 GetNativeMethodAnnotationAccessFlags (only if boot-loading doesn't set kAccCriticalNative from dex annotation). BOARD UNSTABLE: hung D-state toybox process mmaps stale libart; after restart runs stop early at icuEntry (runtime-state, not code) — NEXT SESSION must fix that first, restore DecorView, then apply boot-cp fix.

**MY INPUT LINE = 100% STAGED, needs no change**, covered by the boot-cp fix. Deliverables (scratchpad input-line/): INTEGRATION.md (turnkey) + FINDINGS + WestlakeTouchPump/WestlakeInputAttach/WestlakeInputTestView.java + westlake_input.cpp(touch+key monitor)/westlake_devinput.cpp(/dev/input P1)/ensure_motionevent_stub.cpp(52/52+KeyEvent stub) + oh_input_bridge-patch.md + chain-proof.sh/mmi-health-probe.sh + dispatcher-proto/(libffi shorty ALL-GREEN, backup). Verification points I gave B: after boot-cp+libart-rebuild, assert RegisterNatives(MotionEvent)==0 AND MotionEvent.nativeGetAction->IsCriticalNative()==true. Also contributed to the SHARED @CriticalNative blocker: 27 input shorties, generic-coverage constraint, late-binding vs boot-cp analysis. MILESTONE (unchanged): board restored → boot-cp fix → graphics → DecorView → show() → ensureInputStubs+setJavaVM → tap → onClick fires (chain-proof.sh, WLTEST markers).

**STATUS (2026-07-07 晚, migration GO):** Input is now a **self-contained deployable drop-in .so** `libwestlake_input.so` (JNI_OnLoad auto: setJavaVM + register 52 MotionEvent normal-variant + 7 VelocityTracker + 3 KeyEvent stubs + start westlake_tap poller), staged `scratchpad-shared/wl-input-d/` (+ 3 Java + DEPLOY.md), built with OHOS clang. **Ran a 5-dim adversarial audit of the whole input line → 0 confirmed blockers, but hardened 3 real issues → .so v2 md5 `bd6d2a778b835143616fe7d66e305909` (supersedes 1f69fc9e which C already put on board — C must re-place v2, B must use the updated WestlakeInputTestView.java):** ①classloader pitfall — poller was FindClass'ing app-dex WestlakeTouchPump on a DETACHED native thread (system classloader → miss → poller dies); fixed by resolving on the app thread in JNI_OnLoad/registerAllNormal, caching global ref, poller uses cache; ②poller truncates westlake_tap at startup (no stale phantom tap); ③WestlakeInputTestView logs literal "WLTEST CLICK"/"WLTEST touch" tokens (were "CLICK #" → grep miss). **TWO VARIANTS by libart crit-flag state (NOT board):** normal-variant (ensure_motionevent_stub_normal.cpp, all 52 with env) for a libart WITHOUT crit-flag root fix; crit-variant (ensure_motionevent_stub.cpp, 36 crit no-env/16 with env) for a libart WITH it. **THIS SESSION I am OFFLINE: no hdc, no JDK** (OHOS clang works for native) → I can't board-place or dex-compile; delegate: C places the .so, B compiles the 3 Java into the probe dex (same dex/classloader as C's WestlakeUpscreen). **TWO TRACKS:** Track A = engine proof (C pureColorView/my WestlakeInputTestView + my tap) is **arsc-independent** (programmatic View, no framework-res) → nearest milestone, needs only ART-up on 5583f5be + force-raw nDrawColor (C, :751 already) + **:2262 WestlakeGenericJni** (marshals my nativeInitialize 19-arg + 27 shorties; E confirmed WestlakeGenericJni exists in interpreter.cc:214 but is NOT wired — must wire at BOTH :769 crit + :2262 normal elses; my dispatcher-proto is the ready reference). Track B = real uptodown UI (B's arsc-native cascade DONE via register-and-use/JNI_OnLoad + E's sSystem root-fix with 76a92b8f). B migrating to 5583f5be (5ce2dcee degraded/USB-drop lottery). My input contribution complete; fires WLTEST CLICK when ART+:2262+my-Java-dex land.

## UPDATE 2026-07-08 17:18 (D3 continuation)
**板 5583f5be 已部署件 (file send/recv verified)**:
- `upscreen-render.dex.jar` md5=`8c837fba` (9601 bytes, **6 classes**): WestlakeUpscreen + TouchPump + InputTestView + InputAttach + TextPump + Harness
- `libwestlake_input.so` md5=`4fdbd3e4` (36992 bytes)
- `@CriticalNative stripped framework.jar` md5=`00a1dac2`, in verified-builds/

**新交付**:补全缺失的 WestlakeTextPump.java，编译进 dexjar 上板。秘书 CHAT line 74 IME 指令完成。
**板**:5583f5be 空闲，等 B show()。Paint ULE → 立即推 00a1dac2 framework.jar。
